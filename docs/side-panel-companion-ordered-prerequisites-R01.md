# Side-panel companion ordered prerequisites R01

Status: ordered work requested by the user through coder return
`20260905T231226Z_000008_from_coder`; the user directed the specifier to process
that handoff. Starting QA specification: `8e1ece9e392f85bb5d16e630a9d59b544948c918`.

The order is mandatory. Each stage starts from the preceding reviewed QA
descendant. Issue only the next stage, through the ordinary coder, refactorer,
architect, and specifier route. A specification commit does not satisfy an
implementation prerequisite. No stage authorizes master promotion or an
all-runnable-pack feature checkpoint. No parallel implementation is requested.

| Order | Stable task | Completion boundary |
|---|---|---|
| 1 | `serena-use-assessment` | Effective instructions, suitable symbol use, and fallback proof reviewed and integrated into QA |
| 2 | `architecture-module-declaration-ownership` | Bounded declaration planning with complete architecture validation reviewed and integrated into QA |
| 3 | `project-library-dialog-decomposition` | Production dialog extraction with preserved behaviour reviewed and integrated into QA |
| 4 | `side-panel-companion-brand-correction` | Resume the original approved product task with fresh installed evidence |

## Current stage

Stages 1 and 2 are integrated into QA. Stage 2 is accepted at
`780e721edb110e444307368b31e9fdcd728a971b` through architect handoff
`20260906T152607Z_000895_from_architect`. Both bound review records passed
validation. The final raw receipt matched its recorded digest and passed all
133 focused tasks, including properties and fresh package proof. Results are in
`docs/side-panel-companion-stage-1-qa-scorecard.md` and
`docs/side-panel-companion-stage-2-qa-scorecard.md`.

Issue Stage 3, `project-library-dialog-decomposition`, from the documentation
recording descendant of that exact QA head. Section 3 and its existing feature
contract remain the implementation authority. Read-only intent classification
comes before edits. Declaration treatment requires actual base and candidate
repository evidence; a path-only intent remains conservative. Stage 4 remains
the ordered successor, and its branding draft remains a recovery reference.
The standing instruction to issue the next stage after reviewed QA integration
applies without another routine approval request.

## 1. Serena use assessment

Classification: tooling and process. Contract:
`features/swarmforge-serena-use-assessment.feature`.
The first handoff authorizes this stage only; later stages remain ordered work.

Required instruction: docs/serena-child-dispatch-owner-transition-R01.md

The user approved this exact owner-transition correction on 2026-09-06 and
authorized direct delivery to the specifier. Resume `serena-use-assessment`
from the QA specification commit that contains this correction. Scenario 009
in `features/verification-process-exact-slice-execution.feature` retains its six
prior transitions and adds only the historical-planning parent and child split.
Use that exact specification commit for the authenticated successor authority;
a feature row or same-owner digest refresh alone cannot change the binding.
Preserve the bounded extraction draft, inventory proof, incident proof, and
remaining parent cases. Require fresh focused evidence and package proof before
ordinary review. Candidate `80cddb9368` remains blocked; its ancestry is not
part of this specification correction. Stage 2 starts only after Stage 1 has
reviewed implementation proof integrated into QA.

The existing shared rule already reaches all four generated startup instructions.
Specifier checks of real generated instructions confirmed one include per role.
`test/serena-startup-reading-test.mjs` and `test/serena-usage-rule-test.mjs` both
passed on the starting QA tree. The remaining failure is the absence of an
explicit assessment before suitable code reading, not a proved missing include.
The specifier initially read a complete renderer; the coder reports the same
miss for the project controller. Neither is acceptable evidence of Serena use.

Make each role assess its next exploration question before a broad read of
unfamiliar supported code or a definition/caller investigation. If a symbol
overview, body, or reference query can answer it with less reading, use Serena
first. State the chosen route briefly in ordinary progress reporting. Use file
search for paths, text, CSS, markup, configuration, and documents; use the
ownership helper for verification. Complete architecture review still needs the
full candidate diff. A suitable query must answer a real task question, not
serve as a ceremonial call before an unchanged whole-file read.

Keep this rule in one shared source and prove its effective delivery to
specifier, coder, refactorer, and architect. Test the generated startup plus
required-include resolution, not only an isolated source-string assertion.
Do not duplicate a catalogue or a large instruction block into four role files.
Preserve conditional duties and once-per-resolved-path include handling.
For existing sessions, use the normal handoff's required instruction reading;
do not restart another busy role or send routine direct tmux messages.

Missing, failed, stale, or unsupported Serena results permit an explained
ordinary-tool fallback. Keep the tool optional, read-only, worktree-bound, and
offline at startup. No new download, mandatory call count, global tool gate,
telemetry, history memory, model change, or repeated benchmark is authorized.
Use naturally occurring implementation and review queries to report actual
helped/neutral/impeded outcomes. Generated text alone cannot prove agent use.

Development focus: the two existing tests named above, followed by the existing
Serena launch/runtime tests only if launch delivery changes. Keep all four role
cases and the unsupported/missing/stale fallback cases. QA forecast: `shell`
and `verification_process`. The shared tool-use path currently selects the
`shell.swarmforge-handoff-control` slice (51 tasks in its path query); the Serena
instruction generator also selects `shell.serena_development_tools` (63 tasks
in that query). Counts overlap and are not a combined plan.

Likely paths: `swarmforge/scripts/shared-articles/tool-use.prompt`, existing
`swarmforge/scripts/serena/instructions.clj` and
`swarmforge/scripts/role-agent-instruction.bb` only if delivery requires a change,
and the existing focused tests and Serena acceptance handler. No new production
prefix is proposed. Any new test helper must declare parent `shell`, slice
`serena_development_tools`, and the current handoff/evidence consumers before
coding. Do not broaden startup reading or alter queue and lease semantics.

Effort expectation: 60 minutes, with a 30-minute progress checkpoint. Preserve
current focused review and package requirements. Reviewers check effective
instructions and relevant actual use. After architect QA-ready integration,
the specifier records the result and issues stage 2 without another routine
approval request while this ordered scope remains unchanged.

## 2. Architecture module declaration ownership

Classification: verification tooling. Contract:
`features/verification-architecture-module-declarations.feature`.

The bounded discovery found `verification/manifests/shell.json` declares
`architecture/` as global impact. The impact planner uses path matching, while
`allPackClassification` can report `genuinely-global` when no credible boundary
is described. The reported new-module intent therefore selected all 21 current
runnable packs. No all-pack run occurred. Architecture itself was not edited.

Distinguish changes to per-source entries in
`architecture/data-layer-boundaries.json` from changes to shared architecture
policy. A bounded entry delta selects complete architecture validation and the
affected modules, owners, prerequisites, and consumers from both base and
candidate. Cover additions, edits, deletions, renamed paths, and changed
contract edges. Do not trust a caller-supplied task name, ownership list, or
declaration-only label as authority. Mixed policy and declaration changes retain
the stronger scope. Invalid, unavailable, or ambiguous evidence fails closed or
uses the conservative route; it cannot silently drop a consumer.

Preserve the full architecture checker, global-rule handling, current/base
coverage, acceptance conservation, packaging, and terminal release obligation.
Do not simply remove the `architecture/` global-impact rule. Do not weaken a
layer, module, import, or contract restriction. A decomposition exception and
an artificial task-specific exemption are prohibited.

Development focus: direct planner/readiness tests with small synthetic
registries, plus complete architecture validation for a temporary copy of a
real bounded new-module declaration. Prove global shared-rule and mixed-change
cases as well as bounded cases. QA forecast: `shell` and
`verification_process`, subject to exact ownership. Likely existing paths are
the Shell manifest, `scripts/verification-planner/ownership/impact.mjs`, the
planner's changed-path adapter, and readiness classification where required.
Keep semantic-delta code in a focused module. Proposed prefix
`scripts/verification-planner/architecture-declarations/`: parent
`verification_process`, proposed slice `architecture_module_declarations`,
exact consumers `shell` planning and architecture-validation targets.
The coder must classify this proposal before edits; it does not register itself.

Effort expectation: two hours, checkpoint after one hour. Run only focused
diagnostics during repair. If the correction's own intent exposes another
global boundary, use a bounded independently reviewed preparation or report
the exact semantic blocker; do not recursively add a verifier hardening program.
After review and QA integration, issue stage 3 from that exact descendant.

## 3. Project Library dialog decomposition

Classification: behaviour-preserving production refactor. Contract:
`features/project-library-dialog-decomposition.feature`.

The current `src/data-layer-project-library-ui.ts` is about 27 KB in 56 long
lines. Its coordination also constructs edit, switch-review, creation, and
import-review dialogs. The low line count does not make this a small module.

Use Serena outlines and references to separate those four dialog duties and
their metadata and focus helpers from coordination. Keep
`src/data-layer-project-library-presentation-ui.ts` separate. Each module has
one clear purpose and normal readable formatting; do not relocate the same
monolith or hide it in compressed lines. Keep repository, save, subscription,
and transport orchestration in the coordinator, with explicit dialog callbacks.

Preserve callback identities and inputs, project identity and activation,
revision/save/Undo semantics, import resource release and cancellation, dialog
validation, and focus after success, cancellation, Escape, and native close.
Register every extracted module and its real contract edges. Use stage 2's
reviewed declaration semantics; complete architecture validation remains required.
Do not port the unfinished branding layout into this prerequisite.

Development focus: existing Project Library/transport tests and installed
`test/twatility-projects-browser-test.mjs`, with focused dialog lifecycle cases.
QA forecast starts at `project_management` and `shell`; retain current
controller consumers `durable_project_repository`, `project_event_transport`,
`flow_graph`, `flow_export`, `live_flow_testing`, `layered_schema`,
`property_set_flow_sections`, and `guided_test_cases` where exact planning binds
them. Proposed prefix `src/project-library-dialogs/`: parent
`project_management`, proposed slice `project_library_dialogs`, with those exact
current controller consumers. Read-only intent must include architecture entries,
the new prefix, coordinator, direct tests, and any verification registration.
Effort expectation: two hours, checkpoint after one hour. Preserve all existing
behaviour before ordinary review and QA integration. No exception to decomposition
or complete architecture validation is authorized.

## 4. Product resumption and preserved draft

`75be4ec6c62f6e12abcc164365ee4fe3d48c206d` is an immutable recovery reference,
not a coherent review candidate or a prerequisite ancestor. Do not merge it into
QA. After stages 1–3, reissue `side-panel-companion-brand-correction` from the
exact reviewed QA descendant. Port only applicable source changes and explain
adaptations required by the reviewed decomposition. Do not blindly replay stale
generated `dist/` files; regenerate them under the existing delivery rules.

Known touched sources: six style modules (`shell`, `shared`, `projects-repository`,
`live-transport`, `defects-schemas`, `workflow-structure`), `side-panel.html`, both
Project Library UI modules, `test/support/side-panel-companion/{chrome,presentation-test}.mjs`,
and the existing Projects and side-panel shell browser tests. Include these paths
in resumption intent. Keep unrelated untracked files and the named coder stash.

Unfinished checks remain binding: the shell diagnostic reported 2 px workspace
overflow; the Create project dialog focus-return check failed. The coder reports
a close-event correction, but no subsequent passing Projects check. Installed
contrast and state coverage, all remaining actions, acceptance registration and
execution, properties, package proof, exact preflight, and review evidence are
still required. No earlier draft receipt supplies that proof.

The original all-seven-view product contracts and six-hour implementation/review
expectation remain active. Record prerequisite effort separately. Later review
must report the actual cause of any remaining delay and the cost of each
preparation. Process failures were missed suitable Serena use and a coarse
declaration boundary. What went well: the draft was preserved and no all-pack
feature run was launched. Recommendation: complete these bounded stages in order;
do not add more process work without a new concrete blocking cause.
