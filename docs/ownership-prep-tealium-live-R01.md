# Tealium manifest and background ownership preparation R01

Status: standing-authorized on 2026-09-09 under the approved Tealium task and
`outcome-bounded-autonomy-v1`. Specification authority is not implementation proof.
Stable preparation task: `ownership-prep-tealium-live`.
Preserved product task: `tealium-live`.
Starting QA: `4e7f9c4dfdc77717906811f6a25cfa56c32a5001`.

## Required instruction includes

- `docs/ownership-prep-tealium-live-stages-R01.md`
- `docs/ownership-prep-tealium-live-stage-b-R01.md`
- `docs/tealium-live-R01.md`
- `docs/tealium-live-verification-R01.md`
- `docs/qa-verification-ownership-readiness-R01.md`
- `docs/qa-verification-granularity-ratchet-R01.md`
- `docs/swarmforge-outcome-bounded-autonomy-and-unblocker-handoffs-R01.md`

Read each resolved include once. Product acceptance remains unchanged.

## Assessment and decision

Coder note `20260909T143434Z_000030_from_coder` reports the plan-only intent
at the exact starting QA. Its five existing paths select 21 packs, 268 tasks,
and an estimated 268,000 ms. This is an estimate, not measured verification.
The three proposed private Tealium prefixes are declarations only.

The canonical result is `genuinely-global`. The manifest has no declared
credible boundary. Background code has the unsliced `shell_platform_runtime`
boundary. This specification does not change that result or permit its execution
in feature mode. The other three paths already have host or delivery boundaries:
`src/utility-contributions/index.ts`, `src/utility-host/page-client.ts`, and
`build-delivered-dependencies.json`.

Read-only inspection found a concrete preparation hypothesis. `manifest.json`
contains extension metadata, entry points, commands, and permissions. The build
copies it unchanged. A future DevTools entry is one declaration with observable
packaging and runtime effects. `src/background.ts` starts the durable repository,
opens the side panel from action and command gestures, and sends the Hotkeys
focus message. These duties must remain observable separately from a future
DevTools connection registration.

Select one independent preparation for these two related paths. A declaration
module and a background composition seam can be tested without implementing
Tealium. This is a credible hypothesis, not proof that narrower ownership is safe.
Use the existing stage-aware shared-boundary mechanism where necessary. Do not
declare the complete manifest or background private to Tealium. Do not add a
planner framework, incident exception, or new permission to make a plan pass.

## Bounded program

The staged correction in the required include governs the sequence below.
Complete its Stage A through implementation-bearing QA before structural work.
Stage A alone does not complete this preparation or resume the product.

1. Run read-only intent for the exact proposed preparation paths. Inspect the
   existing shared-boundary support and list every direct observation and real
   consumer. Include any build change in the causal path union. Establish a
   bounded preparation route before product coding or full evidence execution.
2. Separate only the manifest contribution/delivery responsibility required for
   a later DevTools declaration. Keep the installed manifest semantically equal
   to the current manifest during preparation. Preserve root manifest loading
   and the repository's supported package/build entry points. Do not silently
   make one installation route require a new build procedure.
3. Separate background composition from existing repository startup and
   action/command behavior. Keep startup order, errors, side-panel user gestures,
   current-tab fallback, and focus-message behavior unchanged. The preparation
   has no active Tealium handler or DevTools page. A minimal inert registration
   seam is permitted only if it is necessary and independently observable.
4. Add exact ownership declarations and direct boundary checks. Keep all current
   tasks, assertions, prerequisites, host consumers, package checks, and terminal
   obligations. Retain conservative ownership for unrelated manifest fields,
   permission changes, unknown contributions, and background behavior outside
   the proved seam. Preserve the union of old and new coverage for this range.
5. Obtain independent refactorer and architect review and integrate the exact
   implementation into QA. The product may use the new boundary only after this
   step. Return a preparation scorecard with the precise future touch paths.

Likely existing paths are `manifest.json`, `src/background.ts`,
`scripts/build.mjs`, the canonical verification declarations, and
`test/background-command-test.mjs`. New small modules belong beside their
responsibility. Name exact prefixes and consumers in intent before adding them.
Do not split unrelated platform modules or the Data Layer implementation.

## Verification and acceptance

Forecast the Shell packaging and background observations, durable repository
startup, action/command and Hotkeys consumers, the installed utility host
consumers, and the existing Flow Export delivery consumer if delivery changes.
The approved product forecast contains these 14 packs: `shell`,
`durable_project_repository`, `project_management`, `command-palette`, `hotkeys`,
`capture`, `event-library`, `project_event_transport`, `schemas`, `defects`,
`replay`, `live_flow_testing`, `verification_process`, and `flow_export`.
This is a forecast, not an invented consumer mapping or a measured prep plan.
Derive exact slices and prerequisites with the canonical ownership query.

Acceptance requires independent observations of all of the following:

- Build/package output retains all existing manifest fields and permissions.
  Packaged entry points resolve. The contribution seam does not allow unrelated
  fields to acquire private ownership.
- Repository startup still occurs, including its failure handling. Action and
  command gestures open the same target panel. Current-tab fallback and the
  Hotkeys focus message remain correct.
- Existing Data Layer runtime message consumers still work. The preparation
  assessment covers DevTools-related message-response compatibility; a future
  handler must not capture or respond to unrelated messages. Existing listeners
  inspected here return void, but that fact alone is not an installed proof.
- Exact-pack task closure and terminal coverage remain complete. Focused
  selection includes each real consumer and prerequisite. Unknown or materially
  broader edits retain conservative fallback. No consumer is removed merely to
  reduce the selected pack count.
- The packaged extension passes the applicable installed host checks. A metadata
  assertion or mock background test alone cannot prove installed compatibility.

Use bounded development checks, then exact committed plan-only preflight, then
one settled focused review-ready run with properties and package proof. Preserve
stage-aware terminal-full obligations for the later user-requested master gate.
No all-runnable-pack feature run or Gherkin mutation is authorized. This is a
behavior-preserving structural task; the existing acceptance contracts remain
authoritative. Add direct conservation and boundary tests where needed.

If the hypothesis fails, report the exact failed premise and the smallest sound
alternative under the same authority. Do not repeat the same assessment with a
new label. A truly global unresolved plan remains stopped; only a material
outcome, risk, authority, or cost change needs a new user decision.

## Queue and product conservation

Coder reported no production changes or product remainder. Bind the replacement
control message to active handoff `20260909T143248Z_000905_from_specifier` and
the exact queued preparation handoff. Use the official unblocker helpers only.
The replaced product handoff is paused for this prerequisite, not completed as
a feature. Preserve its approved specification and stable task identity.

After architect review and implementation-bearing QA integration, the specifier
reissues `tealium-live` from that exact QA head. A specification-only commit
cannot satisfy this dependency. If product changes are found despite the note,
preserve them through the official stack mechanism before replacement.

Allow two active hours for preparation implementation and review within the
product's existing planning allowance. Report after one hour: exact paths and
plan, boundary proof, retained consumers, remaining work, and forecast. Report
variance and continue within standing authority when a sound bounded route
remains; elapsed time alone is not a new approval requirement.
