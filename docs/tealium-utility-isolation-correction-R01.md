# Tealium utility isolation: active outcome correction R01

Status: current user clarification on 2026-09-09. This restores the already
approved utility expansion outcome. Stable task remains
`ownership-prep-tealium-live`; product requirements remain `tealium-live`.

## Binding outcome

The user requires new utilities to avoid unrelated existing Data Layer work.
Fewer than all runnable packs is not success. The existing utility expansion
verification contract already says that parent-pack fallback with unrelated
work is not a successful expansion route. This instruction takes precedence
over the Stage B permission to proceed with the broad conservative plan.

The 824-task Stage B intent is an unresolved ownership result, not an approved
execution target. Do not launch that broad evidence run. Finish any running
command without discarding its output, preserve current work and receipts, then
continue bounded inspection and direct checks at the next safe boundary.

## Retain what works; correct the missing selection

At QA `fceab38a0880109df317b2d9d8e439875901fbc9`, canonical path queries for
`src/utility-contributions/index.ts` and `src/utility-host/page-client.ts` each
select 32 checks across 13 pack identities. They select exact installed host
consumer slices, with no parent-pack fallback. Thus pack count alone does not
describe the burden. Retain this independently reviewed utility-host route.

Stage A's canonical manifest field evidence, conservative permission fallback,
and retained terminal coverage are useful and remain accepted. Its path-level
background declaration does not by itself meet the utility isolation outcome.
Do not count Stage A completion as proof of the final additive feature route.

Use the existing exact utility-host consumer slices for genuinely shared host
observations. Add only independently justified background and DevTools checks.
Do not replace those slices with whole Data Layer parent packs. Preserve all
other checks under their actual owners and in the terminal plan.

Reassess whether background extraction is necessary for the approved bridge.
It was an implementation proposal, not a user requirement. A simpler isolated
extension-page connection is an admissible hypothesis if direct browser proof
shows correct identity, lifecycle, and source navigation. Do not assert that
hypothesis works without proof. Prefer the smallest sound route; do not perform
an extraction merely because an earlier preparation document listed it.

## Required result before feature routing

Produce one concrete implementation and selection route for all of:

1. Add a normal utility: select its own checks and the existing exact shared host
   checks, without unrelated Data Layer feature suites.
2. Add Tealium with DevTools: select Tealium's detection, Live, source-navigation,
   packaging, and exact shared host/bridge observations. State each additional
   task and its causal reason. Use valid canonical field evidence and preserve
   independent registration/activation sequencing.
3. Edit private Tealium code: select its direct checks and actual consumers,
   without complete Shell or unrelated Data Layer families.
4. Change permissions or shared semantics: retain all real affected consumers
   and conservative fallback. Isolation must not weaken these cases.

Use canonical plans and independent expected task identities. Do not manufacture
a low task count, delete tests, waive current/base coverage, or let a product
candidate introduce ownership rules and use them to narrow its own range.
Where a separate declaration integration is necessary, present the smallest
concrete delta and its bounded plan under this same task. Do not invent another
preparation label, restart broad discovery, or add a verification framework.

Report task counts and selected families, plus direct host overhead. Pack counts
are secondary context. The existing 32-check registration result is a useful
comparison, not an arbitrary ceiling for Tealium's own new checks.

No renewed product specification or user approval is needed for this correction.
The user has clarified the acceptance goal, not changed the Tealium feature.
Preserve the existing effort accounting. Report current progress and any material
cost variance promptly; do not silently open another multi-stage program.
