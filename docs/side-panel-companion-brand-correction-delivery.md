# Side-panel companion correction delivery

Task: `side-panel-companion-brand-correction`.
Received specification: `c3f4e6e66c9d3cac51ad703f871f169b9b9b1c1f`.
The coder resumed on 2026-09-06 after the three reviewed prerequisites reached QA.
This is a functional presentation correction for QA review. Master promotion
remains separate. The bound review record, when recorded, is the verification
authority for the exact candidate.

## Product changes

- All seven views use warm surfaces and dark text. Ordinary controls are flat.
  The masthead retains the existing artwork and one Commands launcher.
- The workspace selector has two columns. The six subordinate tabs use two rows
  at the tested widths, with a gold selection rule and a distinct focus ring.
- Projects has one record per project. Names and metadata are separate. Native
  details contain the complete identifier and exact saved time. Filtering the
  active record out of view does not change the active project.
- The reviewed dialog modules remain in use. The coordinator supplies metadata
  and timestamps and restores Edit focus in the current list. Close project now
  restores focus to the saved record's Switch action after rendering.
- Empty Library operation outputs have no decorated strip. Populated messages
  and the real storage-recovery presentation remain readable.

Applicable draft styles, HTML, and presentation changes were ported from
`75be4ec6c62f6e12abcc164365ee4fe3d48c206d`. Its ancestry and generated output were
not replayed. The model and focus changes were adapted to the reviewed dialog
split. Delivery assets were rebuilt. Studio styles and project domain code were
not changed.

## Focused development evidence

The installed Projects leaf now records all seven views at 360, 420, and 512 CSS
px before and after population. Fixtures use the real repository and product
renderers for events, templates, sessions, defects, schemas, and projects.
The lowest observed text contrast in those captures is 5.23:1.

Native tests cover 12 dialog close cases, coordinator actions, metadata save and
Undo, archive export and import review, picker cancellation, Studio launch,
long-name containment, long-identifier keyboard disclosure, and filtering.
The file-picker adapter limits writes to 5 MiB and calls the production export
path. Storage failure is injected through the production recovery API in an
isolated copy of its real markup; retry, export, enabled recovery controls,
focus, and the unchanged stored snapshot are checked.

Each accessibility mode traverses all 21 visible sequential controls with native
Tab events. Modes are keyboard only, forced colours, reduced motion, and the
200% layout equivalent: a 180 CSS-px viewport at device scale 2 for a 360-pixel
panel. This tests zoom reflow; it does not claim to operate Chrome's zoom menu.
Focus contrast is checked against the surrounding surface. The local focus
colour has 3.62:1 contrast on raised paper and 3.44:1 on the navy masthead.

Fresh screenshots and reports are under `tmp/companion-evidence/`. Each review
run creates fresh installed observations. Direct model, registration, acceptance
dispatch, complete architecture, and build checks are also required. Acceptance
rejects missing views, widths, lifecycle checks, recovery, archive, Studio,
long-record coverage, or insufficient contrast. No registered test starts the
aggregate verification runner. Browser polling and helper commands have bounds.

## Verification judgment

Read-only intent selected 19 packs and 892 tasks. The plan is bounded, but shared
styles and project-model consumers include Flow, Documentation, and Schema
semantics outside the immediate visual changes. The reviewed style slice has a
clear source boundary; a smaller safe consumer split has not been established.
The decision is an evidence-backed parent fallback. A new mapping program would
add work and risk dropping a consumer. Retain the exact conservative plan, use
one worker, and reassess if the exact plan becomes all-runnable-pack or a failure
has no bounded causal explanation. Exact planning and review evidence follow
the first coherent commit. No terminal gate is authorized.

Exact planning at `cf791596` selected 1,036 tasks across 20 of the 21 current
packs. Acceptance registration adds `verification_process` to the forecast.
The read-only result remains `granularity-assessment-required` with the
`continue-parent-fallback` disposition. This is a conservative feature review
plan, with properties and packaging; it is not a release checkpoint.

## Repair family and process assessment

The bounded family is companion presentation and its direct test registration.
Findings addressed during development were the draft's unclosed media rules,
negative tab margins causing 2 px overflow, stale featured-card test selectors,
lost focus after Close project, grey disabled ordinary actions, low focus-ring
contrast, three empty Library strips, an ambiguous acceptance step, and missing
direct helper consumers. The ambiguous step now names the companion control.
Only the explicitly superseded blue-paper and filled-selection examples in the
older paper-first contracts were updated. Other assertions remain in place.
The shell check also waits for the installed runtime marker before route tests;
one earlier direct route observation failed before that readiness condition.

What went well: the preserved draft and reviewed dialog boundary allowed a small
production change. Runtime checks found contrast, focus, and empty-state faults.
What failed: the draft was not coherent, and initial registration omitted new
helper consumers. Recommendation: inspect the complete changed-state matrix and
declare direct helper consumers before the settled review run.

Serena was impeded: the exposed tools require `initial_instructions`, which was
not available in this session. No Serena query, installation, or restart occurred.
Scoped ordinary inspection was used. No setup saving is claimed.


### Prelaunch identity correction

The first review preflight stopped before any test, receipt, or incident was
created. The existing authenticated Shell consumer-plan digest was stale after
registration. Canonical comparison proves six additions: the two companion unit
checks and parse/generate tasks for both new features. Only the Shell acceptance
session identity also changed; no former task was removed. The Phase 2 session
identity and count are unchanged. A read-only intent for the existing declaration
is bounded-ready under `verification_process` (28 tasks).

The repair refreshes only `consumerPlanDigest` from that canonical calculation.
Source commits, source trees, incidents, receipts, succession authority, policy,
and all checks retain their existing values and meaning. This is registration
maintenance in the same repair family, with no new verification mechanism.


### Legacy presentation expectation

Review receipt `1580722-0ae16fea-1d5f-4f01-a471-291a29e0350b` stopped after 62
passing tasks. Incident `a709ef30-0b62-4e9e-8e4a-a7f76a1df210` records the failed
`unit:test/side-panel-direct-compatibility-capture-test.mjs`. Its workspace
assertion still required the decorative utility directory to be visible.
The approved correction removes that presentation. Only the expected visibility
was changed; utility identities, labels, ownership, panel relationships, and
Hotkeys activation assertions are preserved. The direct leaf then passed all
373 assertions. The native causal regression uses installed badge observations
against the exact failed candidate expectation and checks conservation of the
remaining assertion fields. A visible-directory counterexample still fails.
