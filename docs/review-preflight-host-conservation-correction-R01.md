# Review preflight host conservation correction R01

Authorized causal continuation of `verification-registration-review-preflight`.
Start from QA `87f5919e053f5fff78aa20ffd59977b6886d40b0`, preserving all accepted
configuration specifications. Feature integration into QA only.

## Finding and disposition

Coder note `20260913T074346Z_000078_from_coder` reports incident
`751f02c8-3dec-427c-9c10-d511efa2cba7` from preparation candidate
`1bb6488272244a27d4a2068fc4662e0e9365c74f`, base `87f5919e05`, receipt
`1540616-a88e5e52-e33f-4560-a211-7524c932f3a1`. The 432-task plan stopped
after 141 passes, one failure, and two cancellations. It is not review-ready.

The failed task is `unit:test/utility-tab-expansion/planning-test.mjs`.
Its host conservation helper compares the complete modular architecture test
with the extraction from immutable source `d5c876f400`. Accepted process commits
`4ec797f9e4bab84324c67c38882cdef9fcb1bd08` and
`3d19d36ed32a64ddc3a999416f3fe0bfa731da3b` added a repair regression branch
and review-feature option assertions. The comparison does not admit those
additions. The specifier reproduced the exact strict-equality failure on QA.
The preparation candidate changes none of these paths.

The incident CLI has no supported pre-existing deterministic-failure deferral.
Do not classify this failure as flaky, waive it, or change incident machinery.
Return the causal defect to the original process task, separately from the
configuration preparation. This is repair of its accepted regression, not a new
process program or an unrelated product repair.

## Bounded correction

Repair family: review-preflight-host-assertion-conservation.
Boundary: the two accepted additions, their host conservation comparison, and
the direct planning and modular architecture test consumers.
Completed bounded discovery: one defect, host comparison rejects authorized
additions. Preserve the same family for further causal findings in this boundary.

Preserve every original assertion and fixture, the immutable extraction source,
both accepted additions, and their existing execution and repair identities.
Prove exact historical conservation independently of the current candidate.
A bounded projection may admit only the exact additions authenticated by the
accepted commits above, at their exact locations and with their exact bytes.
Reject a missing, duplicated, altered, moved, or extra addition and any changed
historical assertion. Do not replace the historical expected body with current
output, accept arbitrary inserted text, or remove the conservation check.
Use a small helper if needed; do not enlarge the modular test with another duty.

Use existing incident repair and evidence helpers only. Preserve the original
failure receipt and all earlier repair identities. Keep any integration gate
pending until the existing validators accept its exact evidence. A passing
diagnostic alone is not an incident disposition or review-ready proof.

## Checks and review

Read-only intent covers `test/utility-tab-expansion/host-assertion-conservation.mjs`
and actual correction paths before edits. The existing owner is
`shell.utility_workspace_host`: its path query selects 36 checks across 13 packs
through declared consumers. This is a forecast, not permission to omit checks.
Any new helper belongs to this same existing boundary and its consumers.
Keep registry ownership, runner policy, and the current/base union unchanged.

Development focus: the failing planning leaf, modular architecture leaf, and
small deterministic negative cases for the exact projection. Then use the
complete canonical exact plan with properties and package proof, bound to this
stable process task and the received specification base. No all-pack gate or
new verification framework is authorized. Existing preflight scenarios remain
unchanged; this correction adds no user-visible behavior.

Forecast: one hour, with a thirty-minute assessment of remaining work, exact
scope, cost, and confidence. Refactorer and architect review independently.
Only exact architect qa-ready evidence permits QA integration.

## Configuration resumption

Preserve preparation candidate `1bb6488272` and its entire delta from `87f5919e05`
as an immutable reference. Do not mix it into this process correction. After QA
accepts the correction, automatically reissue
`verification-slice-complete-configuration-portability` on that exact descendant.
Conserve the preparation delta, review any conflict, and obtain fresh exact
preparation evidence through the supported incident route. Then resume approved
product task `complete-configuration-portability` after preparation QA.

Specification assessment: cause confirmed by source comparison and a direct
failed check. Correction runtime proof: pending. The earlier process review
missed this conservation consumer; keep that gap in the process scorecard.
Do not expand this bounded correction into a general ownership audit.

## Accepted-base specification inventory correction

Coder note `20260913T075807Z_000079_from_coder` reports candidate `9d0af5c2`
on specification `247bbbd76d`. Direct correction checks pass, but review
execution stops before tasks because approved configuration feature files have
no inventory assignment. No review receipt exists. This is a separate
specification inventory omission, not recurrence of the host assertion defect.

Continue the same task and preserve the five-file correction delta from
`247bbbd76d..9d0af5c2` when applying this specification update. Add only these
existing approved contracts to `project_management.plannedFeatures` in the
canonical manifest and regenerate the registry through its existing generator:

- `features/configuration-portability-ownership-preparation.feature`
- `features/complete-configuration-portability.feature`
- `features/complete-configuration-portability-runtime.feature`

This is bounded maintenance of the accepted specification inventory. The
existing inventory validator admits planned features. Do not activate these
contracts as executable features, add their handlers, add the proposed source
prefix or slice, change consumers, or import the paused preparation code.
The sole exception to the earlier registry restriction permits these three
planned inventory assignments and their canonical generated output.
Preserve all existing executable task identities and current/base obligations.

Prove inventory validation succeeds and the added planned entries introduce no
executable acceptance tasks. Repeat exact read-only planning on the settled
candidate; the reported 222-task, 13-pack correction scope remains a forecast.
Then obtain fresh focused review evidence with properties and package proof.
Do not omit selected checks or change evidence policy to preserve that forecast.
The existing one-hour correction estimate and independent reviews still apply.

Preserve preparation `1bb6488272` unchanged as the reference. On its later
resumption, recognize these exact three entries as already integrated, prove
their equality, and conserve all remaining preparation changes. Do not remove
or duplicate the entries merely to replay the old patch mechanically.

The specifier's configuration specifications omitted planned inventory
registration. Record this process failure with the correction scorecard.
The blocker has a supported existing representation; no incident deferral,
base rollback, new task, or new verifier mechanism is needed.
