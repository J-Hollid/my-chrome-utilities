# Tealium preparation Stage A: coder record

Task: `ownership-prep-tealium-live`. Specification base:
`a3034336ad5973d1b57b818a0465eb7c434b78b8`.
This is an intermediate verification-support stage. It does not complete the
preparation or permit the Tealium product to resume.

## Change and specification checks

- A manifest adapter reads immutable Git snapshots. Only a semantic change to
  `devtools_page` can use its field-qualified declaration. Duplicate decoded JSON
  keys, invalid local HTML paths, absent or ambiguous delivery, missing entries,
  and every other changed field retain the existing global path rule.
- The exact before/after commits, path list, and status entries bind the evidence.
  Copying or changing the evidence object cannot supply a focused claim.
  The accepted base must contain the executing adapter and its build/selection
  policy. Policy or registry changes retain conservative selection in that range.
- The packaged-entry check requires an existing regular, nonempty HTML resource
  and the unchanged build's exact source/destination delivery declaration, or an
  existing static HTML entry. Root and packaged resource names remain equal.
- Path-only manifest queries stay conservative. The background composition
  declaration retains the 12 installed host consumers and a terminal obligation.
  The manifest declaration retains the same consumers and terminal obligation.
- All old runnable-pack tasks are compared with the immutable specification
  registry. Every old task and terminal task remains present. The non-runnable
  catalogue entry is unchanged. Four direct adapter tests are added.
- The manifest, build, background, permissions, and browser product are unchanged.

The current Stage A intent is `bounded-ready`: Shell and Verification Process,
167 planned tasks before the new tests and exact-candidate calculation. This is
a planning result, not test evidence.

## Observed development checks

The field, Git evidence, planner, and registry-conservation tests exercise real
temporary Git repositories. They prove focused selection, invalid and missing
evidence fallback, policy conservation, and retained terminal task lists. The
existing architecture-declaration planner regression also passes.

These are verification-tool runtime checks. They do not prove a running Tealium
page, DevTools navigation, or Stage B background extraction. Fresh focused
evidence and package proof are required before forwarding Stage A.

The first exact candidate plan selected two packs and 177 tasks. Evidence
prelaunch stopped first on two pre-existing untracked files, then on duplicate
background ownership. The files were preserved under
`tmp/tealium-stage-a-preserved/` for restoration after the handoff. The old
platform group no longer repeats the new background composition path. Full
registry validation now runs in the direct conservation test. No product test
had executed at either stop.
Full registry validation then found the six approved Tealium specifications
unassigned in the received base. They are registered as planned features only;
no product scenario was made executable or claimed satisfied. Full registry
validation and task conservation pass after these two declaration corrections.

The first executing review run selected 184 tasks with properties and packaging.
It found a causal stale-count assertion in
`test/verification-contracts/exact-slice-shell-ownership-contract-test.mjs`:
Stage A added four direct checks to the expected 19-task closure. Incident
`053d6795-cfb6-461d-babe-062cdebc3ee0` retains the failed receipt. The repair
asserts all four additions exactly once and preserves the original 19-task
projection and its digest. The failing leaf passes. The repair proof replays
the rejected old count and records the conserved original tasks. Fresh governed
repair and review evidence remain required; the incident is not a pass.
The governed repair also found an old background-global expectation in
`ownership-shell-contract-test.mjs`. Its table now checks the independent
installed-host consumer list for background composition. Every other platform
path still requires global ownership. This is part of the same declaration
conservation repair; it does not open another task or change runtime behavior.
The historical utility-boundary calibration then exposed the same moved
background path in its old global projection. The historical copy now restores
both earlier root boundaries while the current copy separately checks each
approved consumer list. A direct guard-selection test also found that the
shared-boundary helper lacked a consumer relation to the new adapter checks.
Its Shell slice retains the existing boundary tests and selects
`verification_process.task_batching`. Parent task conservation still passes.

The next review run found stale generated compact records for the changed Shell
ownership test. The canonical generator changed only that source identity and
checksum. The assertion inventory and semantic output remain unchanged. A small
repair helper rejects the exact stale record from commit `861de2e016` and checks
the regenerated record. Both failed review leaves remain recorded for governed
repair and fresh review evidence.
The modularization check also assumed historical planning had no consumers.
Its extracted closure assertion now includes the required task-batching tests
only for that caller. Both failed leaves pass after these focused repairs.

## Process and next stage

The first structural sequence could not retain old/current ownership and also
obtain a bounded plan. The corrected specification separated this adapter stage.
Queue replacement retained an old lease; the existing activity helper cleared
that exact identity. No queue-system code changed.

Tests were written before each implementation step. Fixture corrections included
the browser-observation path, non-runnable catalogue handling, and deletion of
an intentionally modified fixture manifest. These did not change product rules.
Serena outlines helped locate the build and adapter boundaries. The canonical
ownership query supplied consumers. Some retrieval output was cut off and was
recovered with smaller reads; avoid large combined instruction reads.

After independent Stage A review and QA integration, reissue this same task for
Stage B. Keep `scripts/build.mjs` unchanged. Prove the background extraction and
installed consumers from that new base. A later product can change only the root
manifest's valid `devtools_page` field and its actual delivered entry; a path-only
intent remains conservative until canonical committed field evidence exists.
