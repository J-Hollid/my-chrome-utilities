# Shell acceptance prerequisite recovery

User instruction on 2026-09-14: find the path to proceeding and get it going.
This authorizes the bounded causal prerequisite repair below now, superseding
prior task-local instructions to leave this Shell omission for master. It does
not authorize a full regression run, incident waiver, or new evidence framework.

Task: `shell-acceptance-prerequisite-recovery`. Start from accepted QA
`a8e428b2fb` and this specification descendant. Preserve declaration candidate
`ffe4609aa72a9af09befc6c86dbf0fe2b5b98708`, its exact base
`a8e428b2fb72faf5f7b6cba7b44e585b3c0e83ff`, and combined product `4ee990e6`.

## Completed bounded discovery

Repair family: `shell-acceptance-prerequisite-closure`.
Boundary: `modular-architecture-shell-session`.
Discovery is complete for the observed feature's prepared-task consumers and
its failed focused declaration plan. No repository-wide audit is required.

The accepted-QA declaration range selects 17 packs and 1,075 tasks. Its Shell
session consumes prepared evidence absent from the plan. The current and
historical populations agree. Incident
`cf934caa-4a5b-40de-984d-f77dfe583b63` records the first missing task.

The bounded scan of `modular_architecture*.clj` prepared-task consumers found
these seven missing `unit:test/verification-contracts/` tasks, including after
existing execution-prerequisite expansion:

- `execution-binding-contract-test.mjs`
- `reliability-regression-routing-contract-test.mjs`
- `execution-attempt-store-contract-test.mjs`
- `reliability-incident-store-contract-test.mjs`
- `execution-runner-integration-contract-test.mjs`
- `reliability-prerequisite-contract-test.mjs`
- `execution-coordinator-contract-test.mjs`

Defects: `missing-shell-prepared-prerequisites` and
`missing-focused-shell-closure-regression`. Current administration dependency
checks validate producer/consumer text bindings but do not prove this focused
Shell session has every consumed prerequisite.

## Required result

Use the existing acceptance-session prerequisite declarations and closure logic
to schedule these consumed tasks before Shell acceptance, once per task, with
canonical identities. Keep all historical coverage and unrelated pack selection.
An absent canonical producer must still fail before execution. Preserve strict
receipt consumption; no fallback execution, synthetic production receipt, task
suppression, or omission-to-pass conversion.

Check the proposed source impact before editing. Expected implementation surface:
`scripts/verification-acceptance-session-prerequisites.mjs`, a small focused
regression in the existing administration dependency test boundary, and only the
existing repair-protocol integration needed for this incident. Split an oversized
test/helper if necessary. Do not extend the runner monolith or add a new platform.
The known existing protocol seam is
`test/fixtures/verification-administration-repair-protocol.mjs`.

Test the real canonical focused declaration shape, not an all-pack plan. Prove
all seven tasks are present before the consuming session, are not duplicated,
and missing canonical producers remain rejected. Prove the negative baseline
and corrected result with a deterministic regression. Check the strict prepared
receipt consumer that previously failed. Include any directly exposed same-family
omissions in this continuous repair; do not request a new task or grant for each.

Use the existing governed causal-repair route for the preserved incident, with
fresh exact repair evidence, required properties, and package proof. Then use
ordinary eligible-repair admission and terminal deferral only when the existing
helpers accept all required identities and proof. Do not label it unrelated or
flaky, abandon it, rewrite its source receipt, or claim it resolved before the
required later terminal proof. If a concrete helper rejects the changed repair,
report its exact rejection and inputs; do not stop at the old absence of an
unrelated-deferral route, which this causal repair replaces.

## Delivery and automatic resumption

Obtain independent refactorer and architect review and exact QA-ready evidence
for the repair. Use focused checks; no all-runnable-pack gate. Forecast: 45 minutes
with a 20-minute assessment. Keep cost and any scope change visible.

After repair QA integration, automatically resume
`verification-slice-portability-durable-state-declaration` with the conserved
`ffe4609a` remainder from that accepted descendant. Obtain fresh stage evidence
and QA acceptance. Then resume `complete-configuration-portability` with all
remaining product changes and original runtime acceptance requirements. The
specifier owns each QA integration and resumption handoff. Master stays unchanged.
