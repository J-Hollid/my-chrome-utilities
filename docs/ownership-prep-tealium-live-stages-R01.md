# Tealium ownership preparation: staged correction R01

Status: standing-authorized on 2026-09-09. This corrects sequencing within
`ownership-prep-tealium-live`; it does not create another task or change Tealium.
It takes precedence over the sequence in the parent preparation specification.

## Failed premise

Coder note `20260909T144505Z_000031_from_coder` proved that the first proposed
structural range remains global: 21 packs, 941 tasks, estimated 941,000 ms at
`b5733102819a0e90cba4ea0c18e7e0da0d276932`. No test execution or product change
occurred. The historical/current ownership union correctly retains the former
owners. Path-based shared boundaries cannot distinguish a DevTools declaration
from a permission change. An unused declaration file does not fix this problem.

The specifier accepts this failed premise. Do not try the same combined range
again or bypass the historical union. Instead, independently integrate the
declaration support and conservation checks before any structural extraction.

## Stage A: declaration support and conservation

Leave `manifest.json`, `scripts/build.mjs`, `src/background.ts`, and all browser
product source unchanged. Add only the narrow verification support and direct
checks needed to establish a safe later route.

The existing architecture-declaration adapter demonstrates a suitable local
pattern: canonical Git snapshots bind immutable before/after evidence to the
change set; absent evidence retains ordinary selection. Extend this pattern
for the manifest in small separate modules. This specific adapter is authorized
within the preparation. The prohibition on a new planner framework remains.
Do not redesign verification, add incident exceptions, or hard-code task names.

Permit a focused manifest declaration result only when canonical evidence proves
that `devtools_page` is the only semantic field changed. Validate its supported
local resource form and the packaged entry. Retain the complete existing global
fallback for permission, command, background, host, metadata, unknown-field,
mixed-field, invalid, unavailable, added/deleted, or ambiguous manifest changes.
A filename or caller assertion alone is not evidence. Reject ambiguous duplicate
JSON keys or retain global fallback; parsing must not hide a changed field.

Require the adapter and applicable declarations in the accepted base. Changes
to their implementation, declarations, consumers, or eligibility rules cannot
use their own new policy to narrow that same range. Preserve canonical ancestry,
actual change-set identity, historical/current union, exact-pack closure, and
terminal-full behavior. Path-only intent without field evidence remains
conservative; report the reason rather than fabricate a bounded intent result.

Declare the manifest's field-qualified observation and the existing background
composition observation with every real consumer and existing terminal-full
obligation. The field check must run before a shared path rule can narrow a
manifest change. A blanket narrow `manifest.json` declaration is prohibited.
Background staging must retain repository startup, errors, action and command
gestures, active-tab fallback, Hotkeys focus, message consumers, and installed
host checks. Retain global terminal coverage for its broader reach.

Use exact direct checks to prove focused selection, conservative negative cases,
current/base conservation, and terminal completeness. Include immutable Git
fixtures for canonical and forged/stale/missing evidence. Fixtures must compare
expected consumers and task closures independently of the selector under test.
Keep test families small. No real DevTools page or permission change belongs here.

The likely adapter prefix is
`scripts/verification-planner/manifest-declarations/`, owned by
`verification_process.task_batching`. Its caller and tests must be in the same
exact closure. Existing candidate paths are
`scripts/verification-planner/tasks/planner.mjs`,
`scripts/verification-planner/history/changes.mjs`,
`scripts/verification-planner/ownership/affected-path.mjs`,
`scripts/verification-shared-boundaries.mjs`, and the Shell and
Verification Process declarations in the canonical registry. Omit unnecessary
changes and declare any additional direct test paths in intent.

The specifier ran read-only intent for these paths and the proposed prefix from
the exact QA base above. It is `bounded-ready`: Shell and Verification Process,
176 tasks, estimated 176,000 ms, no expansion causes or terminal obligations.
The saved result is `docs/tealium-live-experiment-evidence/preparation-stage-a-intent.json`.
This proves a planning route only. It does not prove the implementation, exact
candidate plan, consumer mapping, or installed behavior. Repeat intent for the
actual chosen paths, then obtain exact committed preflight and one settled
focused review-ready run with properties and package proof. Use the normal
refactorer and architect route. Do not run all packs or acceptance mutation.

## Stage B: prove the structural route from integrated Stage A

After architect review and QA integration of Stage A, the specifier reissues the
same `ownership-prep-tealium-live` task from that exact implementation-bearing
QA head. Mark the Stage A result explicitly as an intermediate stage. It cannot
satisfy the product prerequisite by itself.

Use the independently integrated rules for the remaining background extraction
and its direct checks. Keep the manifest semantically unchanged in preparation.
The existing build copies the root manifest unchanged. A field-safe direct edit
to that manifest in the later product can therefore preserve both installation
routes without a build composer. Prefer this smaller route; an unused manifest
contribution module is not required. Leave `scripts/build.mjs` unchanged unless
a separate concrete need is proved. Its existing global fallback remains.

Run one Stage B plan for the actual extraction paths from the new QA base, then
the required independent review and exact evidence. Do not transfer Stage A
evidence to Stage B. Record the exact later product touch paths, installed
consumer proof, terminal obligations, and the satisfied preparation scope.
Only the final architect-reviewed preparation implementation in QA permits the
specifier to reissue `tealium-live`. If Stage A proves that an extraction is
unnecessary, the architect must review that exact conclusion and all remaining
preparation obligations before the specifier records final satisfaction.

## Authority, limits, and progress

The two stages use the existing approved outcome and planning allowance. No new
user decision is needed. Preserve the same task identity and use official queue
helpers. Keep the product paused. Do not repeat the failed combined intent or
start a new discovery loop; report any new failed premise and its exact effect.
The nonblocking progress-lease observation in the coder note is outside this
preparation and does not authorize a queue-system change.

Scorecard: the original one-stage sequence failed; the revised Stage A intent
is bounded; runtime and consumer-conservation proof are pending. Refine future
preparation specifications by checking base/current sequencing before routing
structural changes.
