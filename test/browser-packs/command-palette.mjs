import { runRenderedWorkflow, workflowPreamble } from "./shared-harness.mjs";
await runRenderedWorkflow("command-palette",`${workflowPreamble}q('#open-palette').click();q('#palette-filter').value='hello';q('#palette-filter').dispatchEvent(new Event('input',{bubbles:true}));return {passed:visible(q('#palette'))&&document.activeElement===q('#palette-filter')&&q('#palette-results').children.length>0,width:innerWidth,overflow:document.documentElement.scrollWidth>innerWidth};`);
