import assert from "node:assert/strict";
import {runRenderedWorkflow,workflowPreamble} from "./shared-harness.mjs";

const setup=`
  const {createSpecificationProject}=await import('./data-layer-specification-project.js'),durable=await import('./data-layer-durable-project-repository.js'),separation=await import('./data-layer-property-set-flow-section.js'),repository=await durable.openIndexedDbProjectRepository(),entity=(id,name,extra={})=>({id,name,...extra}),make=(projectId,name)=>createSpecificationProject({name,site:'shop.example',id:(kind)=>kind==='project'?projectId:kind+':'+projectId});
  const legacy=make('project:shop','Shop');legacy.project.collections.profiles=[entity('profile:sitewide','Sitewide',{requirements:[]}),entity('profile:commerce','Commerce',{requirements:[]})];legacy.project.collections.pageGroups=[entity('property-set:checkout','Checkout base',{schemaConstraints:[{path:'/funnel_step',expectedValue:'checkout'},{path:'/checkout_type',documentation:'Checkout'}],pageIds:['page:cart'],profileIds:['profile:commerce']}),entity('property-set:retail','Retail commerce',{schemaConstraints:[{path:'/funnel_step',expectedValue:'retail'}],applicabilitySetId:'applicability:retail'})];legacy.project.collections.pages=[entity('page:cart','Cart',{pageGroupIds:['property-set:checkout','property-set:retail'],localSchemaContributions:[{path:'/checkout_type',documentation:'Cart description'}]}),entity('page:product','Product detail',{pageGroupIds:['property-set:checkout','property-set:retail']})];legacy.project.collections.events=[entity('event:view','Page View',{eventName:'page_view'})];legacy.project.collections.applicabilitySets=[entity('applicability:retail','Retail customers',{condition:{kind:'predicate',field:'segment',operator:'equals',value:'retail'}})];legacy.project.collections.flows=[entity('flow:checkout','Checkout journey')];legacy.project.collections.assignments=[entity('assignment:retail','Retail checkout',{targetKind:'Property Set',targetId:'property-set:checkout',eventId:'event:view',eventName:'page_view',sourceId:'browser',target:'payload',priority:10})];legacy.project.documentationFlowGraphs={'flow:checkout':{pageGroupIds:['property-set:checkout','property-set:retail'],sections:[],pageFrames:[entity('frame:cart','Cart',{pageId:'page:cart',pageGroupId:'property-set:checkout',position:{x:60,y:100}}),entity('frame:product','Product detail',{pageId:'page:product',pageGroupId:'property-set:retail',position:{x:380,y:280}})],occurrences:[entity('occurrence:view','Page View',{pageFrameId:'frame:cart',pageId:'page:cart',eventId:'event:view'})],relationships:[entity('relationship:next','Cart to Product',{sourceEndpoint:{kind:'page-frame',id:'frame:cart'},targetEndpoint:{kind:'page-frame',id:'frame:product'},sourcePort:'right',targetPort:'left'})]}};
  const active=separation.upgradePageGroupsToPropertySets(legacy,(kind)=>kind+':active:'+crypto.randomUUID()),activeCart=active.project.collections.pages.find(({id})=>id==='page:cart'),activeProduct=active.project.collections.pages.find(({id})=>id==='page:product'),activeGraph=active.project.documentationFlowGraphs['flow:checkout'];delete activeCart.propertySetApplications.find(({propertySetId})=>propertySetId==='property-set:retail').applicabilitySetId;activeProduct.propertySetApplications.find(({propertySetId})=>propertySetId==='property-set:retail').applicabilitySetId='applicability:retail';active.project.collections.pages.push(entity('page:help','Help',{propertySetApplications:[]}));activeGraph.sections=[];for(const frame of activeGraph.pageFrames)delete frame.sectionId;await repository.putProjectMetadataOnly(active,{active:true,draftToken:'shop-separation-1',draftSequence:1,publishedRevision:0,navigation:{kind:'pages',id:'page:cart'}});
  const migration=make('project:migration','Legacy migration');migration.project.collections=structuredClone(legacy.project.collections);migration.project.documentationFlowGraphs=structuredClone(legacy.project.documentationFlowGraphs);migration.project.collections.assignments[0].targetKind='Page Group';const migrationGraph=migration.project.documentationFlowGraphs['flow:checkout'];migrationGraph.pageFrames.push(entity('frame:free','Product detail outside',{pageId:'page:product',freePageRegion:'after-lanes',position:{x:24,y:410}}));migrationGraph.occurrences.push(entity('occurrence:free','Page View outside',{pageFrameId:'frame:free',pageId:'page:product',eventId:'event:view',position:{x:24,y:70}}));migrationGraph.relationships.push(entity('relationship:free','Outside to Product',{sourceEndpoint:{kind:'page-frame',id:'frame:free'},targetEndpoint:{kind:'page-frame',id:'frame:product'},sourcePort:'right',targetPort:'left'}));await repository.putProjectMetadataOnly(migration,{active:false,draftToken:'migration-separated-1',draftSequence:1,publishedRevision:0});
  return true;
`;

const workflow=`
  ${workflowPreamble}
  const pause=(ms=30)=>new Promise(resolve=>setTimeout(resolve,ms)),until=async(test,label)=>{for(let attempt=0;attempt<240;attempt+=1){const value=await test();if(value)return value;await pause();}throw new Error('Timed out: '+label+' · '+(document.querySelector('iframe')?.contentDocument?.body?.innerText??''));},button=(text,root=document)=>[...root.querySelectorAll('button')].find(control=>control.textContent.trim()===text),repository=await (await import('./data-layer-durable-project-repository.js')).openIndexedDbProjectRepository();
  q('#data-layer-view-schemas').click();await until(()=>q('#schema-list').textContent.includes('Property Sets'),'Property Set schema tree');const treeText=q('#schema-list').textContent,tree001=treeText.includes('Checkout base')&&treeText.includes('Cart')&&!treeText.includes('Page Group')&&!treeText.includes('Section');
  const studioFrame=document.createElement('iframe');studioFrame.title='Property Set and Flow Section production workspace';studioFrame.style.cssText='inline-size:100%;block-size:820px;border:0';studioFrame.src='./specification-builder.html?project=project%3Ashop&kind=pages&entity=page%3Acart';document.body.append(studioFrame);await until(()=>studioFrame.contentDocument?.querySelector('[data-project-entity-workspace="page:cart"]'),'Cart workspace');const studio=studioFrame.contentWindow,studioDoc=studio.document;await until(()=>studioDoc.querySelector('[aria-label="Page details"]'),'hydrated Cart details');const studioButton=(text,root=studioDoc)=>[...root.querySelectorAll('button')].find(control=>control.textContent.trim()===text),composition=await until(()=>studioDoc.querySelector('[aria-label^="Property composition"]'),'Property composition'),nav=studioDoc.querySelector('#project-tree').textContent,compositionText=composition.textContent;
  const runtime001=nav.includes('Property Sets')&&!nav.includes('Page Groups')&&compositionText.includes('Checkout base')&&compositionText.includes('Retail commerce')&&compositionText.includes('Property Set');
  const runtime002=compositionText.includes('general-to-specific')&&compositionText.includes('applicability')&&compositionText.includes('provenance')&&studioButton('Open Property Set',composition)&&studioButton('Move earlier',composition)&&studioButton('Remove application',composition)&&composition.querySelector('[aria-label="Search Property Sets to apply"]')&&!compositionText.includes('Page belongs to');
  const layeredProject=await import('./data-layer-layered-schema-project.js'),layered=await import('./data-layer-layered-schema.js'),compilePage=(loaded,pageId,observation)=>{const page=loaded.state.project.collections.pages.find(({id})=>id===pageId),path=layeredProject.layeredContributorPath(loaded.state,page,'Page'),contributors=layeredProject.layeredContributorsForPath(loaded.state,path,observation);return{contributors,compiled:layered.compileLayeredSchema(contributors,{eventId:'event:view',eventRole:'interaction'})};},beforeOrder=await repository.loadProject('project:shop'),beforeSequence=beforeOrder.draftSequence,beforeCompiled=compilePage(beforeOrder,'page:cart',{segment:'retail'}).compiled,moveEarlier=[...composition.querySelectorAll('button')].filter(control=>control.textContent.trim()==='Move earlier').at(-1);if(!moveEarlier)throw new Error('Move earlier missing: '+composition.textContent);moveEarlier.click();await pause(120);const reviewText=composition.querySelector('[aria-label="Property composition reorder review"]').textContent,sequenceDuringReview=(await repository.loadProject('project:shop')).draftSequence,confirmOrder=studioButton('Confirm application reorder',composition);if(!confirmOrder)throw new Error('Application reorder review missing: '+composition.textContent);confirmOrder.click();await until(async()=>{const loaded=await repository.loadProject('project:shop');return loaded.draftSequence===beforeSequence+1;},'one application reorder');const afterOrder=await repository.loadProject('project:shop'),cartAfter=afterOrder.state.project.collections.pages.find(({id})=>id==='page:cart'),orderedIds=cartAfter.propertySetApplications.map(({propertySetId})=>propertySetId),afterCompiled=compilePage(afterOrder,'page:cart',{segment:'retail'}).compiled,runtime003=beforeCompiled.properties['/funnel_step'].expectedValue==='retail'&&afterOrder.draftSequence===beforeSequence+1&&sequenceDuringReview===beforeSequence&&orderedIds.join('|')==='property-set:retail|property-set:checkout'&&afterCompiled.properties['/funnel_step'].expectedValue==='checkout'&&afterCompiled.properties['/funnel_step'].superseded.some(({contributorName})=>contributorName==='Retail commerce')&&reviewText.includes('/funnel_step')&&reviewText.includes('Checkout base')&&reviewText.includes('Retail commerce')&&reviewText.includes('invariant');
  const sets=afterOrder.state.project.collections.propertySets,retail=sets.find(({id})=>id==='property-set:retail'),cart=cartAfter,product=afterOrder.state.project.collections.pages.find(({id})=>id==='page:product'),applications=(page)=>page.propertySetApplications,applicationBytes=JSON.stringify({sets,applicabilitySets:afterOrder.state.project.collections.applicabilitySets,pages:[cart,product]}),cartWholesale=compilePage(afterOrder,'page:cart',{segment:'wholesale'}),productWholesale=compilePage(afterOrder,'page:product',{segment:'wholesale'}),productRetail=compilePage(afterOrder,'page:product',{segment:'retail'}),runtime004=applications(cart).find(({propertySetId})=>propertySetId===retail.id).applicabilitySetId===undefined&&applications(product).find(({propertySetId})=>propertySetId===retail.id).applicabilitySetId==='applicability:retail'&&cartWholesale.contributors.find(({id})=>id===retail.id).active!==false&&productWholesale.contributors.find(({id})=>id===retail.id).active===false&&productRetail.contributors.find(({id})=>id===retail.id).active===true&&productWholesale.compiled.properties['/funnel_step'].expectedValue==='checkout'&&productRetail.compiled.properties['/funnel_step'].expectedValue==='retail'&&JSON.stringify({sets:afterOrder.state.project.collections.propertySets,applicabilitySets:afterOrder.state.project.collections.applicabilitySets,pages:[cart,product]})===applicationBytes;
  const separated=await import('./data-layer-property-set-flow-section.js'),durable=await import('./data-layer-durable-project-repository.js'),changedState=separated.changePropertySetSchema(afterOrder.state,'property-set:checkout',[{path:'/funnel_step',expectedValue:'checkout'},{path:'/checkout_type',documentation:'Changed parent description'},{path:'/checkout_version',type:'string'}]),changedResult=await repository.saveDraft(durable.durableDraftCommand(afterOrder,changedState,{commandId:'change-checkout-schema',label:'Change Checkout base schema'}));if(changedResult.status==='conflict')throw new Error('Checkout schema change conflicted');const changed=await repository.loadProject('project:shop'),cartChanged=compilePage(changed,'page:cart',{segment:'retail'}).compiled,productChanged=compilePage(changed,'page:product',{segment:'retail'}).compiled,stagedState=separated.stagePropertySetParentAddition(changed.state,'property-set:checkout','profile:commerce',{path:'/commerce_new',type:'string'}),stagedResult=await repository.saveDraft(durable.durableDraftCommand(changed,stagedState,{commandId:'stage-parent-addition',label:'Stage Commerce Parent addition'}));if(stagedResult.status==='conflict')throw new Error('Parent addition staging conflicted');const staged=await repository.loadProject('project:shop'),stagedCart=compilePage(staged,'page:cart',{segment:'retail'}).compiled,stagedProduct=compilePage(staged,'page:product',{segment:'retail'}).compiled,includedState=separated.includePropertySetParentAddition(staged.state,'property-set:checkout','/commerce_new'),includedResult=await repository.saveDraft(durable.durableDraftCommand(staged,includedState,{commandId:'include-parent-addition',label:'Include Commerce Parent addition'}));if(includedResult.status==='conflict')throw new Error('Parent addition inclusion conflicted');const included=await repository.loadProject('project:shop'),includedCart=compilePage(included,'page:cart',{segment:'retail'}).compiled,includedProduct=compilePage(included,'page:product',{segment:'retail'}).compiled,runtime005=cartChanged.properties['/checkout_type'].documentation==='Cart description'&&productChanged.properties['/checkout_type'].documentation==='Changed parent description'&&Boolean(cartChanged.properties['/checkout_version'])&&Boolean(productChanged.properties['/checkout_version'])&&!stagedCart.properties['/commerce_new']&&!stagedProduct.properties['/commerce_new']&&Boolean(includedCart.properties['/commerce_new'])&&Boolean(includedProduct.properties['/commerce_new'])&&included.state.project.collections.propertySets.find(({id})=>id==='property-set:checkout').parentAdditions.length===0;
  const assignment=afterOrder.state.project.collections.assignments[0],runtime006=assignment.targetKind==='Property Set'&&assignment.targetId==='property-set:checkout'&&treeText.includes('Property Sets')&&!treeText.includes('Sections');
  studio.location.href='./specification-builder.html?project=project%3Ashop&kind=flows&entity=flow%3Acheckout';let sectionWorkspace=await until(()=>studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]'),'Flow Section workspace');await pause(700);sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');const initialFlow=await repository.loadProject('project:shop'),initialGraph=initialFlow.state.project.documentationFlowGraphs['flow:checkout'],oneCanvas=studio.document.querySelectorAll('[aria-label="Interactive directional Flow canvas"]').length===1&&Boolean(studio.document.querySelector('.documentary-flow')),preservedPresence=Boolean(sectionWorkspace.querySelector('[data-occurrence-id="occurrence:view"]')&&sectionWorkspace.querySelector('[data-relationship-id="relationship:next"]')&&sectionWorkspace.querySelector('[data-output-port-for="frame:cart"]')&&sectionWorkspace.querySelector('[data-input-port-for="frame:product"]')),
  outline=sectionWorkspace.querySelector('[aria-label="Synchronized editable Flow outline"]'),cartOutline=outline.querySelector('[data-page-frame-id="frame:cart"] button');cartOutline.click();sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');const pageOutlineOperated=Boolean(sectionWorkspace.querySelector('[aria-label="Selected Page instance inline actions"]')),occurrenceOutline=sectionWorkspace.querySelector('[aria-label="Synchronized editable Flow outline"] [data-occurrence-id="occurrence:view"] button');occurrenceOutline.click();sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');let occurrenceActions=sectionWorkspace.querySelector('[aria-label="Selected Event occurrence inline actions"]');const occurrenceOutlineOperated=Boolean(occurrenceActions&&occurrenceActions.querySelector('[aria-label="Containing Page frame"]'));
  let occurrenceNode=sectionWorkspace.querySelector('[aria-label="Interactive directional Flow canvas"] [data-occurrence-id="occurrence:view"]');occurrenceNode.focus();const occurrenceStoredBefore=initialGraph.occurrences.find(({id})=>id==='occurrence:view').position,occurrenceBefore=structuredClone(occurrenceStoredBefore??{x:24,y:70});occurrenceNode.dispatchEvent(new PointerEvent('pointerdown',{pointerId:51,clientX:100,clientY:100,bubbles:true}));studio.dispatchEvent(new PointerEvent('pointerup',{pointerId:51,clientX:100,clientY:85,bubbles:true}));await until(async()=>{const loaded=await repository.loadProject('project:shop'),position=loaded.state.project.documentationFlowGraphs['flow:checkout'].occurrences.find(({id})=>id==='occurrence:view').position;return position?.y===Math.max(55,occurrenceBefore.y-15);},'occurrence pointer move');studio.document.querySelector('#undo-project').click();await until(async()=>{const loaded=await repository.loadProject('project:shop'),position=loaded.state.project.documentationFlowGraphs['flow:checkout'].occurrences.find(({id})=>id==='occurrence:view').position;return occurrenceStoredBefore?position?.x===occurrenceBefore.x&&position?.y===occurrenceBefore.y:position===undefined;},'occurrence move undo');
  sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');sectionWorkspace.querySelector('[aria-label="Synchronized editable Flow outline"] [data-occurrence-id="occurrence:view"] button').click();sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');occurrenceActions=sectionWorkspace.querySelector('[aria-label="Selected Event occurrence inline actions"]');const pageChoice=occurrenceActions.querySelector('[aria-label="Containing Page frame"]');pageChoice.value='frame:product';pageChoice.dispatchEvent(new Event('change',{bubbles:true}));studioButton('Confirm Page change',occurrenceActions).click();await until(async()=>{const loaded=await repository.loadProject('project:shop');return loaded.state.project.documentationFlowGraphs['flow:checkout'].occurrences.find(({id})=>id==='occurrence:view').pageFrameId==='frame:product';},'occurrence Page reassignment');studio.document.querySelector('#undo-project').click();await until(async()=>{const loaded=await repository.loadProject('project:shop');return loaded.state.project.documentationFlowGraphs['flow:checkout'].occurrences.find(({id})=>id==='occurrence:view').pageFrameId==='frame:cart';},'occurrence Page reassignment undo');
  sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');sectionWorkspace.querySelector('[aria-label="Synchronized editable Flow outline"] [data-occurrence-id="occurrence:view"] button').click();sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');occurrenceActions=sectionWorkspace.querySelector('[aria-label="Selected Event occurrence inline actions"]');studioButton('Duplicate occurrence',occurrenceActions).click();const duplicateOccurrence=await until(async()=>{const loaded=await repository.loadProject('project:shop');return loaded.state.project.documentationFlowGraphs['flow:checkout'].occurrences.find(({id})=>id!=='occurrence:view');},'duplicated occurrence');sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');sectionWorkspace.querySelector('[aria-label="Synchronized editable Flow outline"] [data-occurrence-id="'+duplicateOccurrence.id+'"] button').click();sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');studioButton('Remove',sectionWorkspace.querySelector('[aria-label="Selected Event occurrence inline actions"]')).click();await until(async()=>{const loaded=await repository.loadProject('project:shop');return loaded.state.project.documentationFlowGraphs['flow:checkout'].occurrences.length===1;},'removed duplicated occurrence');
  sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');const occurrenceDetails=sectionWorkspace.querySelector('[aria-label="Synchronized editable Flow outline"] [data-occurrence-id="occurrence:view"] details');occurrenceDetails.open=true;const occurrenceExampleOperated=occurrenceDetails.open&&Boolean(occurrenceDetails.querySelector('[data-readonly-example="occurrence:view"]'));sectionWorkspace.querySelector('[aria-label="Synchronized editable Flow outline"] [data-occurrence-id="occurrence:view"] button').click();sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');occurrenceActions=sectionWorkspace.querySelector('[aria-label="Selected Event occurrence inline actions"]');studioButton('Open schema contribution',occurrenceActions).click();const occurrenceSchemaOpened=await until(()=>studioButton('Return to Flow',studio.document),'occurrence schema contribution');studioButton('Return to Flow',studio.document).click();await until(()=>studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]'),'returned from occurrence schema');
  sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');const keyboardPort=sectionWorkspace.querySelector('[aria-label="Interactive directional Flow canvas"] [data-output-port-for="frame:product"]');keyboardPort.dispatchEvent(new KeyboardEvent('keydown',{key:'Enter',bubbles:true}));keyboardPort.dispatchEvent(new KeyboardEvent('keydown',{key:'Enter',bubbles:true}));const temporaryRelationship=await until(async()=>{const loaded=await repository.loadProject('project:shop');return loaded.state.project.documentationFlowGraphs['flow:checkout'].relationships.find(({id})=>id!=='relationship:next');},'keyboard relationship');let relationshipPopover=await until(()=>studio.document.querySelector('[aria-label="Inline relationship popover"]'),'relationship popover');const optionalLabel=relationshipPopover.querySelector('[aria-label="Optional relationship label"]');optionalLabel.value='Temporary keyboard edge';relationshipPopover.requestSubmit();await until(async()=>{const loaded=await repository.loadProject('project:shop');return loaded.state.project.documentationFlowGraphs['flow:checkout'].relationships.find(({id})=>id===temporaryRelationship.id)?.label==='Temporary keyboard edge';},'relationship edit');const relationshipFocused=await until(()=>studio.document.activeElement?.dataset.relationshipId===temporaryRelationship.id,'saved relationship focus');studio.document.activeElement.dispatchEvent(new MouseEvent('click',{bubbles:true}));relationshipPopover=await until(()=>studio.document.querySelector('[aria-label="Inline relationship popover"]'),'edited relationship popover');studioButton('Delete relationship',relationshipPopover).click();await until(async()=>{const loaded=await repository.loadProject('project:shop');return !loaded.state.project.documentationFlowGraphs['flow:checkout'].relationships.some(({id})=>id===temporaryRelationship.id);},'relationship deletion');const deletionFocusRecovered=await until(()=>studio.document.activeElement?.dataset.pageFrameId==='frame:product','relationship deletion source focus');
  sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');sectionWorkspace.querySelector('[aria-label="Synchronized editable Flow outline"] [data-page-frame-id="frame:cart"] button').click();sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');studioButton('Open schema contribution',sectionWorkspace.querySelector('[aria-label="Selected Page instance inline actions"]')).click();const pageSchemaOpened=await until(()=>studio.document.body.textContent.includes('Flow Page-instance')&&studioButton('Return to Flow',studio.document),'Page-instance schema contribution');studioButton('Return to Flow',studio.document).click();await until(()=>studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]'),'returned from Page-instance schema');
  sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');const preservedGraph=preservedPresence&&pageOutlineOperated&&occurrenceOutlineOperated&&occurrenceExampleOperated&&Boolean(occurrenceSchemaOpened)&&Boolean(pageSchemaOpened)&&Boolean(relationshipFocused)&&Boolean(deletionFocusRecovered),originalPositions=Object.fromEntries(initialGraph.pageFrames.map(({id,position})=>[id,structuredClone(position)])),newName=sectionWorkspace.querySelector('[aria-label="New Section name"]');if(initialGraph.sections.length)throw new Error('Current Flow fixture unexpectedly contains legacy Sections');newName.value='Checkout phase';newName.form.requestSubmit();const created=await until(async()=>{const loaded=await repository.loadProject('project:shop'),section=loaded.state.project.documentationFlowGraphs['flow:checkout'].sections?.find(({name})=>name==='Checkout phase');return section&&{loaded,section};},'created Section'),organize=async(frameId,sectionId)=>{const workspace=await until(()=>studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]'),'current Flow workspace'),frame=workspace.querySelector('[aria-label="Page frame to organize"]'),destination=workspace.querySelector('[aria-label="Destination Section"]');frame.value=frameId;if(sectionId){destination.value=sectionId;studioButton('Place in Section',workspace).click();}else studioButton('Move outside every Section',workspace).click();return until(async()=>{const loaded=await repository.loadProject('project:shop'),candidate=loaded.state.project.documentationFlowGraphs['flow:checkout'].pageFrames.find(({id})=>id===frameId);return (sectionId?candidate.sectionId===sectionId:candidate.sectionId===undefined)&&{loaded,frame:candidate};},'organized '+frameId);};await organize('frame:cart',created.section.id);const placed=await organize('frame:product',created.section.id),placedGraph=placed.loaded.state.project.documentationFlowGraphs['flow:checkout'],relativeBefore=[originalPositions['frame:product'].x-originalPositions['frame:cart'].x,originalPositions['frame:product'].y-originalPositions['frame:cart'].y],relativeAfter=[placedGraph.pageFrames.find(({id})=>id==='frame:product').position.x-placedGraph.pageFrames.find(({id})=>id==='frame:cart').position.x,placedGraph.pageFrames.find(({id})=>id==='frame:product').position.y-placedGraph.pageFrames.find(({id})=>id==='frame:cart').position.y];sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');studioButton('Pages',sectionWorkspace).click();studioButton('Add Help',sectionWorkspace).click();const help=await until(async()=>{const loaded=await repository.loadProject('project:shop'),frame=loaded.state.project.documentationFlowGraphs['flow:checkout'].pageFrames.find(({pageId})=>pageId==='page:help');return frame&&{loaded,frame};},'schema-independent Help frame');sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');const pointerSource=sectionWorkspace.querySelector('[aria-label="Interactive directional Flow canvas"] [data-output-port-for="frame:cart"]'),pointerTarget=sectionWorkspace.querySelector('[aria-label="Interactive directional Flow canvas"] [data-input-port-for="'+help.frame.id+'"]');pointerSource.dispatchEvent(new PointerEvent('pointerdown',{pointerId:41,bubbles:true}));pointerTarget.dispatchEvent(new PointerEvent('pointerup',{pointerId:41,bubbles:true}));await until(async()=>{const loaded=await repository.loadProject('project:shop');return loaded.state.project.documentationFlowGraphs['flow:checkout'].relationships.length===2;},'cross-Section relationship');await organize(help.frame.id,created.section.id);const helpPlaced=await repository.loadProject('project:shop');await organize(help.frame.id,undefined);sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');const flowCopy=sectionWorkspace.textContent,runtime007=oneCanvas&&created.section.id.startsWith('flow-section:')&&relativeBefore.join('|')===relativeAfter.join('|')&&helpPlaced.state.project.documentationFlowGraphs['flow:checkout'].pageFrames.find(({id})=>id===help.frame.id).sectionId===created.section.id&&flowCopy.includes('Sections')&&flowCopy.includes('Pages')&&flowCopy.includes('Events')&&!flowCopy.includes('Property Set')&&!flowCopy.includes('lane order')&&sectionWorkspace.querySelectorAll('[aria-label="Interactive directional Flow canvas"] [data-relationship-id]').length===2;
  const flowSchemaBytes=(loaded)=>JSON.stringify({pages:loaded.state.project.collections.pages,propertySets:loaded.state.project.collections.propertySets,assignments:loaded.state.project.collections.assignments,compiled:compilePage(loaded,'page:cart',{segment:'retail'})}),schemaFingerprint=flowSchemaBytes(await repository.loadProject('project:shop')),undo=async(predicate,label)=>{studio.document.querySelector('#undo-project').click();return until(async()=>{const loaded=await repository.loadProject('project:shop');return predicate(loaded)&&loaded;},'Undo '+label);};sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');const reviewName=sectionWorkspace.querySelector('[aria-label="New Section name"]');reviewName.value='Review phase';reviewName.form.requestSubmit();const reviewSection=await until(async()=>{const loaded=await repository.loadProject('project:shop'),section=loaded.state.project.documentationFlowGraphs['flow:checkout'].sections.find(({name})=>name==='Review phase');return section&&section;},'Review phase');const movedToReview=await organize('frame:cart',reviewSection.id),reviewSequence=movedToReview.loaded.draftSequence;const undoReview=await undo((loaded)=>loaded.state.project.documentationFlowGraphs['flow:checkout'].pageFrames.find(({id})=>id==='frame:cart').sectionId===created.section.id,'move between Sections'),outsideState=await organize('frame:cart',undefined),outsideSequence=outsideState.loaded.draftSequence,undoOutside=await undo((loaded)=>loaded.state.project.documentationFlowGraphs['flow:checkout'].pageFrames.find(({id})=>id==='frame:cart').sectionId===created.section.id,'move outside');let checkoutRow=studio.document.querySelector('[data-flow-section-id="'+created.section.id+'"]'),x=checkoutRow.querySelector('[aria-label^="Section x"]'),y=checkoutRow.querySelector('[aria-label^="Section y"]'),beforeSectionMove=await repository.loadProject('project:shop'),beforeSection=beforeSectionMove.state.project.documentationFlowGraphs['flow:checkout'].sections.find(({id})=>id===created.section.id),beforeCart=beforeSectionMove.state.project.documentationFlowGraphs['flow:checkout'].pageFrames.find(({id})=>id==='frame:cart');x.value=String(beforeSection.bounds.x+40);y.value=String(beforeSection.bounds.y+30);studioButton('Move Section',checkoutRow).click();const movedSection=await until(async()=>{const loaded=await repository.loadProject('project:shop'),section=loaded.state.project.documentationFlowGraphs['flow:checkout'].sections.find(({id})=>id===created.section.id);return section.bounds.x===beforeSection.bounds.x+40&&loaded;},'moved Section'),movedCart=movedSection.state.project.documentationFlowGraphs['flow:checkout'].pageFrames.find(({id})=>id==='frame:cart'),sectionDelta=movedCart.position.x-beforeCart.position.x===40&&movedCart.position.y-beforeCart.position.y===30,undoSectionMove=await undo((loaded)=>loaded.state.project.documentationFlowGraphs['flow:checkout'].sections.find(({id})=>id===created.section.id).bounds.x===beforeSection.bounds.x,'move Section');checkoutRow=studio.document.querySelector('[data-flow-section-id="'+created.section.id+'"]');const rename=checkoutRow.querySelector('[aria-label^="Section name"]'),width=checkoutRow.querySelector('[aria-label^="Section width"]'),height=checkoutRow.querySelector('[aria-label^="Section height"]');rename.value='Checkout renamed';width.value=String(beforeSection.bounds.width+80);height.value=String(beforeSection.bounds.height+40);studioButton('Save name and size',checkoutRow).click();const resized=await until(async()=>{const loaded=await repository.loadProject('project:shop'),section=loaded.state.project.documentationFlowGraphs['flow:checkout'].sections.find(({id})=>id===created.section.id);return section.name==='Checkout renamed'&&section.bounds.width===beforeSection.bounds.width+80&&loaded;},'renamed and resized Section'),undoResize=await undo((loaded)=>loaded.state.project.documentationFlowGraphs['flow:checkout'].sections.find(({id})=>id===created.section.id).name==='Checkout phase','resize and rename'),runtime008=sectionDelta&&reviewSequence===undoReview.draftSequence-1&&outsideSequence===undoOutside.draftSequence-1&&[movedToReview.loaded,undoReview,outsideState.loaded,undoOutside,movedSection,undoSectionMove,resized,undoResize].every((loaded)=>flowSchemaBytes(loaded)===schemaFingerprint);
  const beforeDefault=await repository.loadProject('project:shop'),defaultGraph=beforeDefault.state.project.documentationFlowGraphs['flow:checkout'],defaultPositions=JSON.stringify(defaultGraph.pageFrames.filter(({sectionId})=>sectionId===created.section.id).map(({id,position})=>({id,position}))),defaultOccurrences=JSON.stringify(defaultGraph.occurrences),defaultRelationships=JSON.stringify(defaultGraph.relationships);checkoutRow=studio.document.querySelector('[data-flow-section-id="'+created.section.id+'"]');studioButton('Remove Section and retain Page frames',checkoutRow).click();const retained=await until(async()=>{const loaded=await repository.loadProject('project:shop'),graph=loaded.state.project.documentationFlowGraphs['flow:checkout'];return !graph.sections.some(({id})=>id===created.section.id)&&graph.pageFrames.filter(({id})=>id==='frame:cart'||id==='frame:product').every(({sectionId})=>sectionId===undefined)&&{loaded,graph};},'retained frames'),defaultStable=JSON.stringify(retained.graph.pageFrames.filter(({id})=>id==='frame:cart'||id==='frame:product').map(({id,position})=>({id,position})))===defaultPositions&&JSON.stringify(retained.graph.occurrences)===defaultOccurrences&&JSON.stringify(retained.graph.relationships)===defaultRelationships,undoDefault=await undo((loaded)=>loaded.state.project.documentationFlowGraphs['flow:checkout'].sections.some(({id})=>id===created.section.id),'default Section removal');await organize('frame:product',reviewSection.id);const beforeReviewRemoval=await repository.loadProject('project:shop'),beforeReviewBytes=JSON.stringify(beforeReviewRemoval.state.project.documentationFlowGraphs['flow:checkout']);let reviewRow=studio.document.querySelector('[data-flow-section-id="'+reviewSection.id+'"]');studioButton('Review remove Section with contents',reviewRow).click();const destructiveReview=reviewRow.querySelector('[aria-label^="Remove Review phase with contents review"]'),reviewCopy=destructiveReview.textContent,unchangedBeforeConfirm=JSON.stringify((await repository.loadProject('project:shop')).state.project.documentationFlowGraphs['flow:checkout'])===beforeReviewBytes;studioButton('Confirm remove Section with contents',destructiveReview).click();const destructivelyRemoved=await until(async()=>{const loaded=await repository.loadProject('project:shop'),graph=loaded.state.project.documentationFlowGraphs['flow:checkout'];return !graph.sections.some(({id})=>id===reviewSection.id)&&!graph.pageFrames.some(({id})=>id==='frame:product')&&loaded;},'destructive Section removal'),undoDestructive=await undo((loaded)=>loaded.state.project.documentationFlowGraphs['flow:checkout'].pageFrames.some(({id})=>id==='frame:product')&&loaded.state.project.documentationFlowGraphs['flow:checkout'].relationships.some(({id})=>id==='relationship:next'),'destructive Section removal'),runtime009=defaultStable&&unchangedBeforeConfirm&&reviewCopy.includes('Product detail')&&reviewCopy.includes('Cart to Product')&&destructivelyRemoved.state.project.documentationFlowGraphs['flow:checkout'].relationships.every(({id})=>id!=='relationship:next')&&JSON.stringify(undoDestructive.state.project.documentationFlowGraphs['flow:checkout'])===beforeReviewBytes;
  const migrated=await repository.loadProject('project:migration'),migratedCollections=migrated.state.project.collections,migratedGraph=migrated.state.project.documentationFlowGraphs['flow:checkout'],migratedFree=migratedGraph.pageFrames.find(({id})=>id==='frame:free'),recovery=await repository.exportRepositoryRecoveryBundle(),migrationReceipt=recovery.migrationReceipts.find(({key})=>key==='property-set-flow-sections-v1:project:migration'),migrationBackup=recovery.migrationBackups.find(({key})=>key==='property-set-flow-sections-v1:project:migration'),exportedMigration=await repository.exportProject('project:migration'),exportedMigrationBytes=JSON.stringify(exportedMigration),roundTripId='project:migration-roundtrip:'+innerWidth;await repository.importProject(exportedMigration,{projectId:roundTripId,name:'Migration round trip'});const roundTrip=await repository.loadProject(roundTripId),roundTripSets=new Set(roundTrip.state.project.collections.propertySets.map(({id})=>id)),runtime010=!Object.hasOwn(migratedCollections,'pageGroups')&&migratedCollections.propertySets.map(({id})=>id).join('|')==='property-set:checkout|property-set:retail'&&migratedCollections.pages.every(page=>Array.isArray(page.propertySetApplications))&&migratedGraph.sections.length===2&&migratedGraph.pageFrames.filter(({id})=>id!=='frame:free').every(frame=>frame.sectionId&&!Object.hasOwn(frame,'pageGroupId'))&&migratedFree?.name==='Product detail outside'&&migratedFree.pageId==='page:product'&&migratedFree.position.x===924&&migratedFree.position.y===410&&!migratedFree.sectionId&&migratedGraph.occurrences.some(({id,pageFrameId})=>id==='occurrence:free'&&pageFrameId==='frame:free')&&migratedGraph.relationships.some(({id})=>id==='relationship:free')&&!JSON.stringify(migratedGraph).includes('freePageRegion')&&!JSON.stringify(migratedGraph).includes('pageGroupId')&&migratedCollections.assignments[0].targetKind==='Property Set'&&migrationReceipt?.value.verified===true&&migrationReceipt.value.projectIds.includes('project:migration')&&Boolean(migrationBackup?.value.project.collections.pageGroups)&&!exportedMigrationBytes.includes('pageGroups')&&!exportedMigrationBytes.includes('freePageRegion')&&roundTrip.state.project.collections.pages.every(page=>page.propertySetApplications.every(({propertySetId})=>roundTripSets.has(propertySetId)))&&!Object.hasOwn(roundTrip.state.project.collections,'pageGroups');
  sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');let cartCard=sectionWorkspace.querySelector('[data-page-frame-id="frame:cart"]'),productCard=sectionWorkspace.querySelector('[data-page-frame-id="frame:product"]');cartCard.click();cartCard.focus();const beforeKeyboard=(await repository.loadProject('project:shop')).state.project.documentationFlowGraphs['flow:checkout'].pageFrames.find(({id})=>id==='frame:cart').position.x;cartCard.dispatchEvent(new KeyboardEvent('keydown',{key:'ArrowRight',bubbles:true}));const keyboardMoved=await until(async()=>{const loaded=await repository.loadProject('project:shop'),frame=loaded.state.project.documentationFlowGraphs['flow:checkout'].pageFrames.find(({id})=>id==='frame:cart');return frame.position.x===beforeKeyboard+20&&loaded;},'keyboard Page-frame move');sectionWorkspace=studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]');productCard=sectionWorkspace.querySelector('[data-page-frame-id="frame:product"]');const beforePointer=(await repository.loadProject('project:shop')).state.project.documentationFlowGraphs['flow:checkout'].pageFrames.find(({id})=>id==='frame:product').position;productCard.dispatchEvent(new PointerEvent('pointerdown',{pointerId:1,clientX:10,clientY:10,bubbles:true}));productCard.dispatchEvent(new PointerEvent('pointerup',{pointerId:1,clientX:30,clientY:25,bubbles:true}));const pointerMoved=await until(async()=>{const loaded=await repository.loadProject('project:shop'),frame=loaded.state.project.documentationFlowGraphs['flow:checkout'].pageFrames.find(({id})=>id==='frame:product');return frame.position.x===beforePointer.x+20&&frame.position.y===beforePointer.y+15&&loaded;},'pointer Page-frame move'),persistedBounds=JSON.stringify(pointerMoved.state.project.documentationFlowGraphs['flow:checkout'].sections.map(({id,bounds})=>({id,bounds})));studio.location.reload();sectionWorkspace=await until(()=>studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]'),'reloaded Flow workspace');const reloaded=await repository.loadProject('project:shop'),selectedRestored=sectionWorkspace.querySelector('[data-page-frame-id="frame:cart"]')?.getAttribute('aria-pressed')==='true',focusRestored=await until(()=>studio.document.activeElement?.getAttribute('aria-label')?.startsWith('Page frame Product detail'),'restored invoking focus'),controls=[...studio.document.querySelectorAll('.flow-section-workspace button,.flow-section-workspace input,.flow-section-workspace select,[aria-label="Property composition"] button,[aria-label="Property composition"] input')].filter(control=>control.getClientRects().length),named=controls.every(control=>control.getAttribute('aria-label')||control.labels?.length||control.textContent.trim()),runtime011=named&&selectedRestored&&focusRestored&&JSON.stringify(reloaded.state.project.documentationFlowGraphs['flow:checkout'].sections.map(({id,bounds})=>({id,bounds})))===persistedBounds&&studio.document.documentElement.scrollWidth<=studio.document.documentElement.clientWidth&&sectionWorkspace.getBoundingClientRect().right<=studio.innerWidth+1&&sectionWorkspace.querySelectorAll('[aria-label="Interactive directional Flow canvas"]').length===1;
  studio.location.href='./specification-builder.html?project=project%3Amigration&kind=flows&entity=flow%3Acheckout';const projectSwitch=await until(()=>studioButton('Confirm project context and continue',studio.document),'migration project confirmation');projectSwitch.click();const migratedWorkspace=await until(()=>studio.document.querySelector('[data-flow-section-workspace="flow:checkout"]'),'migrated Flow workspace'),migratedCopy=migratedWorkspace.textContent,migratedUi=Boolean(migratedWorkspace.querySelector('[data-page-frame-id="frame:free"]')&&migratedWorkspace.querySelector('[data-occurrence-id="occurrence:free"]')&&migratedWorkspace.querySelector('[data-relationship-id="relationship:free"]'))&&!migratedWorkspace.querySelector('[data-free-page-edge-target]')&&!migratedCopy.includes('before lanes')&&!migratedCopy.includes('after lanes')&&!migratedCopy.includes('Property Set membership')&&!migratedCopy.includes('lane order');
  const evidence={installedBoundary:true,preservedGraph,runtime001,runtime002,runtime003,runtime004,runtime005,runtime006,runtime007,runtime008,runtime009,runtime010:runtime010&&migratedUi,runtime011},diagnostics={compositionText,compositionLabels:[...composition.querySelectorAll('[aria-label]')].map(node=>node.getAttribute('aria-label')),compositionButtons:[...composition.querySelectorAll('button')].map(node=>node.textContent.trim()),beforeSequence,afterSequence:afterOrder.draftSequence,orderedIds,runtime007Values:{oneCanvas,sectionId:created.section.id,relativeBefore,relativeAfter,helpPlacedSection:helpPlaced.state.project.documentationFlowGraphs['flow:checkout'].pageFrames.find(({id})=>id===help.frame.id).sectionId,hasSections:flowCopy.includes('Sections'),hasPages:flowCopy.includes('Pages'),hasEvents:flowCopy.includes('Events'),hasPropertySet:flowCopy.includes('Property Set'),hasLaneOrder:flowCopy.includes('lane order'),relationshipElements:sectionWorkspace.querySelectorAll('[data-relationship-id]').length},runtime011Values:{named,selectedRestored,focusRestored,boundsRight:sectionWorkspace.getBoundingClientRect().right,innerWidth:studio.innerWidth,oneCanvasCount:sectionWorkspace.querySelectorAll('[aria-label="Interactive directional Flow canvas"]').length},runtime005Values:{cartDocumentation:cartChanged.properties['/checkout_type']?.documentation,productDocumentation:productChanged.properties['/checkout_type']?.documentation,cartVersion:Boolean(cartChanged.properties['/checkout_version']),productVersion:Boolean(productChanged.properties['/checkout_version']),stagedCart:Boolean(stagedCart.properties['/commerce_new']),stagedProduct:Boolean(stagedProduct.properties['/commerce_new']),includedCart:Boolean(includedCart.properties['/commerce_new']),includedProduct:Boolean(includedProduct.properties['/commerce_new']),parentAdditions:included.state.project.collections.propertySets.find(({id})=>id==='property-set:checkout').parentAdditions}};studioFrame.remove();return{passed:Object.values(evidence).every(Boolean),width:innerWidth,overflow:document.documentElement.scrollWidth>document.documentElement.clientWidth,evidence,diagnostics};
`;

const observations=[];for(const width of[360,1440])observations.push(await runRenderedWorkflow("schemas",workflow,{fullPanel:true,setup,width}));const evidence=Object.fromEntries(Object.keys(observations[0].evidence).map((key)=>[key,observations.every((observation)=>observation.evidence[key])]));console.log(JSON.stringify({propertySetFlowSections:evidence}));for(const[key,value]of Object.entries(evidence))assert.equal(value,true,key);
