import { runRenderedWorkflow, workflowPreamble } from "./shared-harness.mjs";
await runRenderedWorkflow("event-library",`${workflowPreamble}q('#data-layer-view-library').click();q('#add-new-event').click();q('#save-template-revision').click();q('#export-event-library').click();const result=q('#event-library-transfer-result'),stored=JSON.parse(localStorage.getItem('my-chrome-utilities.event-template-library.v1'));return {passed:visible(q('#event-property-editor'))&&document.activeElement===q('#event-template-name')&&stored[0].version===2&&stored[0].revisionHistory.length===1&&result.dataset.transferTemplates==='1'&&Number(result.dataset.transferBytes)>0&&result.textContent.includes('exported and imported'),width:innerWidth,overflow:document.documentElement.scrollWidth>innerWidth};`);
