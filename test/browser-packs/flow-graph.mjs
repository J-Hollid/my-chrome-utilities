import assert from "node:assert/strict";
import {spawn} from "node:child_process";
import {mkdtemp,rm} from "node:fs/promises";
import net from "node:net";
import os from "node:os";
import path from "node:path";
import {headlessChromeArguments,removeChromeProfile,resolveChromeExecutable,stopHeadlessChrome} from "../support/headless-chrome.mjs";
import {FLOW_RUNTIME_KEYS,flowInterruptionReport} from "../support/flow-evidence-reporter.mjs";
import {wait} from "./shared-harness.mjs";
import {flowGraphCorrectiveWorkflow,flowGraphEventExampleIncompleteEvidence,flowGraphEventExampleSeed,flowGraphEventExampleStateEvidence,flowGraphLegacyContextEvidence,flowGraphLegacyContextSeed,flowGraphPageExampleIncompleteEvidence,flowGraphPageExampleSeed,flowGraphPageExampleStateEvidence,flowGraphRelationshipKindEvidence,flowGraphRelationshipKindSeed,flowGraphReloadEvidence,flowGraphRepeatedInstanceEvidence,flowGraphRepeatedInstanceSeed} from "../support/flow-graph-corrective-workflow.mjs";
import {flowR02GeometryEvidence,flowR02ItemActivationResult,flowR02PanProbe,flowR02PanResult,flowR02PreparePanGraph,flowR02RestorePanGraph,flowR02ViewStorageKey} from "../support/flow-r02-correction-evidence.mjs";

class DevtoolsSocket{constructor(url){this.url=new URL(url);this.nextId=1;this.pending=new Map();this.handlers=new Map();this.buffer=Buffer.alloc(0);}async connect(){await new Promise((resolve,reject)=>{this.socket=net.createConnection({host:this.url.hostname,port:Number(this.url.port)});this.socket.once("error",reject);this.socket.once("connect",()=>{const key=Buffer.from(String(Math.random())).toString("base64");this.socket.write([`GET ${this.url.pathname}${this.url.search} HTTP/1.1`,`Host: ${this.url.host}`,"Upgrade: websocket","Connection: Upgrade",`Sec-WebSocket-Key: ${key}`,"Sec-WebSocket-Version: 13","\r\n"].join("\r\n"));});let handshake="";const receive=(chunk)=>{handshake+=chunk.toString("binary");const end=handshake.indexOf("\r\n\r\n");if(end<0)return;this.socket.off("data",receive);if(!handshake.startsWith("HTTP/1.1 101"))return reject(new Error("DevTools WebSocket upgrade failed"));const remaining=Buffer.from(handshake.slice(end+4),"binary");this.socket.on("data",(data)=>this.receive(data));if(remaining.length)this.receive(remaining);resolve();};this.socket.on("data",receive);});}receive(chunk){this.buffer=Buffer.concat([this.buffer,chunk]);while(this.buffer.length>=2){const first=this.buffer[0];let length=this.buffer[1]&127,offset=2;if(length===126){if(this.buffer.length<4)return;length=this.buffer.readUInt16BE(2);offset=4;}else if(length===127){if(this.buffer.length<10)return;length=Number(this.buffer.readBigUInt64BE(2));offset=10;}if(this.buffer.length<offset+length)return;const payload=this.buffer.subarray(offset,offset+length);this.buffer=this.buffer.subarray(offset+length);if((first&15)!==1)continue;const message=JSON.parse(payload.toString("utf8")),pending=this.pending.get(message.id);if(!pending){this.handlers.get(message.method)?.(message.params);continue;}this.pending.delete(message.id);message.error?pending.reject(new Error(message.error.message)):pending.resolve(message.result);}}send(payload){const body=Buffer.from(JSON.stringify(payload)),mask=Buffer.from([1,2,3,4]);let header;if(body.length<126)header=Buffer.from([129,128|body.length]);else{header=Buffer.alloc(4);header[0]=129;header[1]=254;header.writeUInt16BE(body.length,2);}for(let index=0;index<body.length;index+=1)body[index]^=mask[index%4];this.socket.write(Buffer.concat([header,mask,body]));}call(method,params={}){const id=this.nextId++;this.send({id,method,params});return new Promise((resolve,reject)=>this.pending.set(id,{resolve,reject}));}on(method,handler){this.handlers.set(method,handler);}close(){this.socket?.destroy();}}

const browserShard=process.env.FLOW_GRAPH_BROWSER_SHARD??"core",profile=await mkdtemp(path.join(os.tmpdir(),"flow-instance-runtime-")),extensionRoot=path.resolve("dist"),args=headlessChromeArguments(profile,extensionRoot);args.splice(-1,0,`--load-extension=${extensionRoot}`);const chrome=spawn(resolveChromeExecutable(),args,{stdio:["ignore","ignore","pipe"]});let socket,activePhase="startup";
try{let port;await new Promise((resolve,reject)=>{let output="";const timeout=setTimeout(()=>reject(new Error(`Chrome did not expose a debugging port: ${output}`)),15000);chrome.stderr.on("data",(chunk)=>{output+=chunk;const match=output.match(/ws:\/\/127\.0\.0\.1:(\d+)\//);if(match){clearTimeout(timeout);port=Number(match[1]);resolve();}});chrome.once("error",reject);});let extension;for(let attempt=0;attempt<100;attempt+=1){const targets=await fetch(`http://127.0.0.1:${port}/json/list`).then((response)=>response.json()),worker=targets.find(({type,url})=>type==="service_worker"&&url.startsWith("chrome-extension://")&&new URL(url).pathname==="/background.js");if(worker){extension=new URL(worker.url).hostname;break;}await wait(20);}const pageUrl=`chrome-extension://${extension}/specification-builder.html`;await fetch(`http://127.0.0.1:${port}/json/new?${encodeURIComponent(pageUrl)}`,{method:"PUT"});await wait(300);const target=(await fetch(`http://127.0.0.1:${port}/json/list`).then((response)=>response.json())).find(({type,url})=>type==="page"&&url.startsWith(pageUrl));if(!target)throw new Error("Installed Specification Builder target is unavailable.");socket=new DevtoolsSocket(target.webSocketDebuggerUrl);await socket.connect();await socket.call("Runtime.enable");await socket.call("Page.enable");for(const name of["flowEvidencePhase","flowNativeKey"])await socket.call("Runtime.addBinding",{name});socket.on("Runtime.bindingCalled",async({name,payload})=>{if(name==="flowEvidencePhase"){activePhase=payload;return;}const{key}=JSON.parse(payload),code=key===" "?"Space":key,virtualKeyCode=key==="Enter"?13:key==="Escape"?27:key.charCodeAt(0);await socket.call("Input.dispatchKeyEvent",{type:"keyDown",key,code,windowsVirtualKeyCode:virtualKeyCode,nativeVirtualKeyCode:virtualKeyCode});await socket.call("Input.dispatchKeyEvent",{type:"keyUp",key,code,windowsVirtualKeyCode:virtualKeyCode,nativeVirtualKeyCode:virtualKeyCode});});const evaluate=async(expression)=>{const result=await socket.call("Runtime.evaluate",{expression,returnByValue:true,awaitPromise:true,userGesture:true});if(result.exceptionDetails)throw new Error(result.exceptionDetails.exception?.description??result.exceptionDetails.text);return result.result.value;};
  for(let attempt=0;attempt<200;attempt+=1){if(await evaluate("document.readyState==='complete'&&Boolean(document.querySelector('#create-project-form'))"))break;await wait(25);}
  activePhase="seed";const seeded=await evaluate(`(async()=>{const {createSpecificationProject,addProjectEntity}=await import('./data-layer-specification-project.js'),{createFlowSection,addFlowPageFrameToSection}=await import('./data-layer-property-set-flow-section.js'),{addGraphOccurrence,saveGraphRelationship}=await import('./data-layer-flow-graph.js'),{openIndexedDbProjectRepository}=await import('./data-layer-durable-project-repository.js');let n=0,id=(kind)=>kind+':runtime:'+ ++n,state=createSpecificationProject({name:'Flow runtime',site:'runtime.example',id});const add=(kind,entity)=>{state=addProjectEntity(state,kind,entity,id);return state.project.collections[kind].at(-1);},propertySet=add('propertySets',{name:'Checkout',schemaConstraints:[{path:'/currency',type:'string',examples:['EUR']}]}),application=(name)=>({id:id('application'),name:'Checkout',propertySetId:propertySet.id}),confirmation=add('pages',{name:'Confirmation',propertySetApplications:[application()]}),payment=add('pages',{name:'Payment',propertySetApplications:[application()]}),receipt=add('pages',{name:'Receipt',propertySetApplications:[application()]}),purchase=add('events',{name:'Purchase',eventName:'purchase',schemaConstraints:[{path:'/event',type:'string',examples:['purchase']}]}),review=add('events',{name:'Review',eventName:'review'}),flow=add('flows',{name:'Checkout journey',steps:[]}),otherFlow=add('flows',{name:'Returns journey',steps:[]});state=addFlowPageFrameToSection(state,otherFlow.id,receipt.id,undefined,id);state=createFlowSection(state,flow.id,{name:'Checkout',bounds:{x:20,y:20,width:760,height:300}},id);state=createFlowSection(state,flow.id,{name:'Completion',bounds:{x:20,y:360,width:760,height:260}},id);let graph=state.project.documentationFlowGraphs[flow.id],sections=graph.sections;for(const [page,sectionId]of[[confirmation,sections[0].id],[payment,sections[0].id],[receipt,sections[1].id],[confirmation,undefined]])state=addFlowPageFrameToSection(state,flow.id,page.id,sectionId,id);graph=state.project.documentationFlowGraphs[flow.id];const frames=graph.pageFrames;state=addGraphOccurrence(state,flow.id,{name:'Purchase',pageFrameId:frames[0].id,pageId:confirmation.id,eventId:purchase.id,obligation:'Required',minimum:1,maximum:1,x:24,y:70},id);state=addGraphOccurrence(state,flow.id,{name:'Review',pageFrameId:frames[1].id,pageId:payment.id,eventId:review.id,obligation:'Required',minimum:1,maximum:1,x:24,y:70},id);state=saveGraphRelationship(state,flow.id,frames[0].id,{toStepId:frames[1].id,sourcePort:'right',targetPort:'left',label:'Checkout route'},id);state=saveGraphRelationship(state,flow.id,frames[0].id,{toStepId:frames[2].id,sourcePort:'top',targetPort:'bottom'},id);graph=state.project.documentationFlowGraphs[flow.id];const repository=await openIndexedDbProjectRepository();await repository.putProject(state,{active:true,navigation:{kind:'flows',id:flow.id}});return{projectId:state.project.id,flowId:flow.id,otherFlowId:otherFlow.id,pageIds:[confirmation.id,payment.id,receipt.id],frameIds:graph.pageFrames.map(({id})=>id),occurrenceIds:graph.occurrences.map(({id})=>id),relationshipIds:graph.relationships.map(({id})=>id),sectionIds:graph.sections.map(({id})=>id)};})()`);
  const runtime={};if(browserShard==="core"){const geometryRows=[[360,800,false,false,"narrowHiddenClosed"],[360,800,false,true,"narrowHiddenOpen"],[360,800,true,false,"narrowVisibleClosed"],[360,800,true,true,"narrowVisibleOpen"],[1440,900,false,false,"wideHiddenClosed"],[1440,900,false,true,"wideHiddenOpen"],[1440,900,true,false,"wideVisibleClosed"],[1440,900,true,true,"wideVisibleOpen"]],geometryEvidence={};for(const[width,height,visible,inspectorOpen,label]of geometryRows){await socket.call("Emulation.setDeviceMetricsOverride",{width,height,deviceScaleFactor:1,mobile:false});await evaluate(`(()=>{const key=${JSON.stringify(flowR02ViewStorageKey(seeded))},prior=JSON.parse(sessionStorage.getItem(key)??'{}');sessionStorage.setItem(key,JSON.stringify({...prior,navigationVisible:${visible}}));})()`);await socket.call("Page.reload",{ignoreCache:true});for(let attempt=0;attempt<200;attempt+=1){if(await evaluate("document.readyState==='complete'&&Boolean(document.querySelector('#project-tree'))"))break;await wait(25);}await evaluate(`(async()=>{if(document.querySelector('[aria-label="Flow toolbar"]')?.getBoundingClientRect().width)return;let flows;for(let attempt=0;attempt<160;attempt+=1){flows=document.querySelector('[data-kind="flows"]');if(flows)break;await new Promise(resolve=>setTimeout(resolve,25));}flows?.click();for(let attempt=0;attempt<160;attempt+=1){const row=[...document.querySelectorAll('.entity-row button')].find(item=>item.textContent==='Checkout journey');if(row){row.click();return;}await new Promise(resolve=>setTimeout(resolve,25));}})()`);await evaluate(`(()=>{const inspector=document.querySelector('#project-inspector'),toggle=document.querySelector('#toggle-project-inspector');if(Boolean(inspector&&!inspector.hidden)!==${inspectorOpen})toggle?.click();})()`);let ready=false,stable=0;for(let attempt=0;attempt<300;attempt+=1){ready=await evaluate("(document.querySelector('[aria-label=\"Flow toolbar\"]')?.getBoundingClientRect().width??0)>0&&(document.querySelector('.flow-canvas-viewport')?.getBoundingClientRect().height??0)>0");stable=ready?stable+1:0;if(stable>=10)break;await wait(25);}assert.equal(stable>=10,true,`Flow geometry row ${label} did not open with stable live geometry`);const row=await evaluate(flowR02GeometryEvidence());for(const[key,value]of Object.entries(row))geometryEvidence[`${label}_${key}`]=value;}activePhase="runtime001";
  await socket.call("Page.reload",{ignoreCache:true});for(let attempt=0;attempt<200;attempt+=1){if(await evaluate("document.readyState==='complete'&&Boolean(document.querySelector('[aria-label=\"Flow canvas viewport\"]'))"))break;await wait(25);}
  activePhase="runtime027";const originalPanState=await evaluate(flowR02PreparePanGraph(seeded));await socket.call("Page.reload",{ignoreCache:true});for(let attempt=0;attempt<200;attempt+=1){if(await evaluate("document.readyState==='complete'&&Boolean(document.querySelector('[aria-label=\"Flow canvas viewport\"]'))"))break;await wait(25);}const panRows=[
    [false,"primary",120,80,"mainPrimaryBlank"],[true,"primary",-90,-60,"focusPrimaryBlank"],
    [false,"space",110,-70,"mainSpaceItem"],[true,"space",-100,75,"focusSpaceItem"],
    [false,"middle",95,65,"mainMiddleBlank"],[true,"middle",-85,-55,"focusMiddleBlank"],
    [false,"touch",105,-65,"mainTouch"],[true,"touch",-95,70,"focusTouch"],
    [false,"keyboard",80,60,"mainKeyboard"],[true,"keyboard",-80,-60,"focusKeyboard"]
  ],panEvidence={};
  const mousePan=async(point,dx,dy,button="left")=>{const buttons=button==="middle"?4:1;await socket.call("Input.dispatchMouseEvent",{type:"mousePressed",x:point.x,y:point.y,button,buttons,clickCount:1});await socket.call("Input.dispatchMouseEvent",{type:"mouseMoved",x:point.x+dx,y:point.y+dy,button,buttons});await socket.call("Input.dispatchMouseEvent",{type:"mouseReleased",x:point.x+dx,y:point.y+dy,button,buttons:0,clickCount:1});};
  const keyboardPan=async(dx,dy)=>{for(const[key,distance]of[[dx>0?"ArrowRight":"ArrowLeft",Math.abs(dx)],[dy>0?"ArrowDown":"ArrowUp",Math.abs(dy)]])for(let offset=0;offset<distance;offset+=20){await socket.call("Input.dispatchKeyEvent",{type:"keyDown",key,code:key,windowsVirtualKeyCode:key==="ArrowLeft"?37:key==="ArrowUp"?38:key==="ArrowRight"?39:40});await socket.call("Input.dispatchKeyEvent",{type:"keyUp",key,code:key,windowsVirtualKeyCode:key==="ArrowLeft"?37:key==="ArrowUp"?38:key==="ArrowRight"?39:40});}};
  for(const[focused,kind,dx,dy,label]of panRows){await socket.call("Emulation.setTouchEmulationEnabled",{enabled:false});await socket.call("Page.reload",{ignoreCache:true});for(let attempt=0;attempt<200;attempt+=1){if(await evaluate("document.readyState==='complete'&&Boolean(document.querySelector('[aria-label=\"Flow canvas viewport\"]'))"))break;await wait(25);}if(kind==="touch")await socket.call("Emulation.setTouchEmulationEnabled",{enabled:true,maxTouchPoints:1});await evaluate(`(async()=>{const toolbar=document.querySelector('[aria-label="Flow toolbar"]'),button=(text)=>{const found=[...toolbar.querySelectorAll('button')].find(item=>item.textContent.trim()===text);if(!found)throw new Error('Missing pan setup control '+text+' from '+[...toolbar.querySelectorAll('button')].map(item=>item.textContent.trim()).join('|'));return found;},active=document.body.classList.contains('flow-focus-canvas');if(active!==${focused})button(active?'Exit Focus Canvas':'Focus Canvas').click();await new Promise(resolve=>setTimeout(resolve,80));button('Fit Flow').click();for(let count=0;count<20&&document.querySelector('[aria-label="Flow zoom percentage"]').textContent!=='200%';count+=1)button('Zoom in').click();document.querySelector('[aria-label="Flow canvas viewport"]').focus();})()`);const before=await evaluate(flowR02PanProbe(seeded));if(kind==="keyboard")await keyboardPan(dx,dy);else if(kind==="touch"){await socket.call("Input.dispatchTouchEvent",{type:"touchStart",touchPoints:[{x:before.blank.x,y:before.blank.y,id:77,radiusX:1,radiusY:1,force:1}]});await wait(20);await socket.call("Input.dispatchTouchEvent",{type:"touchMove",touchPoints:[{x:before.blank.x+dx,y:before.blank.y+dy,id:77,radiusX:1,radiusY:1,force:1}]});await socket.call("Input.dispatchTouchEvent",{type:"touchEnd",touchPoints:[]});await socket.call("Emulation.setTouchEmulationEnabled",{enabled:false});}else{if(kind==="space")await socket.call("Input.dispatchKeyEvent",{type:"rawKeyDown",key:" ",code:"Space",windowsVirtualKeyCode:32});await mousePan(kind==="space"?before.item:before.blank,dx,dy,kind==="middle"?"middle":"left");if(kind==="space")await socket.call("Input.dispatchKeyEvent",{type:"keyUp",key:" ",code:"Space",windowsVirtualKeyCode:32});}await wait(30);const result=await evaluate(flowR02PanResult(seeded,before,dx,dy));const continuation=await evaluate(flowR02PanProbe(seeded));await mousePan(continuation.blank,35,25);const continued=await evaluate(flowR02PanResult(seeded,continuation,35,25));for(const[key,value]of Object.entries({...result,continued:continued.exactDelta&&continued.zoomStable}))panEvidence[`${label}_${key}`]=value;}
  for(const[focused,label]of[[false,"mainPanPinch"],[true,"focusPanPinch"]]){await socket.call("Emulation.setTouchEmulationEnabled",{enabled:false});await evaluate(`(()=>{const key=${JSON.stringify(flowR02ViewStorageKey(seeded))},prior=JSON.parse(sessionStorage.getItem(key)??'{}');sessionStorage.setItem(key,JSON.stringify({...prior,selectedItems:[]}));})()`);await socket.call("Page.reload",{ignoreCache:true});for(let attempt=0;attempt<200;attempt+=1){if(await evaluate("document.readyState==='complete'&&Boolean(document.querySelector('[aria-label=\"Flow canvas viewport\"]'))"))break;await wait(25);}await evaluate(`(async()=>{const toolbar=document.querySelector('[aria-label="Flow toolbar"]'),button=(text)=>[...toolbar.querySelectorAll('button')].find(item=>item.textContent.trim()===text),active=document.body.classList.contains('flow-focus-canvas');if(active!==${focused})button(active?'Exit Focus Canvas':'Focus Canvas').click();await new Promise(resolve=>setTimeout(resolve,80));button('Fit Flow').click();document.querySelector('[aria-label="Flow canvas viewport"]').focus();})()`);await socket.call("Emulation.setTouchEmulationEnabled",{enabled:true,maxTouchPoints:2});const before=await evaluate(flowR02PanProbe(seeded)),first={x:before.blank.x+45,y:before.blank.y+30,id:91,radiusX:1,radiusY:1,force:1},second={x:first.x+70,y:first.y,id:92,radiusX:1,radiusY:1,force:1};await socket.call("Input.dispatchTouchEvent",{type:"touchStart",touchPoints:[{...first,x:before.blank.x,y:before.blank.y}]});await socket.call("Input.dispatchTouchEvent",{type:"touchMove",touchPoints:[first]});await socket.call("Input.dispatchTouchEvent",{type:"touchStart",touchPoints:[first,second]});await socket.call("Input.dispatchTouchEvent",{type:"touchMove",touchPoints:[{...first,x:first.x-25}, {...second,x:second.x+25}]});await socket.call("Input.dispatchTouchEvent",{type:"touchEnd",touchPoints:[]});await socket.call("Emulation.setTouchEmulationEnabled",{enabled:false});await wait(40);const after=await evaluate(flowR02PanProbe(seeded));await mousePan(after.item,0,0);await wait(40);const itemActivated=await evaluate(flowR02ItemActivationResult(after.item.id)),control=await evaluate(`(()=>{const button=[...document.querySelectorAll('[aria-label="Flow toolbar"] button')].find(item=>item.textContent.trim()==='Zoom in'),box=button.getBoundingClientRect();return{x:box.left+box.width/2,y:box.top+box.height/2,zoom:JSON.parse(document.querySelector('[aria-label="Interactive directional Flow canvas"]').dataset.viewport).zoom};})()`);await mousePan(control,0,0);await wait(40);const controlActivated=await evaluate(`JSON.parse(document.querySelector('[aria-label="Interactive directional Flow canvas"]').dataset.viewport).zoom>${control.zoom}`);for(const[key,value]of Object.entries({gestureChanged:JSON.stringify(after.camera)!==JSON.stringify(before.camera),canonicalStable:after.graph===before.graph&&after.revision===before.revision&&after.undo===before.undo,selectionClearBefore:before.selection==='[]',itemActivated,controlActivated}))panEvidence[`${label}_${key}`]=value;}runtime.runtime027=panEvidence;
  await evaluate(`(()=>{if(document.body.classList.contains('flow-focus-canvas'))[...document.querySelectorAll('[aria-label="Flow toolbar"] button')].find(button=>button.textContent.trim()==='Exit Focus Canvas')?.click();})()`);await wait(50);await evaluate(flowR02RestorePanGraph(seeded,originalPanState));await socket.call("Page.reload",{ignoreCache:true});for(let attempt=0;attempt<200;attempt+=1){if(await evaluate("document.readyState==='complete'&&Boolean(document.querySelector('#project-tree'))"))break;await wait(25);}await evaluate(`(async()=>{if(document.querySelector('[aria-label="Flow toolbar"]'))return;let flows;for(let attempt=0;attempt<160;attempt+=1){flows=document.querySelector('[data-kind="flows"]');if(flows)break;await new Promise(resolve=>setTimeout(resolve,25));}flows?.click();for(let attempt=0;attempt<160;attempt+=1){const row=[...document.querySelectorAll('.entity-row button')].find(item=>item.textContent==='Checkout journey');if(row){row.click();return;}await new Promise(resolve=>setTimeout(resolve,25));}})()`);for(let attempt=0;attempt<200;attempt+=1){if(await evaluate("Boolean(document.querySelector('[aria-label=\"Flow toolbar\"]'))"))break;await wait(25);}Object.assign(runtime,await evaluate(flowGraphCorrectiveWorkflow(seeded)));runtime.runtime001=geometryEvidence;runtime.runtime027=panEvidence;await socket.call("Page.reload",{ignoreCache:true});for(let attempt=0;attempt<200;attempt+=1){if(await evaluate("document.readyState==='complete'&&Boolean(document.querySelector('[aria-label=\"Interactive directional Flow canvas\"]'))"))break;await wait(25);}
  const reloadEvidence=await evaluate(flowGraphReloadEvidence(seeded));for(const[key,value]of Object.entries(reloadEvidence))runtime[key]={...runtime[key],...value};}
  if(browserShard==="legacy"){const legacyContext=await evaluate(flowGraphLegacyContextSeed(seeded));await socket.call("Page.reload",{ignoreCache:true});for(let attempt=0;attempt<200;attempt+=1){if(await evaluate("document.readyState==='complete'&&Boolean(document.querySelector('#project-tree'))"))break;await wait(25);}runtime.runtime017=await evaluate(flowGraphLegacyContextEvidence(seeded,legacyContext));
  const legacyKinds=await evaluate(flowGraphRelationshipKindSeed(seeded));await socket.call("Page.reload",{ignoreCache:true});for(let attempt=0;attempt<200;attempt+=1){if(await evaluate("document.readyState==='complete'&&Boolean(document.querySelector('[aria-label=\"Interactive directional Flow canvas\"]'))"))break;await wait(25);}runtime.runtime022=await evaluate(flowGraphRelationshipKindEvidence(seeded,legacyKinds));}
  if(browserShard==="examples"){const eventExample=await evaluate(flowGraphEventExampleSeed(seeded,"incomplete"));await socket.call("Page.reload",{ignoreCache:true});for(let attempt=0;attempt<200;attempt+=1){if(await evaluate("document.readyState==='complete'&&Boolean(document.querySelector('#project-tree'))"))break;await wait(25);}const eventExampleEvidence=await evaluate(flowGraphEventExampleIncompleteEvidence(seeded,eventExample));await evaluate(flowGraphEventExampleSeed(seeded,"invalid"));await socket.call("Page.reload",{ignoreCache:true});for(let attempt=0;attempt<200;attempt+=1){if(await evaluate("document.readyState==='complete'&&Boolean(document.querySelector('#project-tree'))"))break;await wait(25);}const invalidEventExample=await evaluate(flowGraphEventExampleStateEvidence(seeded,eventExample,"Invalid","/quantity","TYPE"));await evaluate(flowGraphEventExampleSeed(seeded,"blocked"));await socket.call("Page.reload",{ignoreCache:true});for(let attempt=0;attempt<200;attempt+=1){if(await evaluate("document.readyState==='complete'&&Boolean(document.querySelector('#project-tree'))"))break;await wait(25);}const blockedEventExample=await evaluate(flowGraphEventExampleStateEvidence(seeded,eventExample,"Blocked","/runtime_conflict","CONFLICT"));runtime.runtime021={...eventExampleEvidence,invalid:invalidEventExample,blocked:blockedEventExample};
  const pageExample=await evaluate(flowGraphPageExampleSeed(seeded,"incomplete"));await socket.call("Page.reload",{ignoreCache:true});for(let attempt=0;attempt<200;attempt+=1){if(await evaluate("document.readyState==='complete'&&Boolean(document.querySelector('#project-tree'))"))break;await wait(25);}const pageExampleEvidence=await evaluate(flowGraphPageExampleIncompleteEvidence(seeded,pageExample));await evaluate(flowGraphPageExampleSeed(seeded,"invalid"));await socket.call("Page.reload",{ignoreCache:true});for(let attempt=0;attempt<200;attempt+=1){if(await evaluate("document.readyState==='complete'&&Boolean(document.querySelector('#project-tree'))"))break;await wait(25);}const invalidPageExample=await evaluate(flowGraphPageExampleStateEvidence(pageExample,"Invalid","/typed_page","TYPE"));await evaluate(flowGraphPageExampleSeed(seeded,"blocked"));await socket.call("Page.reload",{ignoreCache:true});for(let attempt=0;attempt<200;attempt+=1){if(await evaluate("document.readyState==='complete'&&Boolean(document.querySelector('#project-tree'))"))break;await wait(25);}const blockedPageExample=await evaluate(flowGraphPageExampleStateEvidence(pageExample,"Blocked","/page_runtime_conflict","CONFLICT"));runtime.runtime025={...pageExampleEvidence,invalid:invalidPageExample,blocked:blockedPageExample};}
  if(browserShard==="core"){const repeatedInstances=await evaluate(flowGraphRepeatedInstanceSeed(seeded));await socket.call("Page.reload",{ignoreCache:true});for(let attempt=0;attempt<200;attempt+=1){if(await evaluate("document.readyState==='complete'&&Boolean(document.querySelector('#project-tree'))"))break;await wait(25);}runtime.runtime024={...runtime.runtime024,...await evaluate(flowGraphRepeatedInstanceEvidence(seeded,repeatedInstances))};}
  const supplemental=new Set(["runtime017","runtime021","runtime022","runtime025"]),missing=browserShard==="core"?FLOW_RUNTIME_KEYS.filter(key=>!supplemental.has(key)&&!runtime[key]).map(path=>({path,value:"unexecuted"})):[],falseLeaves=Object.entries(runtime).flatMap(([runtimeKey,evidence])=>Object.entries(evidence).filter(([,value])=>value!==true).map(([key,value])=>({path:`${runtimeKey}.${key}`,value}))),shardFailures=[...missing,...falseLeaves,...(browserShard==="core"&&runtime.installedBoundary!==true?[{path:"installedBoundary",value:runtime.installedBoundary}]:[])];assert.deepEqual(shardFailures,[],`Flow browser ${browserShard} evidence contains a false value`);console.log(JSON.stringify({flowGraph:runtime,flowGraphShard:browserShard}));
}catch(error){throw new Error(`Flow runtime interruption: ${JSON.stringify(flowInterruptionReport(activePhase,error))}`,{cause:error});}finally{socket?.close();await stopHeadlessChrome(chrome);await removeChromeProfile(profile,{targetId:"flow-graph"});}
