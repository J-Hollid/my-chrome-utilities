import { runRenderedWorkflow, workflowPreamble } from "./shared-harness.mjs";
await runRenderedWorkflow("schemas",`${workflowPreamble}q('#data-layer-view-schemas').click();q('#create-schema').click();q('#recheck-schema-validation').click();q('#export-schema').click();const result=q('#schema-result');return {passed:visible(q('#schema-editor'))&&q('#data-layer-panel-schemas').contains(q('#schema-editor'))&&result.dataset.validationState==='1 issues'&&Number(result.dataset.transferBytes)>0&&result.textContent.includes('exported'),width:innerWidth,overflow:document.documentElement.scrollWidth>innerWidth};`);
