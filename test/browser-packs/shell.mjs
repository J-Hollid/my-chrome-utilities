import { runRenderedWorkflow, workflowPreamble } from "./shared-harness.mjs";
await runRenderedWorkflow("shell",`${workflowPreamble}const owners=[...document.querySelectorAll('[data-utility-owner]')];return {passed:q('#utility-directory').children.length===3&&owners.length>=3&&q('#side-panel-root').dataset.activeUtilities==='command-palette,hotkeys,data-layer',width:innerWidth,overflow:document.documentElement.scrollWidth>innerWidth};`);
