import {addCanonicalProperty,canonicalPropertyPath,canonicalTableRows,setCanonicalProperty} from "./data-layer-canonical-schema.js";
import type {CanonicalIdFactory,CanonicalPropertyNode,CanonicalPropertyType,CanonicalRule,CanonicalSchemaDocument} from "./data-layer-canonical-schema.js";
import type {LayerConstraint} from "./data-layer-layered-schema.js";

const clone=<T>(value:T):T=>structuredClone(value);
const orderWithin=(document:CanonicalSchemaDocument,parentId?:string):CanonicalPropertyNode[]=>Object.values(document.nodes).filter((node)=>node.parentId===parentId).sort((a,b)=>a.order-b.order||a.id.localeCompare(b.id));
const orderedIds=(document:CanonicalSchemaDocument,parentId?:string):string[]=>orderWithin(document,parentId).flatMap((node)=>[node.id,...orderedIds(document,node.id)]);
export function canonicalConstraints(document:CanonicalSchemaDocument):LayerConstraint[]{return orderedIds(document).map((id)=>{const node=document.nodes[id]!,conditional=node.presence.mode.endsWith("-when"),patterns=node.rules.flatMap((rule)=>rule.kind==="pattern"&&rule.pattern?[rule.pattern]:[]),ranges=node.rules.filter(({kind})=>kind==="range"),cardinality=node.rules.filter(({kind})=>kind==="cardinality"),minimum=ranges.map(({minimum})=>minimum).filter((value):value is number=>value!==undefined).sort((a,b)=>b-a)[0],maximum=ranges.map(({maximum})=>maximum).filter((value):value is number=>value!==undefined).sort((a,b)=>a-b)[0],minItems=cardinality.map((rule)=>rule.minItems).filter((value):value is number=>value!==undefined).sort((a,b)=>b-a)[0],maxItems=cardinality.map((rule)=>rule.maxItems).filter((value):value is number=>value!==undefined).sort((a,b)=>a-b)[0];return{path:canonicalPropertyPath(document,id),type:node.type,...(node.itemType?{itemType:node.itemType}:{}),...(node.allowedValues.length?{allowedValues:node.allowedValues.map(({value})=>clone(value)),allowedValueIds:node.allowedValues.map(({id})=>id)}:{}),...(node.presence.mode.startsWith("required")?{presence:"required" as const}:node.presence.mode.startsWith("forbidden")?{presence:"forbidden" as const}:{}),...(conditional&&node.presence.condition?{condition:clone(node.presence.condition) as unknown as Record<string,unknown>}:{ }),...(node.rules.length?{rules:clone(node.rules) as unknown as Record<string,unknown>[]}:{ }),...(patterns.length?{patterns}:{}),...(minimum!==undefined?{minimum}:{}),...(maximum!==undefined?{maximum}:{}),...(minItems!==undefined?{minItems}:{}),...(maxItems!==undefined?{maxItems}:{}),...(node.documentation.description?{documentation:node.documentation.description}:{}),...(node.documentation.example.method!=="blank"?{examples:[clone(node.documentation.example.value)]}:{}),...(node.expectedValue!==undefined?{expectedValue:clone(node.expectedValue)}:{}),...(node.enforcement?{enforcement:node.enforcement}:{}),...(node.target?{target:node.target}:{}),definitionId:id,...(node.overrideReferences.length?{overrideReferences:clone(node.overrideReferences)}:{})};});}
export function canonicalSchemaWithConstraint(document:CanonicalSchemaDocument,constraint:LayerConstraint,id:CanonicalIdFactory):CanonicalSchemaDocument{let next=document,parentId:string|undefined,current="";for(const name of constraint.path.split("/").filter(Boolean)){current+=`/${name}`;const existing=canonicalTableRows(next).find(({path})=>path===current)?.id;if(existing){parentId=existing;continue;}const result=addCanonicalProperty(next,{baseRevision:next.revision,name,type:name===constraint.path.split("/").filter(Boolean).at(-1)?(constraint.type as CanonicalPropertyType|undefined)??"string":"object",...(parentId?{parentId}:{}),id});if(result.status!=="applied"&&result.status!=="rebased")throw new Error(`Cannot add canonical property ${current}.`);next=result.document;parentId=next.selectedPropertyId;}if(!parentId)throw new Error("A canonical constraint needs a generated property path.");const node=next.nodes[parentId]!,rules:CanonicalRule[]=[...node.rules,...((constraint.patterns??[]).map((pattern)=>({id:id("rule"),kind:"pattern" as const,pattern,severity:"error" as const,message:"Pattern mismatch"}))),...(constraint.minimum!==undefined||constraint.maximum!==undefined?[{id:id("rule"),kind:"range" as const,severity:"error" as const,message:"Outside range",...(constraint.minimum!==undefined?{minimum:constraint.minimum}:{}),...(constraint.maximum!==undefined?{maximum:constraint.maximum}:{})}]:[]),...(constraint.minItems!==undefined||constraint.maxItems!==undefined?[{id:id("rule"),kind:"cardinality" as const,severity:"error" as const,message:"Outside cardinality",...(constraint.minItems!==undefined?{minItems:constraint.minItems}:{}),...(constraint.maxItems!==undefined?{maxItems:constraint.maxItems}:{})}]:[])];const result=setCanonicalProperty(next,{baseRevision:next.revision,propertyId:parentId,patch:{type:(constraint.type as CanonicalPropertyType|undefined)??node.type,allowedValues:(constraint.allowedValues??[]).map((value,index)=>({id:constraint.allowedValueIds?.[index]??id("allowed-value"),value:clone(value)})),presence:{mode:constraint.presence==="required"?"required":constraint.presence==="forbidden"?"forbidden":"optional"},rules,documentation:{...node.documentation,description:constraint.documentation??node.documentation.description,...(constraint.examples?.length?{example:{method:"custom",value:clone(constraint.examples[0])}}:{})},...(constraint.expectedValue!==undefined?{expectedValue:clone(constraint.expectedValue)}:{}),...(constraint.enforcement?{enforcement:constraint.enforcement}:{}),...(constraint.target?{target:constraint.target}:{}),overrideReferences:[...(constraint.overrideReferences??[])]}});if(result.status!=="applied"&&result.status!=="rebased")throw new Error(`Cannot update canonical property ${constraint.path}.`);return result.document;}
export function canonicalRequirements(document:CanonicalSchemaDocument){return orderedIds(document).map((id)=>{const node=document.nodes[id]!;return{path:canonicalPropertyPath(document,id),type:node.type,...(node.presence.mode.startsWith("required")?{required:true}:{}),...(node.presence.mode.startsWith("forbidden")?{forbidden:true}:{}),...(node.allowedValues.length?{allowedValues:node.allowedValues.map(({value})=>clone(value))}:{}),...(node.documentation.description?{description:node.documentation.description}:{}),...(node.documentation.example.method!=="blank"?{examples:[clone(node.documentation.example.value)]}:{}),...(node.rules.length?{rules:clone(node.rules) as unknown as Record<string,unknown>[]}:{})};});}
