import {addComposedConditionGroup,addComposedConditionPredicate,composedConditionPredicate,evaluateComposedCondition,removeComposedConditionBranch,typedComposedValue} from "../data-layer-composed-schema-builders.js";
import type {ConditionGroup,ComposedCondition} from "./builder-types.js";
import type {BuilderRenderContext} from "./facet-builder-context.js";
import {button,labeled,option} from "./facet-builder-context.js";
const pathFor=(choices:{path:string;definitionId:string}[],id:string):string=>choices.find((choice)=>choice.definitionId===id)?.path??id;
const text=(condition:ComposedCondition,choices:{path:string;definitionId:string}[]):string=>condition.kind==="predicate"?`${pathFor(choices,condition.propertyId)} ${condition.operator}${condition.value===undefined?"":` ${String(condition.value)}`}`:`${condition.kind==="all"?"All":condition.kind==="any"?"Any":"Not"} (${condition.children.map((child)=>text(child,choices)).join(condition.kind==="any"?" or ":" and ")})`;
const groups=(root:ConditionGroup)=>{const entries:{path:number[];label:string}[]=[];const visit=(group:ConditionGroup,path:number[])=>{entries.push({path,label:`${group.kind==="all"?"All":group.kind==="any"?"Any":"Not"} ${path.length?path.join("."):"root"}`});group.children.forEach((child,index)=>{if(child.kind!=="predicate")visit(child,[...path,index]);});};visit(root,[]);return entries;};
export function renderConditionBuilder(context:BuilderRenderContext):HTMLFieldSetElement{const{options,draft,setDraft,setFeedback,render}=context,condition=document.createElement("fieldset"),legend=document.createElement("legend"),target=document.createElement("select"),property=document.createElement("select"),operator=document.createElement("select"),value=document.createElement("input"),branches=document.createElement("ul"),plain=document.createElement("output"),observation=document.createElement("textarea"),evaluate=document.createElement("button"),result=document.createElement("output");condition.setAttribute("aria-label","Composed condition builder");legend.textContent="Nested All / Any / Not condition builder";for(const entry of groups(draft().condition))target.append(option(entry.path.join("."),entry.label));property.append(option("","Choose property"),...options.propertyChoices.map(({path,definitionId})=>option(definitionId,path)));for(const name of["Equals","Does not equal","Exists","Does not exist","Starts with","Contains","Matches pattern","Greater than","At least","Less than","At most"])operator.append(option(name));plain.textContent=text(draft().condition,options.propertyChoices);for(const branch of draft().condition.children.entries()){const item=document.createElement("li");item.append(text(branch[1],options.propertyChoices)," ",button("Remove condition branch",()=>{setDraft(removeComposedConditionBranch(draft(),[branch[0]]));render();}));branches.append(item);}const path=()=>target.value?target.value.split(".").map(Number):[],addGroup=(kind:ConditionGroup["kind"])=>{try{setDraft(addComposedConditionGroup(draft(),path(),kind));setFeedback("");}catch(error){setFeedback(error instanceof Error?error.message:String(error));}render();},addPredicate=()=>{const selected=options.propertyChoices.find((choice)=>choice.definitionId===property.value);if(!selected){setFeedback("Choose a referenced property.");render();return;}try{const typed=["Exists","Does not exist"].includes(operator.value)?undefined:typedComposedValue(selected.type,value.value);setDraft(addComposedConditionPredicate(draft(),path(),composedConditionPredicate(selected,operator.value,typed)));setFeedback("");}catch(error){setFeedback(error instanceof Error?error.message:String(error));}render();};observation.value="{}";evaluate.type="button";evaluate.textContent="Evaluate condition";evaluate.addEventListener("click",()=>{try{result.textContent=evaluateComposedCondition(draft().condition,JSON.parse(observation.value),options.propertyChoices)?"Matched":"Did not match";}catch(error){result.textContent=error instanceof Error?error.message:String(error);}});const evaluation=options.includeConditionEvaluation===false?[]:[labeled("Condition observation",observation),evaluate,result];condition.append(legend,branches,labeled("Target group",target),button("Add All group",()=>addGroup("all")),button("Add Any group",()=>addGroup("any")),button("Add Not group",()=>addGroup("not")),labeled("Property",property),labeled("Typed operator",operator),labeled("Typed value",value),button("Add condition predicate",addPredicate),button("Clear condition",()=>{setDraft(removeComposedConditionBranch(draft(),[]));render();}),plain,...evaluation);return condition;}
