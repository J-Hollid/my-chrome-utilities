import type {CanonicalCommand,CanonicalCommandResult,CanonicalPropertyNode,CanonicalPropertyType,CanonicalPresenceMode,CanonicalSchemaDocument} from "./data-layer-canonical-schema.js";
import type {FocusedPropertySection} from "./data-layer-focused-schema-property-ui.js";
import {renderCanonicalFocusedRules} from "./data-layer-canonical-schema-focused-rules.js";
import {renderCanonicalFocusedCondition} from "./data-layer-canonical-schema-focused-conditions.js";

export interface CanonicalFocusedSectionContext {
  dom:Document;current:()=>CanonicalSchemaDocument;node:CanonicalPropertyNode;getWorking:()=>CanonicalPropertyNode|undefined;setWorking:(value:CanonicalPropertyNode|undefined)=>void;
  activeSection:FocusedPropertySection;setActiveSection:(value:FocusedPropertySection)=>void;removedRuleIds:Set<string>;id:(kind:string)=>string;render:()=>void;
  patchFor:(node:CanonicalPropertyNode,original:CanonicalPropertyNode)=>Partial<Omit<CanonicalPropertyNode,"id"|"parentId"|"order"|"provenance">>;command:(command:CanonicalCommand)=>CanonicalCommandResult;select:(id:string|undefined)=>void;feedback:(message:string)=>void;
}
const types:CanonicalPropertyType[]=["string","number","integer","boolean","object","array","null"];
const labeled=(dom:Document,text:string,control:HTMLElement):HTMLLabelElement=>{const label=dom.createElement("label");label.append(text,control);return label;};
const input=(dom:Document,name:string,value="",type="text"):HTMLInputElement=>{const control=dom.createElement("input");control.name=name;control.type=type;control.value=value;return control;};
const button=(dom:Document,text:string,run:()=>void):HTMLButtonElement=>{const control=dom.createElement("button");control.type="button";control.textContent=text;control.addEventListener("click",run);return control;};
const presenceText=(mode:CanonicalPresenceMode):string=>({optional:"Optional",required:"Required","required-when":"Required when",forbidden:"Forbidden","forbidden-when":"Forbidden when"})[mode];

export function renderCanonicalFocusedSection(host:HTMLElement,context:CanonicalFocusedSectionContext):void {
  const {dom}=context,working=context.getWorking();if(!working)return;host.dataset.focusedSection=context.activeSection;
  if(context.activeSection==="definition"){const name=input(dom,"propertyName",working.name),type=dom.createElement("select");type.name="propertyType";type.append(...types.map((entry)=>new Option(entry,entry)));type.value=working.type;name.addEventListener("input",()=>{const next=context.getWorking();if(next)next.name=name.value;});type.addEventListener("change",()=>{const next=context.getWorking();if(next)next.type=type.value as CanonicalPropertyType;});const rename=button(dom,"Rename",()=>{const next=context.getWorking();if(!next)return;const original=context.current().nodes[next.id];if(!original)return;const result=context.command({kind:"set",baseRevision:context.current().revision,propertyId:next.id,patch:context.patchFor(next,original)});if(result.status==="applied"||result.status==="rebased"){context.setWorking(undefined);context.render();}}),addChild=button(dom,"Add child",()=>{const next=context.getWorking();if(!next)return;const result=context.command({kind:"add",baseRevision:context.current().revision,parentId:next.id,name:"child",type:"string",id:context.id});if((result.status==="applied"||result.status==="rebased")&&result.document){context.select(result.document.selectedPropertyId);context.setWorking(undefined);context.render();}}),addSibling=button(dom,"Add sibling",()=>{const next=context.getWorking();if(!next)return;const result=context.command({kind:"add",baseRevision:context.current().revision,...(next.parentId?{parentId:next.parentId}:{}),afterId:next.id,name:"property",type:"string",id:context.id});if((result.status==="applied"||result.status==="rebased")&&result.document){context.select(result.document.selectedPropertyId);context.setWorking(undefined);context.render();}});host.append(labeled(dom,"Property name",name),labeled(dom,"Type",type),rename,addChild,addSibling);}
  if(context.activeSection==="presence"){const presence=dom.createElement("select");presence.name="presenceMode";presence.append(...(["optional","required","required-when","forbidden","forbidden-when"] as const).map((entry)=>new Option(presenceText(entry),entry)));presence.value=working.presence.mode;presence.addEventListener("change",()=>{const next=context.getWorking();if(next)next.presence={...next.presence,mode:presence.value as CanonicalPresenceMode};});host.append(labeled(dom,"Presence",presence));if(working.presence.condition)renderCanonicalFocusedCondition(host,context);}
  if(context.activeSection==="values"){const list=dom.createElement("div");working.allowedValues.forEach((entry,index)=>{const row=dom.createElement("div"),value=input(dom,`allowedValue-${entry.id}`,String(entry.value));value.setAttribute("aria-label",`Allowed value ${index+1}`);value.addEventListener("input",()=>{const next=context.getWorking();if(next)next.allowedValues[index]={...entry,value:value.value};});row.append(labeled(dom,`Value ${index+1}`,value),button(dom,"Remove",()=>{const next=context.getWorking();if(next){next.allowedValues.splice(index,1);context.render();}}));list.append(row);});host.append(list,button(dom,"Add allowed value",()=>{const next=context.getWorking();if(next){next.allowedValues.push({id:context.id("allowed-value"),value:""});context.render();}}));}
  if(context.activeSection==="conditions")renderCanonicalFocusedCondition(host,context);
  if(context.activeSection==="rules")renderCanonicalFocusedRules(host,context);
  if(context.activeSection==="documentation"){const display=input(dom,"displayText",working.documentation.displayText),description=dom.createElement("textarea"),comments=dom.createElement("textarea");description.name="description";description.value=working.documentation.description;comments.name="comments";comments.value=working.documentation.comments;display.addEventListener("input",()=>{const next=context.getWorking();if(next)next.documentation={...next.documentation,displayText:display.value};});description.addEventListener("input",()=>{const next=context.getWorking();if(next)next.documentation={...next.documentation,description:description.value};});comments.addEventListener("input",()=>{const next=context.getWorking();if(next)next.documentation={...next.documentation,comments:comments.value};});host.append(labeled(dom,"Display text",display),labeled(dom,"Description",description),labeled(dom,"Comments",comments));}
  if(context.activeSection==="example"){const method=dom.createElement("select"),value=input(dom,"exampleValue",String(working.documentation.example.value??""));method.name="exampleMethod";method.append(...(["allowed-value","custom","blank"] as const).map((entry)=>new Option(entry,entry)));method.value=working.documentation.example.method;method.addEventListener("change",()=>{const next=context.getWorking();if(next)next.documentation={...next.documentation,example:{method:method.value as any,value:method.value==="blank"?undefined:value.value}};});value.addEventListener("input",()=>{const next=context.getWorking();if(next)next.documentation={...next.documentation,example:{method:method.value as any,value:value.value}};});host.append(labeled(dom,"Example method",method),labeled(dom,"Example value",value));}
  if(context.activeSection==="structure")host.append(Object.assign(dom.createElement("p"),{textContent:`Stable identity ${working.id} · ${context.current().id}`}),labeled(dom,"Name",input(dom,"structureName",working.name)),button(dom,"Add child",()=>{}),button(dom,"Add sibling",()=>{}));
}
