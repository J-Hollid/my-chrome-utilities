import type {ProjectDocumentationSelection,ProjectDocumentationSnapshot,ProjectDocumentationTable} from "../data-layer-project-documentation-workspace.js";
import {selectProjectDocumentationTables} from "../data-layer-project-documentation-workspace.js";
import {fitProjectDocumentationLogo} from "../data-layer-project-documentation-records.js";
import {documentationTemplateAssignment} from "./template-library.js";
import {prepareDocumentationTemplateContext} from "./template-context.js";
import {excelTemplateGuideFor,renderExcelTemplateGrid,type ExcelTemplateArea,type ExcelTemplateCell,type ExcelTemplatePrototype} from "./excel-template.js";
import {validateExcelTemplateWorkbook} from "./excel-workbook.js";
import {safeWorksheetName} from "./template-contract.js";

interface ExcelCell {address:string;value:unknown;note?:unknown;style:unknown}
interface ExcelRow {height?:number;eachCell(options:{includeEmpty:boolean},visit:(cell:ExcelCell)=>void):void}
interface ExcelColumn {width?:number;hidden?:boolean;outlineLevel?:number}
interface ExcelImageRange {tl:{nativeRow?:number;nativeCol?:number;row?:number;col?:number};br?:{nativeRow?:number;nativeCol?:number;row?:number;col?:number};ext?:{width:number;height:number};editAs?:string}
interface ExcelWorksheet {name:string;rowCount:number;columnCount:number;columns:ExcelColumn[];pageSetup:unknown;headerFooter:unknown;model?:{merges?:string[]};eachRow(options:{includeEmpty:boolean},visit:(row:ExcelRow,rowNumber:number)=>void):void;getRow(row:number):ExcelRow;getColumn(column:number):ExcelColumn;getCell(address:string):ExcelCell;getCell(row:number,column:number):ExcelCell;mergeCells(range:string):void;addTable?(value:unknown):void;getTable?(name:string):{table:{rows?:unknown[][];tableRef?:string}};getImages?():{imageId:number;range:ExcelImageRange}[];addImage?(imageId:number,range:ExcelImageRange|string):void}
interface ExcelImageData {buffer?:ArrayBuffer|Uint8Array;base64?:string;extension?:"png"|"jpeg"|"gif"}
interface ExcelWorkbook {worksheets:ExcelWorksheet[];definedNames:{add(range:string,name:string):void;model:{name:string;ranges:string[]}[]};addWorksheet(name:string):ExcelWorksheet;getWorksheet?(name:string):ExcelWorksheet|undefined;addImage?(data:ExcelImageData):number;getImage?(id:number):ExcelImageData|undefined;xlsx:{load(bytes:ArrayBuffer):Promise<void>;writeBuffer():Promise<ArrayBuffer>}}
interface ExcelJsGlobal {Workbook:new()=>ExcelWorkbook}
const excelJs=():ExcelJsGlobal=>{const value=(globalThis as typeof globalThis&{ExcelJS?:ExcelJsGlobal}).ExcelJS;if(!value)throw new Error("The packaged Excel template renderer is unavailable.");return value;};
const literalCellValue=(value:unknown):string|number|boolean=>typeof value==="string"||typeof value==="number"||typeof value==="boolean"?value:value==null?"":String(value);

const starterPrototype=(kind:ProjectDocumentationSnapshot["set"]["sections"][number]["kind"]):ExcelTemplatePrototype=>{
  if(kind==="overview")return{kind,contractVersion:2,worksheetName:"Template",cells:[{address:"A1",value:"{{section.name}}"},{address:"A3",value:"{{field.label}}"},{address:"B3",value:"{{field.value}}"}],areas:[{name:"FieldRow",type:"repeat",source:"overview.fields",direction:"down",range:"A3:B3"}],merges:[]};
  if(kind==="flow")return{kind,contractVersion:2,worksheetName:"Template",cells:[{address:"A1",value:"{{section.name}}"},{address:"A3",value:"{{page.stepLabel}}"},{address:"B3",value:"{{page.pageName}}"},{address:"A5",value:"{{event.eventName}}"}],areas:[{name:"PageCard",type:"repeat",source:"flow.pages",direction:"across",range:"A3:D8"},{name:"EventRow",type:"repeat",source:"page.events",direction:"down",range:"A5:B5"},{name:"PageVisual",type:"image",source:"page.visual.image",range:"C5:D7"},{name:"ThemeLogo",type:"image",source:"theme.logo",range:"C1:D2"}],merges:[]};
  if(kind==="matrix")return{kind,contractVersion:2,worksheetName:"Template",cells:[{address:"A1",value:"{{section.name}}"},{address:"A3",value:"{{row.property}}"},{address:"B3",value:"{{cell.value}}"}],areas:[{name:"RowPattern",type:"repeat",source:"matrix.rows",direction:"down",range:"A3:C4"},{name:"CellPattern",type:"repeat",source:"row.cells",direction:"across",range:"B3:C3"}],merges:[]};
  return{kind,contractVersion:2,worksheetName:"Template",cells:[{address:"A1",value:"{{section.name}}"},{address:"A3",value:"{{concept.name}}"},{address:"A4",value:"{{row.property}}"},{address:"B4",value:"{{cell.value}}"}],areas:[{name:"ConceptPattern",type:"repeat",source:"profile.concepts",direction:"down",range:"A3:D6"},{name:"RowPattern",type:"repeat",source:"concept.rows",direction:"down",range:"A4:D4"},{name:"CellPattern",type:"repeat",source:"row.cells",direction:"across",range:"B4:C4"}],merges:[]};
};

const absoluteNamedRange=(range:string)=>`'Template'!${range.split(":").map(cell=>`$${cell.replace(/([A-Z]+)(\d+)/u,"$1$$$2")}`).join(":")}`;
export async function writeDocumentationTemplateStarter(kind:ProjectDocumentationSnapshot["set"]["sections"][number]["kind"]):Promise<Uint8Array>{const workbook=new (excelJs().Workbook)(),prototype=starterPrototype(kind),worksheet=workbook.addWorksheet("Template"),guide=workbook.addWorksheet("Template Guide"),catalogue=excelTemplateGuideFor(kind);for(const cell of prototype.cells){worksheet.getCell(cell.address).value=cell.value;worksheet.getCell(cell.address).style={font:{name:"Arial",size:11},alignment:{vertical:"top",wrapText:true}};}worksheet.getColumn(1).width=28;worksheet.getColumn(2).width=34;for(const area of prototype.areas)workbook.definedNames.add(absoluteNamedRange(area.range),area.name);guide.getCell("A1").value="Guided Excel template";guide.getCell("A2").value="Use exact {{value.paths}} for single values. A repeat area copies the complete named Template range in configured order.";guide.getCell("A3").value="Examples: Down rows, Across cards, and nested repeats. Empty repeatable data produces no copy.";guide.addTable?.({name:"TemplateSettings",ref:"A5",headerRow:true,columns:[{name:"Setting"},{name:"Value"}],rows:[["Contract",2],["Kind",kind]]});guide.addTable?.({name:"TemplateAreas",ref:"A10",headerRow:true,columns:[{name:"Area"},{name:"Type"},{name:"Source"},{name:"Direction"}],rows:prototype.areas.map(area=>[area.name,area.type==="repeat"?"Repeat":"Image",area.source,area.type==="repeat"?(area.direction==="across"?"Across":"Down"):""])});guide.getCell("F1").value="Single values";catalogue.values.forEach((entry,index)=>{guide.getCell(index+2,6).value=`${entry.placeholder} — ${entry.meaning} — example ${entry.example} — ${entry.available}`;});const collectionStart=catalogue.values.length+3;guide.getCell(collectionStart,6).value="Repeatable data";catalogue.collections.forEach((entry,index)=>{guide.getCell(collectionStart+index+1,6).value=`${entry.path} — ${entry.meaning}; item prefix ${entry.itemPrefix}; fields ${entry.fields.join(", ")||"none"}; nested ${entry.nestedCollections.join(", ")||"none"}; ${entry.directions.join(" or ")}; ${entry.emptyResult}; ${entry.copyBehavior}; Template Guide example ${entry.example}`;});const examplesStart=collectionStart+catalogue.collections.length+2;guide.getCell(examplesStart,6).value="Copyable TemplateAreas examples";catalogue.areaExamples.forEach((entry,index)=>{guide.getCell(examplesStart+index+1,6).value=`${entry.area} | ${entry.type} | ${entry.source} | ${entry.direction} | ${entry.range} | parent ${entry.parent??"none"}`;});guide.getColumn(6).width=120;return new Uint8Array(await workbook.xlsx.writeBuffer());}

function prototypeFromWorkbook(workbook:ExcelWorkbook,kind:ProjectDocumentationSnapshot["set"]["sections"][number]["kind"]):ExcelTemplatePrototype{
  const worksheet=workbook.getWorksheet?.("Template")??workbook.worksheets.find(({name})=>name==="Template")!,guide=workbook.getWorksheet?.("Template Guide")??workbook.worksheets.find(({name})=>name==="Template Guide")!,cells=new Map<string,ExcelTemplateCell>();worksheet.eachRow({includeEmpty:true},row=>row.eachCell({includeEmpty:true},cell=>{if(cell.value!==null&&cell.value!==undefined||Object.keys(cell.style as object).length)cells.set(cell.address,{address:cell.address,sourceAddress:cell.address,value:literalCellValue(cell.value),style:structuredClone(cell.style)});}));const ensure=(address:string)=>{if(!cells.has(address))cells.set(address,{address,sourceAddress:address,value:"",style:{}});};for(const merge of worksheet.model?.merges??[]){const[start,end=start]=merge.split(":");ensure(start!);ensure(end!);}for(const image of worksheet.getImages?.()??[]){const start=rangePoint(image.range.tl),end=image.range.br?rangePoint(image.range.br):start;ensure(cellAddress(start.row,start.column));ensure(cellAddress(end.row,end.column));}const named=new Map(workbook.definedNames.model.map(item=>[item.name,item.ranges[0]?.replace(/^'?Template'?!/u,"").replaceAll("$","")])),table=guide.getTable?.("TemplateAreas").table,ref=/^([A-Z]+)(\d+):([A-Z]+)(\d+)$/u.exec(table?.tableRef??""),columnNumber=(letters:string)=>[...letters].reduce((value,letter)=>value*26+letter.charCodeAt(0)-64,0),rows=table?.rows??(ref?Array.from({length:Number(ref[4])-Number(ref[2])},(_,rowOffset)=>Array.from({length:columnNumber(ref[3]!)-columnNumber(ref[1]!)+1},(_,columnOffset)=>guide.getCell(Number(ref[2])+1+rowOffset,columnNumber(ref[1]!)+columnOffset).value)):[]),areas=rows.flatMap<ExcelTemplateArea>(row=>{const name=String(row[0]??""),type=String(row[1]??"").toLowerCase(),source=String(row[2]??""),direction=String(row[3]??"").toLowerCase(),range=named.get(name);if(!range)return[];return type==="repeat"&&(direction==="across"||direction==="down")?[{name,type:"repeat",source,direction,range}]:type==="image"&&(source==="theme.logo"||source==="page.visual.image")?[{name,type:"image",source,range}]:[];});const areaBounds=areas.map(area=>{const[start,end=start]=area.range.split(":"),first=cellPoint(start!),last=cellPoint(end!);return{first,last};});for(const area of areas)if(area.type==="image"){const[start,end=start]=area.range.split(":");ensure(start!);ensure(end!);}for(let row=1;row<=worksheet.rowCount;row+=1)if(worksheet.getRow(row).height!==undefined){ensure(cellAddress(row,1));for(const{first,last}of areaBounds)if(row>=first.row&&row<=last.row)ensure(cellAddress(row,first.column));}for(let column=1;column<=worksheet.columns.length;column+=1){const source=worksheet.getColumn(column);if(source.width===undefined&&source.hidden===undefined&&source.outlineLevel===undefined)continue;ensure(cellAddress(1,column));for(const{first,last}of areaBounds)if(column>=first.column&&column<=last.column)ensure(cellAddress(first.row,column));}return{kind,contractVersion:2,worksheetName:"Template",cells:[...cells.values()],areas,merges:[...(worksheet.model?.merges??[])]};
}
function fillBuiltIn(worksheet:ExcelWorksheet,table:ProjectDocumentationTable):void{worksheet.getCell("A1").value=table.title;table.headings.forEach((value,index)=>worksheet.getCell(`${String.fromCharCode(65+index)}2`).value=value);table.rows.forEach((row,rowIndex)=>row.forEach((value,columnIndex)=>worksheet.getCell(`${String.fromCharCode(65+columnIndex)}${rowIndex+3}`).value=value));}
const cellPoint=(address:string)=>{const match=/^([A-Z]+)([1-9]\d*)$/u.exec(address);let column=0;for(const value of match?.[1]??"")column=column*26+value.charCodeAt(0)-64;return{row:Number(match?.[2]??1),column};};
const cellAddress=(row:number,column:number)=>{let letters="",value=column;while(value>0){value-=1;letters=String.fromCharCode(65+value%26)+letters;value=Math.floor(value/26);}return`${letters}${row}`;};
const rangePoint=(value:{nativeRow?:number;nativeCol?:number;row?:number;col?:number})=>({row:(value.nativeRow??value.row??0)+1,column:(value.nativeCol??value.col??0)+1});
const dataUrlBytes=(value:string):Uint8Array=>Uint8Array.from(atob(value.slice(value.indexOf(",")+1)),character=>character.charCodeAt(0));
export const documentationRasterDimensions=(value:string):{width:number;height:number}|undefined=>{const bytes=dataUrlBytes(value);if(bytes[0]===0x89&&bytes[1]===0x50&&bytes.length>=24){const view=new DataView(bytes.buffer,bytes.byteOffset,bytes.byteLength);return{width:view.getUint32(16),height:view.getUint32(20)};}if(bytes[0]===0x47&&bytes.length>=10){const view=new DataView(bytes.buffer,bytes.byteOffset,bytes.byteLength);return{width:view.getUint16(6,true),height:view.getUint16(8,true)};}if(bytes[0]===0xff&&bytes[1]===0xd8){for(let offset=2;offset+8<bytes.length;){if(bytes[offset]!==0xff){offset+=1;continue;}const marker=bytes[offset+1]!,length=(bytes[offset+2]!<<8)+bytes[offset+3]!;if((marker>=0xc0&&marker<=0xc3)||(marker>=0xc5&&marker<=0xc7)||(marker>=0xc9&&marker<=0xcb)||(marker>=0xcd&&marker<=0xcf))return{height:(bytes[offset+5]!<<8)+bytes[offset+6]!,width:(bytes[offset+7]!<<8)+bytes[offset+8]!};if(length<2)break;offset+=2+length;}}if(bytes.length>=30&&String.fromCharCode(...bytes.slice(0,4))==="RIFF"&&String.fromCharCode(...bytes.slice(8,12))==="WEBP"){const kind=String.fromCharCode(...bytes.slice(12,16)),payload=20;if(kind==="VP8X")return{width:1+bytes[payload+4]!+(bytes[payload+5]!<<8)+(bytes[payload+6]!<<16),height:1+bytes[payload+7]!+(bytes[payload+8]!<<8)+(bytes[payload+9]!<<16)};if(kind==="VP8L"&&bytes[payload]===0x2f)return{width:1+bytes[payload+1]!+((bytes[payload+2]!&0x3f)<<8),height:1+((bytes[payload+2]!&0xc0)>>6)+(bytes[payload+3]!<<2)+((bytes[payload+4]!&0x0f)<<10)};if(kind==="VP8 "&&bytes[payload+3]===0x9d&&bytes[payload+4]===0x01&&bytes[payload+5]===0x2a)return{width:(bytes[payload+6]!+(bytes[payload+7]!<<8))&0x3fff,height:(bytes[payload+8]!+(bytes[payload+9]!<<8))&0x3fff};}return undefined;};
export type DocumentationWebpToPng=(webpDataUrl:string)=>Promise<string>;
const browserWebpToPng:DocumentationWebpToPng=async(value)=>{if(typeof createImageBitmap!=="function")throw new Error("WebP conversion is unavailable in this renderer.");const sourceBytes=Uint8Array.from(dataUrlBytes(value)),source=new Blob([sourceBytes.buffer],{type:"image/webp"}),bitmap=await createImageBitmap(source);try{let png:Blob;if(typeof OffscreenCanvas!=="undefined"){const canvas=new OffscreenCanvas(bitmap.width,bitmap.height),context=canvas.getContext("2d");if(!context)throw new Error("WebP conversion canvas is unavailable.");context.drawImage(bitmap,0,0);png=await canvas.convertToBlob({type:"image/png"});}else{const canvas=document.createElement("canvas"),context=canvas.getContext("2d");canvas.width=bitmap.width;canvas.height=bitmap.height;if(!context)throw new Error("WebP conversion canvas is unavailable.");context.drawImage(bitmap,0,0);png=await new Promise<Blob>((resolve,reject)=>canvas.toBlob(result=>result?resolve(result):reject(new Error("WebP conversion could not encode PNG output.")),"image/png"));}const bytes=new Uint8Array(await png.arrayBuffer());let binary="";for(let offset=0;offset<bytes.length;offset+=32768)binary+=String.fromCharCode(...bytes.slice(offset,offset+32768));return`data:image/png;base64,${btoa(binary)}`;}finally{bitmap.close();}};
const embeddedImage=async(value:unknown,convertWebpToPng:DocumentationWebpToPng):Promise<ExcelImageData|undefined>=>{if(typeof value!=="string")return undefined;const match=/^data:image\/(png|jpeg|gif|webp);base64,/iu.exec(value);if(!match)return undefined;const extension=match[1]!.toLowerCase();if(extension==="webp"){const converted=await convertWebpToPng(value);if(!/^data:image\/png;base64,/iu.test(converted))throw new Error("WebP conversion did not produce a PNG image.");return{base64:converted,extension:"png"};}return{base64:value,extension:extension as "png"|"jpeg"|"gif"};};
const rectanglePixels=(sheet:ExcelWorksheet,start:{row:number;column:number},end:{row:number;column:number})=>{let width=0,height=0;for(let column=start.column;column<=end.column;column+=1)width+=(sheet.getColumn(column).width??8.43)*7;for(let row=start.row;row<=end.row;row+=1)height+=(sheet.getRow(row).height??15)*4/3;return{width,height};};

async function copyImages(source:ExcelWorkbook,sourceSheet:ExcelWorksheet,targetBook:ExcelWorkbook,targetSheet:ExcelWorksheet,rendered:ReturnType<typeof renderExcelTemplateGrid>,prototype:ExcelTemplatePrototype,context:ReturnType<typeof prepareDocumentationTemplateContext>,convertWebpToPng:DocumentationWebpToPng,imageCache:Map<string,Promise<ExcelImageData|undefined>>):Promise<void>{
  if(!targetBook.addImage||!targetSheet.addImage)return;for(const image of sourceSheet.getImages?.()??[]){const data=source.getImage?.(image.imageId);if(!data)continue;const start=rangePoint(image.range.tl),end=image.range.br?rangePoint(image.range.br):start,members=rendered.cells.filter(cell=>{const point=cellPoint(cell.sourceAddress??cell.address);return point.row>=start.row&&point.row<=end.row&&point.column>=start.column&&point.column<=end.column;}),deltas=new Map<string,{row:number;column:number}>();for(const cell of members){const from=cellPoint(cell.sourceAddress??cell.address),to=cellPoint(cell.address),delta={row:to.row-from.row,column:to.column-from.column};deltas.set(`${delta.row}:${delta.column}`,delta);}for(const delta of deltas.values())targetSheet.addImage(targetBook.addImage(data),{tl:{row:start.row-1+delta.row,col:start.column-1+delta.column},br:{row:end.row+delta.row,col:end.column+delta.column},editAs:"oneCell"});}
  for(const area of prototype.areas){if(area.type!=="image")continue;const[startText,endText=startText]=area.range.split(":"),start=cellPoint(startText!),end=cellPoint(endText!),bounds=rectanglePixels(sourceSheet,start,end),targets=rendered.cells.filter(item=>item.sourceAddress===startText).map(item=>cellPoint(item.address));if(area.source==="theme.logo"){const value=(context.theme as {logo?:unknown}|undefined)?.logo,intrinsic=typeof value==="string"?documentationRasterDimensions(value):undefined,image=await embeddedImage(value,convertWebpToPng);if(!image||!intrinsic)continue;const imageId=targetBook.addImage(image),fitted=fitProjectDocumentationLogo(intrinsic.width,intrinsic.height,bounds.width,bounds.height);for(const target of targets)targetSheet.addImage(imageId,{tl:{row:target.row-1,col:target.column-1},ext:fitted,editAs:"oneCell"});continue;}const pages=(context.flow as {pages?:unknown[]}|undefined)?.pages??[];for(const [index,target] of targets.entries()){const value=(pages[index] as {visual?:{image?:unknown}}|undefined)?.visual?.image,intrinsic=typeof value==="string"?documentationRasterDimensions(value):undefined;if(typeof value!=="string"||!intrinsic)continue;let pending=imageCache.get(value);if(!pending){pending=embeddedImage(value,convertWebpToPng);imageCache.set(value,pending);}const image=await pending;if(!image)continue;const fitted=fitProjectDocumentationLogo(intrinsic.width,intrinsic.height,bounds.width,bounds.height);targetSheet.addImage(targetBook.addImage(image),{tl:{row:target.row-1,col:target.column-1},ext:fitted,editAs:"oneCell"});}}
}

async function renderCustomInto(output:ExcelWorkbook,body:Blob,snapshot:ProjectDocumentationSnapshot,table:ProjectDocumentationTable,worksheetName:string,convertWebpToPng:DocumentationWebpToPng,imageCache:Map<string,Promise<ExcelImageData|undefined>>):Promise<void>{const section=snapshot.set.sections.find(({id})=>id===table.id)!;const validation=await validateExcelTemplateWorkbook(body,section.kind);if(!validation.valid)throw new Error(validation.findings.map(({location,message})=>`${location}: ${message}`).join("\n"));const source=new (excelJs().Workbook)();await source.xlsx.load(await body.arrayBuffer());const worksheet=source.getWorksheet?.("Template")??source.worksheets.find(({name})=>name==="Template")!,prototype=prototypeFromWorkbook(source,section.kind),context=prepareDocumentationTemplateContext(snapshot,table.id),rendered=renderExcelTemplateGrid(prototype,context),target=output.addWorksheet(worksheetName);for(const cell of rendered.cells){const destination=target.getCell(cell.address),sourcePoint=cellPoint(cell.sourceAddress??cell.address),targetPoint=cellPoint(cell.address);destination.value=cell.value;destination.style=structuredClone(cell.style??{});if(cell.note)destination.note=structuredClone(cell.note);const sourceRow=worksheet.getRow(sourcePoint.row);if(sourceRow.height!==undefined)target.getRow(targetPoint.row).height=sourceRow.height;const sourceColumn=worksheet.getColumn(sourcePoint.column),targetColumn=target.getColumn(targetPoint.column);if(sourceColumn.width!==undefined)targetColumn.width=sourceColumn.width;if(sourceColumn.hidden!==undefined)targetColumn.hidden=sourceColumn.hidden;if(sourceColumn.outlineLevel!==undefined)targetColumn.outlineLevel=sourceColumn.outlineLevel;}for(const merge of rendered.merges)target.mergeCells(merge);target.pageSetup=structuredClone(worksheet.pageSetup);target.headerFooter=structuredClone(worksheet.headerFooter);await copyImages(source,worksheet,output,target,rendered,prototype,context,convertWebpToPng,imageCache);}
const uniqueWorksheetName=(raw:string,used:Set<string>)=>{const base=safeWorksheetName(raw);let value=base,sequence=1;while(used.has(value.toLocaleLowerCase())){sequence+=1;const suffix=` (${sequence})`;value=`${base.slice(0,31-suffix.length)}${suffix}`;}used.add(value.toLocaleLowerCase());return value;};

export async function writeProjectDocumentationWorkbookWithTemplates(snapshot:ProjectDocumentationSnapshot,selection:ProjectDocumentationSelection&{confirmIncomplete?:boolean},readBody:(digest:string)=>Promise<Blob>,options:{convertWebpToPng?:DocumentationWebpToPng}={}):Promise<Uint8Array>{
  if(snapshot.incomplete&&!selection.confirmIncomplete)throw new Error("Confirm incomplete documentation before export.");const tables=selectProjectDocumentationTables(snapshot,selection);if(!tables.length)throw new Error("Choose at least one documentation section.");const templates=new Map((snapshot.templates??[]).map(template=>[template.id,template])),output=new (excelJs().Workbook)(),usedNames=new Set<string>(),imageCache=new Map<string,Promise<ExcelImageData|undefined>>(),convertWebpToPng=options.convertWebpToPng??browserWebpToPng;
  for(const table of tables){const section=snapshot.set.sections.find(({id})=>id===table.id)!,assigned=documentationTemplateAssignment(snapshot.set,"excel",section.kind),worksheetName=uniqueWorksheetName(section.name,usedNames);if(assigned==="builtin"){fillBuiltIn(output.addWorksheet(worksheetName),table);continue;}const template=templates.get(assigned);if(!template||template.format!=="excel"||template.kind!==section.kind||!template.validation.valid||!template.body)throw new Error(`${section.name} Excel template is unavailable or invalid. Open Templates to repair it.`);await renderCustomInto(output,await readBody(template.body.digest),snapshot,table,worksheetName,convertWebpToPng,imageCache);}
  return new Uint8Array(await output.xlsx.writeBuffer());
}
