import {canonicalPropertyPath,type CanonicalPropertyNode,type CanonicalRule,type CanonicalSchemaDocument} from "./data-layer-canonical-schema.js";
import {copyProfileInheritanceRecipe,createProfileInheritanceRecipe,profileInheritanceCurrentImpact,profileInheritanceSelection,profileInheritanceSummary,searchProfileInheritanceProperties,type ProfileInheritanceFilters,type ProfileInheritanceRecipe,type ProfileInheritanceStartingPoint} from "./data-layer-selective-profile-inheritance.js";

export interface ProfileInheritanceCopySource{label:string;recipe:ProfileInheritanceRecipe}
export interface ProfileInheritanceCompositionPreview{sources:readonly string[];rows:readonly {path:string;source:string;effective:string}[];conflicts:string;status:"ready"|"blocked"}
export interface SelectiveProfileInheritanceUiOptions{
  host:HTMLElement;profile:{id:string;name:string;canonicalSchema:CanonicalSchemaDocument};target:{id:string;name:string};recipe:ProfileInheritanceRecipe;
  copySources?:readonly ProfileInheritanceCopySource[];compositionPreview?:(recipe:ProfileInheritanceRecipe)=>ProfileInheritanceCompositionPreview;id:(kind:string)=>string;onApply:(recipe:ProfileInheritanceRecipe)=>void;
}

const clone=<T>(value:T):T=>structuredClone(value);
const stable=(values:readonly string[]):string[]=>[...new Set(values)];
const descendants=(document:CanonicalSchemaDocument,propertyId:string):string[]=>{const children=Object.values(document.nodes).filter(({parentId})=>parentId===propertyId).sort((left,right)=>left.order-right.order);return[propertyId,...children.flatMap(({id})=>descendants(document,id))];};
const ruleById=(document:CanonicalSchemaDocument,ruleId:string):{propertyId:string;rule:CanonicalRule}|undefined=>{for(const node of Object.values(document.nodes)){const rule=node.rules.find(({id})=>id===ruleId);if(rule)return{propertyId:node.id,rule};}}
const option=(value:string,label:string)=>new Option(label,value);

export function mountSelectiveProfileInheritance(options:SelectiveProfileInheritanceUiOptions):HTMLElement{
  const {host,profile,target}=options,document=profile.canonicalSchema,card=globalThis.document.createElement("section"),heading=globalThis.document.createElement("h3"),summaryText=globalThis.document.createElement("p"),resolutionText=globalThis.document.createElement("p"),impactList=globalThis.document.createElement("ul"),edit=globalThis.document.createElement("button"),workspace=globalThis.document.createElement("section");let staged=clone(options.recipe),open=false,filters:ProfileInheritanceFilters={query:"",concept:"all",type:"all",required:"any",selection:"any"},replacementDraft:{sourceRuleId:string;propertyId:string;presence?:"required"|"optional"|"forbidden";rule?:CanonicalRule;keepCondition:boolean;reviewed:boolean}|undefined;
  card.className="profile-inheritance-card";card.dataset.profileInheritanceCard=profile.id;card.tabIndex=-1;card.setAttribute("aria-label",`${profile.name} inheritance summary`);heading.textContent=profile.name;edit.type="button";edit.textContent="Edit selection";edit.setAttribute("aria-expanded","false");workspace.className="profile-inheritance-workspace";workspace.dataset.profileInheritanceWorkspace=profile.id;workspace.hidden=true;
  const updateCard=()=>{const summary=profileInheritanceSummary(document,options.recipe),impact=options.recipe.sourceImpact??profileInheritanceCurrentImpact(document,options.recipe),impactText=impact.stale?` · Source update needs review: ${impact.addedEffectivePropertyIds.length} added, ${impact.removedPropertyIds.length} removed, ${impact.changedPaths.length} changed paths, ${impact.changedRuleIds.length} changed rules, ${impact.newMissingRuleDependencies.length} new dependencies`:" · Current with source";summaryText.textContent=`${summary.synchronizedConcepts} synchronized concepts · ${summary.fixedProperties} fixed selections · ${summary.exclusions} exclusions · ${summary.ruleOverrides} rule overrides · ${summary.effective} effective properties${summary.missingSelections?` · ${summary.missingSelections} missing selections`:""}${impactText}`;resolutionText.setAttribute("aria-label","Shared Profile rule resolution provenance");resolutionText.hidden=!options.recipe.excludedRuleIds.length&&!options.recipe.ruleReplacements.length&&!options.recipe.presenceReplacements.length;resolutionText.textContent=`${profile.name} source rules at ${target.name} boundary · Excluded ${options.recipe.excludedRuleIds.join(", ")||"none"} · Replaced ${[...options.recipe.ruleReplacements.map(({sourceRuleId})=>sourceRuleId),...options.recipe.presenceReplacements.map(({sourceRuleId})=>sourceRuleId)].join(", ")||"none"}`;impactList.setAttribute("aria-label","Shared Profile source impact");impactList.hidden=!impact.stale;impactList.replaceChildren(...impact.addedEffectivePropertyIds.map((id)=>Object.assign(globalThis.document.createElement("li"),{textContent:`Added effective path ${document.nodes[id]?canonicalPropertyPath(document,id):id}`})),...impact.removedPropertyIds.map((id)=>Object.assign(globalThis.document.createElement("li"),{textContent:`Removed effective path ${options.recipe.sourceSnapshot?.propertyPaths[id]??id}`})),...impact.changedPaths.map(({before,after})=>Object.assign(globalThis.document.createElement("li"),{textContent:`Changed effective path ${before} → ${after}`})),...impact.changedRuleIds.map((id)=>Object.assign(globalThis.document.createElement("li"),{textContent:`Changed source rule ${id}`})),...impact.newMissingRuleDependencies.map(({sourceRuleId,propertyId})=>Object.assign(globalThis.document.createElement("li"),{textContent:`New dependency ${sourceRuleId} needs ${document.nodes[propertyId]?canonicalPropertyPath(document,propertyId):propertyId}`})));};
  const focusAfter=(selector:string)=>queueMicrotask(()=>workspace.querySelector<HTMLElement>(selector)?.focus({preventScroll:true}));
  const renderWorkspace=()=>{
    workspace.replaceChildren();const title=globalThis.document.createElement("h3"),intro=globalThis.document.createElement("p"),composition=globalThis.document.createElement("section"),starting=globalThis.document.createElement("select"),startingLabel=globalThis.document.createElement("label"),copySelect=globalThis.document.createElement("select"),copyButton=globalThis.document.createElement("button"),copyLabel=globalThis.document.createElement("label"),search=globalThis.document.createElement("input"),searchLabel=globalThis.document.createElement("label"),filterRow=globalThis.document.createElement("div"),conceptFilter=globalThis.document.createElement("select"),typeFilter=globalThis.document.createElement("select"),requiredFilter=globalThis.document.createElement("select"),selectionFilter=globalThis.document.createElement("select"),concepts=globalThis.document.createElement("section"),results=globalThis.document.createElement("ul"),dependencies=globalThis.document.createElement("section"),sticky=globalThis.document.createElement("aside"),apply=globalThis.document.createElement("button"),cancel=globalThis.document.createElement("button");
    title.textContent=`Choose what ${target.name} inherits from ${profile.name}`;intro.textContent="Concepts stay synchronized with the source. Property choices are pinned to stable identities. Explicit exclusions continue to win.";startingLabel.textContent="Starting point";for(const [value,label] of [["everything","Everything"],["concepts","Choose concepts"],["properties","Choose properties"],["empty","Start empty"]] as const)starting.append(option(value,label));starting.value=staged.startingPoint;startingLabel.append(starting);
    starting.addEventListener("change",()=>{const next=createProfileInheritanceRecipe({id:staged.id,profileId:staged.profileId,targetId:staged.targetId,startingPoint:starting.value as ProfileInheritanceStartingPoint,sourceRevision:document.revision});staged=next;renderWorkspace();focusAfter("select");});
    const sources=options.copySources??[];copyLabel.textContent="Copy selection from";copySelect.append(option("","Choose a target"));for(const source of sources)copySelect.append(option(source.recipe.id,source.label));copyButton.type="button";copyButton.textContent="Copy selection";copyButton.disabled=!sources.length;copyButton.addEventListener("click",()=>{const source=sources.find(({recipe})=>recipe.id===copySelect.value);if(!source)return;staged=copyProfileInheritanceRecipe(source.recipe,{id:staged.id,targetId:target.id});staged.profileId=profile.id;staged.sourceRevision=document.revision;renderWorkspace();focusAfter("[data-profile-copy]");});copyLabel.append(copySelect,copyButton);copyLabel.dataset.profileCopy="";
    searchLabel.textContent="Search source properties";search.type="search";search.value=filters.query;search.placeholder="Name, path, description, or example";search.addEventListener("input",()=>{filters={...filters,query:search.value};renderWorkspace();focusAfter("input[type=search]");});searchLabel.append(search);
    const conceptsAvailable=stable(Object.values(document.nodes).flatMap(({concept})=>concept?[concept]:[])),types=stable(Object.values(document.nodes).map(({type})=>type));conceptFilter.append(option("all","All concepts"),...conceptsAvailable.map((value)=>option(value,value)));typeFilter.append(option("all","All types"),...types.map((value)=>option(value,value)));requiredFilter.append(option("any","Any required state"),option("required","Required"),option("optional","Optional"));selectionFilter.append(option("any","Any selection state"),option("selected","Selected"),option("unselected","Unselected"));for(const [control,key] of [[conceptFilter,"concept"],[typeFilter,"type"],[requiredFilter,"required"],[selectionFilter,"selection"]] as const){control.value=filters[key];control.setAttribute("aria-label",`${key} filter`);control.addEventListener("change",()=>{filters={...filters,[key]:control.value};renderWorkspace();focusAfter(`[aria-label='${key} filter']`);});filterRow.append(control);}filterRow.className="profile-inheritance-filters";
    concepts.setAttribute("aria-label","Concept selections");concepts.append(Object.assign(globalThis.document.createElement("h4"),{textContent:"Live concept selections"}));const selected=new Set(profileInheritanceSelection(document,staged).directPropertyIds);for(const concept of conceptsAvailable){const ids=Object.values(document.nodes).filter((node)=>node.concept===concept).map(({id})=>id),count=ids.filter((id)=>selected.has(id)).length,button=globalThis.document.createElement("button"),active=staged.conceptSelections.includes(concept);button.type="button";button.dataset.concept=concept;button.setAttribute("role","checkbox");button.setAttribute("aria-checked",count===0?"false":count===ids.length?"true":"mixed");button.textContent=`${concept}: ${count} of ${ids.length} selected${active?" · synchronized":""}`;button.addEventListener("click",()=>{staged.conceptSelections=active?staged.conceptSelections.filter((value)=>value!==concept):stable([...staged.conceptSelections,concept]);renderWorkspace();focusAfter(`[data-concept='${CSS.escape(concept)}']`);});concepts.append(button);}
    const matches=searchProfileInheritanceProperties(document,filters,staged),windowed=matches.slice(0,80);results.className="profile-inheritance-results";results.setAttribute("aria-label",`Source properties, showing ${windowed.length} of ${matches.length}`);for(const node of windowed){const item=globalThis.document.createElement("li"),toggle=globalThis.document.createElement("button"),branchIds=descendants(document,node.id),selectedCount=branchIds.filter((id)=>selected.has(id)).length,isSelected=selected.has(node.id),path=canonicalPropertyPath(document,node.id);toggle.type="button";toggle.dataset.profileProperty=node.id;toggle.setAttribute("role","checkbox");toggle.setAttribute("aria-checked",selectedCount===0?"false":selectedCount===branchIds.length?"true":"mixed");toggle.textContent=`${path} · ${node.type} · ${selectedCount} of ${branchIds.length} selected${isSelected?" · selected":" · unselected"}`;toggle.addEventListener("click",()=>{if(selectedCount===branchIds.length){staged.propertySelections=staged.propertySelections.filter((id)=>!branchIds.includes(id));staged.excludedPropertyIds=stable([...staged.excludedPropertyIds,...branchIds]);}else{staged.propertySelections=stable([...staged.propertySelections,...branchIds]);staged.excludedPropertyIds=staged.excludedPropertyIds.filter((id)=>!branchIds.includes(id));}renderWorkspace();focusAfter(`[data-profile-property='${CSS.escape(node.id)}']`);});const detail=globalThis.document.createElement("small");detail.textContent=[node.documentation.displayText,node.documentation.description,node.documentation.example.value].filter(Boolean).join(" · ");item.append(toggle,detail);results.append(item);}
    dependencies.setAttribute("aria-label","Missing rule dependencies");dependencies.append(Object.assign(globalThis.document.createElement("h4"),{textContent:"Rule dependencies"}));const closure=profileInheritanceSelection(document,staged);if(!closure.missingRuleDependencies.length)dependencies.append(Object.assign(globalThis.document.createElement("p"),{textContent:"No unresolved rule dependencies."}));for(const dependency of closure.missingRuleDependencies){
      const row=globalThis.document.createElement("div"),description=globalThis.document.createElement("p"),include=globalThis.document.createElement("button"),exclude=globalThis.document.createElement("button"),replace=globalThis.document.createElement("button"),source=ruleById(document,dependency.sourceRuleId),invariant=source?.rule.enforcement==="invariant";row.dataset.ruleDependency=dependency.sourceRuleId;description.textContent=`${canonicalPropertyPath(document,dependency.sourcePropertyId)} rule ${dependency.sourceRuleId} needs ${canonicalPropertyPath(document,dependency.propertyId)}`;include.type=exclude.type=replace.type="button";include.textContent=`Include ${canonicalPropertyPath(document,dependency.propertyId)}`;exclude.textContent="Exclude this rule";replace.textContent="Replace rule for this target";exclude.disabled=replace.disabled=Boolean(invariant);if(invariant)description.textContent+=` · invariant ${dependency.sourceRuleId} cannot be excluded or weakened`;
      for(const [control,action]of[[include,"Include dependency"],[exclude,"Exclude source rule"],[replace,"Replace source rule"]] as const)control.setAttribute("aria-label",`${action} ${dependency.sourceRuleId} for ${target.name}`);include.addEventListener("click",()=>{staged.includedDependencyPropertyIds=stable([...staged.includedDependencyPropertyIds,dependency.propertyId]);replacementDraft=undefined;renderWorkspace();focusAfter("[data-rule-dependency] button,[aria-label='Selection summary'] button");});exclude.addEventListener("click",()=>{staged.excludedRuleIds=stable([...staged.excludedRuleIds,dependency.sourceRuleId]);replacementDraft=undefined;renderWorkspace();focusAfter("[data-rule-dependency] button,[aria-label='Selection summary'] button");});replace.addEventListener("click",()=>{replacementDraft={sourceRuleId:dependency.sourceRuleId,propertyId:dependency.sourcePropertyId,...(dependency.sourceRuleId.startsWith("presence:")?{presence:"required" as const}:source?{rule:clone(source.rule)}:{}),keepCondition:false,reviewed:false};renderWorkspace();focusAfter("[data-replacement-editor] input,[data-replacement-editor] select");});row.append(description,include,exclude,replace);
      if(replacementDraft?.sourceRuleId===dependency.sourceRuleId){const editor=globalThis.document.createElement("section"),editorHeading=globalThis.document.createElement("h5"),keep=globalThis.document.createElement("input"),keepLabel=globalThis.document.createElement("label"),review=globalThis.document.createElement("button"),cancelReplacement=globalThis.document.createElement("button");editor.dataset.replacementEditor=dependency.sourceRuleId;editor.setAttribute("aria-label",`Target-specific replacement for ${dependency.sourceRuleId}`);editorHeading.textContent="Review target-specific replacement";keep.type="checkbox";keep.checked=replacementDraft.keepCondition;keepLabel.append(keep,"Keep source condition and its dependencies");keep.addEventListener("change",()=>{replacementDraft!.keepCondition=keep.checked;replacementDraft!.reviewed=false;});review.type=cancelReplacement.type="button";review.textContent="Review replacement preview";cancelReplacement.textContent="Cancel replacement";cancelReplacement.addEventListener("click",()=>{replacementDraft=undefined;renderWorkspace();focusAfter(`[data-rule-dependency='${CSS.escape(dependency.sourceRuleId)}'] button:last-of-type`);});
        if(replacementDraft.presence){const presence=globalThis.document.createElement("select"),label=globalThis.document.createElement("label");label.textContent="Target presence";for(const value of["required","optional","forbidden"] as const)presence.append(option(value,value));presence.value=replacementDraft.presence;presence.addEventListener("change",()=>{replacementDraft!.presence=presence.value as "required"|"optional"|"forbidden";replacementDraft!.reviewed=false;});label.append(presence);editor.append(editorHeading,label);}
        else if(replacementDraft.rule){const rule=replacementDraft.rule,fields=globalThis.document.createElement("div"),field=(labelText:string,value:string,onInput:(value:string)=>void,type="text")=>{const label=globalThis.document.createElement("label"),input=globalThis.document.createElement("input");label.textContent=labelText;input.type=type;input.value=value;input.addEventListener("input",()=>{onInput(input.value);replacementDraft!.reviewed=false;});label.append(input);fields.append(label);};if(rule.kind==="pattern")field("Target pattern",rule.pattern??"",(value)=>{rule.pattern=value;});else if(rule.kind==="range"){field("Target minimum",String(rule.minimum??""),(value)=>{if(value)rule.minimum=Number(value);else delete rule.minimum;},"number");field("Target maximum",String(rule.maximum??""),(value)=>{if(value)rule.maximum=Number(value);else delete rule.maximum;},"number");}else if(rule.kind==="cardinality"){field("Target minimum items",String(rule.minItems??""),(value)=>{if(value)rule.minItems=Number(value);else delete rule.minItems;},"number");field("Target maximum items",String(rule.maxItems??""),(value)=>{if(value)rule.maxItems=Number(value);else delete rule.maxItems;},"number");}else field("Target expected value",JSON.stringify(rule.expectedValue??""),(value)=>{try{rule.expectedValue=JSON.parse(value);}catch{rule.expectedValue=value;}});field("Target rule message",rule.message??"",(value)=>{rule.message=value;});editor.append(editorHeading,fields,keepLabel);}
        review.addEventListener("click",()=>{replacementDraft!.reviewed=true;renderWorkspace();focusAfter("[data-confirm-replacement]");});editor.append(review,cancelReplacement);if(replacementDraft.reviewed){const preview=globalThis.document.createElement("pre"),confirm=globalThis.document.createElement("button");preview.textContent=JSON.stringify(replacementDraft.presence?{presence:replacementDraft.presence,condition:replacementDraft.keepCondition?document.nodes[dependency.sourcePropertyId]?.presence.condition:undefined}:{...replacementDraft.rule,...(!replacementDraft.keepCondition?{condition:undefined}:{})},null,2);confirm.type="button";confirm.dataset.confirmReplacement="";confirm.textContent="Use reviewed replacement";confirm.addEventListener("click",()=>{if(replacementDraft!.presence)staged.presenceReplacements=[...staged.presenceReplacements,{sourceRuleId:dependency.sourceRuleId,propertyId:dependency.sourcePropertyId,presence:replacementDraft!.presence}];else if(replacementDraft!.rule){const rule={...clone(replacementDraft!.rule),id:options.id("rule"),replacesRuleId:dependency.sourceRuleId};if(!replacementDraft!.keepCondition)delete rule.condition;staged.excludedRuleIds=stable([...staged.excludedRuleIds,dependency.sourceRuleId]);staged.ruleReplacements=[...staged.ruleReplacements,{sourceRuleId:dependency.sourceRuleId,propertyId:dependency.sourcePropertyId,rule}];}replacementDraft=undefined;renderWorkspace();focusAfter("[data-rule-dependency] button,[aria-label='Selection summary'] button");});editor.append(preview,confirm);}row.append(editor);}
      dependencies.append(row);
    }
    const preview=options.compositionPreview?.(staged);if(preview){composition.setAttribute("aria-label","Recalculated Shared Profile composition preview");composition.append(Object.assign(globalThis.document.createElement("h4"),{textContent:`${preview.status==="blocked"?"Blocked":"Ready"} composition · ${preview.sources.join(" + ")}`}),Object.assign(globalThis.document.createElement("p"),{textContent:preview.conflicts}));const previewRows=globalThis.document.createElement("ul");for(const row of preview.rows.slice(0,40))previewRows.append(Object.assign(globalThis.document.createElement("li"),{textContent:`${row.path} · ${row.source} · ${row.effective}`}));composition.append(previewRows);}
    const totals=profileInheritanceSummary(document,staged),unresolved=profileInheritanceSelection(document,staged).missingRuleDependencies.length;sticky.className="profile-inheritance-summary";sticky.setAttribute("aria-label","Selection summary");sticky.textContent=`Direct ${totals.direct} · Structural ${totals.structural} · Rule dependencies ${totals.ruleDependencies} · Conflicts ${totals.conflicts} · Effective total ${totals.effective}`;apply.type=cancel.type="button";apply.textContent="Apply inheritance";apply.disabled=!preview||unresolved>0;apply.setAttribute("aria-description",!preview?"Waiting for target-specific preview":unresolved?`${unresolved} rule dependencies require review`:preview.status==="blocked"?"Preview reviewed; unresolved peer conflicts will remain blocked in the Draft":"Target-specific preview reviewed and ready");cancel.textContent="Cancel";apply.addEventListener("click",()=>options.onApply({...clone(staged),sourceRevision:document.revision}));cancel.addEventListener("click",()=>{staged=clone(options.recipe);open=false;workspace.hidden=true;edit.setAttribute("aria-expanded","false");edit.focus({preventScroll:true});});sticky.append(apply,cancel);workspace.append(title,intro,...(preview?[composition]:[]),startingLabel,...(sources.length?[copyLabel]:[]),searchLabel,filterRow,concepts,results,dependencies,sticky);
  };
  edit.addEventListener("click",()=>{open=!open;workspace.hidden=!open;edit.setAttribute("aria-expanded",String(open));if(open){staged=clone(options.recipe);renderWorkspace();queueMicrotask(()=>workspace.querySelector<HTMLElement>("select,input,button")?.focus({preventScroll:true}));}});updateCard();card.append(heading,summaryText,resolutionText,impactList,edit,workspace);host.append(card);return card;
}
