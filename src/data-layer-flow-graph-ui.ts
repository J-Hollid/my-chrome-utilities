import {
  addFlowPageFrame,
  addFreePageFrame,
  addEventOccurrenceToPage,
  addGraphOccurrence,
  addInteractionOccurrenceToPage,
  deriveFlowOccurrenceExample,
  documentaryFlowGraph,
  flowOccurrenceExampleEditorRows,
  FLOW_GRAPH_GEOMETRY,
  flowRelationshipText,
  inspectFreePageEdgeMove,
  migrateLegacyFlowContextBindings,
  migrateLegacyFlowRelationshipKinds,
  moveFlowPageFrame,
  moveFreePageFrame,
  moveGraphOccurrence,
  projectFlowGraph,
  reviewLegacyFlowContextMigration,
  removeFlowPageFrame,
  removeFlowRelationship,
  removeGraphOccurrence,
  reorderFlowPageGroupLane,
  saveGraphRelationship,
  setFlowOccurrenceExample,
  setFlowPageGroupLanes,
  type DocumentaryPageFrameRecord,
  type FlowLayout,
  type FreePageRegion,
  type FlowPortSide,
} from "./data-layer-flow-graph.js";
import {orderedPageGroupIds} from "./utilities/data-layer/page-group-membership.js";
import {type IdFactory,type ProjectEntity,type ProjectState} from "./utilities/data-layer/schemas.js";

export interface FlowGraphBuilderContext {state?:ProjectState;flowId?:string;revision?:number}

export function contextSettingPageLabel(pageName:string):string{return `${pageName} · Context-setting Page`;}
export interface FlowGraphBuilderIntegration {render():void;renderSelectors():void}
interface IntegrationOptions {context:()=>FlowGraphBuilderContext;persist:(state:ProjectState)=>void;id:IdFactory;openOccurrenceSchema?:(occurrenceId:string,path?:string)=>boolean}

const nodeWidth=FLOW_GRAPH_GEOMETRY.eventWidth,nodeHeight=FLOW_GRAPH_GEOMETRY.eventHeight;
const q=<T extends Element>(selector:string,root:ParentNode=document):T=>{const element=root.querySelector<T>(selector);if(!element)throw new Error(`Missing ${selector}`);return element;};
const svg=<K extends keyof SVGElementTagNameMap>(tag:K):SVGElementTagNameMap[K]=>document.createElementNS("http://www.w3.org/2000/svg",tag);
const button=(text:string,action:()=>void):HTMLButtonElement=>{const control=document.createElement("button");control.type="button";control.textContent=text;control.addEventListener("click",action);return control;};
const entityName=(entities:readonly ProjectEntity[],id:unknown,fallback="Unknown"):string=>entities.find((entity)=>entity.id===id)?.name??fallback;
const elementByData=<T extends HTMLElement|SVGElement>(attribute:string,id:string):T|undefined=>Array.from(document.querySelectorAll<T>(`[${attribute}]`)).find((element)=>element.getAttribute(attribute)===id);

function renderOccurrenceExampleControls(host:HTMLElement,state:ProjectState,flowId:string,occurrenceId:string,persist:(next:ProjectState)=>void,id:IdFactory):void{
  host.setAttribute("aria-label","Occurrence example controls");for(const row of flowOccurrenceExampleEditorRows(state.project,flowId,occurrenceId).filter(({type})=>type!=="object"&&type!=="array")){const item=document.createElement("label"),value=document.createElement("input"),save=document.createElement("button");item.dataset.exampleEditorPath=row.path;item.append(`${row.path} · ${row.type??"unknown"} `);value.setAttribute("aria-label",`Example value for ${row.path}`);value.value=row.value===undefined?"":String(row.value);save.type="button";save.textContent="Save example";save.addEventListener("click",()=>persist(setFlowOccurrenceExample(state,flowId,occurrenceId,row.path,value.value,id)));item.append(value,save);host.append(item);}
}

export const ownsPointerDrag=(activePointerId:number|undefined,eventPointerId:number):boolean=>activePointerId!==undefined&&activePointerId===eventPointerId;
export function restorePointerCancellationFocus(target:{readonly isConnected:boolean;focus():void},scheduleMicrotask:(callback:()=>void)=>unknown=(callback)=>queueMicrotask(callback),scheduleSettledTask:(callback:()=>void)=>unknown=(callback)=>setTimeout(callback,0)):void{const restore=()=>{if(target.isConnected)target.focus();};restore();scheduleMicrotask(restore);scheduleSettledTask(restore);}
type SessionFlowView={selectedItem?:{kind:"page-frame"|"occurrence"|"relationship";id:string}|undefined;viewport?:{x:number;y:number;zoom:number}};
export interface RelationshipDeletionFocusIntent{id:string;sourceKind:"page-frame"|"event-occurrence";sourceId:string;sourceFocused:boolean}
export function flowViewAfterRelationshipDeletion(view:SessionFlowView,relationshipId:string):SessionFlowView{if(view.selectedItem?.kind!=="relationship"||view.selectedItem.id!==relationshipId)return view;const{selectedItem,...retained}=view;void selectedItem;return retained;}
export function consumeRelationshipDeletionFocus(intent:RelationshipDeletionFocusIntent|undefined,relationshipRestored:boolean):{target?:"source"|"relationship";next?:RelationshipDeletionFocusIntent}{if(!intent)return{};if(relationshipRestored)return{target:"relationship"};if(!intent.sourceFocused)return{target:"source",next:{...intent,sourceFocused:true}};return{next:intent};}
export function flowRelationshipDeletionAccessibleName(label:string|undefined,sourceName:string,targetName:string):string{return`Delete relationship ${[label,`${sourceName} to ${targetName}`].filter(Boolean).join(", ")}`;}

const flowPortPoint=(layout:{x:number;y:number},size:{width:number;height:number},port:FlowPortSide):{x:number;y:number}=>port==="left"?{x:layout.x,y:layout.y+size.height/2}:port==="right"?{x:layout.x+size.width,y:layout.y+size.height/2}:port==="top"?{x:layout.x+size.width/2,y:layout.y}:{x:layout.x+size.width/2,y:layout.y+size.height};
export function flowEdgeGeometry(source:{x:number;y:number},target:{x:number;y:number},sourceSize:{width:number;height:number}={width:nodeWidth,height:nodeHeight},targetSize:{width:number;height:number}=sourceSize,sourcePort:FlowPortSide="right",targetPort:FlowPortSide="left"):{startX:number;startY:number;endX:number;endY:number;arrow:string}{
  const start=flowPortPoint(source,sourceSize,sourcePort),end=flowPortPoint(target,targetSize,targetPort),startX=start.x,startY=start.y,endX=end.x,endY=end.y,dx=endX-startX,dy=endY-startY,length=Math.hypot(dx,dy),unitX=length<0.001?1:dx/length,unitY=length<0.001?0:dy/length,baseX=endX-unitX*12,baseY=endY-unitY*12,normalX=-unitY*7,normalY=unitX*7;
  return{startX,startY,endX,endY,arrow:`${baseX+normalX},${baseY+normalY} ${endX},${endY} ${baseX-normalX},${baseY-normalY}`};
}

export function installFlowGraphBuilder(options:IntegrationOptions):FlowGraphBuilderIntegration{
  const inspector=q<HTMLElement>("#project-inspector"),advanced=q<HTMLElement>("#flow-step-editor"),inspectorContext=document.createElement("section");
  inspectorContext.id="flow-inspector-context";inspector.insertBefore(inspectorContext,advanced);
  let selected:{kind:"page-frame"|"occurrence"|"relationship";id:string}|undefined;
  let connection:{sourceId:string;sourcePort?:FlowPortSide;targets:string[];targetIndex:number;preview?:SVGLineElement}|undefined;
  let relationshipPopoverFocusIntent:{id:string;revision:number;optimisticFocused:boolean}|undefined;
  let relationshipEdgeFocusIntent:{id:string;revision:number;optimisticFocused:boolean}|undefined;
  let relationshipDeletionFocusIntent:RelationshipDeletionFocusIntent|undefined;
  let relationshipDeletionFocusTimer:ReturnType<typeof setTimeout>|undefined;
  let pageFrameFocusIntent:{id:string;revision:number;optimisticFocused:boolean}|undefined;
  let suppressNodeClick=false;
  let statusMessage="";
  let statusRepairHref="";
  let activeCatalogPayload:{kind:string;id:string}|undefined;
  const viewKey=(projectId:string,flowId:string)=>`my-chrome-utilities.flow-view.v1:${projectId}:${flowId}`;
  const readView=(projectId:string,flowId:string):{selectedItem?:typeof selected;viewport?:{x:number;y:number;zoom:number}}=>{try{return JSON.parse(sessionStorage.getItem(viewKey(projectId,flowId))??"{}");}catch{return{};}};
  const writeView=(projectId:string,flowId:string,view:{selectedItem?:typeof selected;viewport?:{x:number;y:number;zoom:number}}):void=>sessionStorage.setItem(viewKey(projectId,flowId),JSON.stringify(view));
  const clearPageDropStates=()=>document.querySelectorAll<HTMLElement>("[data-lane-dropzone]").forEach((lane)=>delete lane.dataset.pageDropState);
  const markPageDropStates=(pageId:string)=>{const {state}=current();if(!state)return;const memberships=orderedPageGroupIds(state.project,pageId);document.querySelectorAll<HTMLElement>("[data-lane-dropzone]").forEach((lane)=>{lane.dataset.pageDropState=memberships.includes(String(lane.dataset.laneDropzone))?"valid":"invalid";});};
  const clearActiveCatalogPayload=()=>{activeCatalogPayload=undefined;clearPageDropStates();};
  window.addEventListener("pointerup",clearActiveCatalogPayload);window.addEventListener("mouseup",clearActiveCatalogPayload);

  const current=()=>{const context=options.context(),flow=context.flowId&&context.state?.project.collections.flows.find(({id})=>id===context.flowId),graph=flow&&context.state?documentaryFlowGraph(context.state.project,flow.id):undefined;return{...context,flow,graph};};
  const persist=(next:ProjectState,feedback=""):void=>{try{statusMessage=feedback;statusRepairHref="";options.persist(next);render();}catch(error){statusMessage=error instanceof Error?error.message:String(error);render();}};
  const pageFrame=(frameId:string):DocumentaryPageFrameRecord|undefined=>current().graph?.pageFrames.find(({id})=>id===frameId);
  const selectedFrameForPage=(pageId:string):DocumentaryPageFrameRecord|undefined=>current().graph?.pageFrames.find((frame)=>frame.pageId===pageId);
  const saveSelection=(value:typeof selected):void=>{selected=value;const {state,flow}=current();if(state&&flow)writeView(state.project.id,flow.id,value?{selectedItem:value}:{});render();};

  function renderInspector():void{
    inspectorContext.replaceChildren();const {state,flow,graph}=current();if(!state||!flow){inspectorContext.hidden=true;return;}inspectorContext.hidden=false;const heading=document.createElement("h3"),copy=document.createElement("p");heading.textContent="Flow details";
    if(!selected){copy.textContent=`${flow.name}. Select a lane, Page frame, occurrence, or relationship for provenance and details. All graph commands remain in the main workspace.`;inspectorContext.append(heading,copy);return;}
    const occurrence=graph?.occurrences.find(({id})=>id===selected!.id),relationship=graph?.relationships.find(({id})=>id===selected!.id),frame=graph?.pageFrames.find(({id})=>id===selected!.id);copy.textContent=occurrence?`${occurrence.name} · stable occurrence ${occurrence.id}`:relationship?`Stable relationship ${relationship.id}`:frame?`${entityName(state.project.collections.pages,frame.pageId)} · stable Page frame ${frame.id}`:"Selection details unavailable";inspectorContext.append(heading,copy);
  }

  function catalog(kind:"Page Groups"|"Pages"|"Events",entities:readonly ProjectEntity[],activate:(entity:ProjectEntity)=>void):HTMLElement{
    const section=document.createElement("section"),heading=document.createElement("h4"),search=document.createElement("input"),items=document.createElement("div");section.setAttribute("aria-label",`${kind} catalog`);heading.textContent=kind;search.type="search";search.placeholder=`Search ${kind}`;search.setAttribute("aria-label",`Search ${kind}`);
    const renderItems=()=>{const term=search.value.trim().toLowerCase();items.replaceChildren(...entities.filter(({name})=>name.toLowerCase().includes(term)).map((entity)=>{const control=button(entity.name,()=>activate(entity));control.draggable=true;control.dataset.componentKind=kind==="Page Groups"?"page-group":kind==="Pages"?"page":"event";control.dataset.componentId=entity.id;if(kind==="Pages"){const {state}=current(),owners=state?orderedPageGroupIds(state.project,entity.id).map((id)=>entityName(state.project.collections.pageGroups,id)):[];control.textContent=`${entity.name} · ${owners.join(" → ")||"Ungrouped"}`;}const payload=()=>({kind:String(control.dataset.componentKind),id:entity.id});control.addEventListener("pointerdown",()=>{activeCatalogPayload=payload();if(kind==="Pages")markPageDropStates(entity.id);});control.addEventListener("dragstart",(event)=>{activeCatalogPayload=payload();event.dataTransfer?.setData("application/x-flow-component",JSON.stringify(activeCatalogPayload));if(kind==="Pages")event.dataTransfer?.setData("application/x-flow-page-component",entity.id);});control.addEventListener("dragend",clearActiveCatalogPayload);return control;}));};
    search.addEventListener("input",renderItems);renderItems();section.append(heading,search,items);return section;
  }

  function addLane(group:ProjectEntity):void{const {state,flow,graph}=current();if(!state||!flow||graph?.pageGroupIds.includes(group.id))return;persist(setFlowPageGroupLanes(state,flow.id,[...(graph?.pageGroupIds??[]),group.id]));}
  function insertPage(page:ProjectEntity,targetGroupId?:string):void{
    const {state,flow,graph}=current();if(!state||!flow||!graph)return;const memberships=orderedPageGroupIds(state.project,page.id),selectedGroups=state.project.collections.pageGroups.filter((group)=>graph.pageGroupIds.includes(group.id)&&memberships.includes(group.id)),target=targetGroupId?selectedGroups.find(({id})=>id===targetGroupId):selectedGroups[0];
    if(target){const count=graph.pageFrames.filter(({pageGroupId})=>pageGroupId===target.id).length,next=addFlowPageFrame(state,flow.id,{pageId:page.id,pageGroupId:target.id,x:40+count*240,y:120},options.id);if(next===state){statusMessage=`${page.name} is already placed or does not belong to ${target.name}. Open Page Group membership.`;render();}else persist(next);return;}
    if(targetGroupId){const attempted=state.project.collections.pageGroups.find(({id})=>id===targetGroupId);statusMessage=`${page.name} does not belong to ${attempted?.name??targetGroupId}. Open Page Group membership to repair placement.`;statusRepairHref=`?kind=pages&entity=${encodeURIComponent(page.id)}&field=pageGroupIds`;render();return;}
    if(memberships.length){statusMessage=`Add ${entityName(state.project.collections.pageGroups,memberships[0])} to the lane order before placing ${page.name}.`;render();return;}
    statusMessage=`Drag ${page.name} beyond the selected lanes to place it before or after the lanes.`;render();
  }
  function insertFreePage(page:ProjectEntity,region:FreePageRegion,x:number,y:number):void{const {state,flow}=current();if(!state||!flow)return;try{persist(addFreePageFrame(state,flow.id,{pageId:page.id,region,x,y},options.id));}catch(error){statusMessage=error instanceof Error?error.message:String(error);statusRepairHref=`?kind=pages&entity=${encodeURIComponent(page.id)}&field=pageGroupIds`;render();}}
  function insertEvent(event:ProjectEntity,frameId?:string):void{const {state,flow,graph}=current(),selectedFrameId=frameId??(selected&&selected.kind==="page-frame"?selected.id:undefined),frame=selectedFrameId?pageFrame(selectedFrameId):undefined;if(!state||!flow||!graph||!frame){statusMessage="Select a Page frame before inserting an Event.";render();return;}const trigger=typeof event.trigger==="string"&&event.trigger.trim()?event.trigger.trim():undefined,count=graph.occurrences.filter((occurrence)=>occurrence.pageFrameId===frame.id).length;persist(addEventOccurrenceToPage(state,flow.id,{name:event.name,pageFrameId:frame.id,...(frame.pageGroupId?{pageGroupId:frame.pageGroupId}:{}),pageId:frame.pageId,eventId:event.id,...(trigger?{trigger}:{}),obligation:"Required",minimum:1,maximum:1,x:24+count*210,y:70},options.id));}

  function occurrenceExampleDetails(state:ProjectState,flowId:string,occurrenceId:string,label:string):HTMLDetailsElement{
    const example=deriveFlowOccurrenceExample(state.project,flowId,occurrenceId),details=document.createElement("details"),summary=document.createElement("summary"),pre=document.createElement("pre"),provenance=document.createElement("ul"),issues=document.createElement("ul"),exampleControls=document.createElement("section");
    renderOccurrenceExampleControls(exampleControls,state,flowId,occurrenceId,persist,options.id);details.dataset.eventExampleFor=occurrenceId;details.dataset.exampleStatus=example.status;summary.textContent=`${label} · ${example.status} · Derived JSON example`;pre.dataset.readonlyExample=occurrenceId;pre.textContent=example.formattedJson;
    for(const[path,source]of Object.entries(example.provenance)){const item=document.createElement("li");item.dataset.examplePath=path;item.dataset.exampleSource=source;item.textContent=`${path} · ${source}`;provenance.append(item);}
    for(const issue of example.issues){const item=document.createElement("li"),repair=document.createElement("a"),value=document.createElement("input"),save=button("Save example",()=>persist(setFlowOccurrenceExample(current().state!,flowId,occurrenceId,issue.path,value.value,options.id)));item.dataset.exampleIssuePath=issue.path;item.dataset.exampleIssueCode=issue.code;repair.href=issue.editHref;repair.textContent="Edit examples";repair.addEventListener("click",(event)=>{if(options.openOccurrenceSchema?.(occurrenceId,issue.path)){event.preventDefault();}});value.setAttribute("aria-label",`Example value for ${issue.path}`);item.append(`${issue.path} · ${issue.message} `,repair," ",value,save);issues.append(item);}
    details.append(summary,pre,provenance,exampleControls,issues);return details;
  }

  function renderLaneControls(host:HTMLElement):void{
    const {state,flow,graph}=current();if(!state||!flow||!graph)return;const heading=document.createElement("h4"),list=document.createElement("ol");heading.textContent="Flow lane order";host.setAttribute("aria-label","Page Group lane controls");
    for(const groupId of graph.pageGroupIds){const group=state.project.collections.pageGroups.find(({id})=>id===groupId);if(!group)continue;const item=document.createElement("li");item.dataset.pageGroupId=group.id;item.append(group.name,button("Move earlier",()=>persist(reorderFlowPageGroupLane(current().state!,flow.id,group.id,-1))),button("Move later",()=>persist(reorderFlowPageGroupLane(current().state!,flow.id,group.id,1))),button("Remove lane",()=>{try{persist(setFlowPageGroupLanes(current().state!,flow.id,current().graph!.pageGroupIds.filter((id)=>id!==group.id)));}catch(error){statusMessage=error instanceof Error?error.message:String(error);render();}}));list.append(item);}host.append(heading,list);
  }

  function dropPayload(event:DragEvent):{kind:string;id:string}|undefined{const raw=event.dataTransfer?.getData("application/x-flow-component");if(!raw)return activeCatalogPayload;try{return JSON.parse(raw) as {kind:string;id:string};}catch{return activeCatalogPayload;}}
  function appendPageFrameCardControls(card:HTMLElement,title:HTMLButtonElement,state:ProjectState,flow:ProjectEntity,graph:ReturnType<typeof documentaryFlowGraph>,frame:DocumentaryPageFrameRecord):void{
    card.append(title);const memberships=orderedPageGroupIds(state.project,frame.pageId);for(const groupId of graph.pageGroupIds.filter((id)=>memberships.includes(id)&&id!==frame.pageGroupId)){const group=state.project.collections.pageGroups.find(({id})=>id===groupId),move=button(`Move to ${group?.name??groupId}`,()=>persist(moveFlowPageFrame(current().state!,flow.id,frame.id,{pageGroupId:groupId,...(frame.position.x===undefined?{}:{x:frame.position.x}),y:frame.position.y})));move.dataset.movePageFrameTo=groupId;move.addEventListener("keydown",(event)=>{if(event.key==="Enter"){event.preventDefault();move.click();}});card.append(move);}card.append(button("Open schema contribution",()=>options.openOccurrenceSchema?.(frame.id)),button("Remove Page frame",()=>persist(removeFlowPageFrame(current().state!,flow.id,frame.id))));
  }
  function renderFrameCards(host:HTMLElement):void{
    const {state,flow,graph}=current();if(!state||!flow||!graph)return;const heading=document.createElement("h4");heading.textContent="Page frames";host.setAttribute("aria-label","Flow Page frames");host.append(heading);
    for(const frame of graph.pageFrames){const page=state.project.collections.pages.find(({id})=>id===frame.pageId),group=state.project.collections.pageGroups.find(({id})=>id===frame.pageGroupId),card=document.createElement("article"),region=frame.freePageRegion==="before-lanes"?"before lanes":"after lanes",title=button(frame.freePageRegion?`Free ${region} / ${contextSettingPageLabel(page?.name??frame.pageId)}`:`${group?.name??"Page"} / ${contextSettingPageLabel(page?.name??frame.pageId)}`,()=>saveSelection({kind:"page-frame",id:frame.id}));card.dataset.pageFrameId=frame.id;card.dataset.pageId=frame.pageId;if(frame.pageGroupId)card.dataset.pageGroupId=frame.pageGroupId;if(frame.freePageRegion){card.dataset.freePageFrameId=frame.id;card.dataset.freePageRegion=frame.freePageRegion;}card.addEventListener("dragover",(event)=>event.preventDefault());card.addEventListener("drop",(event)=>{event.preventDefault();const payload=dropPayload(event);if(payload?.kind==="event"){const entity=current().state?.project.collections.events.find(({id})=>id===payload.id);if(entity)insertEvent(entity,frame.id);}else if(payload?.kind==="page"){statusMessage="A Page frame cannot contain another Page. Use its owning lane.";render();}});appendPageFrameCardControls(card,title,state,flow,graph,frame);host.append(card);}
  }

  function cancelConnection(announce=true,suppressClick=false):void{const sourceId=connection?.sourceId,sourcePort=connection?.sourcePort;connection?.preview?.remove();connection=undefined;document.querySelector(".flow-canvas-scroll")?.classList.remove("is-connecting");document.querySelectorAll(".is-valid-target,.is-invalid-target").forEach((element)=>element.classList.remove("is-valid-target","is-invalid-target"));if(announce)statusMessage="Connection cancelled; canonical state was not changed.";if(suppressClick){suppressNodeClick=true;setTimeout(()=>{suppressNodeClick=false;},0);}document.querySelector<HTMLElement>(`[data-flow-port-for="${CSS.escape(sourceId??"")}"][data-flow-port-side="${sourcePort??"right"}"]`)?.focus();}
  function clearSelectedRelationshipForConnection():void{if(selected?.kind!=="relationship")return;selected=undefined;relationshipPopoverFocusIntent=undefined;relationshipEdgeFocusIntent=undefined;document.querySelector<HTMLElement>('[aria-label="Inline relationship popover"]')?.remove();}
  function commitConnection(targetId:string|undefined,targetPort?:FlowPortSide):void{const {state,flow,graph,revision}=current(),sourceId=connection?.sourceId,sourcePort=connection?.sourcePort??"right",inferredTargetPort=targetPort??(sourcePort==="right"?"left":sourcePort==="top"?"bottom":sourcePort==="bottom"?"top":undefined);if(!state||!flow||!graph||!sourceId||!targetId||!inferredTargetPort||sourceId===targetId){cancelConnection(true,true);return;}const before=new Set(graph.relationships.map(({id})=>id)),next=saveGraphRelationship(state,flow.id,sourceId,{toStepId:targetId,sourcePort,targetPort:inferredTargetPort},options.id),created=documentaryFlowGraph(next.project,flow.id).relationships.find(({id})=>!before.has(id));if(!created){cancelConnection(true,true);return;}connection=undefined;document.querySelector(".flow-canvas-scroll")?.classList.remove("is-connecting");selected={kind:"relationship",id:created.id};relationshipPopoverFocusIntent={id:created.id,revision:Number(revision??0),optimisticFocused:false};persist(next);}

  function renderRelationshipPopover(host:HTMLElement):void{
    if(selected?.kind!=="relationship")return;
    const {state,flow,revision}=current();if(!state||!flow)return;
    const projection=projectFlowGraph(state.project,flow.id).graph,relationship=projection.relationships.find(({id})=>id===selected!.id);if(!relationship)return;
    const sourceId=relationship.sourceEndpoint.id,targetId=relationship.targetEndpoint.id,source=projection.connectionEndpoints.find(({id})=>id===sourceId),target=projection.connectionEndpoints.find(({id})=>id===targetId),form=document.createElement("form"),heading=document.createElement("h4"),endpoints=document.createElement("p"),inferredKind=document.createElement("p"),group=document.createElement("input"),label=document.createElement("input"),condition=document.createElement("textarea"),expectation=document.createElement("textarea"),save=document.createElement("button"),cancel=document.createElement("button"),remove=document.createElement("button"),relationshipName=[relationship.label,`${source?.name??sourceId} to ${target?.name??targetId}`].filter(Boolean).join(", ");
    form.dataset.relationshipPopover=relationship.id;form.setAttribute("aria-label","Inline relationship popover");heading.textContent="Relationship details";endpoints.dataset.relationshipEndpoints=relationship.id;endpoints.textContent=`${source?.name??sourceId} ${relationship.sourcePort} → ${target?.name??targetId} ${relationship.targetPort}`;inferredKind.dataset.inferredRelationshipKind=relationship.kind;inferredKind.textContent=`Inferred kind: ${relationship.kind}`;group.value=relationship.group??"";group.setAttribute("aria-label","Relationship group");label.value=relationship.label??"";label.setAttribute("aria-label","Optional relationship label");condition.value=relationship.documentationCondition??"";condition.setAttribute("aria-label","Documentation condition");expectation.value=relationship.expectation??"";expectation.setAttribute("aria-label","Relationship expectation");save.type="submit";save.textContent="Save relationship";cancel.type="button";cancel.textContent="Cancel";
    remove.type="button";remove.textContent="Delete relationship";remove.setAttribute("aria-label",flowRelationshipDeletionAccessibleName(relationship.label,source?.name??sourceId,target?.name??targetId));remove.addEventListener("click",()=>{relationshipPopoverFocusIntent=undefined;relationshipEdgeFocusIntent=undefined;relationshipDeletionFocusIntent={id:relationship.id,sourceKind:relationship.sourceEndpoint.kind,sourceId,sourceFocused:false};selected=undefined;const context=current(),view=readView(context.state!.project.id,flow.id);writeView(context.state!.project.id,flow.id,flowViewAfterRelationshipDeletion(view,relationship.id));persist(removeFlowRelationship(context.state!,flow.id,relationship.id),`Deleted relationship ${relationshipName}. Saved Draft; documentation preview stale; Undo available.`);});
    cancel.addEventListener("click",()=>{relationshipPopoverFocusIntent=undefined;relationshipEdgeFocusIntent=undefined;selected=undefined;render();document.querySelector<HTMLElement>(`[data-flow-port-for="${CSS.escape(sourceId)}"][data-flow-port-side="${relationship.sourcePort}"]`)?.focus();});
    form.addEventListener("submit",(event)=>{event.preventDefault();relationshipPopoverFocusIntent=undefined;relationshipEdgeFocusIntent={id:relationship.id,revision:Number(revision??0),optimisticFocused:false};persist(saveGraphRelationship(current().state!,flow.id,sourceId,{id:relationship.id,toStepId:targetId,sourcePort:relationship.sourcePort,targetPort:relationship.targetPort,group:group.value.trim(),label:label.value.trim(),documentationCondition:condition.value.trim(),expectation:expectation.value.trim()},options.id));queueMicrotask(()=>document.querySelector<HTMLElement>(`[data-relationship-id="${CSS.escape(relationship.id)}"]`)?.focus());});
    const labeled=(text:string,control:HTMLElement)=>{const wrapper=document.createElement("label");wrapper.append(text,control);return wrapper;};form.append(heading,endpoints,inferredKind,labeled("Group",group),labeled("Optional label",label),labeled("Condition",condition),labeled("Expectation",expectation),save,cancel,remove);host.append(form);
    const intent=relationshipPopoverFocusIntent;if(intent?.id===relationship.id){const renderRevision=Number(revision??0),replacement=intent.optimisticFocused&&renderRevision>intent.revision;if(!intent.optimisticFocused){intent.optimisticFocused=true;intent.revision=renderRevision;}queueMicrotask(()=>{if(!label.isConnected)return;label.focus();if(replacement&&relationshipPopoverFocusIntent===intent)relationshipPopoverFocusIntent=undefined;});}
  }

  function renderActions(host:HTMLElement):void{
    const {state,flow,graph}=current();if(!state||!flow||!graph)return;if(selected?.kind==="page-frame"){const frame=graph.pageFrames.find(({id})=>id===selected!.id);if(!frame)return;const actions=document.createElement("section");actions.setAttribute("aria-label","Selected Page instance inline actions");actions.append(button("Move",()=>document.querySelector<HTMLElement>(`[data-page-frame-id="${CSS.escape(frame.id)}"]`)?.focus()),button("Connect",()=>document.querySelector<HTMLElement>(`[data-output-port-for="${CSS.escape(frame.id)}"]`)?.focus()),button("Open schema contribution",()=>options.openOccurrenceSchema?.(frame.id)),button("Remove",()=>persist(removeFlowPageFrame(current().state!,flow.id,frame.id))));host.append(actions);return;}if(selected?.kind!=="occurrence")return;const occurrence=graph.occurrences.find(({id})=>id===selected!.id),node=projectFlowGraph(state.project,flow.id).graph.nodes.find(({id})=>id===selected!.id);if(!occurrence||!node)return;const actions=document.createElement("section"),migration=reviewLegacyFlowContextMigration(state.project,flow.id);actions.setAttribute("aria-label","Selected node inline actions");const connect=()=>document.querySelector<HTMLElement>(`[data-output-port-for="${CSS.escape(occurrence.id)}"]`)?.focus();const duplicate=()=>{const next=addGraphOccurrence(current().state!,flow.id,{name:`${occurrence.name} copy`,...(occurrence.pageFrameId?{pageFrameId:String(occurrence.pageFrameId)}:{}),...(occurrence.pageGroupId?{pageGroupId:String(occurrence.pageGroupId)}:{}),...(occurrence.freePageFrameId?{freePageFrameId:String(occurrence.freePageFrameId)}:{}),pageId:String(occurrence.pageId),eventId:node.eventId,role:node.role,...(node.trigger?{trigger:node.trigger}:{}),obligation:String(occurrence.obligation??"Required") as "Required",minimum:Number(occurrence.minimum??1),maximum:Number(occurrence.maximum??1),y:Number((occurrence.position as {y?:number}|undefined)?.y??70)+24},options.id);persist(next);},duplicateButton=button("Duplicate occurrence",duplicate);duplicateButton.disabled=Boolean(migration.items.length||migration.blockers.length);if(duplicateButton.disabled)duplicateButton.title="Confirm the Page-context migration before changing this graph.";const openSchema=()=>{document.querySelector<HTMLElement>(`[data-occurrence-id="${CSS.escape(occurrence.id)}"]`)?.dispatchEvent(new MouseEvent("click",{bubbles:true}));queueMicrotask(()=>{const open=Array.from(document.querySelectorAll<HTMLButtonElement>('[aria-label="Schema constraints summary"] button')).find(({textContent})=>textContent?.includes("Open complete schema editor"));open?.click();});};actions.append(button("Move",()=>document.querySelector<HTMLElement>(`[data-occurrence-id="${CSS.escape(occurrence.id)}"]`)?.focus()),button("Connect",connect),duplicateButton,button("Remove",()=>persist(removeGraphOccurrence(current().state!,flow.id,occurrence.id))),button("Open schema contribution",openSchema));host.append(actions);
  }

  function renderGraph(flow:ProjectEntity):void{
    const preMigrationState=current().state;if(preMigrationState){const migrated=migrateLegacyFlowRelationshipKinds(preMigrationState,flow.id);if(migrated!==preMigrationState){persist(migrated);return;}}
    const host=q<HTMLElement>("#flow-graph-workspace");host.replaceChildren();const {state,graph:stored}=current();if(!state||!stored)return;const migration=reviewLegacyFlowContextMigration(state.project,flow.id);if(migration.items.length||migration.blockers.length){const review=document.createElement("section"),heading=document.createElement("h3"),list=document.createElement("ul"),confirm=button("Confirm Page-context migration",()=>persist(migrateLegacyFlowContextBindings(current().state!,flow.id)));review.setAttribute("aria-label","Flow Page-context migration review");heading.textContent="Review Page-context migration";for(const item of migration.items){const row=document.createElement("li");row.textContent=`${item.flowName} / ${item.pageName} / ${item.eventName} · ${item.trigger} · occurrence ${item.occurrenceName}`;list.append(row);}for(const blocker of migration.blockers){const row=document.createElement("li");row.textContent=blocker.message;list.append(row);}confirm.disabled=Boolean(migration.blockers.length)||!migration.items.length;review.append(heading,list,confirm);host.append(review);}const transientView=readView(state.project.id,flow.id),selectionExists=selected&&(selected.kind==="page-frame"?stored.pageFrames:selected.kind==="occurrence"?stored.occurrences:stored.relationships).some(({id})=>id===selected!.id);if(!selectionExists)selected=transientView.selectedItem;const projection=projectFlowGraph(state.project,flow.id),section=document.createElement("section"),heading=document.createElement("h3"),boundary=document.createElement("p"),toolbar=document.createElement("section"),laneControls=document.createElement("section"),status=document.createElement("p"),frames=document.createElement("section"),views=document.createElement("div"),canvasScroll=document.createElement("div"),canvas=svg("svg"),outline=document.createElement("ol"),popover=document.createElement("section"),actions=document.createElement("section");
    const freeRoots=stored.pageFrames.filter(({freePageRegion})=>Boolean(freePageRegion)),hasBefore=freeRoots.some(({freePageRegion})=>freePageRegion==="before-lanes"),hasAfter=freeRoots.some(({freePageRegion})=>freePageRegion==="after-lanes"),laneOffset=hasBefore?200:0,laneLeft=laneOffset+10,namedRight=Math.max(laneLeft+700,...projection.graph.connectionEndpoints.filter(({freePageRegion})=>!freePageRegion).map((endpoint)=>endpoint.layout.x+endpoint.width+60)),viewWidth=Math.max(960,namedRight+100,...projection.graph.connectionEndpoints.map((endpoint)=>endpoint.layout.x+endpoint.width+100)),viewHeight=Math.max(780,...projection.graph.connectionEndpoints.map((endpoint)=>endpoint.layout.y+endpoint.height+100));
    const targetPortFor=(sourcePort:FlowPortSide):FlowPortSide|undefined=>sourcePort==="right"?"left":sourcePort==="top"?"bottom":sourcePort==="bottom"?"top":undefined;
    const targetPortElement=(endpointId:string,sourcePort:FlowPortSide):SVGCircleElement|undefined=>{const targetPort=targetPortFor(sourcePort);return targetPort?document.querySelector<SVGCircleElement>(`[data-flow-port-for="${CSS.escape(endpointId)}"][data-flow-port-side="${targetPort}"]`)??undefined:undefined;};
    const beginPortConnection=(endpoint:typeof projection.graph.connectionEndpoints[number],sourcePort:FlowPortSide,port:SVGCircleElement):void=>{clearSelectedRelationshipForConnection();connection?.preview?.remove();const targetPort=targetPortFor(sourcePort),targets=targetPort?projection.graph.connectionEndpoints.map(({id})=>id).filter((id)=>id!==endpoint.id):[];if(!targets.length){statusMessage=targetPort?"Add another Page frame or Event occurrence before drawing a relationship.":"This port cannot start a relationship.";render();return;}const start=flowPortPoint(endpoint.layout,{width:endpoint.width,height:endpoint.height},sourcePort),preview=svg("line");preview.classList.add("flow-connection-preview");preview.setAttribute("x1",String(start.x));preview.setAttribute("y1",String(start.y));preview.setAttribute("x2",String(start.x));preview.setAttribute("y2",String(start.y));canvas.append(preview);connection={sourceId:endpoint.id,sourcePort,targets,targetIndex:0,preview};statusMessage=`Connection mode: choose a ${targetPort} port; Escape cancels.`;targetPortElement(targets[0]!,sourcePort)?.classList.add("is-valid-target");port.focus();};
    section.className="documentary-flow";heading.textContent="Canvas-first directional Flow";boundary.className="status-text";boundary.textContent="Documentary journey expectations are checked manually. Each Event payload schema validates independently.";toolbar.setAttribute("aria-label","Flow component catalogs");const toggleInspector=button(inspector.hidden?"Open Inspector":"Close Inspector",()=>{inspector.hidden=!inspector.hidden;toggleInspector.textContent=inspector.hidden?"Open Inspector":"Close Inspector";});toolbar.append(catalog("Page Groups",state.project.collections.pageGroups,addLane),catalog("Pages",state.project.collections.pages,(page)=>insertPage(page)),catalog("Events",state.project.collections.events,(event)=>insertEvent(event)),toggleInspector);renderLaneControls(laneControls);renderFrameCards(frames);status.setAttribute("role","status");status.textContent=statusMessage||(!stored.pageGroupIds.length?"Add a Page Group to begin. No fallback lanes exist.":!stored.pageFrames.length&&!freeRoots.length?"Add a Page from the Pages catalog.":"Draw between matching relationship ports, or press Enter on a port.");if(statusRepairHref){const repair=document.createElement("a");repair.href=statusRepairHref;repair.textContent="Open Page Group membership";status.append(" ",repair);}canvas.classList.add("flow-graph-canvas");canvas.setAttribute("aria-label","Interactive directional Flow canvas");canvas.setAttribute("role","application");canvas.dataset.viewport=JSON.stringify(transientView.viewport??{x:0,y:0,zoom:1});canvas.setAttribute("viewBox",`-116 0 ${viewWidth+116} ${viewHeight}`);canvas.style.width=`${viewWidth+116}px`;canvas.style.height=`${viewHeight}px`;const resizeCanvasHeight=()=>{const expanded=Array.from(canvas.querySelectorAll<SVGForeignObjectElement>("[data-event-example-node]")).filter((candidate)=>candidate.querySelector("details")?.open),height=Math.max(viewHeight,...expanded.map((candidate)=>{const parent=candidate.parentNode as SVGGElement,parentY=parent.transform.baseVal.consolidate()?.matrix.f??0;return parentY+Number(candidate.getAttribute("y")??0)+Number(candidate.getAttribute("height")??0);}));canvas.setAttribute("viewBox",`-116 0 ${viewWidth+116} ${height}`);canvas.style.height=`${height}px`;};outline.setAttribute("aria-label","Synchronized editable Flow outline");views.className="flow-projections";
    projection.laneBands.forEach((band)=>{const lane=projection.lanes.find(({id})=>id===band.id)! ,group=svg("g"),rect=svg("rect"),label=svg("text"),x=laneLeft,mark=(event:DragEvent)=>{const payload=dropPayload(event),page=payload?.kind==="page"?current().state?.project.collections.pages.find(({id})=>id===payload.id):undefined;if(page)rect.dataset.pageDropState=orderedPageGroupIds(state.project,page.id).includes(lane.id)?"valid":"invalid";};group.dataset.pageGroupId=lane.id;group.dataset.laneBand=lane.id;rect.setAttribute("x",String(x));rect.setAttribute("y",String(band.y));rect.setAttribute("width",String(namedRight-x));rect.setAttribute("height",String(band.height));rect.setAttribute("class","flow-lane");rect.dataset.laneDropzone=lane.id;rect.addEventListener("dragenter",mark);rect.addEventListener("dragover",(event)=>{event.preventDefault();mark(event);});rect.addEventListener("dragleave",()=>delete rect.dataset.pageDropState);rect.addEventListener("drop",(event)=>{event.preventDefault();const payload=dropPayload(event),page=payload?.kind==="page"?current().state?.project.collections.pages.find(({id})=>id===payload.id):undefined;if(page)insertPage(page,lane.id);});label.classList.add("flow-lane-label");label.setAttribute("x",String(x+12));label.setAttribute("y",String(band.y+25));label.textContent=lane.name;group.append(rect,label);canvas.append(group);});
    const clearEdgeTargets=()=>canvas.querySelectorAll("[data-free-page-edge-target]").forEach((target)=>target.remove()),showEdgeTargets=()=>{clearEdgeTargets();for(const region of["before-lanes","after-lanes"] as const){const target=svg("g"),rect=svg("rect"),label=svg("text"),x=region==="before-lanes"?-112:namedRight+10;target.dataset.freePageEdgeTarget=region;target.setAttribute("role","button");target.setAttribute("aria-label",region==="before-lanes"?"Place before lanes":"Place after lanes");rect.setAttribute("x",String(x));rect.setAttribute("y","58");rect.setAttribute("width","72");rect.setAttribute("height","168");rect.setAttribute("rx","10");rect.classList.add("flow-free-edge-target");label.setAttribute("x",String(x+6));label.setAttribute("y","82");label.textContent=region==="before-lanes"?"Place before lanes":"Place after lanes";target.addEventListener("dragover",(event)=>{event.preventDefault();rect.setAttribute("width","88");});target.addEventListener("drop",(event)=>{event.preventDefault();event.stopPropagation();const payload=dropPayload(event),page=payload?.kind==="page"?current().state?.project.collections.pages.find(({id})=>id===payload.id):undefined;if(page)insertFreePage(page,region,24,90);});target.append(rect,label);canvas.append(target);}};
    toolbar.querySelectorAll<HTMLElement>('[data-component-kind="page"]').forEach((control)=>control.addEventListener("pointerdown",showEdgeTargets));
    canvas.addEventListener("dragenter",(event)=>{if(!canvas.querySelector("[data-free-page-edge-target]")&&(Array.from(event.dataTransfer?.types??[]).includes("application/x-flow-page-component")||dropPayload(event)?.kind==="page"))showEdgeTargets();});canvas.addEventListener("dragleave",(event)=>{const bounds=canvas.getBoundingClientRect();if(event.clientX<=bounds.left||event.clientX>=bounds.right||event.clientY<=bounds.top||event.clientY>=bounds.bottom)clearEdgeTargets();});
    const placeActiveCatalogPage=(event:Event)=>{const payload=activeCatalogPayload,page=payload?.kind==="page"?current().state?.project.collections.pages.find(({id})=>id===payload.id):undefined;if(!page)return;const element=event.target as Element,edge=element.closest<SVGElement>("[data-free-page-edge-target]"),lane=element.closest<SVGElement>("[data-lane-dropzone]");if(edge?.dataset.freePageEdgeTarget)insertFreePage(page,edge.dataset.freePageEdgeTarget as FreePageRegion,24,90);else if(lane?.dataset.laneDropzone)insertPage(page,lane.dataset.laneDropzone);};canvas.addEventListener("pointerup",placeActiveCatalogPage);canvas.addEventListener("mouseup",placeActiveCatalogPage);
    for(const frame of stored.pageFrames.filter(({pageGroupId,freePageRegion})=>Boolean(pageGroupId)&&!freePageRegion)){
      const endpoint=projection.graph.connectionEndpoints.find(({kind,id})=>kind==="page-frame"&&id===frame.id)! ,x=endpoint.layout.x,y=endpoint.layout.y,group=svg("g"),rect=svg("rect"),label=svg("text"),inputPort=svg("circle"),outputPort=svg("circle"),page=state.project.collections.pages.find(({id})=>id===frame.pageId),memberships=orderedPageGroupIds(state.project,frame.pageId),eligibleLanes=stored.pageGroupIds.filter((id)=>memberships.includes(id)),reject=(targetId?:string)=>{const target=state.project.collections.pageGroups.find(({id})=>id===targetId);statusMessage=`${page?.name??frame.pageId} cannot move to ${target?.name??"that lane"}; add Page Group membership before moving it.`;statusRepairHref=`?kind=pages&entity=${encodeURIComponent(frame.pageId)}&field=pageGroupIds`;render();setTimeout(()=>elementByData<SVGElement>("data-page-frame-id",frame.id)?.focus(),50);},moveTo=(targetId:string,nextX:number,nextY:number)=>{if(!eligibleLanes.includes(targetId)){reject(targetId);return;}const band=projection.laneBands.find(({id})=>id===targetId),currentState=current().state!,next=moveFlowPageFrame(currentState,flow.id,frame.id,{pageGroupId:targetId,x:Math.max(20,Math.round(nextX-laneOffset)),y:Math.max(40,Math.round(nextY-(band?.y??0)))});if(next!==currentState)persist(next);setTimeout(()=>elementByData<SVGElement>("data-page-frame-id",frame.id)?.focus(),50);},beginConnection=()=>{clearSelectedRelationshipForConnection();connection?.preview?.remove();const targets=projection.graph.connectionEndpoints.map(({id})=>id).filter((id)=>id!==frame.id);if(!targets.length){statusMessage="Add another Page frame or Event occurrence before drawing a relationship.";render();return;}const preview=svg("line");preview.classList.add("flow-connection-preview");preview.setAttribute("x1",String(x+endpoint.width));preview.setAttribute("y1",String(y+endpoint.height/2));preview.setAttribute("x2",String(x+endpoint.width+20));preview.setAttribute("y2",String(y+endpoint.height/2));canvas.append(preview);connection={sourceId:frame.id,targets,targetIndex:0,preview};statusMessage="Connection mode: choose a valid input port; Escape cancels.";elementByData("data-input-port-for",targets[0]??"")?.classList.add("is-valid-target");outputPort.focus();};
      group.dataset.pageFrameId=frame.id;group.dataset.flowEndpointId=frame.id;group.dataset.flowEndpointKind="page-frame";group.dataset.pageId=frame.pageId;if(frame.pageGroupId)group.dataset.pageGroupId=frame.pageGroupId;group.setAttribute("transform",`translate(${x} ${y})`);group.tabIndex=0;rect.setAttribute("width",String(endpoint.width));rect.setAttribute("height",String(endpoint.height));rect.setAttribute("rx","12");rect.classList.add("flow-page-frame");label.setAttribute("x","10");label.setAttribute("y","22");label.textContent=endpoint.name;inputPort.setAttribute("cx","0");inputPort.setAttribute("cy",String(endpoint.height/2));inputPort.setAttribute("r","8");inputPort.tabIndex=0;inputPort.dataset.inputPortFor=frame.id;inputPort.setAttribute("aria-label",`Input port for ${endpoint.name}`);outputPort.setAttribute("cx",String(endpoint.width));outputPort.setAttribute("cy",String(endpoint.height/2));outputPort.setAttribute("r","8");outputPort.tabIndex=0;outputPort.dataset.outputPortFor=frame.id;outputPort.setAttribute("aria-label",`Output port for ${endpoint.name}`);outputPort.addEventListener("pointerdown",(event)=>{event.stopPropagation();beginConnection();});outputPort.addEventListener("keydown",(event)=>{if(event.key==="Enter"&&!connection){event.preventDefault();beginConnection();return;}if(event.key==="Escape"&&connection){event.preventDefault();cancelConnection();return;}if(event.key==="Enter"&&connection){event.preventDefault();commitConnection(connection.targets[connection.targetIndex]);}});inputPort.addEventListener("pointerup",(event)=>{event.stopPropagation();commitConnection(frame.id);});
      label.textContent=`${endpoint.name} · Context-setting Page`;group.setAttribute("aria-label",`${endpoint.name}. Context-setting Page frame. Drag or use Arrow keys to move.`);group.addEventListener("dragover",(event)=>event.preventDefault());group.addEventListener("drop",(event)=>{event.preventDefault();event.stopPropagation();const payload=dropPayload(event);if(payload?.kind!=="event")return;const entity=current().state?.project.collections.events.find(({id})=>id===payload.id);if(entity)insertEvent(entity,frame.id);});
      let start:{pointerId:number;clientX:number;clientY:number}|undefined,targetRegion:FreePageRegion|undefined,suppressPointerClick=false;const releaseClickSuppression=()=>setTimeout(()=>{suppressPointerClick=false;},1000),move=(event:PointerEvent)=>{if(!ownsPointerDrag(start?.pointerId,event.pointerId))return;group.setAttribute("transform",`translate(${x+event.clientX-start!.clientX} ${y+event.clientY-start!.clientY})`);const edge=(document.elementFromPoint(event.clientX,event.clientY) as Element|null)?.closest<SVGElement>("[data-free-page-edge-target]");targetRegion=edge?.dataset.freePageEdgeTarget as FreePageRegion|undefined;},finish=(up:PointerEvent)=>{if(!ownsPointerDrag(start?.pointerId,up.pointerId))return;window.removeEventListener("pointermove",move);window.removeEventListener("pointerup",finish);window.removeEventListener("pointercancel",cancel);const initial=start!,nextX=x+up.clientX-initial.clientX,nextY=y+up.clientY-initial.clientY,target=Array.from(canvas.querySelectorAll<SVGElement>("[data-lane-dropzone]")).find((lane)=>{const bounds=lane.getBoundingClientRect();return up.clientX>=bounds.left&&up.clientX<=bounds.right&&up.clientY>=bounds.top&&up.clientY<=bounds.bottom;})?.dataset.laneDropzone,pointerId=initial.pointerId,region=targetRegion;if(group.hasPointerCapture(pointerId))group.releasePointerCapture(pointerId);start=undefined;targetRegion=undefined;if(suppressPointerClick)releaseClickSuppression();if(region){persist(moveFreePageFrame(current().state!,flow.id,frame.id,{region,x:24,y:Math.max(55,Math.round(nextY))}));return;}if(target)moveTo(target,nextX,nextY);else{group.setAttribute("transform",`translate(${x} ${y})`);clearEdgeTargets();}},cancel=(event:PointerEvent)=>{if(!ownsPointerDrag(start?.pointerId,event.pointerId))return;window.removeEventListener("pointermove",move);window.removeEventListener("pointerup",finish);window.removeEventListener("pointercancel",cancel);if(group.hasPointerCapture(start!.pointerId))group.releasePointerCapture(start!.pointerId);start=undefined;targetRegion=undefined;group.setAttribute("transform",`translate(${x} ${y})`);if(suppressPointerClick)releaseClickSuppression();};group.addEventListener("focus",showEdgeTargets);group.addEventListener("pointerdown",(event)=>{if((event.target as Element).closest("circle"))return;if(start){suppressPointerClick=true;return;}start={pointerId:event.pointerId,clientX:event.clientX,clientY:event.clientY};showEdgeTargets();window.addEventListener("pointermove",move);window.addEventListener("pointerup",finish);window.addEventListener("pointercancel",cancel);try{group.setPointerCapture(event.pointerId);}catch{/* Synthetic tests have no active device pointer. */}});group.addEventListener("keydown",(event)=>{if(!event.key.startsWith("Arrow")||(event.target as Element).closest("circle"))return;event.preventDefault();const dx=event.key==="ArrowLeft"?-20:event.key==="ArrowRight"?20:0,dy=event.key==="ArrowUp"?-20:event.key==="ArrowDown"?20:0;moveTo(String(frame.pageGroupId),x+dx,y+dy);});group.addEventListener("click",(event)=>{if((event.target as Element).closest("circle"))return;if(suppressPointerClick){event.stopPropagation();return;}saveSelection({kind:"page-frame",id:frame.id});});group.append(rect,label,inputPort,outputPort);canvas.append(group);const outlineRow=document.createElement("li"),outlineControl=button(`${endpoint.name} · Page instance`,()=>saveSelection({kind:"page-frame",id:frame.id}));outlineRow.dataset.pageFrameId=frame.id;outlineRow.dataset.pageId=frame.pageId;outlineRow.append(outlineControl);outline.append(outlineRow);
      outlineControl.textContent=`${endpoint.name} · Context-setting Page`;
    }
    for(const storedFrame of freeRoots){const frame=svg("g"),rect=svg("rect"),label=svg("text"),page=state.project.collections.pages.find(({id})=>id===storedFrame.pageId),position=storedFrame.position,endpoint=projection.graph.connectionEndpoints.find(({kind,id})=>kind==="page-frame"&&id===storedFrame.id),x=Number(endpoint?.layout.x??position.x??24);frame.dataset.pageFrameId=storedFrame.id;frame.dataset.freePageFrameCanvas=storedFrame.id;frame.dataset.freePageRegion=storedFrame.freePageRegion!;frame.setAttribute("transform",`translate(${x-10} ${position.y-12})`);frame.tabIndex=0;frame.addEventListener("dragover",(event)=>event.preventDefault());frame.addEventListener("drop",(event)=>{event.preventDefault();const payload=dropPayload(event);if(payload?.kind!=="event")return;const entity=current().state?.project.collections.events.find(({id})=>id===payload.id);if(entity)insertEvent(entity,storedFrame.id);});rect.setAttribute("width","190");rect.setAttribute("height","108");rect.setAttribute("rx","12");rect.classList.add("flow-page-frame","flow-free-page-frame");label.setAttribute("x","10");label.setAttribute("y","20");label.setAttribute("textLength","170");label.setAttribute("lengthAdjust","spacingAndGlyphs");label.textContent=`${storedFrame.freePageRegion==="before-lanes"?"Before":"After"} lanes · ${contextSettingPageLabel(page?.name??storedFrame.pageId)}`;let start:{clientX:number;clientY:number;pointerId:number}|undefined,targetRegion:FreePageRegion|undefined,suppressPointerClick=false;const owns=(event:PointerEvent)=>ownsPointerDrag(start?.pointerId,event.pointerId),stop=(pointerId:number)=>{window.removeEventListener("pointermove",move);window.removeEventListener("pointerup",finish);window.removeEventListener("pointercancel",cancel);if(frame.hasPointerCapture(pointerId))frame.releasePointerCapture(pointerId);start=undefined;targetRegion=undefined;},move=(event:PointerEvent)=>{if(!owns(event))return;const edge=(document.elementFromPoint(event.clientX,event.clientY) as Element|null)?.closest<SVGElement>("[data-free-page-edge-target]");if(edge?.dataset.freePageEdgeTarget)targetRegion=edge.dataset.freePageEdgeTarget as FreePageRegion;},finish=(event:PointerEvent)=>{if(!owns(event))return;const initial=start!,region=targetRegion??storedFrame.freePageRegion!,afterStart=namedRight,nextX=region==="before-lanes"?Math.max(12,Math.round(x+event.clientX-initial.clientX)):Math.max(12,Math.round(x+event.clientX-initial.clientX-afterStart)),nextY=Math.max(55,Math.round(position.y+event.clientY-initial.clientY));stop(initial.pointerId);persist(moveFreePageFrame(current().state!,flow.id,storedFrame.id,{region,x:nextX,y:nextY}));setTimeout(()=>elementByData<SVGElement>("data-free-page-frame-canvas",storedFrame.id)?.focus(),50);},cancel=(event:PointerEvent)=>{if(!owns(event))return;const pointerId=start!.pointerId;stop(pointerId);clearEdgeTargets();restorePointerCancellationFocus(frame);};frame.addEventListener("focus",showEdgeTargets);frame.addEventListener("pointerdown",(event)=>{if(start){suppressPointerClick=true;setTimeout(()=>{suppressPointerClick=false;},500);return;}start={clientX:event.clientX,clientY:event.clientY,pointerId:event.pointerId};targetRegion=undefined;showEdgeTargets();window.addEventListener("pointermove",move);window.addEventListener("pointerup",finish);window.addEventListener("pointercancel",cancel);try{frame.setPointerCapture(event.pointerId);}catch{/* Synthetic regression events have no active device pointer to capture. */}});frame.addEventListener("click",(event)=>{if(suppressPointerClick)event.stopPropagation();});frame.addEventListener("keydown",(event)=>{if(!event.key.startsWith("Arrow"))return;event.preventDefault();const region:FreePageRegion=event.key==="ArrowLeft"?"before-lanes":event.key==="ArrowRight"?"after-lanes":storedFrame.freePageRegion!,dy=event.key==="ArrowUp"?-20:event.key==="ArrowDown"?20:0,currentState=current().state!,next=moveFreePageFrame(currentState,flow.id,storedFrame.id,{region,x:Number(position.x??24),y:Math.max(55,position.y+dy)});if(next===currentState){pageFrameFocusIntent=undefined;queueMicrotask(()=>elementByData<SVGElement>("data-free-page-frame-canvas",storedFrame.id)?.focus());return;}pageFrameFocusIntent={id:storedFrame.id,revision:Number(current().revision??0),optimisticFocused:false};persist(next);});frame.append(rect,label);canvas.append(frame);}
    for(const endpoint of projection.graph.connectionEndpoints.filter(({kind,freePageRegion})=>kind==="page-frame"&&Boolean(freePageRegion))){const ports=svg("g"),input=svg("circle"),output=svg("circle"),begin=()=>{clearSelectedRelationshipForConnection();connection?.preview?.remove();const targets=projection.graph.connectionEndpoints.map(({id})=>id).filter((id)=>id!==endpoint.id);if(!targets.length)return;const preview=svg("line");preview.classList.add("flow-connection-preview");preview.setAttribute("x1",String(endpoint.layout.x+endpoint.width));preview.setAttribute("y1",String(endpoint.layout.y+endpoint.height/2));preview.setAttribute("x2",String(endpoint.layout.x+endpoint.width+20));preview.setAttribute("y2",String(endpoint.layout.y+endpoint.height/2));canvas.append(preview);connection={sourceId:endpoint.id,targets,targetIndex:0,preview};elementByData("data-input-port-for",targets[0]??"")?.classList.add("is-valid-target");output.focus();};ports.dataset.flowEndpointId=endpoint.id;ports.dataset.flowEndpointKind="page-frame";ports.setAttribute("transform",`translate(${endpoint.layout.x} ${endpoint.layout.y})`);input.setAttribute("cx","0");input.setAttribute("cy",String(endpoint.height/2));input.setAttribute("r","8");input.tabIndex=0;input.dataset.inputPortFor=endpoint.id;input.setAttribute("aria-label",`Input port for ${endpoint.name}`);output.setAttribute("cx",String(endpoint.width));output.setAttribute("cy",String(endpoint.height/2));output.setAttribute("r","8");output.tabIndex=0;output.dataset.outputPortFor=endpoint.id;output.setAttribute("aria-label",`Output port for ${endpoint.name}`);output.addEventListener("pointerdown",(event)=>{event.stopPropagation();begin();});output.addEventListener("keydown",(event)=>{if(event.key==="Enter"&&!connection){event.preventDefault();begin();return;}if(event.key==="Escape"&&connection){event.preventDefault();cancelConnection();return;}if(event.key==="Enter"&&connection){event.preventDefault();commitConnection(connection.targets[connection.targetIndex]);}});input.addEventListener("pointerup",(event)=>{event.stopPropagation();commitConnection(endpoint.id);});ports.append(input,output);canvas.append(ports);}
    for(const relationship of projection.graph.relationships){
      const source=projection.graph.connectionEndpoints.find(({id,kind})=>id===relationship.sourceEndpoint.id&&kind===relationship.sourceEndpoint.kind),target=projection.graph.connectionEndpoints.find(({id,kind})=>id===relationship.targetEndpoint.id&&kind===relationship.targetEndpoint.kind);if(!source||!target)continue;
      const geometry=flowEdgeGeometry(source.layout,target.layout,{width:source.width,height:source.height},{width:target.width,height:target.height},relationship.sourcePort,relationship.targetPort),edge=svg("g"),line=svg("line"),arrow=svg("polygon"),label=svg("text"),selectRelationship=()=>saveSelection({kind:"relationship",id:relationship.id});edge.classList.add("flow-edge");edge.dataset.relationshipId=relationship.id;edge.dataset.sourceEndpointKind=relationship.sourceEndpoint.kind;edge.dataset.sourceEndpointId=relationship.sourceEndpoint.id;edge.dataset.sourcePort=relationship.sourcePort;edge.dataset.targetEndpointKind=relationship.targetEndpoint.kind;edge.dataset.targetEndpointId=relationship.targetEndpoint.id;edge.dataset.targetPort=relationship.targetPort;edge.dataset.relationshipKind=relationship.kind;edge.dataset.directed="true";edge.tabIndex=0;edge.setAttribute("role","button");edge.setAttribute("aria-label",flowRelationshipText(projection.graph,relationship));line.setAttribute("x1",String(geometry.startX));line.setAttribute("y1",String(geometry.startY));line.setAttribute("x2",String(geometry.endX));line.setAttribute("y2",String(geometry.endY));arrow.setAttribute("points",geometry.arrow);label.setAttribute("x",String((geometry.startX+geometry.endX)/2));label.setAttribute("y",String((geometry.startY+geometry.endY)/2-8));label.textContent=relationship.label??"";edge.addEventListener("click",selectRelationship);edge.addEventListener("keydown",(event)=>{if(event.key!=="Enter"&&event.key!==" ")return;event.preventDefault();selectRelationship();});edge.append(line,arrow);if(relationship.label)edge.append(label);canvas.append(edge);const row=document.createElement("li"),control=button(flowRelationshipText(projection.graph,relationship),selectRelationship);row.dataset.relationshipId=relationship.id;row.dataset.sourceEndpointKind=relationship.sourceEndpoint.kind;row.dataset.sourcePort=relationship.sourcePort;row.dataset.targetEndpointKind=relationship.targetEndpoint.kind;row.dataset.targetPort=relationship.targetPort;row.append(control);outline.append(row);
    }
    for(const nodeData of projection.graph.nodes){if(!nodeData.layout)continue;const group=svg("g"),box=svg("rect"),title=svg("text"),detail=svg("text"),input=svg("circle"),output=svg("circle"),layout=nodeData.layout;group.classList.add("flow-node");group.dataset.occurrenceId=nodeData.id;group.dataset.flowEndpointId=nodeData.id;group.dataset.flowEndpointKind="event-occurrence";group.dataset.keyboardOutputFor=nodeData.id;group.setAttribute("transform",`translate(${layout.x} ${layout.y})`);group.setAttribute("tabindex","0");group.setAttribute("role","button");group.setAttribute("aria-label",`${nodeData.name}. Drag or use Arrow keys to move.`);box.setAttribute("width",String(nodeWidth));box.setAttribute("height",String(nodeHeight));box.setAttribute("rx","10");title.setAttribute("x","12");title.setAttribute("y","30");title.textContent=nodeData.name;detail.setAttribute("x","12");detail.setAttribute("y","55");detail.textContent=nodeData.role==="context-setting"?`Context-setting Event${nodeData.trigger?` · ${nodeData.trigger}`:""}`:"Interaction Event";input.setAttribute("cx","0");input.setAttribute("cy",String(nodeHeight/2));input.setAttribute("r","8");input.setAttribute("tabindex","0");input.dataset.inputPortFor=nodeData.id;input.setAttribute("aria-label",`Input port for ${nodeData.name}`);output.setAttribute("cx",String(nodeWidth));output.setAttribute("cy",String(nodeHeight/2));output.setAttribute("r","8");output.setAttribute("tabindex","0");output.dataset.outputPortFor=nodeData.id;output.setAttribute("aria-label",`Output port for ${nodeData.name}`);
      detail.textContent=`Interaction Event${nodeData.trigger?` · ${nodeData.trigger}`:""}`;group.setAttribute("aria-label",`${nodeData.name}. Interaction Event. Drag or use Arrow keys to move.`);
      const startConnection=()=>{clearSelectedRelationshipForConnection();connection?.preview?.remove();const targets=projection.graph.connectionEndpoints.map(({id})=>id).filter((id)=>id!==nodeData.id);if(!targets.length){connection=undefined;statusMessage="Add another Page frame or Event occurrence before drawing a relationship.";render();queueMicrotask(()=>elementByData("data-output-port-for",nodeData.id)?.focus());return;}const preview=svg("line");preview.classList.add("flow-connection-preview");preview.setAttribute("x1",String(layout.x+nodeWidth));preview.setAttribute("y1",String(layout.y+nodeHeight/2));preview.setAttribute("x2",String(layout.x+nodeWidth+20));preview.setAttribute("y2",String(layout.y+nodeHeight/2));canvas.append(preview);connection={sourceId:nodeData.id,targets,targetIndex:0,preview};statusMessage="Connection mode: choose a valid input port; Escape cancels.";elementByData("data-input-port-for",targets[0]??"")?.classList.add("is-valid-target");output.focus();};
      output.addEventListener("pointerdown",(event)=>{event.stopPropagation();startConnection();});output.addEventListener("keydown",(event)=>{event.stopPropagation();if(event.key==="Enter"&&!connection){event.preventDefault();startConnection();return;}if(!connection)return;if(event.key==="Escape"){event.preventDefault();cancelConnection();return;}if(event.key.startsWith("Arrow")){event.preventDefault();document.querySelectorAll(".is-valid-target").forEach((element)=>element.classList.remove("is-valid-target"));connection.targetIndex=(connection.targetIndex+(event.key==="ArrowLeft"||event.key==="ArrowUp"?-1:1)+connection.targets.length)%connection.targets.length;elementByData("data-input-port-for",connection.targets[connection.targetIndex]??"")?.classList.add("is-valid-target");return;}if(event.key==="Enter"){event.preventDefault();commitConnection(connection.targets[connection.targetIndex]!);}});input.addEventListener("pointerup",(event)=>{event.stopPropagation();commitConnection(nodeData.id);});
      const storedOccurrence=stored.occurrences.find(({id})=>id===nodeData.id)!,storedPosition=storedOccurrence.position as {x?:number;y?:number},focusNode=()=>queueMicrotask(()=>elementByData<SVGElement>("data-occurrence-id",nodeData.id)?.focus()),containingPageFrame=projection.graph.connectionEndpoints.find(({kind,id})=>kind==="page-frame"&&id===nodeData.pageFrameId),containedMoveAllowed=(x:number,y:number)=>Boolean(containingPageFrame&&x>=FLOW_GRAPH_GEOMETRY.eventMinX&&y>=FLOW_GRAPH_GEOMETRY.eventMinY&&x+nodeWidth<=containingPageFrame.width&&y+nodeHeight<=containingPageFrame.height),rejectContainedMove=()=>{group.setAttribute("transform",`translate(${layout.x} ${layout.y})`);statusMessage="Add the predefined Event to another Page frame instead.";statusRepairHref="";render();focusNode();},moveContained=(x:number,y:number)=>{if(!containedMoveAllowed(x,y)){rejectContainedMove();return;}persist(moveGraphOccurrence(current().state!,flow.id,nodeData.id,{x,y}));focusNode();},rejectMembershipMove=()=>{if(nodeData.freePageFrame){const page=state.project.collections.pages.find(({id})=>id===nodeData.pageId);statusMessage=`${page?.name??nodeData.name} requires explicit Page Group membership before entering a named lane.`;statusRepairHref=`?kind=pages&entity=${encodeURIComponent(nodeData.pageId)}&field=pageGroupIds`;}else if(nodeData.role==="context-setting"){const review=inspectFreePageEdgeMove(current().state!.project,flow.id,nodeData.id,nodeData.freePageRegion??"after-lanes");statusMessage=review.message;statusRepairHref=review.guidance;}else{statusMessage="Add the predefined Event to another Page frame instead.";statusRepairHref="";}render();focusNode();};
      let dragStart:{x:number;y:number;clientX:number;clientY:number;pointerId:number}|undefined;const ownsDrag=(event:PointerEvent)=>ownsPointerDrag(dragStart?.pointerId,event.pointerId),stopDragTracking=(pointerId:number)=>{window.removeEventListener("pointermove",moveDraggedNode);window.removeEventListener("pointerup",finishDraggedNode);window.removeEventListener("pointercancel",cancelDraggedNode);if(group.hasPointerCapture(pointerId))group.releasePointerCapture(pointerId);dragStart=undefined;},moveDraggedNode=(event:PointerEvent)=>{if(!ownsDrag(event))return;group.setAttribute("transform",`translate(${dragStart!.x+event.clientX-dragStart!.clientX} ${dragStart!.y+event.clientY-dragStart!.clientY})`);},cancelDraggedNode=(event:PointerEvent)=>{if(!ownsDrag(event))return;const pointerId=dragStart!.pointerId;stopDragTracking(pointerId);group.setAttribute("transform",`translate(${layout.x} ${layout.y})`);},finishDraggedNode=(event:PointerEvent)=>{if(!ownsDrag(event))return;const initial=dragStart!,x=Math.round(initial.x+event.clientX-initial.clientX),y=Math.round(initial.y+event.clientY-initial.clientY);stopDragTracking(initial.pointerId);if(nodeData.pageFrameId){const relativeX=x-Number(containingPageFrame?.layout.x??0),relativeY=y-Number(containingPageFrame?.layout.y??0);moveContained(relativeX,relativeY);return;}persist(moveGraphOccurrence(current().state!,flow.id,nodeData.id,{lane:layout.lane,x,y:Math.max(55,y)}));focusNode();};group.addEventListener("pointerdown",(event)=>{if((event.target as Element).closest("circle,foreignObject")||dragStart)return;dragStart={x:layout.x,y:layout.y,clientX:event.clientX,clientY:event.clientY,pointerId:event.pointerId};window.addEventListener("pointermove",moveDraggedNode);window.addEventListener("pointerup",finishDraggedNode);window.addEventListener("pointercancel",cancelDraggedNode);try{group.setPointerCapture(event.pointerId);}catch{/* Synthetic regression events have no active device pointer to capture. */}});group.addEventListener("keydown",(event)=>{if((event.target as Element).closest("foreignObject,input,button,a,summary,details"))return;if(event.key==="Enter"&&!connection){event.preventDefault();startConnection();return;}if(connection?.sourceId===nodeData.id){if(event.key==="Escape"){event.preventDefault();cancelConnection();return;}if(event.key.startsWith("Arrow")){event.preventDefault();document.querySelectorAll(".is-valid-target").forEach((element)=>element.classList.remove("is-valid-target"));connection.targetIndex=(connection.targetIndex+(event.key==="ArrowLeft"||event.key==="ArrowUp"?-1:1)+connection.targets.length)%connection.targets.length;elementByData("data-input-port-for",connection.targets[connection.targetIndex]??"")?.classList.add("is-valid-target");return;}if(event.key==="Enter"){event.preventDefault();commitConnection(connection.targets[connection.targetIndex]!);return;}}if(!event.key.startsWith("Arrow"))return;event.preventDefault();const dx=event.key==="ArrowLeft"?-20:event.key==="ArrowRight"?20:0,dy=event.key==="ArrowUp"?-20:event.key==="ArrowDown"?20:0;if(nodeData.pageFrameId){moveContained(Number(storedPosition.x??24)+dx,Number(storedPosition.y??70)+dy);return;}persist(moveGraphOccurrence(current().state!,flow.id,nodeData.id,{lane:layout.lane,x:layout.x+dx,y:Math.max(55,layout.y+dy)}));focusNode();});group.addEventListener("click",(event)=>{if((event.target as Element).closest("foreignObject"))return;if(suppressNodeClick){suppressNodeClick=false;return;}saveSelection({kind:"occurrence",id:nodeData.id});});const canvasExample=occurrenceExampleDetails(state,flow.id,nodeData.id,nodeData.name),exampleHost=svg("foreignObject"),resizeCanvasExample=()=>{const expandedHeight=canvasExample.open?Math.max(260,Math.ceil(canvasExample.scrollHeight)+8):30;exampleHost.setAttribute("height",String(expandedHeight));box.setAttribute("height",String(canvasExample.open?nodeHeight+expandedHeight-30:nodeHeight));resizeCanvasHeight();};exampleHost.dataset.eventExampleNode=nodeData.id;exampleHost.setAttribute("x","4");exampleHost.setAttribute("y","62");exampleHost.setAttribute("width",String(nodeWidth-8));exampleHost.setAttribute("height","30");canvasExample.className="flow-node-example";canvasExample.style.fontSize="14px";canvasExample.style.background="white";new MutationObserver(resizeCanvasExample).observe(canvasExample,{attributes:true,attributeFilter:["open"]});const canvasSummary=canvasExample.querySelector("summary")!;canvasSummary.addEventListener("keydown",(event)=>{if(event.key!=="Enter"&&event.key!==" ")return;event.preventDefault();canvasExample.open=!canvasExample.open;});exampleHost.append(canvasExample);group.append(box,title,detail,input,output,exampleHost);canvas.append(group);const row=document.createElement("li"),control=button(`${nodeData.name} · ${nodeData.role}${nodeData.trigger?` · ${nodeData.trigger}`:""}`,()=>saveSelection({kind:"occurrence",id:nodeData.id})),outlineExample=occurrenceExampleDetails(state,flow.id,nodeData.id,nodeData.name);row.dataset.occurrenceId=nodeData.id;row.append(control,outlineExample);outline.insertBefore(row,outline.querySelector("[data-relationship-id]"));}
    for(const nodeData of projection.graph.nodes){const control=outline.querySelector<HTMLButtonElement>(`[data-occurrence-id="${CSS.escape(nodeData.id)}"] button`);if(control)control.textContent=`${nodeData.name} · Interaction Event${nodeData.trigger?` · ${nodeData.trigger}`:""}`;}
    for(const port of Array.from(canvas.querySelectorAll<SVGCircleElement>("[data-input-port-for],[data-output-port-for]"))){const endpointId=port.dataset.inputPortFor??port.dataset.outputPortFor,endpoint=projection.graph.connectionEndpoints.find(({id})=>id===endpointId);if(!endpoint)continue;const side:FlowPortSide=port.dataset.outputPortFor?"right":"left",point=flowPortPoint(endpoint.layout,{width:endpoint.width,height:endpoint.height},side);port.dataset.flowPortFor=endpoint.id;port.dataset.flowPortSide=side;port.setAttribute("aria-label",`${side} port for ${endpoint.name}`);port.setAttribute("cx",String(point.x));port.setAttribute("cy",String(point.y));canvas.append(port);}
    for(const endpoint of projection.graph.connectionEndpoints){for(const side of["top","bottom"] as const){const port=svg("circle"),point=flowPortPoint(endpoint.layout,{width:endpoint.width,height:endpoint.height},side);port.setAttribute("cx",String(point.x));port.setAttribute("cy",String(point.y));port.setAttribute("r","8");port.tabIndex=0;port.dataset.flowPortFor=endpoint.id;port.dataset.flowPortSide=side;port.dataset.inputPortFor=endpoint.id;port.setAttribute("aria-label",`${side} port for ${endpoint.name}`);port.addEventListener("pointerdown",(event)=>{event.stopPropagation();if(!connection)beginPortConnection(endpoint,side,port);});port.addEventListener("pointerup",(event)=>{event.stopPropagation();if(connection)commitConnection(endpoint.id,side);});port.addEventListener("keydown",(event)=>{event.stopPropagation();if(event.key==="Enter"&&!connection){event.preventDefault();beginPortConnection(endpoint,side,port);return;}if(!connection||connection.sourceId!==endpoint.id||connection.sourcePort!==side)return;if(event.key==="Escape"){event.preventDefault();cancelConnection();return;}if(event.key.startsWith("Arrow")){event.preventDefault();document.querySelectorAll(".is-valid-target").forEach((element)=>element.classList.remove("is-valid-target"));connection.targetIndex=(connection.targetIndex+(event.key==="ArrowLeft"||event.key==="ArrowUp"?-1:1)+connection.targets.length)%connection.targets.length;targetPortElement(connection.targets[connection.targetIndex]!,side)?.classList.add("is-valid-target");return;}if(event.key==="Enter"){event.preventDefault();commitConnection(connection.targets[connection.targetIndex]!,targetPortFor(side));}});canvas.append(port);}}
    canvas.addEventListener("pointermove",(event)=>{if(!connection?.preview)return;canvasScroll.classList.add("is-connecting");const port=(event.target as Element).closest<SVGElement>("[data-flow-port-for]"),scrollBounds=canvasScroll.getBoundingClientRect(),edgeSize=36,edgeStep=28;if(!port){if(event.clientX<=scrollBounds.left+edgeSize)canvasScroll.scrollLeft=Math.max(0,canvasScroll.scrollLeft-edgeStep);else if(event.clientX>=scrollBounds.right-edgeSize)canvasScroll.scrollLeft=Math.min(canvasScroll.scrollWidth-canvasScroll.clientWidth,canvasScroll.scrollLeft+edgeStep);if(event.clientY<=scrollBounds.top+edgeSize)canvasScroll.scrollTop=Math.max(0,canvasScroll.scrollTop-edgeStep);else if(event.clientY>=scrollBounds.bottom-edgeSize)canvasScroll.scrollTop=Math.min(canvasScroll.scrollHeight-canvasScroll.clientHeight,canvasScroll.scrollTop+edgeStep);}const bounds=canvas.getBoundingClientRect(),viewBox=canvas.viewBox.baseVal,scaleX=viewBox.width/bounds.width,scaleY=viewBox.height/bounds.height;connection.preview.setAttribute("x2",String(viewBox.x+(event.clientX-bounds.left)*scaleX));connection.preview.setAttribute("y2",String(viewBox.y+(event.clientY-bounds.top)*scaleY));document.querySelectorAll(".is-valid-target,.is-invalid-target").forEach((element)=>element.classList.remove("is-valid-target","is-invalid-target"));const valid=port&&port.dataset.flowPortFor!==connection.sourceId&&port.dataset.flowPortSide===targetPortFor(connection.sourcePort??"right");(port??canvas).classList.add(valid?"is-valid-target":"is-invalid-target");});
    canvas.addEventListener("pointerup",(event)=>{if(!connection)return;const delivered=(event.target as Element).closest<SVGElement>("[data-flow-port-for]"),hit=document.elementFromPoint(event.clientX,event.clientY)?.closest<SVGElement>("[data-flow-port-for]"),port=hit??delivered,side=port?.dataset.flowPortSide as FlowPortSide|undefined;if(port&&side)commitConnection(port.dataset.flowPortFor,side);else cancelConnection(true,true);});
    canvas.addEventListener("keydown",(event)=>{if(!connection)return;if(event.key==="Escape"){event.preventDefault();event.stopImmediatePropagation();cancelConnection();return;}if(event.key.startsWith("Arrow")){event.preventDefault();event.stopImmediatePropagation();document.querySelectorAll(".is-valid-target").forEach((element)=>element.classList.remove("is-valid-target"));connection.targetIndex=(connection.targetIndex+(event.key==="ArrowLeft"||event.key==="ArrowUp"?-1:1)+connection.targets.length)%connection.targets.length;targetPortElement(connection.targets[connection.targetIndex]!,connection.sourcePort??"right")?.classList.add("is-valid-target");return;}if(event.key==="Enter"){event.preventDefault();event.stopImmediatePropagation();commitConnection(connection.targets[connection.targetIndex]!,targetPortFor(connection.sourcePort??"right"));}},true);
    canvasScroll.className="flow-canvas-scroll";canvasScroll.append(canvas);views.append(canvasScroll,outline);renderRelationshipPopover(popover);renderActions(actions);section.append(heading,boundary,toolbar,laneControls,status,frames,views,actions,popover);host.append(section);document.querySelectorAll<HTMLElement>("[data-occurrence-id],[data-relationship-id],[data-page-frame-id]").forEach((element)=>{const id=element.dataset.occurrenceId??element.dataset.relationshipId??element.dataset.pageFrameId;element.classList.toggle("is-selected",id===selected?.id);});if(relationshipDeletionFocusTimer!==undefined){clearTimeout(relationshipDeletionFocusTimer);relationshipDeletionFocusTimer=undefined;}const deletionIntent=relationshipDeletionFocusIntent;if(deletionIntent){const restored=Boolean(canvas.querySelector(`[data-relationship-id="${CSS.escape(deletionIntent.id)}"]`));if(restored||!deletionIntent.sourceFocused)relationshipDeletionFocusTimer=setTimeout(()=>{relationshipDeletionFocusTimer=undefined;if(relationshipDeletionFocusIntent!==deletionIntent)return;const liveCanvas=document.querySelector<SVGSVGElement>('[aria-label="Interactive directional Flow canvas"]'),selector=restored?`[data-relationship-id="${CSS.escape(deletionIntent.id)}"]`:`[${deletionIntent.sourceKind==="page-frame"?"data-page-frame-id":"data-occurrence-id"}="${CSS.escape(deletionIntent.sourceId)}"]`,target=liveCanvas?.querySelector<HTMLElement>(selector);if(!target?.isConnected)return;target.focus();relationshipDeletionFocusIntent=consumeRelationshipDeletionFocus(deletionIntent,restored).next;},50);}const frameIntent=pageFrameFocusIntent;if(frameIntent){const focusedFrame=document.querySelector<HTMLElement>(`[data-free-page-frame-canvas="${CSS.escape(frameIntent.id)}"]`);if(focusedFrame){const renderRevision=Number(current().revision??0),replacement=frameIntent.optimisticFocused&&renderRevision>frameIntent.revision;if(!frameIntent.optimisticFocused){frameIntent.optimisticFocused=true;frameIntent.revision=renderRevision;}queueMicrotask(()=>{if(!focusedFrame.isConnected)return;focusedFrame.focus();if(replacement&&pageFrameFocusIntent===frameIntent)pageFrameFocusIntent=undefined;});}}const edgeIntent=relationshipEdgeFocusIntent;if(edgeIntent){const edge=document.querySelector<HTMLElement>(`[data-relationship-id="${CSS.escape(edgeIntent.id)}"]`);if(edge){const renderRevision=Number(current().revision??0),replacement=edgeIntent.optimisticFocused&&renderRevision>edgeIntent.revision;if(!edgeIntent.optimisticFocused){edgeIntent.optimisticFocused=true;edgeIntent.revision=renderRevision;}queueMicrotask(()=>{if(!edge.isConnected)return;edge.focus();if(replacement&&relationshipEdgeFocusIntent===edgeIntent)relationshipEdgeFocusIntent=undefined;});}}renderInspector();
  }

  function render():void{const {flow}=current();advanced.hidden=!flow;if(flow)renderGraph(flow);else{document.querySelector<HTMLElement>("#flow-graph-workspace")?.replaceChildren();inspectorContext.replaceChildren();}}
  return{render,renderSelectors:render};
}
