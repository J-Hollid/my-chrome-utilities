import {
  addGraphOccurrence,documentaryFlowGraph,flowOutline,flowRelationshipText,moveGraphOccurrence,projectFlowGraph,
  addInteractionOccurrenceToPage,addUngroupedPageFrame,inspectUngroupedPageDrop,removeGraphOccurrence,reorderGraphOccurrence,saveGraphRelationship,setFlowPageGroupLanes,updateGraphOccurrence,
  type FlowLayout,type FlowObligation,type FlowRelationshipKind,type FlowRole,
} from "./data-layer-flow-graph.js";
import type {IdFactory,ProjectEntity,ProjectState} from "./utilities/data-layer/schemas.js";

export interface FlowGraphBuilderContext {state?:ProjectState;flowId?:string}
export interface FlowGraphBuilderIntegration {render():void;renderSelectors():void}
interface IntegrationOptions {context:()=>FlowGraphBuilderContext;persist:(state:ProjectState)=>void;id:IdFactory}
const lanes=[{name:"Context",x:30},{name:"Shipping",x:230},{name:"Payment",x:430},{name:"Merge",x:630}] as const;
const nodeWidth=170,nodeHeight=82;
const q=<T extends Element>(selector:string):T=>{const element=document.querySelector<T>(selector);if(!element)throw new Error(`Missing ${selector}`);return element;};
const replaceOptions=(select:HTMLSelectElement,entities:readonly ProjectEntity[],placeholder:string)=>{const value=select.value,empty=new Option(placeholder,"");select.replaceChildren(empty,...entities.map(({id,name})=>new Option(name,id)));select.value=value;};

export function flowEdgeGeometry(source:{x:number;y:number},target:{x:number;y:number},width=nodeWidth,height=nodeHeight):{startX:number;startY:number;endX:number;endY:number;arrow:string}{
  const halfWidth=width/2,halfHeight=height/2,sourceCenter={x:source.x+halfWidth,y:source.y+halfHeight},targetCenter={x:target.x+halfWidth,y:target.y+halfHeight},dx=targetCenter.x-sourceCenter.x,dy=targetCenter.y-sourceCenter.y,length=Math.hypot(dx,dy),unitX=length<0.001?1:dx/length,unitY=length<0.001?0:dy/length,borderDistance=Math.min(Math.abs(unitX)<0.001?Infinity:halfWidth/Math.abs(unitX),Math.abs(unitY)<0.001?Infinity:halfHeight/Math.abs(unitY)),candidateStart={x:sourceCenter.x+unitX*borderDistance,y:sourceCenter.y+unitY*borderDistance},candidateEnd={x:targetCenter.x-unitX*borderDistance,y:targetCenter.y-unitY*borderDistance},forwardDistance=(candidateEnd.x-candidateStart.x)*unitX+(candidateEnd.y-candidateStart.y)*unitY,midpoint={x:(sourceCenter.x+targetCenter.x)/2,y:(sourceCenter.y+targetCenter.y)/2},startX=forwardDistance>0?candidateStart.x:midpoint.x-unitX*6,startY=forwardDistance>0?candidateStart.y:midpoint.y-unitY*6,endX=forwardDistance>0?candidateEnd.x:midpoint.x+unitX*6,endY=forwardDistance>0?candidateEnd.y:midpoint.y+unitY*6,baseX=endX-unitX*12,baseY=endY-unitY*12,normalX=-unitY*7,normalY=unitX*7;
  return{startX,startY,endX,endY,arrow:`${baseX+normalX},${baseY+normalY} ${endX},${endY} ${baseX-normalX},${baseY-normalY}`};
}
function svgClientPoint(graph:SVGSVGElement,clientX:number,clientY:number):DOMPoint{const matrix=graph.getScreenCTM();if(!matrix)throw new Error("Interactive Flow canvas has no screen transformation matrix.");return new DOMPoint(clientX,clientY).matrixTransform(matrix.inverse());}
function addSelect(form:HTMLFormElement,id:string,labelText:string,choices:readonly (readonly [string,string])[],before:Element):HTMLSelectElement{const label=document.createElement("label"),select=document.createElement("select");select.id=id;label.textContent=labelText;for(const[value,text]of choices)select.append(new Option(text,value));label.append(select);form.insertBefore(label,before);return select;}

export function installFlowGraphBuilder(options:IntegrationOptions):FlowGraphBuilderIntegration{
  const inspector=q<HTMLElement>("#project-inspector"),advancedEditor=q<HTMLElement>("#flow-step-editor"),addForm=q<HTMLFormElement>("#add-flow-step-form"),list=q<HTMLOListElement>("#flow-step-list"),result=q<HTMLElement>("#flow-step-result"),minimumLabel=q("#flow-step-minimum").closest("label")!,inspectorContext=document.createElement("div"),documentaryEditor=document.createElement("section");
  inspectorContext.id="flow-inspector-context";documentaryEditor.id="flow-documentary-editor";documentaryEditor.append(addForm,list,result);inspector.insertBefore(inspectorContext,q("#add-entity-form"));inspector.insertBefore(documentaryEditor,advancedEditor);
  const role=addSelect(addForm,"flow-step-role","Role",[["interaction","Interaction"],["context-setting","Context-setting"]],minimumLabel),pageGroup=addSelect(addForm,"flow-step-page-group","Page Group",[],minimumLabel),occurrenceType=addSelect(addForm,"flow-step-occurrence-type","Occurrence type",[["interaction","Interaction"],["page-context","Page context"]],minimumLabel),contextBinding=addSelect(addForm,"flow-step-context-binding","Context-event binding",[],minimumLabel),obligation=addSelect(addForm,"flow-step-obligation","Obligation",[["Required","Required"],["Optional","Optional"],["Conditional","Conditional"],["Informational","Informational"]],minimumLabel);
  const addName=q<HTMLInputElement>("#flow-step-name"),addPage=q<HTMLSelectElement>("#flow-step-page"),addEvent=q<HTMLSelectElement>("#flow-step-event");addName.required=addPage.required=addEvent.required=true;
  q("#flow-step-name").closest("label")!.firstChild!.textContent="Occurrence name";q("#flow-step-page").closest("label")!.firstChild!.textContent="Page";q("#flow-step-event").closest("label")!.firstChild!.textContent="Shared Event";q("#flow-step-minimum").closest("label")!.firstChild!.textContent="Expected minimum";q("#flow-step-maximum").closest("label")!.firstChild!.textContent="Expected maximum";q<HTMLInputElement>("#flow-step-optional").closest("label")!.hidden=true;role.closest("label")!.hidden=true;addForm.querySelector<HTMLButtonElement>('button[type="submit"]')!.textContent="Add occurrence";
  let selectedItem:{kind:"occurrence"|"relationship";id:string}|undefined;
  const current=()=>{const context=options.context(),flow:ProjectEntity|undefined=context.flowId?context.state?.project.collections.flows.find(({id})=>id===context.flowId):undefined,graph=flow&&context.state?documentaryFlowGraph(context.state.project,flow.id):{pageGroupIds:[],occurrences:[],relationships:[]};return{...context,flow,pageGroupIds:graph.pageGroupIds,steps:graph.occurrences as ProjectEntity[],relationships:graph.relationships};};
  const synchronizedRole=(eventId:string,fallback:string):{role:FlowRole;authoritative:boolean}=>{const event=current().state?.project.collections.events.find(({id})=>id===eventId),eventRole=event?.role;return eventRole==="context-setting"||eventRole==="interaction"?{role:eventRole,authoritative:true}:{role:fallback==="context-setting"?"context-setting":"interaction",authoritative:false};};
  const syncRoleControl=(eventSelect:HTMLSelectElement,roleSelect:HTMLSelectElement,fallback=roleSelect.value):void=>{const selected=synchronizedRole(eventSelect.value,fallback);roleSelect.value=selected.role;roleSelect.disabled=selected.authoritative;roleSelect.title=selected.authoritative?"This role is defined by the selected Event.":"Choose a fallback role until the Event definition has one.";};
  addEvent.addEventListener("change",()=>syncRoleControl(addEvent,role));
  const syncAddContextBindings=()=>{const page=current().state?.project.collections.pages.find(({id})=>id===addPage.value),bindings=(page?.contextEventBindings as ProjectEntity[]|undefined)??[],value=contextBinding.value;contextBinding.replaceChildren(new Option("Choose Page context binding",""),...bindings.map(({id,name})=>new Option(name,id)));contextBinding.value=value;const contextual=occurrenceType.value==="page-context";contextBinding.closest("label")!.hidden=!contextual;addEvent.closest("label")!.hidden=contextual;contextBinding.required=contextual;addEvent.required=!contextual;};
  addPage.addEventListener("change",syncAddContextBindings);occurrenceType.addEventListener("change",syncAddContextBindings);
  const laneForX=(x:number)=>lanes.reduce((nearest,lane)=>Math.abs(lane.x-x)<Math.abs(nearest.x-x)?lane:nearest,lanes[0]);
  const occurrenceInput=(values:{name:string;pageId:string;eventId:string;fallbackRole:string;obligation:string;minimum:number;maximum:number;lane:string;x:number;y:number})=>{const editorPageGroup=current().state?.project.collections.pageGroups.some(({id})=>id===values.lane)?values.lane:"",pageGroupId=editorPageGroup||pageGroup.value,contextual=editorPageGroup?values.fallbackRole==="context-setting":occurrenceType.value==="page-context",eventReference=editorPageGroup?values.eventId:contextual?contextBinding.value:values.eventId;return pageGroupId?{name:values.name,pageGroupId,pageId:values.pageId,...(contextual?{contextBindingId:eventReference}:{eventId:eventReference}),obligation:values.obligation as FlowObligation,minimum:values.minimum,maximum:values.maximum,y:values.y}:{name:values.name,pageId:values.pageId,eventId:values.eventId,fallbackRole:values.fallbackRole as FlowRole,obligation:values.obligation as FlowObligation,minimum:values.minimum,maximum:values.maximum,layout:{lane:values.lane,x:values.x,y:values.y}};};

  function renderFlowLaneControls():void{
    const {state,flow,pageGroupIds}=current(),form=document.querySelector<HTMLFormElement>("#flow-inspector-context .contextual-editor form");if(!state||!flow||!form||form.querySelector('[aria-label="Page Group lane controls"]'))return;const laneLabel=document.createElement("label"),laneSelect=document.createElement("select"),selectedGroups=pageGroupIds.map((groupId)=>state.project.collections.pageGroups.find(({id})=>id===groupId)).filter((group):group is ProjectEntity=>Boolean(group)),remainingGroups=state.project.collections.pageGroups.filter(({id})=>!pageGroupIds.includes(id));laneLabel.textContent="Ordered Page Group lanes";laneSelect.name="pageGroupIds";laneSelect.multiple=true;laneSelect.setAttribute("aria-label","Ordered Page Group lanes");for(const group of[...selectedGroups,...remainingGroups]){const option=new Option(group.name,group.id);option.selected=pageGroupIds.includes(group.id);laneSelect.append(option);}laneLabel.append(laneSelect);form.insertBefore(laneLabel,form.querySelector(".editor-actions"));const controls=document.createElement("section"),up=document.createElement("button"),down=document.createElement("button"),message=document.createElement("p");controls.setAttribute("aria-label","Page Group lane controls");up.type=down.type="button";up.textContent="Move selected lane up";down.textContent="Move selected lane down";message.setAttribute("role","status");form.addEventListener("submit",(event)=>{const fresh=current().state;if(!fresh)return;try{const next=setFlowPageGroupLanes(fresh,flow.id,Array.from(laneSelect.selectedOptions,({value})=>value));if(next!==fresh)options.persist(next);}catch(error){event.preventDefault();event.stopImmediatePropagation();message.textContent=error instanceof Error?error.message:String(error);}},{capture:true});
    const selectedOption=()=>Array.from(laneSelect.options).find(({selected})=>selected);const move=(delta:number)=>{const option=selectedOption();if(!option)return;const index=Array.from(laneSelect.options).indexOf(option),targetIndex=Math.max(0,Math.min(laneSelect.options.length-1,index+delta));if(index===targetIndex)return;laneSelect.insertBefore(option,delta>0?laneSelect.options[targetIndex]!.nextSibling:laneSelect.options[targetIndex]!);option.selected=true;laneSelect.dispatchEvent(new Event("change",{bubbles:true}));};up.addEventListener("click",()=>move(-1));down.addEventListener("click",()=>move(1));
    controls.append(up,down,message);laneSelect.closest("form")?.append(controls);
  }

  function renderSelectors():void{const {state,pageGroupIds}=current();if(!state)return;replaceOptions(q("#flow-step-page"),state.project.collections.pages,"Choose Page");replaceOptions(q("#flow-step-event"),state.project.collections.events,"Choose Event");const groups=pageGroupIds.map((groupId)=>state.project.collections.pageGroups.find(({id})=>id===groupId)).filter((group):group is ProjectEntity=>Boolean(group));replaceOptions(pageGroup,groups,"Choose Page Group");syncAddContextBindings();}
  function renderInspector():void{
    render(false);synchronizeRenderedOccurrenceControls();
    const {steps,relationships}=current(),selection=selectedItem,sourceId=selection?.kind==="relationship"?relationships.find(({id})=>id===selection.id)?.sourceNodeId:undefined;
    for(const item of Array.from(list.children)){const occurrenceId=item.querySelector<HTMLElement>("[data-edit-occurrence-id]")?.dataset.editOccurrenceId;item.toggleAttribute("hidden",Boolean(selection&&occurrenceId!==(selection.kind==="occurrence"?selection.id:sourceId)));if(selection?.kind==="relationship")for(const form of Array.from(item.querySelectorAll<HTMLFormElement>(":scope > div > form")))form.toggleAttribute("hidden",form.querySelector<HTMLElement>("[data-edit-relationship-id]")?.dataset.editRelationshipId!==selection.id);}
    document.querySelectorAll<HTMLElement>("#flow-graph-workspace [data-occurrence-id],#flow-graph-workspace [data-relationship-id]").forEach((element)=>element.classList.toggle("is-selected",element.dataset[selection?.kind==="occurrence"?"occurrenceId":"relationshipId"]===selection?.id));
    documentaryEditor.querySelector(".flow-selection-heading")?.remove();if(!selection)return;const heading=document.createElement("h3"),occurrence=steps.find(({id})=>id===selection.id),relationship=relationships.find(({id})=>id===selection.id),source=steps.find(({id})=>id===relationship?.sourceNodeId),target=steps.find(({id})=>id===relationship?.targetNodeId);heading.className="flow-selection-heading";heading.tabIndex=-1;heading.textContent=selection.kind==="occurrence"?`Selected occurrence: ${occurrence?.name??selection.id}`:`Selected relationship: ${source?.name??"Unknown source"} → ${target?.name??"Unknown target"}`;documentaryEditor.insertBefore(heading,list);heading.focus({preventScroll:true});
  }
  function selectItem(kind:"occurrence"|"relationship",itemId:string):void{selectedItem={kind,id:itemId};renderInspector();queueMicrotask(()=>document.querySelector<HTMLElement>(kind==="occurrence"?`[data-edit-occurrence-id="${itemId}"]`:`[data-edit-relationship-id="${itemId}"]`)?.focus());}
  function focusOccurrence(nodeId:string):void{selectItem("occurrence",nodeId);}
  function focusRelationship(relationshipId:string):void{selectedItem={kind:"relationship",id:relationshipId};document.querySelectorAll<HTMLElement>("#flow-graph-workspace [data-relationship-id]").forEach((element)=>element.classList.toggle("is-selected",element.dataset.relationshipId===relationshipId));queueMicrotask(()=>document.querySelector<HTMLElement>(`[aria-label="Synchronized editable Flow outline"] [data-relationship-id="${relationshipId}"] button`)?.focus());}
  function saveLayout(flowId:string,nodeId:string,currentLayout:FlowLayout,x:number,y:number):void{const {state,steps}=current();if(!state)return;const occurrence=steps.find(({id})=>id===nodeId),node=document.querySelector<SVGGElement>(`[data-occurrence-id="${nodeId}"]`),semantic=Boolean(occurrence?.pageGroupId||occurrence?.freePageFrame||occurrence?.freePageFrameId),nextY=Math.max(55,Math.round(y));if(semantic&&Math.abs(x-currentLayout.x)>85){node?.setAttribute("transform",`translate(${currentLayout.x} ${currentLayout.y})`);result.textContent="Move rejected: Event occurrences cannot cross Page or Page Group containment boundaries. Add the predefined Event from Components to another Page frame.";node?.focus();return;}if(semantic){if(nextY===currentLayout.y){node?.focus();return;}options.persist(moveGraphOccurrence(state,flowId,nodeId,{y:nextY}));queueMicrotask(()=>document.querySelector<SVGGElement>(`[data-occurrence-id="${nodeId}"]`)?.focus());return;}const lane=laneForX(x),layout={lane:lane.name,x:lane.x,y:nextY};if(layout.x===currentLayout.x&&layout.y===currentLayout.y&&layout.lane===currentLayout.lane){node?.focus();return;}options.persist(moveGraphOccurrence(state,flowId,nodeId,layout));queueMicrotask(()=>document.querySelector<SVGGElement>(`[data-occurrence-id="${nodeId}"]`)?.focus());}

  function renderGraph(flow:ProjectEntity):void{
    const host=q<HTMLElement>("#flow-graph-workspace");host.replaceChildren();const {state}=current();if(!state)return;const projection=projectFlowGraph(state.project,flow.id),section=document.createElement("section"),heading=document.createElement("h3"),boundary=document.createElement("p"),instructions=document.createElement("p"),views=document.createElement("div"),graph=document.createElementNS("http://www.w3.org/2000/svg","svg"),outline=document.createElement("ol");
    const graphLanes=projection.lanes.length?projection.lanes.map(({id,name},index)=>({id,name,x:30+index*200})):lanes.map((lane)=>({...lane,id:lane.name}));if(projection.graph.nodes.some(({freePageFrame,freePageFrameId})=>freePageFrame||freePageFrameId))graphLanes.push({id:"ungrouped",name:"Ungrouped entry pages",x:30+projection.lanes.length*200});
    section.className="documentary-flow";heading.textContent="Directional Flow graph";boundary.className="status-text";boundary.textContent="Specification Flow — Event payload validation remains independent. Sequence, branch, and occurrence expectations are checked manually.";instructions.textContent="Drag a node, or focus it and use arrow keys, to reposition it. Press Enter or Space to edit. Use the synchronized outline to edit occurrence and relationship meaning.";views.className="flow-projections";graph.classList.add("flow-graph-canvas");graph.setAttribute("aria-label","Interactive directional Flow canvas");graph.setAttribute("role","application");outline.setAttribute("aria-label","Synchronized editable Flow outline");
    const palette=document.createElement("section"),paletteHeading=document.createElement("h4"),paletteItems=document.createElement("div"),frames=document.createElement("section"),frameHeading=document.createElement("h4"),guidance=document.createElement("p");palette.setAttribute("aria-label","Flow component palette");paletteHeading.textContent="Components";frames.setAttribute("aria-label","Flow Page frames");frameHeading.textContent="Page frames";guidance.setAttribute("role","status");guidance.textContent="Event occurrences cannot cross Page or Page Group containment boundaries. Add the predefined Event to another Page frame to create a distinct occurrence.";const componentButton=(kind:"page"|"event",entity:ProjectEntity)=>{const button=document.createElement("button");button.type="button";button.draggable=true;button.dataset.componentKind=kind;button.dataset.componentId=entity.id;button.textContent=`${kind==="page"?"Page":"Event"}: ${entity.name}`;button.addEventListener("dragstart",(event)=>{event.dataTransfer?.setData("application/x-flow-component",JSON.stringify({kind,id:entity.id}));});return button;};paletteItems.append(...state.project.collections.pages.map((page)=>componentButton("page",page)),...state.project.collections.events.map((event)=>componentButton("event",event)));palette.append(paletteHeading,paletteItems);
    const acceptDrop=(target:{pageId:string;pageGroupId?:string;freePageFrameId?:string})=>(event:DragEvent)=>{event.preventDefault();const raw=event.dataTransfer?.getData("application/x-flow-component");if(!raw)return;const component=JSON.parse(raw) as {kind:"page"|"event";id:string};if(component.kind==="event"){const selectedEvent=state.project.collections.events.find(({id})=>id===component.id);if(!selectedEvent)return;options.persist(addInteractionOccurrenceToPage(state,flow.id,{name:selectedEvent.name,...target,eventId:selectedEvent.id,obligation:"Required",minimum:1,maximum:1,y:70+projection.graph.nodes.filter(({pageId})=>pageId===target.pageId).length*120},options.id));return;}if(target.pageGroupId){const inspection=inspectUngroupedPageDrop(state.project,flow.id,component.id,target.pageGroupId);guidance.textContent=`${inspection.message} Open membership editor: ${inspection.guidance}`;return;}};
    for(const lane of projection.lanes){const memberIds=(lane.pageIds as string[]|undefined)??[];for(const pageId of memberIds){const page=state.project.collections.pages.find(({id})=>id===pageId);if(!page)continue;const frame=document.createElement("div");frame.tabIndex=0;frame.dataset.pageFrameId=page.id;frame.dataset.pageGroupId=lane.id;frame.textContent=`${lane.name} / ${page.name}`;frame.addEventListener("dragover",(event)=>event.preventDefault());frame.addEventListener("drop",acceptDrop({pageId:page.id,pageGroupId:lane.id}));frames.append(frame);}}
    for(const free of current().steps.filter(({freePageFrame})=>freePageFrame)){const page=state.project.collections.pages.find(({id})=>id===free.pageId),frame=document.createElement("div");frame.tabIndex=0;frame.dataset.freePageFrameId=free.id;frame.textContent=`Ungrouped / ${page?.name??free.name}`;frame.addEventListener("dragover",(event)=>event.preventDefault());frame.addEventListener("drop",acceptDrop({pageId:String(free.pageId),freePageFrameId:free.id}));frames.append(frame);}
    const ungrouped=document.createElement("div");ungrouped.tabIndex=0;ungrouped.dataset.ungroupedDropzone="true";ungrouped.textContent="Ungrouped entry pages";ungrouped.addEventListener("dragover",(event)=>event.preventDefault());ungrouped.addEventListener("drop",(event)=>{event.preventDefault();const raw=event.dataTransfer?.getData("application/x-flow-component");if(!raw)return;const component=JSON.parse(raw) as {kind:string;id:string};if(component.kind!=="page")return;const page=state.project.collections.pages.find(({id})=>id===component.id),binding=((page?.contextEventBindings as ProjectEntity[]|undefined)??[])[0];if(!page||!binding){guidance.textContent="Choose a Page with a context-event binding before creating an ungrouped entry frame.";return;}try{options.persist(addUngroupedPageFrame(state,flow.id,{name:`${page.name} entry`,pageId:page.id,contextBindingId:binding.id,y:70},options.id));}catch(error){guidance.textContent=error instanceof Error?error.message:String(error);}});frames.prepend(frameHeading,ungrouped);section.append(palette,frames,guidance);
    if(!projection.graph.nodes.length){const empty=document.createElement("section"),copy=document.createElement("p"),action=document.createElement("button");empty.className="flow-empty-state";copy.textContent="Page Groups define lanes, Pages bind context Events, and Events own reusable payload schemas. Occurrences retain stable Page Group, Page, binding, and Event references. Per-Event payload validation remains independent; topology, branch, and journey expectations remain manual.";action.type="button";action.className="primary";action.textContent="Start a Page-context occurrence";action.addEventListener("click",()=>{occurrenceType.value="page-context";syncAddContextBindings();q<HTMLInputElement>("#flow-step-name").focus();});empty.append(copy,action);section.append(heading,boundary,empty);host.append(section);return;}
    const positions=new Map(projection.graph.nodes.map((node,index)=>[node.id,node.layout??{lane:node.role==="context-setting"?"Context":"Shipping",x:node.role==="context-setting"?30:230,y:70+index*120}])),canvasHeight=Math.max(360,...Array.from(positions.values(),({y})=>y+150)),canvasWidth=Math.max(840,graphLanes.length*200+40);graph.setAttribute("viewBox",`0 0 ${canvasWidth} ${canvasHeight}`);
    for(const lane of graphLanes){const band=document.createElementNS(graph.namespaceURI,"rect") as SVGRectElement,label=document.createElementNS(graph.namespaceURI,"text");band.classList.add("flow-lane");band.dataset.pageGroupId=lane.id;band.setAttribute("x",String(lane.x-15));band.setAttribute("y","8");band.setAttribute("width","200");band.setAttribute("height",String(canvasHeight-16));label.classList.add("flow-lane-label");label.setAttribute("x",String(lane.x));label.setAttribute("y","32");label.textContent=lane.name;graph.append(band,label);}
    for(const relationship of projection.graph.relationships){const source=positions.get(relationship.sourceNodeId),target=positions.get(relationship.targetNodeId);if(!source||!target)continue;const record=flowRelationshipText(projection.graph,relationship),item=document.createElement("li"),button=document.createElement("button"),edge=document.createElementNS(graph.namespaceURI,"g") as SVGGElement,line=document.createElementNS(graph.namespaceURI,"line"),arrow=document.createElementNS(graph.namespaceURI,"polygon"),label=document.createElementNS(graph.namespaceURI,"text"),geometry=flowEdgeGeometry(source,target),selected=selectedItem?.kind==="relationship"&&relationship.id===selectedItem.id,select=()=>selectItem("relationship",relationship.id);button.type="button";button.textContent=record;button.setAttribute("aria-current",String(selected));button.addEventListener("click",select);item.dataset.relationshipId=relationship.id;item.classList.toggle("is-selected",selected);item.append(button);outline.append(item);edge.dataset.relationshipId=relationship.id;edge.dataset.directed="true";edge.classList.add("flow-edge");edge.classList.toggle("is-selected",selected);edge.tabIndex=0;edge.setAttribute("role","button");edge.setAttribute("aria-label",`Edit relationship ${record}`);edge.addEventListener("click",select);edge.addEventListener("keydown",(event)=>{if(event.key==="Enter"||event.key===" "){event.preventDefault();select();}});line.setAttribute("x1",String(geometry.startX));line.setAttribute("y1",String(geometry.startY));line.setAttribute("x2",String(geometry.endX));line.setAttribute("y2",String(geometry.endY));arrow.setAttribute("points",geometry.arrow);label.setAttribute("x",String((geometry.startX+geometry.endX)/2));label.setAttribute("y",String((geometry.startY+geometry.endY)/2-8));label.textContent=record;edge.append(line,arrow,label);graph.append(edge);}
    for(const row of flowOutline(projection.graph)){const nodeData=projection.graph.nodes.find(({id})=>id===row.nodeId)!,page=projection.catalog.pages.find(({id})=>id===nodeData.pageId),event=projection.catalog.events.find(({id})=>id===nodeData.eventId),position=positions.get(nodeData.id)!,record=`${row.name} · ${row.role} · ${page?.name??"Unresolved Page"} · ${event?.name??"Unresolved Event"} · ${row.obligation} · ${row.expectedMinimum}–${row.expectedMaximum??"many"}`,item=document.createElement("li"),button=document.createElement("button"),node=document.createElementNS(graph.namespaceURI,"g") as SVGGElement,box=document.createElementNS(graph.namespaceURI,"rect"),roleLabel=document.createElementNS(graph.namespaceURI,"text"),nameLabel=document.createElementNS(graph.namespaceURI,"text"),referenceLabel=document.createElementNS(graph.namespaceURI,"text");button.type="button";button.textContent=`${record} · ${position.lane}`;button.addEventListener("click",()=>focusOccurrence(nodeData.id));item.dataset.occurrenceId=nodeData.id;item.append(button);outline.insertBefore(item,outline.querySelector("[data-relationship-id]"));node.dataset.occurrenceId=nodeData.id;node.dataset.lane=position.lane;node.classList.add("flow-node");node.tabIndex=0;node.setAttribute("role","button");node.setAttribute("aria-label",`${record}. Lane ${position.lane}. Drag or use arrow keys to move. Press Enter or Space to edit.`);node.setAttribute("transform",`translate(${position.x} ${position.y})`);box.setAttribute("width",String(nodeWidth));box.setAttribute("height",String(nodeHeight));box.setAttribute("rx","10");roleLabel.classList.add("flow-node-role");roleLabel.setAttribute("x","12");roleLabel.setAttribute("y","19");roleLabel.textContent=row.role;nameLabel.setAttribute("x","12");nameLabel.setAttribute("y","43");nameLabel.textContent=row.name;referenceLabel.classList.add("flow-node-reference");referenceLabel.setAttribute("x","12");referenceLabel.setAttribute("y","65");referenceLabel.textContent=`${page?.name??"No Page"} · ${event?.name??"No Event"} · ${row.obligation}`;node.append(box,roleLabel,nameLabel,referenceLabel);
      node.addEventListener("keydown",(event)=>{if(event.key==="Enter"||event.key===" "){event.preventDefault();focusOccurrence(nodeData.id);return;}if(!event.key.startsWith("Arrow"))return;event.preventDefault();const currentLane=Math.max(0,lanes.findIndex(({name})=>name===position.lane));if(event.key==="ArrowLeft"||event.key==="ArrowRight"){const nextIndex=Math.max(0,Math.min(lanes.length-1,currentLane+(event.key==="ArrowLeft"?-1:1)));saveLayout(flow.id,nodeData.id,position,lanes[nextIndex]!.x,position.y);return;}const distance=event.shiftKey?60:20;saveLayout(flow.id,nodeData.id,position,lanes[currentLane]!.x,Math.max(55,position.y+(event.key==="ArrowUp"?-distance:distance)));});
      node.addEventListener("pointerdown",(event)=>{if(event.button!==0)return;event.preventDefault();node.focus();const origin=svgClientPoint(graph,event.clientX,event.clientY),pointerId=event.pointerId;let nextX=position.x,nextY=position.y,finished=false;try{node.setPointerCapture(pointerId);node.dataset.pointerCapture=node.hasPointerCapture(pointerId)?"native":"synthetic-unavailable";}catch{node.dataset.pointerCapture="synthetic-unavailable";}const move=(moveEvent:PointerEvent)=>{if(moveEvent.pointerId!==pointerId)return;const point=svgClientPoint(graph,moveEvent.clientX,moveEvent.clientY);nextX=Math.max(30,Math.min(630,position.x+point.x-origin.x));nextY=Math.max(55,position.y+point.y-origin.y);node.setAttribute("transform",`translate(${nextX} ${nextY})`);node.dataset.lane=laneForX(nextX).name;},cleanup=()=>{node.removeEventListener("pointermove",move);node.removeEventListener("pointerup",commit);node.removeEventListener("pointercancel",cancel);node.removeEventListener("lostpointercapture",cancel);window.removeEventListener("pointermove",move);window.removeEventListener("pointerup",commit);window.removeEventListener("pointercancel",cancel);if(node.hasPointerCapture(pointerId))node.releasePointerCapture(pointerId);},commit=(commitEvent:PointerEvent)=>{if(finished||commitEvent.pointerId!==pointerId)return;finished=true;cleanup();saveLayout(flow.id,nodeData.id,position,nextX,nextY);},cancel=(cancelEvent:PointerEvent)=>{if(finished||cancelEvent.pointerId!==pointerId)return;finished=true;cleanup();node.setAttribute("transform",`translate(${position.x} ${position.y})`);node.dataset.lane=position.lane;node.focus();};node.addEventListener("pointermove",move);node.addEventListener("pointerup",commit);node.addEventListener("pointercancel",cancel);node.addEventListener("lostpointercapture",cancel);window.addEventListener("pointermove",move);window.addEventListener("pointerup",commit);window.addEventListener("pointercancel",cancel);});graph.append(node);}
    const graphView=document.createElement("section"),outlineView=document.createElement("section"),graphHeading=document.createElement("h4"),outlineHeading=document.createElement("h4"),diagnostics=document.createElement("p");graphHeading.textContent="Interactive canvas";outlineHeading.textContent="Synchronized outline";diagnostics.textContent=projection.diagnostics.length?projection.diagnostics.map(({kind,message})=>`${kind}: ${message}`).join("; "):"No graph integrity diagnostics.";graphView.append(graphHeading,instructions,graph);outlineView.append(outlineHeading,outline);views.append(graphView,outlineView);section.append(heading,boundary,views,diagnostics);host.append(section);
  }

  function render(renderWorkspace=true):void{const list=q<HTMLOListElement>("#flow-step-list");list.replaceChildren();addForm.querySelector(".documentary-flow")?.remove();const {state,flow,steps}=current();if(!state||!flow)return;if(renderWorkspace)renderGraph(flow);steps.forEach((step,index)=>{const item=document.createElement("li"),form=document.createElement("form"),heading=document.createElement("h3"),name=document.createElement("input"),minimum=document.createElement("input"),maximum=document.createElement("input"),nodeRole=document.createElement("select"),nodeObligation=document.createElement("select"),lane=document.createElement("select"),page=document.createElement("select"),event=document.createElement("select"),save=document.createElement("button"),up=document.createElement("button"),down=document.createElement("button"),remove=document.createElement("button"),relationships=document.createElement("div"),position=step.position as {x?:number;y?:number}|undefined;heading.textContent=`${index+1}. ${step.name}`;name.value=step.name;name.dataset.editOccurrenceId=step.id;name.setAttribute("aria-label","Occurrence name");minimum.type=maximum.type="number";minimum.min=maximum.min="0";minimum.value=String(step.minimum??1);maximum.value=String(step.maximum??1);minimum.setAttribute("aria-label","Expected minimum");maximum.setAttribute("aria-label","Expected maximum");for(const[value,text]of[["interaction","Interaction"],["context-setting","Context-setting"]])nodeRole.append(new Option(text,value));nodeRole.value=String(step.fallbackRole??"interaction");nodeRole.setAttribute("aria-label","Role");for(const value of["Required","Optional","Conditional","Informational"])nodeObligation.append(new Option(value,value));nodeObligation.value=String(step.obligation??(step.optional?"Optional":"Required"));nodeObligation.setAttribute("aria-label","Obligation");replaceOptions(page,state.project.collections.pages,"Choose Page");replaceOptions(event,state.project.collections.events,"Choose Event");page.value=String(step.pageId??"");event.value=String(step.eventId??"");page.setAttribute("aria-label","Resolved Page");event.setAttribute("aria-label","Shared Event");for(const value of lanes.map(({name})=>name))lane.append(new Option(value,value));lane.value=String(step.lane??(step.fallbackRole==="context-setting"?"Context":"Shipping"));lane.setAttribute("aria-label","Canvas lane");save.type="submit";save.textContent="Save occurrence";up.type=down.type=remove.type="button";up.textContent="Move up";down.textContent="Move down";remove.textContent="Remove";up.disabled=index===0;down.disabled=index===steps.length-1;form.addEventListener("submit",(submitEvent)=>{submitEvent.preventDefault();const fresh=current().state;if(!fresh)return;const laneX=lanes.find(({name})=>name===lane.value)?.x??230;options.persist(updateGraphOccurrence(fresh,flow.id,step.id,occurrenceInput({name:name.value.trim(),pageId:page.value,eventId:event.value,fallbackRole:nodeRole.value,obligation:nodeObligation.value,minimum:Number(minimum.value),maximum:Number(maximum.value),lane:lane.value,x:laneX,y:position?.y??70+index*120})));queueMicrotask(()=>document.querySelector<HTMLElement>(`[data-edit-occurrence-id="${step.id}"]`)?.focus());});up.addEventListener("click",()=>{const fresh=current().state;if(fresh)options.persist(reorderGraphOccurrence(fresh,flow.id,index,index-1));});down.addEventListener("click",()=>{const fresh=current().state;if(fresh)options.persist(reorderGraphOccurrence(fresh,flow.id,index,index+1));});remove.addEventListener("click",()=>{const fresh=current().state;if(fresh)options.persist(removeGraphOccurrence(fresh,flow.id,step.id));});form.append(heading,name,page,event,nodeRole,nodeObligation,minimum,maximum,lane,save,up,down,remove);
      const appendRelationship=(relationship?:{id:string;toStepId:string;kind?:FlowRelationshipKind;group?:string;label?:string;documentationCondition?:string;expectation?:string})=>{const row=document.createElement("form"),target=document.createElement("select"),kind=document.createElement("select"),relationGroup=document.createElement("input"),label=document.createElement("input"),documentationCondition=document.createElement("input"),expectation=document.createElement("input"),commit=document.createElement("button");replaceOptions(target,steps.filter(({id})=>id!==step.id),"Choose target occurrence");target.value=relationship?.toStepId??"";target.setAttribute("aria-label",`Relationship target from ${step.name}`);for(const value of["expected-next","alternative","parallel","merge"] as const)kind.append(new Option(value,value));kind.value=relationship?.kind??"expected-next";kind.setAttribute("aria-label","Relationship kind");if(relationship)kind.dataset.editRelationshipId=relationship.id;relationGroup.value=relationship?.group??"";relationGroup.setAttribute("aria-label","Relationship group");label.value=relationship?.label??"";label.setAttribute("aria-label",`Relationship label from ${step.name}`);documentationCondition.value=relationship?.documentationCondition??"";documentationCondition.setAttribute("aria-label",`Documentation condition from ${step.name}`);documentationCondition.placeholder="Plain-language documentation condition";expectation.value=relationship?.expectation??"";expectation.setAttribute("aria-label",`Relationship expectation from ${step.name}`);commit.type="submit";commit.textContent=relationship?"Save relationship":"Add relationship";row.addEventListener("submit",(submitEvent)=>{submitEvent.preventDefault();const fresh=current().state;if(!fresh||!target.value)return;const relationshipId=relationship?.id??options.id("flow-relationship");options.persist(saveGraphRelationship(fresh,flow.id,step.id,{id:relationshipId,toStepId:target.value,kind:kind.value as FlowRelationshipKind,...(relationGroup.value.trim()?{group:relationGroup.value.trim()}:{}),...(label.value.trim()?{label:label.value.trim()}:{}),...(documentationCondition.value.trim()?{documentationCondition:documentationCondition.value.trim()}:{}),...(expectation.value.trim()?{expectation:expectation.value.trim()}:{})},options.id));focusRelationship(relationshipId);});row.append(kind,target,relationGroup,label,documentationCondition,expectation,commit);relationships.append(row);};for(const relationship of current().relationships.filter(({sourceNodeId})=>sourceNodeId===step.id))appendRelationship({...relationship,id:relationship.id,toStepId:String(relationship.targetNodeId??"")} as {id:string;toStepId:string;kind?:FlowRelationshipKind;group?:string;label?:string;documentationCondition?:string;expectation?:string});appendRelationship();item.append(form,relationships);list.append(item);});}

  function synchronizeRenderedOccurrenceControls():void{
    for(const item of Array.from(q<HTMLOListElement>("#flow-step-list").children)){
      const occurrenceForm=item.querySelector<HTMLFormElement>("form");if(!occurrenceForm)continue;
      const name=occurrenceForm.querySelector<HTMLInputElement>('[aria-label="Occurrence name"]')!,page=occurrenceForm.querySelector<HTMLSelectElement>('[aria-label="Resolved Page"]')!,event=occurrenceForm.querySelector<HTMLSelectElement>('select[aria-label="Shared Event"]')!,nodeRole=occurrenceForm.querySelector<HTMLSelectElement>('[aria-label="Role"]')!,lane=occurrenceForm.querySelector<HTMLSelectElement>('[aria-label="Canvas lane"]')!,step=current().steps.find(({id})=>id===name.dataset.editOccurrenceId);
      name.required=page.required=event.required=true;
      if(step?.pageGroupId){const {state,pageGroupIds}=current(),groups=pageGroupIds.map((groupId)=>state?.project.collections.pageGroups.find(({id})=>id===groupId)).filter((group):group is ProjectEntity=>Boolean(group));replaceOptions(lane,groups,"Choose Page Group");lane.value=String(step.pageGroupId);lane.setAttribute("aria-label","Page Group");nodeRole.hidden=true;const contextual=Boolean(step.contextBindingId),type=occurrenceForm.querySelector<HTMLOutputElement>('[aria-label="Occurrence type"]')??document.createElement("output");type.setAttribute("aria-label","Occurrence type");type.textContent=contextual?"Page context":"Interaction";if(!type.parentElement)occurrenceForm.insertBefore(type,nodeRole);nodeRole.value=contextual?"context-setting":"interaction";if(contextual){const selectedPage=state?.project.collections.pages.find(({id})=>id===step.pageId),bindings=(selectedPage?.contextEventBindings as ProjectEntity[]|undefined)??[];replaceOptions(event,bindings,"Choose context-event binding");event.value=String(step.contextBindingId);event.setAttribute("aria-label","Context-event binding");}else event.value=String(step.eventId??"");}
      else{syncRoleControl(event,nodeRole);event.addEventListener("change",()=>syncRoleControl(event,nodeRole));}
      if(step?.pageGroupId){lane.disabled=true;page.disabled=true;lane.title=page.title="Containment is fixed. Add the predefined Event from Components to another Page frame.";}
      for(const target of Array.from(item.querySelectorAll<HTMLSelectElement>('[aria-label^="Relationship target"]')))target.required=true;
    }
  }
  addForm.addEventListener("submit",(event)=>{event.preventDefault();const {state,flow,steps}=current();if(!state||!flow){q("#flow-step-result").textContent="Select a Flow before adding an Event occurrence.";return;}try{const projectedRole=synchronizedRole(addEvent.value,role.value).role,nodeObligation=obligation.value as FlowObligation,lane=projectedRole==="context-setting"?lanes[0]:lanes[1];options.persist(addGraphOccurrence(state,flow.id,occurrenceInput({name:addName.value.trim(),pageId:addPage.value,eventId:addEvent.value,fallbackRole:role.value,obligation:nodeObligation,minimum:Number(q<HTMLInputElement>("#flow-step-minimum").value),maximum:Number(q<HTMLInputElement>("#flow-step-maximum").value),lane:lane.name,x:lane.x,y:70+steps.length*120}),options.id));q("#flow-step-result").textContent="Event occurrence saved with stable Page and Event references.";addForm.reset();q<HTMLInputElement>("#flow-step-minimum").value="1";q<HTMLInputElement>("#flow-step-maximum").value="1";}catch(error){q("#flow-step-result").textContent=error instanceof Error?error.message:String(error);}});
  addForm.addEventListener("reset",()=>queueMicrotask(()=>{role.disabled=false;role.value="interaction";}));
  return{render:()=>{const active=Boolean(current().flow);documentaryEditor.hidden=!active;render();synchronizeRenderedOccurrenceControls();syncRoleControl(addEvent,role);renderFlowLaneControls();},renderSelectors};
}
