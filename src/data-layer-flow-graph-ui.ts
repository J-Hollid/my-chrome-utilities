import {
  addEventOccurrenceToPage,
  addGraphOccurrence,
  addInteractionOccurrenceToPage,
  deriveFlowOccurrenceExample,
  deriveFlowPageFrameExample,
  documentaryFlowGraph,
  duplicateFlowPageFrame,
  effectiveFlowPageFrameName,
  flowOccurrenceExampleEditorRows,
  FLOW_GRAPH_GEOMETRY,
  flowRelationshipText,
  inspectOccurrencePageChange,
  migrateLegacyFlowContextBindings,
  migrateLegacyFlowRelationshipKinds,
  moveGraphOccurrence,
  projectFlowGraph,
  reassignFlowOccurrencePage,
  reviewLegacyFlowContextMigration,
  removeFlowPageFrame,
  renameFlowPageFrame,
  resetFlowPageFrameName,
  removeFlowRelationship,
  removeGraphOccurrence,
  saveGraphRelationship,
  setFlowOccurrenceExample,
  type DocumentaryPageFrameRecord,
  type FlowPortSide,
} from "./data-layer-flow-graph.js";
import {type IdFactory,type ProjectEntity,type ProjectState} from "./utilities/data-layer/schemas.js";
import {appendFlowPageFrameCardControls} from "./data-layer-flow-graph-ui-page-frame.js";
import {addFlowPageFrameAndRelationship,addFlowPageFrameAtPosition,addFlowPageFrameToSection,connectFlowPageFrames,createFlowSection,createFlowSectionAroundFrames,inspectSectionRemovalWithContents,moveFlowPageFramePresentation,moveFlowSection,movePageFrameToSection,removeFlowSection,removeFlowSectionWithContents,renameAndResizeFlowSection,tidyFlowPageFrames,type FlowSectionBounds,type FlowSectionRemovalReview} from "./utilities/data-layer/property-set-flow-section.js";
import {button,elementByData,entityName,flowEdgeGeometry,flowPortPoint,nodeHeight,nodeWidth,ownsPointerDrag,q,restorePointerCancellationFocus,svg} from "./flow-graph/ui-primitives.js";
import type {FlowCamera} from "./flow-graph/workspace.js";
import {upgradeFlowWorkspace} from "./flow-graph/workspace-ui.js";
import {flowSelectionContains,primaryFlowSelection,selectionAfterActivation,selectionAfterRemoval,selectionFromStoredView,storedViewWithSelection,type FlowSelection,type StoredFlowSelectionView} from "./flow-graph/workspace-selection.js";
import type {FlowSectionCommand,FlowSectionRemovalImpact} from "./flow-graph/workspace-section-ui.js";

export interface FlowGraphBuilderContext {state?:ProjectState;flowId?:string;revision?:number}

export function contextSettingPageLabel(pageName:string):string{return `${pageName} · Context-setting Page`;}
export interface FlowGraphBuilderIntegration {render():void;renderSelectors():void}
interface IntegrationOptions {context:()=>FlowGraphBuilderContext;persist:(state:ProjectState)=>void;id:IdFactory;openOccurrenceSchema?:(occurrenceId:string,path?:string,originFocus?:HTMLElement)=>boolean}


function renderOccurrenceExampleControls(host:HTMLElement,state:ProjectState,flowId:string,occurrenceId:string,persist:(next:ProjectState)=>void,id:IdFactory):void{
  host.setAttribute("aria-label","Occurrence example controls");for(const row of flowOccurrenceExampleEditorRows(state.project,flowId,occurrenceId).filter(({type})=>type!=="object"&&type!=="array")){const item=document.createElement("label"),value=document.createElement("input"),save=document.createElement("button");item.dataset.exampleEditorPath=row.path;item.append(`${row.path} · ${row.type??"unknown"} `);value.setAttribute("aria-label",`Example value for ${row.path}`);value.value=row.value===undefined?"":String(row.value);save.type="button";save.textContent="Save example";save.addEventListener("click",()=>persist(setFlowOccurrenceExample(state,flowId,occurrenceId,row.path,value.value,id)));item.append(value,save);host.append(item);}
}

export {ownsPointerDrag,restorePointerCancellationFocus,flowEdgeGeometry};
type SessionFlowView=StoredFlowSelectionView&{viewport?:FlowCamera;minimap?:boolean};
export interface RelationshipDeletionFocusIntent{id:string;sourceKind:"page-frame";sourceId:string;sourceFocused:boolean}
export function flowViewAfterRelationshipDeletion(view:SessionFlowView,relationshipId:string):SessionFlowView{const selection=selectionFromStoredView(view);if(!selection.some(({kind,id})=>kind==="relationship"&&id===relationshipId))return view;if(!view.selectedItems){const{selectedItem,...retained}=view;void selectedItem;return retained;}return storedViewWithSelection(view,selectionAfterRemoval(selection,relationshipId));}
export function consumeRelationshipDeletionFocus(intent:RelationshipDeletionFocusIntent|undefined,relationshipRestored:boolean):{target?:"source"|"relationship";next?:RelationshipDeletionFocusIntent}{if(!intent)return{};if(relationshipRestored)return{target:"relationship"};if(!intent.sourceFocused)return{target:"source",next:{...intent,sourceFocused:true}};return{next:intent};}
export function flowRelationshipDeletionAccessibleName(label:string|undefined,sourceName:string,targetName:string):string{return`Delete relationship ${[label,`${sourceName} to ${targetName}`].filter(Boolean).join(", ")}`;}


export function installFlowGraphBuilder(options:IntegrationOptions):FlowGraphBuilderIntegration{
  const inspector=q<HTMLElement>("#project-inspector"),advanced=q<HTMLElement>("#flow-step-editor"),inspectorContext=document.createElement("section");
  inspectorContext.id="flow-inspector-context";inspector.insertBefore(inspectorContext,advanced);
  let selectedItems:FlowSelection[]=[];
  let selected:FlowSelection|undefined;
  let selectionWorkspace="";
  let connection:{sourceId:string;sourcePort?:FlowPortSide;targets:string[];targetIndex:number;preview?:SVGLineElement}|undefined;
  let relationshipPopoverFocusIntent:{id:string;revision:number;optimisticFocused:boolean}|undefined;
  let relationshipEdgeFocusIntent:{id:string;revision:number;optimisticFocused:boolean}|undefined;
  let relationshipDeletionFocusIntent:RelationshipDeletionFocusIntent|undefined;
  let relationshipDeletionFocusTimer:ReturnType<typeof setTimeout>|undefined;
  let pageFrameFocusIntent:{id:string;revision:number;optimisticFocused:boolean}|undefined;
  let suppressNodeClick=false;
  let statusMessage="";
  let statusRepairHref="";
  let activeCatalogPayload:{kind:string;id:string}|undefined;
  const viewKey=(projectId:string,flowId:string)=>`my-chrome-utilities.flow-view.v1:${projectId}:${flowId}`;
  const readView=(projectId:string,flowId:string):SessionFlowView=>{try{return JSON.parse(sessionStorage.getItem(viewKey(projectId,flowId))??"{}");}catch{return{};}};
  const writeView=(projectId:string,flowId:string,view:SessionFlowView):void=>sessionStorage.setItem(viewKey(projectId,flowId),JSON.stringify(view));
  const clearPageDropStates=()=>document.querySelectorAll<HTMLElement>("[data-section-dropzone]").forEach((section)=>delete section.dataset.pageDropState);
  const clearActiveCatalogPayload=()=>{activeCatalogPayload=undefined;clearPageDropStates();};
  window.addEventListener("pointerup",clearActiveCatalogPayload);window.addEventListener("mouseup",clearActiveCatalogPayload);
  const workspaceContent=q<HTMLElement>("#workspace-content"),upgradeWorkspace=()=>queueMicrotask(()=>{const flowRoot=document.querySelector<HTMLElement>("#flow-graph-workspace");if(flowRoot)upgradeFlowWorkspace(flowRoot);});
  new MutationObserver(upgradeWorkspace).observe(workspaceContent,{childList:true,subtree:true});
  workspaceContent.addEventListener("flow-tidy-confirm",(event)=>{const detail=(event as CustomEvent<{placements?:{id:string;position:{x:number;y:number}}[]}>).detail,{state,flow}=current();if(state&&flow&&detail.placements)persist(tidyFlowPageFrames(state,flow.id,detail.placements),"Tidied Flow presentation; Undo available.");});
  workspaceContent.addEventListener("flow-empty-connection-page",(event)=>{const detail=(event as CustomEvent<{pageId?:string;sourceId?:string;sourcePort?:FlowPortSide;targetPort?:FlowPortSide;position?:{x:number;y:number}}>).detail,{state,flow}=current();if(!state||!flow||!detail.pageId||!detail.sourceId||!detail.sourcePort||!detail.targetPort||!detail.position)return;persist(addFlowPageFrameAndRelationship(state,flow.id,{pageId:detail.pageId,sourceId:detail.sourceId,sourcePort:detail.sourcePort,targetPort:detail.targetPort,position:detail.position},options.id),"Created Page instance and relationship in one Flow command; Undo removes both.");});
  workspaceContent.addEventListener("flow-add-page-at",(event)=>{
    const detail=(event as CustomEvent<{pageId?:string;position?:{x:number;y:number}}>).detail,{state,flow,graph}=current();
    if(!state||!flow||!graph||!detail.pageId)return;
    const position=detail.position??{x:80,y:80},sectionId=graph.sections.find((section)=>{const bounds=section.bounds as unknown as FlowSectionBounds;return position.x>=bounds.x&&position.x<=bounds.x+bounds.width&&position.y>=bounds.y&&position.y<=bounds.y+bounds.height;})?.id;
    persist(addFlowPageFrameAtPosition(state,flow.id,detail.pageId,position,sectionId,options.id));
  });
  workspaceContent.addEventListener("flow-section-impact-request",(event)=>{
    const detail=(event as CustomEvent<{sectionId?:string;respond?:(review:FlowSectionRemovalImpact)=>void}>).detail,{state,flow}=current();
    if(!state||!flow||!detail.sectionId||!detail.respond)return;
    detail.respond(inspectSectionRemovalWithContents(state.project,flow.id,detail.sectionId));
  });
  workspaceContent.addEventListener("flow-section-command",(event)=>{
    const detail=(event as CustomEvent<FlowSectionCommand>).detail,{state,flow,graph}=current();
    if(!state||!flow||!graph||!detail)return;
    if(detail.kind==="select"){saveSelection({kind:"section",id:detail.sectionId});return;}
    if(detail.kind==="create"){persist(detail.frameIds.length?createFlowSectionAroundFrames(state,flow.id,{name:detail.name,bounds:detail.bounds,frameIds:detail.frameIds},options.id):createFlowSection(state,flow.id,{name:detail.name,bounds:detail.bounds},options.id));return;}
    const section=graph.sections.find(({id})=>id===detail.sectionId);if(!section)return;
    if(detail.kind==="move"){persist(moveFlowSection(state,flow.id,section.id,detail.position));return;}
    if(detail.kind==="resize"){persist(renameAndResizeFlowSection(state,flow.id,section.id,{name:section.name,bounds:detail.bounds}));return;}
    if(detail.kind==="rename"){persist(renameAndResizeFlowSection(state,flow.id,section.id,{name:detail.name,bounds:section.bounds as unknown as FlowSectionBounds}));return;}
    if(detail.kind==="remove"){persist(removeFlowSection(state,flow.id,section.id),`Removed Section ${section.name}; Page instances and relationships retained. Undo available.`);return;}
    const review=detail.review as unknown as FlowSectionRemovalReview;persist(removeFlowSectionWithContents(state,flow.id,section.id,review),`Removed Section ${section.name} with ${review.pageFrames.length} Page instances, ${review.occurrences.length} occurrences, and ${review.relationships.length} relationships. Undo available.`);
  });

  const current=()=>{const context=options.context(),flow=context.flowId&&context.state?.project.collections.flows.find(({id})=>id===context.flowId),graph=flow&&context.state?documentaryFlowGraph(context.state.project,flow.id):undefined;return{...context,flow,graph};};
  const persist=(next:ProjectState,feedback=""):void=>{try{statusMessage=feedback;statusRepairHref="";options.persist(next);render();}catch(error){statusMessage=error instanceof Error?error.message:String(error);render();}};
  const pageFrame=(frameId:string):DocumentaryPageFrameRecord|undefined=>current().graph?.pageFrames.find(({id})=>id===frameId);
  const selectedFrameForPage=(pageId:string):DocumentaryPageFrameRecord|undefined=>current().graph?.pageFrames.find((frame)=>frame.pageId===pageId);
  const saveSelection=(value:FlowSelection|undefined,extend=false):void=>{selectedItems=value?selectionAfterActivation(selectedItems,value,extend):[];selected=primaryFlowSelection(selectedItems);const {state,flow}=current();if(state&&flow)writeView(state.project.id,flow.id,storedViewWithSelection(readView(state.project.id,flow.id),selectedItems));render();};

  function renderInspector():void{
    inspectorContext.replaceChildren();const {state,flow,graph}=current();if(!state||!flow){inspectorContext.hidden=true;return;}inspectorContext.hidden=false;const heading=document.createElement("h3"),copy=document.createElement("p");heading.textContent="Flow details";
    if(!selected){copy.textContent=`${flow.name}. Select a Section, Page frame, occurrence, or relationship for provenance and details. All graph commands remain in the main workspace.`;inspectorContext.append(heading,copy);return;}
    const occurrence=graph?.occurrences.find(({id})=>id===selected!.id),relationship=graph?.relationships.find(({id})=>id===selected!.id),frame=graph?.pageFrames.find(({id})=>id===selected!.id),section=graph?.sections.find(({id})=>id===selected!.id);copy.textContent=occurrence?`${occurrence.name} · stable occurrence ${occurrence.id}`:relationship?`Stable relationship ${relationship.id}`:frame?`${effectiveFlowPageFrameName(state.project,frame)} · stable Page frame ${frame.id}`:section?`${section.name} · stable Flow Section ${section.id}`:"Selection details unavailable";inspectorContext.append(heading,copy);
  }

  function catalog(kind:"Pages"|"Events",entities:readonly ProjectEntity[],activate:(entity:ProjectEntity)=>void):HTMLElement{
    const section=document.createElement("section"),heading=document.createElement("h4"),search=document.createElement("input"),items=document.createElement("div");section.setAttribute("aria-label",`${kind} catalog`);heading.textContent=kind;search.type="search";search.placeholder=`Search ${kind}`;search.setAttribute("aria-label",`Search ${kind}`);
    const renderItems=()=>{const term=search.value.trim().toLowerCase();items.replaceChildren(...entities.filter(({name})=>name.toLowerCase().includes(term)).map((entity)=>{const control=button(`Add ${entity.name}`,()=>activate(entity));control.draggable=true;control.dataset.componentKind=kind==="Pages"?"page":"event";control.dataset.componentId=entity.id;const payload=()=>({kind:String(control.dataset.componentKind),id:entity.id});control.addEventListener("pointerdown",()=>{activeCatalogPayload=payload();});control.addEventListener("dragstart",(event)=>{activeCatalogPayload=payload();event.dataTransfer?.setData("application/x-flow-component",JSON.stringify(activeCatalogPayload));if(kind==="Pages")event.dataTransfer?.setData("application/x-flow-page-component",entity.id);});control.addEventListener("dragend",clearActiveCatalogPayload);return control;}));};
    search.addEventListener("input",renderItems);renderItems();heading.append(button(kind,()=>search.focus()));section.append(heading,search,items);return section;
  }

  function insertPage(page:ProjectEntity,targetSectionId?:string):void{const {state,flow}=current();if(!state||!flow)return;persist(addFlowPageFrameToSection(state,flow.id,page.id,targetSectionId,options.id));}
  function insertEvent(event:ProjectEntity,frameId?:string):void{const {state,flow,graph}=current(),selectedFrameId=frameId??(selected&&selected.kind==="page-frame"?selected.id:undefined),frame=selectedFrameId?pageFrame(selectedFrameId):undefined;if(!state||!flow||!graph||!frame){statusMessage="Select a Page frame before inserting an Event.";render();return;}const trigger=typeof event.trigger==="string"&&event.trigger.trim()?event.trigger.trim():undefined,count=graph.occurrences.filter((occurrence)=>occurrence.pageFrameId===frame.id).length;persist(addEventOccurrenceToPage(state,flow.id,{name:event.name,pageFrameId:frame.id,pageId:frame.pageId,eventId:event.id,...(trigger?{trigger}:{}),obligation:"Required",minimum:1,maximum:1,x:24+count*210,y:70},options.id));}

  function occurrenceExampleDetails(state:ProjectState,flowId:string,occurrenceId:string,label:string):HTMLDetailsElement{
    const example=deriveFlowOccurrenceExample(state.project,flowId,occurrenceId),details=document.createElement("details"),summary=document.createElement("summary"),pre=document.createElement("pre"),provenance=document.createElement("ul"),issues=document.createElement("ul"),exampleControls=document.createElement("section");
    renderOccurrenceExampleControls(exampleControls,state,flowId,occurrenceId,persist,options.id);details.dataset.eventExampleFor=occurrenceId;details.dataset.exampleStatus=example.status;summary.textContent=`${label} · ${example.status} · Derived JSON example`;pre.dataset.readonlyExample=occurrenceId;pre.textContent=example.formattedJson;
    for(const[path,source]of Object.entries(example.provenance)){const item=document.createElement("li");item.dataset.examplePath=path;item.dataset.exampleSource=source;item.textContent=`${path} · ${source}`;provenance.append(item);}
    for(const issue of example.issues){const item=document.createElement("li"),repair=document.createElement("a"),value=document.createElement("input"),save=button("Save example",()=>persist(setFlowOccurrenceExample(current().state!,flowId,occurrenceId,issue.path,value.value,options.id)));item.dataset.exampleIssuePath=issue.path;item.dataset.exampleIssueCode=issue.code;repair.href=issue.editHref;repair.textContent="Edit examples";repair.addEventListener("click",(event)=>{if(options.openOccurrenceSchema?.(occurrenceId,issue.path)){event.preventDefault();}});value.setAttribute("aria-label",`Example value for ${issue.path}`);item.append(`${issue.path} · ${issue.message} `,repair," ",value,save);issues.append(item);}
    details.append(summary,pre,provenance,exampleControls,issues);return details;
  }
  function pageExampleDetails(state:ProjectState,flowId:string,frameId:string,label:string):HTMLDetailsElement{
    const example=deriveFlowPageFrameExample(state.project,flowId,frameId),details=document.createElement("details"),summary=document.createElement("summary"),pre=document.createElement("pre"),provenance=document.createElement("ul"),issues=document.createElement("ul");details.dataset.pageExampleFor=frameId;details.dataset.exampleStatus=example.status;summary.textContent=`${label} page event · ${example.status} · Derived JSON example`;pre.dataset.readonlyPageExample=frameId;pre.textContent=example.formattedJson;
    for(const[path,source]of Object.entries(example.provenance)){const item=document.createElement("li");item.dataset.examplePath=path;item.dataset.exampleSource=source;item.textContent=`${path} · ${source}`;provenance.append(item);}
    for(const issue of example.issues){const item=document.createElement("li"),repair=document.createElement("a");item.dataset.exampleIssuePath=issue.path;item.dataset.exampleIssueCode=issue.code;repair.href=issue.editHref;repair.textContent="Open Page-frame contribution";repair.addEventListener("click",(event)=>{const originFocus=document.activeElement instanceof HTMLElement?document.activeElement:undefined;const opened=options.openOccurrenceSchema?.(frameId,issue.path,originFocus);saveSelection({kind:"page-frame",id:frameId});if(opened)event.preventDefault();});item.append(`${issue.path} · ${issue.message} `,repair);issues.append(item);}
    details.append(summary,pre,provenance,issues);return details;
  }

  function renderSectionControls(host:HTMLElement):void{
    const {state,flow,graph}=current();if(!state||!flow||!graph)return;const heading=document.createElement("h4"),form=document.createElement("form"),name=document.createElement("input"),list=document.createElement("ol"),add=button("Add Section",()=>{});heading.textContent="Flow Sections";host.dataset.flowSectionWorkspace=flow.id;host.dataset.flowProjectId=state.project.id;host.setAttribute("aria-label","Flow Section controls");name.setAttribute("aria-label","New Section name");form.addEventListener("submit",(event)=>{event.preventDefault();persist(createFlowSection(current().state!,flow.id,{name:name.value,bounds:{x:40+graph.sections.length*360,y:40,width:320,height:220}},options.id));});add.type="submit";form.append(name,add);host.append(heading,form);
    for(const section of graph.sections){const item=document.createElement("li"),sectionName=document.createElement("input"),review=document.createElement("section"),fields=Object.fromEntries((["x","y","width","height"] as const).map((key)=>{const input=document.createElement("input");input.type="number";input.value=String((section.bounds as unknown as FlowSectionBounds)[key]);input.setAttribute("aria-label",`Section ${key} ${section.name}`);return[key,input];})) as Record<keyof FlowSectionBounds,HTMLInputElement>,save=button("Save name and size",()=>persist(renameAndResizeFlowSection(current().state!,flow.id,section.id,{name:sectionName.value,bounds:{x:Number(fields.x.value),y:Number(fields.y.value),width:Number(fields.width.value),height:Number(fields.height.value)}}))),move=button("Move Section",()=>persist(moveFlowSection(current().state!,flow.id,section.id,{x:Number(fields.x.value),y:Number(fields.y.value)}))),remove=button("Remove Section and retain Page frames",()=>persist(removeFlowSection(current().state!,flow.id,section.id))),removeWithContents=button("Review remove Section with contents",()=>{const impact=inspectSectionRemovalWithContents(current().state!.project,flow.id,section.id),summary=document.createElement("p"),confirmRemoval=button("Confirm remove Section with contents",()=>persist(removeFlowSectionWithContents(current().state!,flow.id,section.id,impact))),cancel=button("Cancel",()=>{review.hidden=true;review.replaceChildren();removeWithContents.focus();});summary.textContent=`Remove Page frames: ${impact.pageFrames.map(({name})=>name).join(", ")||"none"}. Remove relationships: ${impact.relationships.map(({name})=>name).join(", ")||"none"}. Nothing changes until confirmed.`;review.replaceChildren(summary,confirmRemoval,cancel);review.hidden=false;confirmRemoval.focus();});item.dataset.flowSectionId=section.id;sectionName.value=section.name;sectionName.setAttribute("aria-label",`Section name ${section.name}`);review.hidden=true;review.setAttribute("aria-label",`Remove ${section.name} with contents review`);item.append(sectionName,fields.x,fields.y,fields.width,fields.height,save,move,remove,removeWithContents,review);list.append(item);}
    host.append(list);const placement=document.createElement("section"),frameChoice=document.createElement("select"),sectionChoice=document.createElement("select"),place=button("Place in Section",()=>{if(frameChoice.value&&sectionChoice.value)persist(movePageFrameToSection(current().state!,flow.id,frameChoice.value,sectionChoice.value));}),outside=button("Move outside every Section",()=>{if(frameChoice.value)persist(movePageFrameToSection(current().state!,flow.id,frameChoice.value));}),connectionControls=document.createElement("section"),source=document.createElement("select"),target=document.createElement("select"),connect=button("Connect Page frames",()=>{if(source.value&&target.value)persist(connectFlowPageFrames(current().state!,flow.id,source.value,target.value,options.id));});frameChoice.setAttribute("aria-label","Page frame to organize");sectionChoice.setAttribute("aria-label","Destination Section");source.setAttribute("aria-label","Relationship source Page frame");target.setAttribute("aria-label","Relationship target Page frame");frameChoice.append(new Option("Choose Page frame",""));source.append(new Option("Choose source",""));target.append(new Option("Choose target",""));for(const frame of graph.pageFrames){const label=effectiveFlowPageFrameName(state.project,frame);frameChoice.append(new Option(label,frame.id));source.append(new Option(label,frame.id));target.append(new Option(label,frame.id));}sectionChoice.append(new Option("Choose Section",""));for(const section of graph.sections)sectionChoice.append(new Option(section.name,section.id));placement.append(frameChoice,sectionChoice,place,outside);connectionControls.append(source,target,connect);host.append(placement,connectionControls);
  }

  function dropPayload(event:DragEvent):{kind:string;id:string}|undefined{const raw=event.dataTransfer?.getData("application/x-flow-component");if(!raw)return activeCatalogPayload;try{return JSON.parse(raw) as {kind:string;id:string};}catch{return activeCatalogPayload;}}
  function renderFrameCards(host:HTMLElement):void{
    const {state,flow,graph}=current();if(!state||!flow||!graph)return;const heading=document.createElement("h4");heading.textContent="Page frames";host.setAttribute("aria-label","Flow Page frames");host.append(heading);
    for(const frame of graph.pageFrames){const effectiveName=effectiveFlowPageFrameName(state.project,frame),section=graph.sections.find(({id})=>id===frame.sectionId),card=document.createElement("article"),title=button(`${section?.name??"Outside Sections"} / ${contextSettingPageLabel(effectiveName)}`,()=>saveSelection({kind:"page-frame",id:frame.id})),move=(dx:number,dy:number)=>{sessionStorage.setItem(`my-chrome-utilities.flow-focus.v1:${state.project.id}:${flow.id}`,frame.id);persist(moveFlowPageFramePresentation(current().state!,flow.id,frame.id,{x:Number(frame.position.x??0)+dx,y:frame.position.y+dy,sectionId:frame.sectionId??null}));};card.dataset.pageFrameId=frame.id;card.dataset.pageId=frame.pageId;if(frame.sectionId)card.dataset.flowSectionId=frame.sectionId;card.tabIndex=0;card.setAttribute("aria-label",`Page frame ${effectiveName}`);card.addEventListener("click",(event)=>{if(event.target===card)saveSelection({kind:"page-frame",id:frame.id});});card.addEventListener("keydown",(event)=>{if(!event.key.startsWith("Arrow"))return;event.preventDefault();move(event.key==="ArrowLeft"?-20:event.key==="ArrowRight"?20:0,event.key==="ArrowUp"?-20:event.key==="ArrowDown"?20:0);});let drag:{pointerId:number;x:number;y:number}|undefined;card.addEventListener("pointerdown",(event)=>{drag={pointerId:event.pointerId,x:event.clientX,y:event.clientY};});card.addEventListener("pointerup",(event)=>{if(!drag||drag.pointerId!==event.pointerId)return;const origin=drag;drag=undefined;move(event.clientX-origin.x,event.clientY-origin.y);});card.addEventListener("dragover",(event)=>event.preventDefault());card.addEventListener("drop",(event)=>{event.preventDefault();const payload=dropPayload(event);if(payload?.kind==="event"){const entity=current().state?.project.collections.events.find(({id})=>id===payload.id);if(entity)insertEvent(entity,frame.id);}else if(payload?.kind==="page"){statusMessage="A Page frame cannot contain another Page.";render();}});appendFlowPageFrameCardControls({card,title,state,flow,graph,frame,entityName,pageExampleDetails,saveSelection:(value)=>saveSelection(value),...(options.openOccurrenceSchema?{openOccurrenceSchema:options.openOccurrenceSchema}:{}),persist:(next)=>persist(next),duplicatePageFrame:(next,flowId,frameId)=>duplicateFlowPageFrame(next,flowId,frameId,options.id),removePageFrame:(next,flowId,frameId)=>removeFlowPageFrame(next,flowId,frameId),renamePageFrame:renameFlowPageFrame,resetPageFrameName:resetFlowPageFrameName});host.append(card);}
  }

  function cancelConnection(announce=true,suppressClick=false):void{const sourceId=connection?.sourceId,sourcePort=connection?.sourcePort;connection?.preview?.remove();connection=undefined;document.querySelector(".flow-canvas-scroll")?.classList.remove("is-connecting");document.querySelectorAll(".is-valid-target,.is-invalid-target").forEach((element)=>element.classList.remove("is-valid-target","is-invalid-target"));if(announce)statusMessage="Connection cancelled; canonical state was not changed.";if(suppressClick){suppressNodeClick=true;setTimeout(()=>{suppressNodeClick=false;},0);}document.querySelector<HTMLElement>(`[data-flow-port-for="${CSS.escape(sourceId??"")}"][data-flow-port-side="${sourcePort??"right"}"]`)?.focus();}
  function clearSelectedRelationshipForConnection():void{if(selected?.kind!=="relationship")return;selectedItems=selectionAfterRemoval(selectedItems,selected.id);selected=primaryFlowSelection(selectedItems);relationshipPopoverFocusIntent=undefined;relationshipEdgeFocusIntent=undefined;document.querySelector<HTMLElement>('[aria-label="Inline relationship popover"]')?.remove();}
  function commitConnection(targetId:string|undefined,targetPort?:FlowPortSide):void{const {state,flow,graph,revision}=current(),sourceId=connection?.sourceId,sourcePort=connection?.sourcePort??"right",inferredTargetPort=targetPort??(sourcePort==="right"?"left":sourcePort==="top"?"bottom":sourcePort==="bottom"?"top":undefined);if(!state||!flow||!graph||!sourceId||!targetId||!inferredTargetPort||sourceId===targetId){cancelConnection(true,true);return;}const before=new Set(graph.relationships.map(({id})=>id)),next=saveGraphRelationship(state,flow.id,sourceId,{toStepId:targetId,sourcePort,targetPort:inferredTargetPort},options.id),created=documentaryFlowGraph(next.project,flow.id).relationships.find(({id})=>!before.has(id));if(!created){cancelConnection(true,true);return;}connection=undefined;document.querySelector(".flow-canvas-scroll")?.classList.remove("is-connecting");selectedItems=[{kind:"relationship",id:created.id}];selected=primaryFlowSelection(selectedItems);writeView(state.project.id,flow.id,storedViewWithSelection(readView(state.project.id,flow.id),selectedItems));relationshipPopoverFocusIntent={id:created.id,revision:Number(revision??0),optimisticFocused:false};persist(next);}

  function renderRelationshipPopover(host:HTMLElement):void{
    if(selected?.kind!=="relationship")return;
    const {state,flow,revision}=current();if(!state||!flow)return;
    const projection=projectFlowGraph(state.project,flow.id).graph,relationship=projection.relationships.find(({id})=>id===selected!.id);if(!relationship)return;
    const sourceId=relationship.sourceEndpoint.id,targetId=relationship.targetEndpoint.id,source=projection.connectionEndpoints.find(({id})=>id===sourceId),target=projection.connectionEndpoints.find(({id})=>id===targetId),form=document.createElement("form"),heading=document.createElement("h4"),endpoints=document.createElement("p"),inferredKind=document.createElement("p"),group=document.createElement("input"),label=document.createElement("input"),condition=document.createElement("textarea"),expectation=document.createElement("textarea"),save=document.createElement("button"),cancel=document.createElement("button"),remove=document.createElement("button"),relationshipName=[relationship.label,`${source?.name??sourceId} to ${target?.name??targetId}`].filter(Boolean).join(", ");
    form.dataset.relationshipPopover=relationship.id;form.setAttribute("aria-label","Inline relationship popover");heading.textContent="Relationship details";endpoints.dataset.relationshipEndpoints=relationship.id;endpoints.textContent=`${source?.name??sourceId} ${relationship.sourcePort} → ${target?.name??targetId} ${relationship.targetPort}`;inferredKind.dataset.inferredRelationshipKind=relationship.kind;inferredKind.textContent=`Inferred kind: ${relationship.kind}`;group.value=relationship.group??"";group.setAttribute("aria-label","Relationship group");label.value=relationship.label??"";label.setAttribute("aria-label","Optional relationship label");condition.value=relationship.documentationCondition??"";condition.setAttribute("aria-label","Documentation condition");expectation.value=relationship.expectation??"";expectation.setAttribute("aria-label","Relationship expectation");save.type="submit";save.textContent="Save relationship";cancel.type="button";cancel.textContent="Cancel";
    remove.type="button";remove.textContent="Delete relationship";remove.setAttribute("aria-label",flowRelationshipDeletionAccessibleName(relationship.label,source?.name??sourceId,target?.name??targetId));remove.addEventListener("click",()=>{relationshipPopoverFocusIntent=undefined;relationshipEdgeFocusIntent=undefined;relationshipDeletionFocusIntent={id:relationship.id,sourceKind:relationship.sourceEndpoint.kind,sourceId,sourceFocused:false};selectedItems=selectionAfterRemoval(selectedItems,relationship.id);selected=primaryFlowSelection(selectedItems);const context=current(),view=readView(context.state!.project.id,flow.id);writeView(context.state!.project.id,flow.id,flowViewAfterRelationshipDeletion(view,relationship.id));persist(removeFlowRelationship(context.state!,flow.id,relationship.id),`Deleted relationship ${relationshipName}. Saved Draft; documentation preview stale; Undo available.`);});
    cancel.addEventListener("click",()=>{relationshipPopoverFocusIntent=undefined;relationshipEdgeFocusIntent=undefined;selectedItems=[];selected=undefined;render();document.querySelector<HTMLElement>(`[data-flow-port-for="${CSS.escape(sourceId)}"][data-flow-port-side="${relationship.sourcePort}"]`)?.focus();});
    form.addEventListener("keydown",(event)=>{if(event.key!=="Escape")return;event.preventDefault();relationshipPopoverFocusIntent=undefined;relationshipEdgeFocusIntent=undefined;document.querySelector<HTMLElement>(`[data-relationship-id="${CSS.escape(relationship.id)}"]`)?.focus();});
    form.addEventListener("submit",(event)=>{event.preventDefault();relationshipPopoverFocusIntent=undefined;relationshipEdgeFocusIntent={id:relationship.id,revision:Number(revision??0),optimisticFocused:false};persist(saveGraphRelationship(current().state!,flow.id,sourceId,{id:relationship.id,toStepId:targetId,sourcePort:relationship.sourcePort,targetPort:relationship.targetPort,group:group.value.trim(),label:label.value.trim(),documentationCondition:condition.value.trim(),expectation:expectation.value.trim()},options.id));queueMicrotask(()=>document.querySelector<HTMLElement>(`[data-relationship-id="${CSS.escape(relationship.id)}"]`)?.focus());});
    const labeled=(text:string,control:HTMLElement)=>{const wrapper=document.createElement("label");wrapper.append(text,control);return wrapper;};form.append(heading,endpoints,inferredKind,labeled("Group",group),labeled("Optional label",label),labeled("Condition",condition),labeled("Expectation",expectation),save,cancel,remove);host.append(form);
    const intent=relationshipPopoverFocusIntent;if(intent?.id===relationship.id){const renderRevision=Number(revision??0),replacement=intent.optimisticFocused&&renderRevision>intent.revision;if(!intent.optimisticFocused){intent.optimisticFocused=true;intent.revision=renderRevision;}queueMicrotask(()=>{if(!label.isConnected)return;label.focus();if(replacement&&relationshipPopoverFocusIntent===intent)relationshipPopoverFocusIntent=undefined;});}
  }

  function renderActions(host:HTMLElement):void{
    const {state,flow,graph}=current();if(!state||!flow||!graph)return;if(selected?.kind==="page-frame"){const frame=graph.pageFrames.find(({id})=>id===selected!.id);if(!frame)return;const actions=document.createElement("section"),openSchema=button("Open schema contribution",()=>options.openOccurrenceSchema?.(frame.id,undefined,openSchema));openSchema.dataset.flowSchemaContribution="true";actions.setAttribute("aria-label","Selected Page instance inline actions");actions.append(button("Move",()=>document.querySelector<HTMLElement>(`[data-page-frame-id="${CSS.escape(frame.id)}"]`)?.focus()),button("Connect",()=>document.querySelector<HTMLElement>(`[data-output-port-for="${CSS.escape(frame.id)}"]`)?.focus()),openSchema,button("Remove",()=>persist(removeFlowPageFrame(current().state!,flow.id,frame.id))));host.append(actions);return;}if(selected?.kind==="relationship"){const actions=document.createElement("section");actions.setAttribute("aria-label","Selected relationship inline actions");host.append(actions);return;}if(selected?.kind!=="occurrence")return;const occurrence=graph.occurrences.find(({id})=>id===selected!.id),node=projectFlowGraph(state.project,flow.id).graph.nodes.find(({id})=>id===selected!.id);if(!occurrence||!node)return;const actions=document.createElement("section"),migration=reviewLegacyFlowContextMigration(state.project,flow.id),pageChoice=document.createElement("select"),impact=document.createElement("output"),confirmPage=button("Confirm Page change",()=>persist(reassignFlowOccurrencePage(current().state!,flow.id,occurrence.id,pageChoice.value)));actions.setAttribute("aria-label","Selected Event occurrence inline actions");pageChoice.setAttribute("aria-label","Containing Page frame");pageChoice.append(new Option("Choose containing Page",""));for(const frame of graph.pageFrames.filter(({id})=>id!==occurrence.pageFrameId)){const page=state.project.collections.pages.find(({id})=>id===frame.pageId);pageChoice.append(new Option(`${page?.name??frame.pageId} · ${frame.id}`,frame.id));}confirmPage.disabled=true;pageChoice.addEventListener("change",()=>{const review=inspectOccurrencePageChange(state.project,flow.id,occurrence.id,pageChoice.value);impact.textContent=review.message;confirmPage.disabled=review.rejected;});const duplicate=()=>{const next=addGraphOccurrence(current().state!,flow.id,{name:`${occurrence.name} copy`,...(occurrence.pageFrameId?{pageFrameId:String(occurrence.pageFrameId)}:{}),...(occurrence.pageGroupId?{pageGroupId:String(occurrence.pageGroupId)}:{}),...(occurrence.freePageFrameId?{freePageFrameId:String(occurrence.freePageFrameId)}:{}),pageId:String(occurrence.pageId),eventId:node.eventId,...(node.trigger?{trigger:node.trigger}:{}),obligation:String(occurrence.obligation??"Required") as "Required",minimum:Number(occurrence.minimum??1),maximum:Number(occurrence.maximum??1),y:Number((occurrence.position as {y?:number}|undefined)?.y??70)+24},options.id);persist(next);},duplicateButton=button("Duplicate occurrence",duplicate);duplicateButton.disabled=Boolean(migration.items.length||migration.blockers.length);if(duplicateButton.disabled)duplicateButton.title="Confirm the Page-context migration before changing this graph.";const openSchema=()=>{document.querySelector<HTMLElement>(`[data-occurrence-id="${CSS.escape(occurrence.id)}"]`)?.dispatchEvent(new MouseEvent("click",{bubbles:true}));queueMicrotask(()=>{const open=Array.from(document.querySelectorAll<HTMLButtonElement>('[aria-label="Schema constraints summary"] button')).find(({textContent})=>textContent?.includes("Open complete schema editor"));open?.click();});};actions.append(button("Move within Page",()=>document.querySelector<HTMLElement>(`[data-occurrence-id="${CSS.escape(occurrence.id)}"]`)?.focus()),pageChoice,impact,confirmPage,duplicateButton,button("Remove",()=>persist(removeGraphOccurrence(current().state!,flow.id,occurrence.id))),button("Open schema contribution",openSchema));host.append(actions);
  }

  function renderGraph(flow:ProjectEntity):void{
    const preMigrationState=current().state;if(preMigrationState){const migrated=migrateLegacyFlowRelationshipKinds(preMigrationState,flow.id);if(migrated!==preMigrationState){persist(migrated);return;}}
    const host=q<HTMLElement>("#flow-graph-workspace");host.replaceChildren();const {state,graph:stored}=current();if(!state||!stored)return;const migration=reviewLegacyFlowContextMigration(state.project,flow.id);if(migration.items.length||migration.blockers.length){const review=document.createElement("section"),heading=document.createElement("h3"),list=document.createElement("ul"),confirm=button("Confirm Page-context migration",()=>persist(migrateLegacyFlowContextBindings(current().state!,flow.id)));review.setAttribute("aria-label","Flow Page-context migration review");heading.textContent="Review Page-context migration";for(const item of migration.items){const row=document.createElement("li");row.textContent=`${item.flowName} / ${item.pageName} / ${item.eventName} · ${item.trigger} · occurrence ${item.occurrenceName}`;list.append(row);}for(const blocker of migration.blockers){const row=document.createElement("li");row.textContent=blocker.message;list.append(row);}confirm.disabled=Boolean(migration.blockers.length)||!migration.items.length;review.append(heading,list,confirm);host.append(review);}const transientView=readView(state.project.id,flow.id),workspaceKey=`${state.project.id}\u0000${flow.id}`,selectionExists=(item:FlowSelection)=>{const collection=item.kind==="section"?stored.sections:item.kind==="page-frame"?stored.pageFrames:item.kind==="occurrence"?stored.occurrences:stored.relationships;return collection.some(({id})=>id===item.id);};if(selectionWorkspace!==workspaceKey){selectedItems=selectionFromStoredView(transientView);selectionWorkspace=workspaceKey;}selectedItems=selectedItems.filter(selectionExists);selected=primaryFlowSelection(selectedItems);const projection=projectFlowGraph(state.project,flow.id),section=document.createElement("section"),heading=document.createElement("h3"),boundary=document.createElement("p"),toolbar=document.createElement("section"),laneControls=document.createElement("section"),status=document.createElement("p"),frames=document.createElement("section"),views=document.createElement("div"),canvasScroll=document.createElement("div"),canvas=svg("svg"),outline=document.createElement("ol"),popover=document.createElement("section"),actions=document.createElement("section");
    const namedRight=Math.max(940,...projection.laneBands.map(({x,width})=>x+width),...projection.graph.connectionEndpoints.map((endpoint)=>endpoint.layout.x+endpoint.width+60)),viewWidth=Math.max(960,namedRight+100),viewHeight=Math.max(780,...projection.laneBands.map(({y,height})=>y+height+80),...projection.graph.connectionEndpoints.map((endpoint)=>endpoint.layout.y+endpoint.height+100));
    const targetPortFor=(sourcePort:FlowPortSide):FlowPortSide|undefined=>sourcePort==="right"?"left":sourcePort==="top"?"bottom":sourcePort==="bottom"?"top":undefined;
    const targetPortElement=(endpointId:string,sourcePort:FlowPortSide):SVGCircleElement|undefined=>{const targetPort=targetPortFor(sourcePort);return targetPort?document.querySelector<SVGCircleElement>(`[data-flow-port-for="${CSS.escape(endpointId)}"][data-flow-port-side="${targetPort}"]`)??undefined:undefined;};
    const beginPortConnection=(endpoint:typeof projection.graph.connectionEndpoints[number],sourcePort:FlowPortSide,port:SVGCircleElement):void=>{clearSelectedRelationshipForConnection();connection?.preview?.remove();const targetPort=targetPortFor(sourcePort),targets=targetPort?projection.graph.connectionEndpoints.map(({id})=>id).filter((id)=>id!==endpoint.id):[];if(!targets.length){statusMessage=targetPort?"Add another Page frame before drawing a relationship.":"This port cannot start a relationship.";render();return;}const start=flowPortPoint(endpoint.layout,{width:endpoint.width,height:endpoint.height},sourcePort),preview=svg("line");preview.classList.add("flow-connection-preview");preview.setAttribute("x1",String(start.x));preview.setAttribute("y1",String(start.y));preview.setAttribute("x2",String(start.x));preview.setAttribute("y2",String(start.y));canvas.append(preview);connection={sourceId:endpoint.id,sourcePort,targets,targetIndex:0,preview};statusMessage=`Connection mode: choose a ${targetPort} port; Escape cancels.`;targetPortElement(targets[0]!,sourcePort)?.classList.add("is-valid-target");port.focus();};
    section.className="documentary-flow";section.dataset.flowSectionWorkspace=flow.id;heading.textContent="Canvas-first directional Flow";boundary.className="status-text";boundary.textContent="Documentary journey expectations are checked manually. Each Event payload schema validates independently.";toolbar.setAttribute("aria-label","Flow component catalogs");const sectionCatalog=button("Sections",()=>laneControls.querySelector<HTMLInputElement>('[aria-label="New Section name"]')?.focus()),toggleInspector=button(inspector.hidden?"Open Inspector":"Close Inspector",()=>{inspector.hidden=!inspector.hidden;toggleInspector.textContent=inspector.hidden?"Open Inspector":"Close Inspector";});toolbar.append(sectionCatalog,catalog("Pages",state.project.collections.pages,(page)=>insertPage(page)),catalog("Events",state.project.collections.events,(event)=>insertEvent(event)),toggleInspector);renderSectionControls(laneControls);renderFrameCards(frames);status.setAttribute("role","status");status.textContent=statusMessage||(!stored.pageFrames.length?"Add a Page from the Pages catalog.":"Draw between matching relationship ports, or press Enter on a port.");canvas.classList.add("flow-graph-canvas");canvas.setAttribute("aria-label","Interactive directional Flow canvas");canvas.setAttribute("role","application");canvas.dataset.viewport=JSON.stringify(transientView.viewport??{x:0,y:0,zoom:1});canvas.setAttribute("viewBox",`0 0 ${viewWidth} ${viewHeight}`);canvas.style.width=`${viewWidth}px`;canvas.style.height=`${viewHeight}px`;const resizeCanvasHeight=()=>{const expanded=Array.from(canvas.querySelectorAll<SVGForeignObjectElement>("[data-event-example-node]")).filter((candidate)=>candidate.querySelector("details")?.open),height=Math.max(viewHeight,...expanded.map((candidate)=>{const parent=candidate.parentNode as SVGGElement,parentY=parent.transform.baseVal.consolidate()?.matrix.f??0;return parentY+Number(candidate.getAttribute("y")??0)+Number(candidate.getAttribute("height")??0);}));canvas.setAttribute("viewBox",`0 0 ${viewWidth} ${height}`);canvas.style.height=`${height}px`;};outline.setAttribute("aria-label","Synchronized editable Flow outline");views.className="flow-projections";
    projection.laneBands.forEach((band)=>{const region=projection.lanes.find(({id})=>id===band.id)!,group=svg("g"),rect=svg("rect"),label=svg("text"),outlineRow=document.createElement("li"),outlineControl=button(`${region.name} · Section`,()=>laneControls.querySelector<HTMLElement>(`[data-flow-section-id="${CSS.escape(region.id)}"] input`)?.focus());group.dataset.flowSectionId=region.id;rect.setAttribute("x",String(band.x));rect.setAttribute("y",String(band.y));rect.setAttribute("width",String(band.width));rect.setAttribute("height",String(band.height));rect.setAttribute("class","flow-lane flow-section-region");rect.dataset.sectionDropzone=region.id;rect.addEventListener("dragover",(event)=>event.preventDefault());rect.addEventListener("drop",(event)=>{event.preventDefault();const payload=dropPayload(event),page=payload?.kind==="page"?current().state?.project.collections.pages.find(({id})=>id===payload.id):undefined;if(page)insertPage(page,region.id);});label.classList.add("flow-lane-label");label.setAttribute("x",String(band.x+12));label.setAttribute("y",String(band.y+25));label.textContent=region.name;group.append(rect,label);canvas.append(group);outlineRow.dataset.flowSectionId=region.id;outlineRow.append(outlineControl);outline.append(outlineRow);});
    const placeActiveCatalogPage=(event:Event)=>{const payload=activeCatalogPayload,page=payload?.kind==="page"?current().state?.project.collections.pages.find(({id})=>id===payload.id):undefined;if(!page)return;const target=(event.target as Element).closest<SVGElement>("[data-section-dropzone]");if(target?.dataset.sectionDropzone)insertPage(page,target.dataset.sectionDropzone);};canvas.addEventListener("pointerup",placeActiveCatalogPage);canvas.addEventListener("mouseup",placeActiveCatalogPage);
    for(const frame of stored.pageFrames){
      const endpoint=projection.graph.connectionEndpoints.find(({kind,id})=>kind==="page-frame"&&id===frame.id)! ,x=endpoint.layout.x,y=endpoint.layout.y,group=svg("g"),rect=svg("rect"),label=svg("text"),inputPort=svg("circle"),outputPort=svg("circle"),moveTo=(targetId:string|undefined,nextX:number,nextY:number)=>{sessionStorage.setItem(`my-chrome-utilities.flow-focus.v1:${state.project.id}:${flow.id}`,frame.id);const currentState=current().state!,next=moveFlowPageFramePresentation(currentState,flow.id,frame.id,{x:Math.max(0,Math.round(nextX)),y:Math.max(0,Math.round(nextY)),sectionId:targetId??null});if(next!==currentState)persist(next);setTimeout(()=>elementByData<SVGElement>("data-page-frame-id",frame.id)?.focus(),50);},beginConnection=()=>{clearSelectedRelationshipForConnection();connection?.preview?.remove();const targets=projection.graph.connectionEndpoints.map(({id})=>id).filter((id)=>id!==frame.id);if(!targets.length){statusMessage="Add another Page frame before drawing a relationship.";render();return;}const preview=svg("line");preview.classList.add("flow-connection-preview");preview.setAttribute("x1",String(x+endpoint.width));preview.setAttribute("y1",String(y+endpoint.height/2));preview.setAttribute("x2",String(x+endpoint.width+20));preview.setAttribute("y2",String(y+endpoint.height/2));canvas.append(preview);connection={sourceId:frame.id,targets,targetIndex:0,preview};statusMessage="Connection mode: choose a valid input port; Escape cancels.";elementByData("data-input-port-for",targets[0]??"")?.classList.add("is-valid-target");outputPort.focus();};
      group.dataset.pageFrameId=frame.id;group.dataset.flowEndpointId=frame.id;group.dataset.flowEndpointKind="page-frame";group.dataset.pageId=frame.pageId;if(frame.sectionId)group.dataset.flowSectionId=frame.sectionId;group.setAttribute("transform",`translate(${x} ${y})`);group.tabIndex=0;rect.setAttribute("width",String(endpoint.width));rect.setAttribute("height",String(endpoint.height));rect.setAttribute("rx","12");rect.classList.add("flow-page-frame");label.setAttribute("x","10");label.setAttribute("y","22");label.textContent=endpoint.name;inputPort.setAttribute("cx","0");inputPort.setAttribute("cy",String(endpoint.height/2));inputPort.setAttribute("r","8");inputPort.tabIndex=0;inputPort.dataset.inputPortFor=frame.id;inputPort.setAttribute("aria-label",`Input port for ${endpoint.name}`);outputPort.setAttribute("cx",String(endpoint.width));outputPort.setAttribute("cy",String(endpoint.height/2));outputPort.setAttribute("r","8");outputPort.tabIndex=0;outputPort.dataset.outputPortFor=frame.id;outputPort.setAttribute("aria-label",`Output port for ${endpoint.name}`);outputPort.addEventListener("pointerdown",(event)=>{event.stopPropagation();beginConnection();});outputPort.addEventListener("keydown",(event)=>{if(event.key==="Enter"&&!connection){event.preventDefault();beginConnection();return;}if(event.key==="Escape"&&connection){event.preventDefault();cancelConnection();return;}if(event.key==="Enter"&&connection){event.preventDefault();commitConnection(connection.targets[connection.targetIndex]);}});inputPort.addEventListener("pointerup",(event)=>{event.stopPropagation();commitConnection(frame.id);});
      label.textContent=`${endpoint.name} · Context-setting Page`;group.setAttribute("aria-label",`Page frame ${endpoint.name}. Context-setting Page. Drag or use Arrow keys to move.`);group.addEventListener("dragover",(event)=>event.preventDefault());group.addEventListener("drop",(event)=>{event.preventDefault();event.stopPropagation();const payload=dropPayload(event);if(payload?.kind!=="event")return;const entity=current().state?.project.collections.events.find(({id})=>id===payload.id);if(entity)insertEvent(entity,frame.id);});
      let start:{pointerId:number;clientX:number;clientY:number}|undefined,suppressPointerClick=false;const releaseClickSuppression=()=>setTimeout(()=>{suppressPointerClick=false;},1000),move=(event:PointerEvent)=>{if(!ownsPointerDrag(start?.pointerId,event.pointerId))return;group.setAttribute("transform",`translate(${x+event.clientX-start!.clientX} ${y+event.clientY-start!.clientY})`);},finish=(up:PointerEvent)=>{if(!ownsPointerDrag(start?.pointerId,up.pointerId))return;window.removeEventListener("pointermove",move);window.removeEventListener("pointerup",finish);window.removeEventListener("pointercancel",cancel);const initial=start!,nextX=x+up.clientX-initial.clientX,nextY=y+up.clientY-initial.clientY,target=Array.from(canvas.querySelectorAll<SVGElement>("[data-section-dropzone]")).find((region)=>{const bounds=region.getBoundingClientRect();return up.clientX>=bounds.left&&up.clientX<=bounds.right&&up.clientY>=bounds.top&&up.clientY<=bounds.bottom;})?.dataset.sectionDropzone,pointerId=initial.pointerId;if(group.hasPointerCapture(pointerId))group.releasePointerCapture(pointerId);start=undefined;if(suppressPointerClick)releaseClickSuppression();moveTo(target,nextX,nextY);},cancel=(event:PointerEvent)=>{if(!ownsPointerDrag(start?.pointerId,event.pointerId))return;window.removeEventListener("pointermove",move);window.removeEventListener("pointerup",finish);window.removeEventListener("pointercancel",cancel);if(group.hasPointerCapture(start!.pointerId))group.releasePointerCapture(start!.pointerId);start=undefined;group.setAttribute("transform",`translate(${x} ${y})`);if(suppressPointerClick)releaseClickSuppression();};group.addEventListener("pointerdown",(event)=>{if((event.target as Element).closest("circle"))return;if(start){suppressPointerClick=true;return;}start={pointerId:event.pointerId,clientX:event.clientX,clientY:event.clientY};window.addEventListener("pointermove",move);window.addEventListener("pointerup",finish);window.addEventListener("pointercancel",cancel);try{group.setPointerCapture(event.pointerId);}catch{/* Synthetic tests have no active device pointer to capture. */}});group.addEventListener("keydown",(event)=>{if(!event.key.startsWith("Arrow")||(event.target as Element).closest("circle"))return;event.preventDefault();const dx=event.key==="ArrowLeft"?-20:event.key==="ArrowRight"?20:0,dy=event.key==="ArrowUp"?-20:event.key==="ArrowDown"?20:0;moveTo(frame.sectionId,x+dx,y+dy);});group.addEventListener("click",(event)=>{if((event.target as Element).closest("circle"))return;if(suppressPointerClick){event.stopPropagation();return;}saveSelection({kind:"page-frame",id:frame.id},event.ctrlKey||event.metaKey||event.shiftKey);});group.append(rect,label,inputPort,outputPort);canvas.append(group);const outlineRow=document.createElement("li"),outlineControl=button(`${endpoint.name} · Page instance`,()=>saveSelection({kind:"page-frame",id:frame.id}));outlineRow.dataset.pageFrameId=frame.id;outlineRow.dataset.pageId=frame.pageId;outlineRow.append(outlineControl);outline.append(outlineRow);
      if(frame.sectionId)outlineRow.dataset.flowSectionId=frame.sectionId;outlineControl.textContent=`${endpoint.name} · Context-setting Page`;
    }
    for(const relationship of projection.graph.relationships){
      const source=projection.graph.connectionEndpoints.find(({id,kind})=>id===relationship.sourceEndpoint.id&&kind===relationship.sourceEndpoint.kind),target=projection.graph.connectionEndpoints.find(({id,kind})=>id===relationship.targetEndpoint.id&&kind===relationship.targetEndpoint.kind);if(!source||!target)continue;
      const geometry=flowEdgeGeometry(source.layout,target.layout,{width:source.width,height:source.height},{width:target.width,height:target.height},relationship.sourcePort,relationship.targetPort),edge=svg("g"),line=svg("line"),arrow=svg("polygon"),label=svg("text"),selectRelationship=()=>saveSelection({kind:"relationship",id:relationship.id});edge.classList.add("flow-edge");edge.dataset.relationshipId=relationship.id;edge.dataset.sourceEndpointKind=relationship.sourceEndpoint.kind;edge.dataset.sourceEndpointId=relationship.sourceEndpoint.id;edge.dataset.sourcePort=relationship.sourcePort;edge.dataset.targetEndpointKind=relationship.targetEndpoint.kind;edge.dataset.targetEndpointId=relationship.targetEndpoint.id;edge.dataset.targetPort=relationship.targetPort;edge.dataset.relationshipKind=relationship.kind;edge.dataset.directed="true";edge.tabIndex=0;edge.setAttribute("role","button");edge.setAttribute("aria-label",flowRelationshipText(projection.graph,relationship));line.setAttribute("x1",String(geometry.startX));line.setAttribute("y1",String(geometry.startY));line.setAttribute("x2",String(geometry.endX));line.setAttribute("y2",String(geometry.endY));arrow.setAttribute("points",geometry.arrow);label.setAttribute("x",String((geometry.startX+geometry.endX)/2));label.setAttribute("y",String((geometry.startY+geometry.endY)/2-8));label.textContent=relationship.label??"";edge.addEventListener("click",selectRelationship);edge.addEventListener("keydown",(event)=>{if(event.key!=="Enter"&&event.key!==" ")return;event.preventDefault();selectRelationship();});edge.append(line,arrow);if(relationship.label)edge.append(label);canvas.append(edge);const row=document.createElement("li"),control=button(flowRelationshipText(projection.graph,relationship),selectRelationship);row.dataset.relationshipId=relationship.id;row.dataset.sourceEndpointKind=relationship.sourceEndpoint.kind;row.dataset.sourcePort=relationship.sourcePort;row.dataset.targetEndpointKind=relationship.targetEndpoint.kind;row.dataset.targetPort=relationship.targetPort;row.append(control);outline.append(row);
    }
    for(const nodeData of projection.graph.nodes){if(!nodeData.layout)continue;const group=svg("g"),box=svg("rect"),title=svg("text"),detail=svg("text"),layout=nodeData.layout;group.classList.add("flow-node");group.dataset.occurrenceId=nodeData.id;group.setAttribute("transform",`translate(${layout.x} ${layout.y})`);group.setAttribute("tabindex","0");group.setAttribute("role","button");group.setAttribute("aria-label",`${nodeData.name}. Drag or use Arrow keys to move.`);box.setAttribute("width",String(nodeWidth));box.setAttribute("height",String(nodeHeight));box.setAttribute("rx","10");title.setAttribute("x","12");title.setAttribute("y","30");title.textContent=nodeData.name;detail.setAttribute("x","12");detail.setAttribute("y","55");detail.textContent="Interaction Event";
      detail.textContent=`Interaction Event${nodeData.trigger?` · ${nodeData.trigger}`:""}`;group.setAttribute("aria-label",`${nodeData.name}. Interaction Event. Drag or use Arrow keys to move.`);
      if(nodeData.pageFrameId)group.dataset.containingPageFrameId=nodeData.pageFrameId;const storedOccurrence=stored.occurrences.find(({id})=>id===nodeData.id)!,storedPosition=storedOccurrence.position as {x?:number;y?:number},focusNode=()=>queueMicrotask(()=>elementByData<SVGElement>("data-occurrence-id",nodeData.id)?.focus()),containingPageFrame=projection.graph.connectionEndpoints.find(({kind,id})=>kind==="page-frame"&&id===nodeData.pageFrameId),containedMoveAllowed=(x:number,y:number)=>Boolean(containingPageFrame&&x>=FLOW_GRAPH_GEOMETRY.eventMinX&&y>=FLOW_GRAPH_GEOMETRY.eventMinY&&x+nodeWidth<=containingPageFrame.width&&y+nodeHeight<=containingPageFrame.height),rejectContainedMove=()=>{group.setAttribute("transform",`translate(${layout.x} ${layout.y})`);statusMessage="Use Change Page to move this Event occurrence to another Page frame.";statusRepairHref="";render();focusNode();},moveContained=(x:number,y:number)=>{if(!containedMoveAllowed(x,y)){rejectContainedMove();return;}persist(moveGraphOccurrence(current().state!,flow.id,nodeData.id,{x,y}));focusNode();};
      let dragStart:{x:number;y:number;clientX:number;clientY:number;pointerId:number}|undefined;const ownsDrag=(event:PointerEvent)=>ownsPointerDrag(dragStart?.pointerId,event.pointerId),stopDragTracking=(pointerId:number)=>{window.removeEventListener("pointermove",moveDraggedNode);window.removeEventListener("pointerup",finishDraggedNode);window.removeEventListener("pointercancel",cancelDraggedNode);if(group.hasPointerCapture(pointerId))group.releasePointerCapture(pointerId);dragStart=undefined;},moveDraggedNode=(event:PointerEvent)=>{if(!ownsDrag(event))return;group.setAttribute("transform",`translate(${dragStart!.x+event.clientX-dragStart!.clientX} ${dragStart!.y+event.clientY-dragStart!.clientY})`);},cancelDraggedNode=(event:PointerEvent)=>{if(!ownsDrag(event))return;const pointerId=dragStart!.pointerId;stopDragTracking(pointerId);group.setAttribute("transform",`translate(${layout.x} ${layout.y})`);},finishDraggedNode=(event:PointerEvent)=>{if(!ownsDrag(event))return;const initial=dragStart!,x=Math.round(initial.x+event.clientX-initial.clientX),y=Math.round(initial.y+event.clientY-initial.clientY);stopDragTracking(initial.pointerId);if(nodeData.pageFrameId){const relativeX=x-Number(containingPageFrame?.layout.x??0),relativeY=y-Number(containingPageFrame?.layout.y??0);moveContained(relativeX,relativeY);return;}persist(moveGraphOccurrence(current().state!,flow.id,nodeData.id,{lane:layout.lane,x,y:Math.max(55,y)}));focusNode();};group.addEventListener("pointerdown",(event)=>{if((event.target as Element).closest("foreignObject")||dragStart)return;dragStart={x:layout.x,y:layout.y,clientX:event.clientX,clientY:event.clientY,pointerId:event.pointerId};window.addEventListener("pointermove",moveDraggedNode);window.addEventListener("pointerup",finishDraggedNode);window.addEventListener("pointercancel",cancelDraggedNode);try{group.setPointerCapture(event.pointerId);}catch{/* Synthetic regression events have no active device pointer to capture. */}});group.addEventListener("keydown",(event)=>{if((event.target as Element).closest("foreignObject,input,button,a,summary,details")||!event.key.startsWith("Arrow"))return;event.preventDefault();const dx=event.key==="ArrowLeft"?-20:event.key==="ArrowRight"?20:0,dy=event.key==="ArrowUp"?-20:event.key==="ArrowDown"?20:0;if(nodeData.pageFrameId){moveContained(Number(storedPosition.x??24)+dx,Number(storedPosition.y??70)+dy);return;}persist(moveGraphOccurrence(current().state!,flow.id,nodeData.id,{lane:layout.lane,x:layout.x+dx,y:Math.max(55,layout.y+dy)}));focusNode();});group.addEventListener("click",(event)=>{if((event.target as Element).closest("foreignObject"))return;if(suppressNodeClick){suppressNodeClick=false;return;}saveSelection({kind:"occurrence",id:nodeData.id},event.ctrlKey||event.metaKey||event.shiftKey);});const canvasExample=occurrenceExampleDetails(state,flow.id,nodeData.id,nodeData.name),exampleHost=svg("foreignObject"),resizeCanvasExample=()=>{const expandedHeight=canvasExample.open?Math.max(260,Math.ceil(canvasExample.scrollHeight)+8):30;exampleHost.setAttribute("height",String(expandedHeight));box.setAttribute("height",String(canvasExample.open?nodeHeight+expandedHeight-30:nodeHeight));resizeCanvasHeight();};exampleHost.dataset.eventExampleNode=nodeData.id;exampleHost.setAttribute("x","4");exampleHost.setAttribute("y","62");exampleHost.setAttribute("width",String(nodeWidth-8));exampleHost.setAttribute("height","30");canvasExample.className="flow-node-example";canvasExample.style.fontSize="14px";canvasExample.style.background="white";new MutationObserver(resizeCanvasExample).observe(canvasExample,{attributes:true,attributeFilter:["open"]});const canvasSummary=canvasExample.querySelector("summary")!;canvasSummary.addEventListener("keydown",(event)=>{if(event.key!=="Enter"&&event.key!==" ")return;event.preventDefault();canvasExample.open=!canvasExample.open;});exampleHost.append(canvasExample);group.append(box,title,detail,exampleHost);canvas.append(group);const row=document.createElement("li"),control=button(`${nodeData.name} · Interaction Event${nodeData.trigger?` · ${nodeData.trigger}`:""}`,()=>saveSelection({kind:"occurrence",id:nodeData.id})),outlineExample=occurrenceExampleDetails(state,flow.id,nodeData.id,nodeData.name);row.dataset.occurrenceId=nodeData.id;if(nodeData.pageFrameId)row.dataset.containingPageFrameId=nodeData.pageFrameId;row.append(control,outlineExample);outline.insertBefore(row,outline.querySelector("[data-relationship-id]"));}
    for(const nodeData of projection.graph.nodes){const control=outline.querySelector<HTMLButtonElement>(`[data-occurrence-id="${CSS.escape(nodeData.id)}"] button`);if(control)control.textContent=`${nodeData.name} · Interaction Event${nodeData.trigger?` · ${nodeData.trigger}`:""}`;}
    for(const port of Array.from(canvas.querySelectorAll<SVGCircleElement>("[data-input-port-for],[data-output-port-for]"))){const endpointId=port.dataset.inputPortFor??port.dataset.outputPortFor,endpoint=projection.graph.connectionEndpoints.find(({id})=>id===endpointId);if(!endpoint)continue;const side:FlowPortSide=port.dataset.outputPortFor?"right":"left",point=flowPortPoint(endpoint.layout,{width:endpoint.width,height:endpoint.height},side);port.dataset.flowPortFor=endpoint.id;port.dataset.flowPortSide=side;port.setAttribute("aria-label",`${side} port for ${endpoint.name}`);port.setAttribute("cx",String(point.x));port.setAttribute("cy",String(point.y));canvas.append(port);}
    for(const endpoint of projection.graph.connectionEndpoints){for(const side of["top","bottom"] as const){const port=svg("circle"),point=flowPortPoint(endpoint.layout,{width:endpoint.width,height:endpoint.height},side);port.setAttribute("cx",String(point.x));port.setAttribute("cy",String(point.y));port.setAttribute("r","8");port.tabIndex=0;port.dataset.flowPortFor=endpoint.id;port.dataset.flowPortSide=side;port.dataset.inputPortFor=endpoint.id;port.setAttribute("aria-label",`${side} port for ${endpoint.name}`);port.addEventListener("pointerdown",(event)=>{event.stopPropagation();if(!connection)beginPortConnection(endpoint,side,port);});port.addEventListener("pointerup",(event)=>{event.stopPropagation();if(connection)commitConnection(endpoint.id,side);});port.addEventListener("keydown",(event)=>{event.stopPropagation();if(event.key==="Enter"&&!connection){event.preventDefault();beginPortConnection(endpoint,side,port);return;}if(!connection||connection.sourceId!==endpoint.id||connection.sourcePort!==side)return;if(event.key==="Escape"){event.preventDefault();cancelConnection();return;}if(event.key.startsWith("Arrow")){event.preventDefault();document.querySelectorAll(".is-valid-target").forEach((element)=>element.classList.remove("is-valid-target"));connection.targetIndex=(connection.targetIndex+(event.key==="ArrowLeft"||event.key==="ArrowUp"?-1:1)+connection.targets.length)%connection.targets.length;targetPortElement(connection.targets[connection.targetIndex]!,side)?.classList.add("is-valid-target");return;}if(event.key==="Enter"){event.preventDefault();commitConnection(connection.targets[connection.targetIndex]!,targetPortFor(side));}});canvas.append(port);}}
    canvas.addEventListener("pointermove",(event)=>{if(!connection?.preview)return;canvasScroll.classList.add("is-connecting");const port=(event.target as Element).closest<SVGElement>("[data-flow-port-for]"),scrollBounds=canvasScroll.getBoundingClientRect(),edgeSize=36,edgeStep=28;if(!port){if(event.clientX<=scrollBounds.left+edgeSize)canvasScroll.scrollLeft=Math.max(0,canvasScroll.scrollLeft-edgeStep);else if(event.clientX>=scrollBounds.right-edgeSize)canvasScroll.scrollLeft=Math.min(canvasScroll.scrollWidth-canvasScroll.clientWidth,canvasScroll.scrollLeft+edgeStep);if(event.clientY<=scrollBounds.top+edgeSize)canvasScroll.scrollTop=Math.max(0,canvasScroll.scrollTop-edgeStep);else if(event.clientY>=scrollBounds.bottom-edgeSize)canvasScroll.scrollTop=Math.min(canvasScroll.scrollHeight-canvasScroll.clientHeight,canvasScroll.scrollTop+edgeStep);}const bounds=canvas.getBoundingClientRect(),viewBox=canvas.viewBox.baseVal,scaleX=viewBox.width/bounds.width,scaleY=viewBox.height/bounds.height;connection.preview.setAttribute("x2",String(viewBox.x+(event.clientX-bounds.left)*scaleX));connection.preview.setAttribute("y2",String(viewBox.y+(event.clientY-bounds.top)*scaleY));document.querySelectorAll(".is-valid-target,.is-invalid-target").forEach((element)=>element.classList.remove("is-valid-target","is-invalid-target"));const valid=port&&port.dataset.flowPortFor!==connection.sourceId&&port.dataset.flowPortSide===targetPortFor(connection.sourcePort??"right");(port??canvas).classList.add(valid?"is-valid-target":"is-invalid-target");});
    canvas.addEventListener("pointerup",(event)=>{if(!connection)return;const delivered=(event.target as Element).closest<SVGElement>("[data-flow-port-for]"),hit=document.elementFromPoint(event.clientX,event.clientY)?.closest<SVGElement>("[data-flow-port-for]"),port=hit??delivered,side=port?.dataset.flowPortSide as FlowPortSide|undefined;if(port&&side){commitConnection(port.dataset.flowPortFor,side);return;}const sourceId=connection.sourceId,sourcePort=connection.sourcePort??"right",targetPort=targetPortFor(sourcePort),bounds=canvas.getBoundingClientRect(),viewBox=canvas.viewBox.baseVal,position={x:Math.round(viewBox.x+(event.clientX-bounds.left)*viewBox.width/bounds.width),y:Math.round(viewBox.y+(event.clientY-bounds.top)*viewBox.height/bounds.height)},sourceElement=canvas.querySelector<HTMLElement>(`[data-flow-port-for="${CSS.escape(sourceId)}"][data-flow-port-side="${sourcePort}"]`);connection.preview?.remove();connection=undefined;canvasScroll.classList.remove("is-connecting");if(!targetPort){cancelConnection(true,true);return;}canvas.dispatchEvent(new CustomEvent("flow-empty-connection-drop",{bubbles:true,detail:{sourceId,sourcePort,targetPort,position,sourceElement}}));});
    canvas.addEventListener("keydown",(event)=>{if(!connection)return;if(event.key==="Escape"){event.preventDefault();event.stopImmediatePropagation();cancelConnection();return;}if(event.key.startsWith("Arrow")){event.preventDefault();event.stopImmediatePropagation();document.querySelectorAll(".is-valid-target").forEach((element)=>element.classList.remove("is-valid-target"));connection.targetIndex=(connection.targetIndex+(event.key==="ArrowLeft"||event.key==="ArrowUp"?-1:1)+connection.targets.length)%connection.targets.length;targetPortElement(connection.targets[connection.targetIndex]!,connection.sourcePort??"right")?.classList.add("is-valid-target");return;}if(event.key==="Enter"){event.preventDefault();event.stopImmediatePropagation();commitConnection(connection.targets[connection.targetIndex]!,targetPortFor(connection.sourcePort??"right"));}},true);
    canvasScroll.className="flow-canvas-scroll";canvasScroll.append(canvas);views.append(canvasScroll,outline);renderRelationshipPopover(popover);renderActions(actions);section.append(heading,boundary,toolbar,laneControls,status,frames,views,actions,popover);host.append(section);document.querySelectorAll<HTMLElement>("[data-occurrence-id],[data-relationship-id],[data-page-frame-id]").forEach((element)=>{const item:FlowSelection=element.dataset.occurrenceId?{kind:"occurrence",id:element.dataset.occurrenceId}:element.dataset.relationshipId?{kind:"relationship",id:element.dataset.relationshipId}:{kind:"page-frame",id:element.dataset.pageFrameId!};element.classList.toggle("is-selected",flowSelectionContains(selectedItems,item));});if(relationshipDeletionFocusTimer!==undefined){clearTimeout(relationshipDeletionFocusTimer);relationshipDeletionFocusTimer=undefined;}const deletionIntent=relationshipDeletionFocusIntent;if(deletionIntent){const restored=Boolean(canvas.querySelector(`[data-relationship-id="${CSS.escape(deletionIntent.id)}"]`));if(restored||!deletionIntent.sourceFocused)relationshipDeletionFocusTimer=setTimeout(()=>{relationshipDeletionFocusTimer=undefined;if(relationshipDeletionFocusIntent!==deletionIntent)return;const liveCanvas=document.querySelector<SVGSVGElement>('[aria-label="Interactive directional Flow canvas"]'),selector=restored?`[data-relationship-id="${CSS.escape(deletionIntent.id)}"]`:`[${deletionIntent.sourceKind==="page-frame"?"data-page-frame-id":"data-occurrence-id"}="${CSS.escape(deletionIntent.sourceId)}"]`,target=liveCanvas?.querySelector<HTMLElement>(selector);if(!target?.isConnected)return;target.focus();relationshipDeletionFocusIntent=consumeRelationshipDeletionFocus(deletionIntent,restored).next;},50);}const frameIntent=pageFrameFocusIntent;if(frameIntent){const focusedFrame=document.querySelector<HTMLElement>(`[data-free-page-frame-canvas="${CSS.escape(frameIntent.id)}"]`);if(focusedFrame){const renderRevision=Number(current().revision??0),replacement=frameIntent.optimisticFocused&&renderRevision>frameIntent.revision;if(!frameIntent.optimisticFocused){frameIntent.optimisticFocused=true;frameIntent.revision=renderRevision;}queueMicrotask(()=>{if(!focusedFrame.isConnected)return;focusedFrame.focus();if(replacement&&pageFrameFocusIntent===frameIntent)pageFrameFocusIntent=undefined;});}}const edgeIntent=relationshipEdgeFocusIntent;if(edgeIntent){const edge=document.querySelector<HTMLElement>(`[data-relationship-id="${CSS.escape(edgeIntent.id)}"]`);if(edge){const renderRevision=Number(current().revision??0),replacement=edgeIntent.optimisticFocused&&renderRevision>edgeIntent.revision;if(!edgeIntent.optimisticFocused){edgeIntent.optimisticFocused=true;edgeIntent.revision=renderRevision;}queueMicrotask(()=>{if(!edge.isConnected)return;edge.focus();if(replacement&&relationshipEdgeFocusIntent===edgeIntent)relationshipEdgeFocusIntent=undefined;});}}renderInspector();
  }

  function render():void{const {state,flow}=current();advanced.hidden=!flow;if(flow){renderGraph(flow);document.querySelectorAll<HTMLElement>("[data-page-frame-id]").forEach((candidate)=>candidate.setAttribute("aria-pressed",String(flowSelectionContains(selectedItems,{kind:"page-frame",id:candidate.dataset.pageFrameId!}))));if(state){const focusId=sessionStorage.getItem(`my-chrome-utilities.flow-focus.v1:${state.project.id}:${flow.id}`);if(focusId)queueMicrotask(()=>(document.querySelector<HTMLElement>(`[aria-label="Interactive directional Flow canvas"] [data-page-frame-id="${CSS.escape(focusId)}"]`)??document.querySelector<HTMLElement>(`article[data-page-frame-id="${CSS.escape(focusId)}"]`))?.focus({preventScroll:true}));}}else{selectionWorkspace="";selectedItems=[];selected=undefined;document.querySelector<HTMLElement>("#flow-graph-workspace")?.replaceChildren();inspectorContext.replaceChildren();}}
  return{render,renderSelectors:render};
}
