import {advanceFlowInstance,conditionMatches as projectConditionMatches,startFlowInstance,type Condition,type FlowInstance,type Profile,type ProjectEntity,type SpecificationProject} from "./data-layer-specification-project.js";
import type {CanonicalProjectEnvelope} from "./data-layer-specification-model.js";

export {applyCanonicalCommand,applyCanonicalSchemaDraftEdits,createCanonicalProjectEnvelope,migrateCanonicalProject} from "./data-layer-specification-model.js";
export type {CanonicalCommand,CanonicalCommandResult,CanonicalProjectEnvelope} from "./data-layer-specification-model.js";
export interface CompilerDiagnostic{code:string;entityId:string;field:string;referenceId:string}
export interface CompiledSchema{schemaId:string;revision:number;document:Record<string,unknown>;required:string[];profileIds:string[]}
export interface CompiledAssignment{assignmentId:string;schemaDraftId:string;applicabilitySetId:string;eventId:string;priority:number;schemaRevision:number}
export interface ExecutableSpecificationPlan{projectId:string;draftId:string;revision:number;sourceProject:SpecificationProject;pages:Record<string,ProjectEntity>;events:Record<string,ProjectEntity>;applicability:Record<string,ProjectEntity>;flows:Record<string,ProjectEntity>;schemas:Record<string,CompiledSchema>;assignments:CompiledAssignment[];provenance:Record<string,{profileId:string;profileName:string;position:number}[]>}
export interface SpecificationEvaluationResult{candidates:(CompiledAssignment&{rejectionReasons:string[]})[];winner?:{assignmentId:string;schemaId:string;schemaRevision:number};ties:string[];activeFlowId:unknown;activeStepId:unknown;effectiveProfiles:string[];effectiveSchemaRevision?:number;issues:string[];provenance:Record<string,{profileId:string;profileName:string;position:number}[]>;stateTransition?:{instances:FlowInstance[];active?:FlowInstance;ambiguity?:{instanceIds:string[];reason:string}}}

const clone=<T>(value:T):T=>structuredClone(value);
function freeze<T>(value:T):T{if(value&&typeof value==="object"&&!Object.isFrozen(value)){Object.freeze(value);for(const child of Object.values(value as Record<string,unknown>))freeze(child);}return value;}
function index(entities:ProjectEntity[]):Record<string,ProjectEntity>{return Object.fromEntries(entities.map((entity)=>[entity.id,clone(entity)]));}
function requirementDocument(profiles:Profile[]):{document:Record<string,unknown>;required:string[];conflicts:{path:string;origins:string[];reason:string}[]}{const root:{type:"object";properties:Record<string,any>;required:string[]}={type:"object",properties:{},required:[]},effective=new Map<string,any>(),origins=new Map<string,string[]>(),conflicts:{path:string;origins:string[];reason:string}[]=[];for(const profile of profiles)for(const requirement of profile.requirements){const prior=effective.get(requirement.path),pathOrigins=origins.get(requirement.path)??[];if(prior?.type&&requirement.type&&prior.type!==requirement.type)conflicts.push({path:requirement.path,origins:[...pathOrigins,profile.id],reason:`Incompatible types ${prior.type} and ${requirement.type}`});if(prior&&(prior.required&&requirement.forbidden||prior.forbidden&&requirement.required))conflicts.push({path:requirement.path,origins:[...pathOrigins,profile.id],reason:"Required and Forbidden are incompatible"});const allowedValues=prior?.allowedValues&&requirement.allowedValues?prior.allowedValues.filter((value:unknown)=>requirement.allowedValues!.some((candidate)=>Object.is(candidate,value))):requirement.allowedValues??prior?.allowedValues;if(prior?.allowedValues&&requirement.allowedValues&&!allowedValues.length)conflicts.push({path:requirement.path,origins:[...pathOrigins,profile.id],reason:"Allowed-value intersection is empty"});const rules=[...(prior?.rules??[])];for(const rule of requirement.rules??[]){const existing=rules.find((candidate:any)=>candidate.id===rule.id);if(existing&&JSON.stringify(existing)!==JSON.stringify(rule))conflicts.push({path:requirement.path,origins:[...pathOrigins,profile.id],reason:`Rule ${String(rule.id)} is incompatible`});else if(!existing)rules.push(clone(rule));}effective.set(requirement.path,{...prior,...clone(requirement),...(allowedValues?{allowedValues}:{}),...(rules.length?{rules}:{})});origins.set(requirement.path,[...pathOrigins,profile.id]);}for(const requirement of effective.values()){const parts=requirement.path.split("/").filter(Boolean);let node=root;for(let at=0;at<parts.length;at+=1){const part=parts[at]!,last=at===parts.length-1;if(last){node.properties[part]={type:requirement.type??"string",...(requirement.allowedValues?{enum:requirement.allowedValues}:{}),...(requirement.description?{description:requirement.description}:{}),...(requirement.examples?{examples:requirement.examples}:{}),...(requirement.rules?.length?{"x-rules":requirement.rules}:{}),...(requirement.forbidden?{"x-forbidden":true}:{})};if(requirement.required&&!node.required.includes(part))node.required.push(part);}else{node.properties[part]??={type:"object",properties:{},required:[]};if(requirement.required&&!node.required.includes(part))node.required.push(part);node=node.properties[part];}}}return{document:root,required:root.required,conflicts};}
function referencedProfiles(project:SpecificationProject,schema:ProjectEntity):Profile[]{const ids=(schema.workingDraft as {profileIds?:string[]}|undefined)?.profileIds??(schema.profileIds as string[]|undefined)??[];return ids.map((id)=>project.collections.profiles.find((profile)=>profile.id===id)).filter((profile):profile is Profile=>Boolean(profile));}
function graphDiagnostics(project:SpecificationProject):CompilerDiagnostic[]{const pages=new Set(project.collections.pages.map(({id})=>id)),events=new Set(project.collections.events.map(({id})=>id)),profiles=new Set(project.collections.profiles.map(({id})=>id)),schemas=new Set(project.collections.schemaDrafts.map(({id})=>id)),applicability=new Set(project.collections.applicabilitySets.map(({id})=>id)),result:CompilerDiagnostic[]=[];for(const flow of project.collections.flows)for(const step of(flow.steps as ProjectEntity[]|undefined)??[]){if(step.pageId&&!pages.has(String(step.pageId)))result.push({code:"dangling-reference",entityId:step.id,field:"pageId",referenceId:String(step.pageId)});if(step.eventId&&!events.has(String(step.eventId)))result.push({code:"dangling-reference",entityId:step.id,field:"eventId",referenceId:String(step.eventId)});for(const profileId of(step.profileIds as string[]|undefined)??[])if(!profiles.has(profileId))result.push({code:"dangling-reference",entityId:step.id,field:"profileIds",referenceId:profileId});}for(const assignment of project.collections.assignments){const references:[[string,unknown,Set<string>],[string,unknown,Set<string>],[string,unknown,Set<string>]]=[["schemaDraftId",assignment.schemaDraftId??assignment.schemaId,schemas],["applicabilitySetId",assignment.applicabilitySetId,applicability],["eventId",assignment.eventId,events]];for(const[field,value,identities]of references)if(!value||!identities.has(String(value)))result.push({code:"dangling-reference",entityId:assignment.id,field,referenceId:String(value??"")});}return result;}
export function compileSpecificationProject(envelope:CanonicalProjectEnvelope):{status:"compiled";plan:ExecutableSpecificationPlan;diagnostics:[]}|{status:"blocked";diagnostics:CompilerDiagnostic[]}{const diagnostics=graphDiagnostics(envelope.project);if(diagnostics.length)return{status:"blocked",diagnostics};const schemas:Record<string,CompiledSchema>={},provenance:ExecutableSpecificationPlan["provenance"]={};for(const schema of envelope.project.collections.schemaDrafts){const profiles=referencedProfiles(envelope.project,schema),compiled=requirementDocument(profiles);if(compiled.conflicts.length)return{status:"blocked",diagnostics:compiled.conflicts.map((conflict)=>({code:"profile-conflict",entityId:schema.id,field:conflict.path,referenceId:`${conflict.origins.join(",")}: ${conflict.reason}`}))};const working=(schema.workingDraft as {document?:Record<string,unknown>}|undefined)?.document,document=profiles.length?compiled.document:clone(working??(schema.document as Record<string,unknown>|undefined)??compiled.document),revision=Number(schema.version??1);schemas[schema.id]={schemaId:schema.id,revision,document,required:compiled.required,profileIds:profiles.map(({id})=>id)};profiles.forEach((profile,position)=>profile.requirements.forEach((requirement)=>{provenance[`${schema.id}:${requirement.path}`]??=[];provenance[`${schema.id}:${requirement.path}`]!.push({profileId:profile.id,profileName:profile.name,position});}));}const assignments=envelope.project.collections.assignments.map((assignment)=>({assignmentId:assignment.id,schemaDraftId:String(assignment.schemaDraftId??assignment.schemaId),applicabilitySetId:String(assignment.applicabilitySetId),eventId:String(assignment.eventId),priority:Number(assignment.priority??0),schemaRevision:Number(assignment.schemaRevision)}));const plan:ExecutableSpecificationPlan={projectId:envelope.project.id,draftId:envelope.draftId,revision:envelope.revision,sourceProject:clone(envelope.project),pages:index(envelope.project.collections.pages),events:index(envelope.project.collections.events),applicability:index(envelope.project.collections.applicabilitySets),flows:index(envelope.project.collections.flows),schemas,assignments,provenance};return{status:"compiled",plan:freeze(plan),diagnostics:[]};}

function readField(observation:Record<string,unknown>,field:string):unknown{return field.split(".").reduce<unknown>((value,key)=>value&&typeof value==="object"?(value as Record<string,unknown>)[key]:undefined,observation);}
function conditionMatches(condition:Condition|undefined,observation:Record<string,unknown>):boolean{if(!condition)return true;if(condition.kind==="predicate"){const actual=readField(observation,condition.field);if(condition.operator==="equals")return actual===condition.value;if(condition.operator==="not equals")return actual!==condition.value;if(condition.operator==="exists")return actual!==undefined;return false;}if(condition.kind==="all")return condition.conditions.every((child)=>conditionMatches(child,observation));if(condition.kind==="any")return condition.conditions.some((child)=>conditionMatches(child,observation));return!condition.conditions.some((child)=>conditionMatches(child,observation));}
function validationIssues(value:unknown,document:any,path=""):string[]{if(!value||typeof value!=="object")return[`${path||"/"}: expected object`];const result:string[]=[];for(const required of document.required??[])if(!(required in(value as Record<string,unknown>)))result.push(`${path}/${required}: required`);for(const[key,child]of Object.entries<any>(document.properties??{})){const nested=(value as Record<string,unknown>)[key];if(nested!==undefined&&child.type==="object")result.push(...validationIssues(nested,child,`${path}/${key}`));}return result;}
function flowTransition(plan:ExecutableSpecificationPlan,observation:Record<string,unknown>,prior:readonly FlowInstance[]):{instances:FlowInstance[];active?:FlowInstance;ambiguity?:{instanceIds:string[];reason:string}}{const sessionId=String(observation.sessionId??""),correlationKey=typeof observation.correlationKey==="string"?observation.correlationKey:undefined,same=(instance:FlowInstance)=>instance.sessionId===sessionId&&(correlationKey===undefined||instance.correlationKey===correlationKey),session=prior.filter((instance)=>same(instance)&&instance.status==="active"),others=prior.filter((instance)=>!same(instance)),eventId=String(observation.eventId??""),pageId=String(observation.pageId??"");if(!eventId&&!pageId)return{instances:[...prior],...(session.length===1?{active:session[0]}:{})};const matches=(step:ProjectEntity)=>Boolean(step.eventId||step.pageId)&&(!step.eventId||step.eventId===eventId)&&(!step.pageId||step.pageId===pageId),existing=new Set(session.map(({flowId})=>flowId)),started=Object.values(plan.flows).filter((flow)=>!existing.has(flow.id)&&matches(((flow.steps as ProjectEntity[]|undefined)??[])[0]??{id:"",name:""})&&(!flow.entryCondition||projectConditionMatches(flow.entryCondition as Condition,observation))).map((flow)=>startFlowInstance(plan.sourceProject,flow.id,sessionId,{...(correlationKey?{correlationKey}:{}),...(typeof observation.observedAt==="string"?{startedAt:observation.observedAt}:{})})),sources=[...session,...started],advanced=sources.map((instance)=>advanceFlowInstance(plan.sourceProject,instance,observation)),changed=advanced.filter((instance,index)=>instance.history.length>(sources[index]?.history.length??0));if(changed.length>1)return{instances:[...prior],ambiguity:{instanceIds:changed.map(({id})=>id),reason:"Multiple flow instances are equally eligible; no instance was advanced."}};return{instances:[...others,...advanced.filter((instance)=>instance.history.length>0)],...(changed[0]?{active:changed[0]}:{})};}
export function evaluateSpecificationObservation(plan:ExecutableSpecificationPlan,observation:Record<string,unknown>,priorInstances?:readonly FlowInstance[]):SpecificationEvaluationResult{const stateTransition=priorInstances?flowTransition(plan,observation,priorInstances):undefined,active=stateTransition?.active,effectiveObservation:Record<string,unknown>={...observation,...(active?{flowId:active.flowId,activeStepId:active.currentStepId}:{})},candidates=plan.assignments.map((assignment)=>{const reasons:string[]=[],event=plan.events[assignment.eventId],applicability=plan.applicability[assignment.applicabilitySetId];if(!event||event.eventName!==effectiveObservation.eventName||event.sourceId!==effectiveObservation.sourceId)reasons.push("event did not match");if(!applicability||!conditionMatches(applicability.condition as Condition|undefined,effectiveObservation))reasons.push("applicability did not match");return{...assignment,rejectionReasons:reasons};}),matching=stateTransition?.ambiguity?[]:candidates.filter(({rejectionReasons})=>rejectionReasons.length===0).sort((left,right)=>right.priority-left.priority),highest=matching[0]?.priority,ties=matching.filter(({priority})=>priority===highest),selected=ties.length===1?ties[0]:undefined,schema=selected?plan.schemas[selected.schemaDraftId]:undefined,winner=selected&&schema?{assignmentId:selected.assignmentId,schemaId:schema.schemaId,schemaRevision:selected.schemaRevision}:undefined;return{candidates,...(winner?{winner}:{}),ties:ties.map(({assignmentId})=>assignmentId),activeFlowId:effectiveObservation.flowId,activeStepId:effectiveObservation.activeStepId,effectiveProfiles:schema?.profileIds??[],...(schema?{effectiveSchemaRevision:schema.revision}:{}),issues:schema?validationIssues(effectiveObservation.payload,schema.document):[],provenance:schema?Object.fromEntries(Object.entries(plan.provenance).filter(([key])=>key.startsWith(`${schema.schemaId}:`))):{},...(stateTransition?{stateTransition}:{})};}
