import {
  addProjectEntity,
  adoptSavedSchema,
  buildReleaseReview,
  commitStagedProjectImport,
  commitSavedSchemaSynchronization,
  confirmCanonicalMigration,
  createSpecificationProject,
  createProjectSchemaDraft,
  exportDocumentation,
  exportSpecificationProjectState,
  redoProjectTransaction,
  restoreReleaseAsDraft,
  saveProjectAssignment,
  searchProjectAssignments,
  stageProjectImport,
  stageSavedSchemaSynchronization,
  transactProject,
  undoProjectTransaction,
  type ProjectEntityKind,
  type ProjectEntity,
  type Condition,
  type ProjectState,
  type StagedProjectImport,
  type SavedSchemaSource,
  type SavedSchemaSynchronizationReview,
} from "./data-layer-specification-project.js";
import {applyStagedBulkAction,commitStagedBulkRequirements,stageBulkRequirements,type BulkFormat,type StagedBulkRequirements} from "./data-layer-specification-bulk.js";
import {buildEffectiveRequirementCoverage,publishCompiledRelease as publishProjectRelease,runProductionFixture,specificationPreflight} from "./data-layer-specification-assurance.js";
import {compileSpecificationProject,createCanonicalProjectEnvelope} from "./data-layer-specification-engine.js";
import {entityPurposeGuidance,projectAuthoringGuidance} from "./data-layer-specification-guidance.js";
import {installExecutableFlowBuilder,type ExecutableFlowBuilderIntegration} from "./data-layer-executable-flow-ui.js";
import {applyFlowPageGroupLaneSelection,flowPageGroupLaneIds,installFlowGraphBuilder,moveFlowPageFrame,removeFlowPageFrame,type FlowGraphBuilderIntegration} from "./utilities/data-layer/flow-graph.js";
import {addPageGroupMembership,confirmPageGroupMembershipMigration,inspectPageGroupMembershipRemoval,movePageGroupMembership,orderedPageGroupIds,pageGroupMembers,removePageGroupMembership,requiresPageGroupMembershipMigration,stagePageGroupMembershipMigration} from "./data-layer-page-group-membership.js";
import {restoreSchemaLibrary,SCHEMA_LIBRARY_STORAGE_KEY} from "./data-layer-schema-verification.js";
const projectPreflight=(current:ProjectState,revision:number)=>specificationPreflight({...createCanonicalProjectEnvelope(current.project,current.draft?.id??"release"),revision});
import {
  CANONICAL_SPECIFICATION_PROJECT_STORAGE_KEY,
  commitCanonicalProjectState,
  inspectCanonicalProjectConflict,
  resolveCanonicalProjectConflict,
  restoreCanonicalProjectEnvelope,
  restoreCanonicalProjectState,
  serializeCanonicalProjectState,
  subscribeCanonicalProjectChanges,
  type CanonicalProjectChangeTarget,
  type CanonicalCommitResult,
} from "./data-layer-specification-repository.js";
import {PROJECT_LIBRARY_STORAGE_KEY,activateProject,activeProjectContextChange,migrateSingletonProject,projectLibrary,projectRecordNeedsSynchronization,recordProjectNavigation,replaceActiveProjectState,resolveProjectNavigation,restoreProjectLibrary,saveProjectState,serializeProjectLibrary,type ProjectLibrary} from "./data-layer-project-library.js";
import {effectivePropertySummary,installLayeredSchemaUi} from "./data-layer-layered-schema-ui.js";
import {compileLayeredSchema} from "./data-layer-layered-schema.js";
import {layeredContributorPath,layeredContributorsForPath} from "./data-layer-layered-schema-project.js";
import {composedSchemaWorkspace,resetComposedSchemaLocalProperty,saveComposedSchemaLocalFacets} from "./data-layer-composed-schema-workspace.js";
import {mountComposedSchemaFacetBuilder} from "./data-layer-composed-schema-builders.js";
import {installFlowDocumentationExportUi} from "./data-layer-flow-table-documentation-export-ui.js";
import {applyCanonicalCommand,canonicalRequirements,createCanonicalSchema,migrateLegacyProfile,type CanonicalSchemaDocument} from "./data-layer-canonical-schema.js";
import {mountCanonicalSchemaEditor} from "./data-layer-canonical-schema-ui.js";
import {createProjectCollectionEntity,hasSavedSchemaAdoptionActions,inspectProjectEntityRemoval,projectCollectionCreationFields,projectCollectionCreationRoute,projectCollectionDefinitions,projectEntityWorkspaceRoute,projectInspectorTogglePresentation,removeProjectCollectionEntity,type ProjectCollectionCreationField,type ProjectEntityRemovalReview} from "./data-layer-project-entity-lifecycle.js";

const STORAGE_KEY=CANONICAL_SPECIFICATION_PROJECT_STORAGE_KEY,NAVIGATION_KEY="my-chrome-utilities.specification-project-navigation.v1",START_PATH_KEY="my-chrome-utilities.specification-project-start.v1",routeParameters=new URLSearchParams(location.search);
const q=<T extends Element>(selector:string):T=>{const element=document.querySelector<T>(selector);if(!element)throw new Error(`Missing ${selector}`);return element;};
const projectInspector=q<HTMLElement>("#project-inspector"),projectInspectorToggle=q<HTMLButtonElement>("#toggle-project-inspector"),projectWorkspace=q<HTMLElement>("#project-workspace");
const setProjectInspectorOpen=(open:boolean):void=>{const presentation=projectInspectorTogglePresentation(open);projectInspector.hidden=!open;projectInspectorToggle.textContent=presentation.label;projectInspectorToggle.setAttribute("aria-expanded",presentation.expanded);projectWorkspace.dataset.inspectorOpen=String(open);};
projectInspectorToggle.addEventListener("click",()=>setProjectInspectorOpen(projectInspector.hidden));
setProjectInspectorOpen(true);
for(const fieldId of["project-assignment-path","project-assignment-value","project-assignment-not-path","project-assignment-not-value"]){const input=document.createElement("input");input.id=fieldId;input.hidden=true;input.tabIndex=-1;q<HTMLFormElement>("#save-project-assignment").append(input);}
q<HTMLSelectElement>("#project-assignment-applicability").required=false;
q<HTMLInputElement>("#project-schema-id").required=false;
q<HTMLInputElement>("#project-schema-id").closest("label")!.hidden=true;
const id=(kind:string):string=>`${kind}:${crypto.randomUUID()}`;
const labels:Record<ProjectEntityKind,string>={profiles:"Shared Profiles",pages:"Pages",pageGroups:"Page Groups",events:"Events",applicabilitySets:"Applicability",flows:"Flows",fixtures:"Fixtures",schemaDrafts:"Schemas",assignments:"Assignments"};
let state:ProjectState|undefined,lastCommittedState:ProjectState|undefined,library:ProjectLibrary=projectLibrary();let canonicalRevision=0,pendingConflict:Extract<CanonicalCommitResult,{status:"conflict"}>|undefined,stagedBulk:StagedBulkRequirements|undefined,selectedKind:ProjectEntityKind="profiles",selectedId:string|undefined,projectOverview=routeParameters.get("route")==="overview",creationKind:ProjectEntityKind|undefined,removalReview:ProjectEntityRemovalReview|undefined,lifecycleStatus="",removedFocus:{kind:ProjectEntityKind;id:string}|undefined,stagedImport:StagedProjectImport|undefined,lastInvokingControl:HTMLElement|undefined,releasePreflight:ReturnType<typeof specificationPreflight>|undefined,pendingSavedSchema:{kind:"adopt";source:SavedSchemaSource}|{kind:"synchronize";review:SavedSchemaSynchronizationReview}|undefined,flowGraphBuilder:FlowGraphBuilderIntegration|undefined,executableFlowBuilder:ExecutableFlowBuilderIntegration|undefined,layeredSchemaUi:ReturnType<typeof installLayeredSchemaUi>|undefined,flowDocumentationExportUi:ReturnType<typeof installFlowDocumentationExportUi>|undefined;
let pageGroupMembershipStatus="";
let pendingPageGroupMembershipReorder:{pageId:string;pageGroupId:string;delta:number}|undefined;
const savedSchemas=():SavedSchemaSource[]=>restoreSchemaLibrary(localStorage.getItem(SCHEMA_LIBRARY_STORAGE_KEY)).filter(({published})=>published).map((schema)=>structuredClone(schema) as unknown as SavedSchemaSource);

function renderCanonicalEntityEditor(host:HTMLElement,kind:"profiles"|"schemaDrafts",entity:ProjectEntity):void{
  if(!state)return;
  const canonical=entity.canonicalSchema as CanonicalSchemaDocument|undefined;
  if(!canonical){
    if(kind==="schemaDrafts"){const section=document.createElement("section"),heading=document.createElement("h3"),summary=document.createElement("p"),initialize=document.createElement("button");heading.textContent="Initialize canonical Schema Draft";summary.textContent="Create one canonical payload definition for this project-owned Draft lineage.";initialize.type="button";initialize.textContent="Initialize canonical Schema";initialize.addEventListener("click",()=>persist(transactProject(state!,`Initialize canonical schema for ${entity.name}`,(project)=>({...project,collections:{...project.collections,schemaDrafts:project.collections.schemaDrafts.map((candidate)=>candidate.id===entity.id?{...candidate,canonicalSchema:createCanonicalSchema({id:id("canonical-schema"),contributorId:entity.id,contributorName:entity.name})}:candidate)}}))));section.append(heading,summary,initialize);host.append(section);return;}
    const review=migrateLegacyProfile(entity,{id}),section=document.createElement("section"),heading=document.createElement("h3"),summary=document.createElement("p"),list=document.createElement("ul"),confirm=document.createElement("button");
    const sourceDefinitions=Object.values(review.document.nodes).reduce((count,node)=>count+node.provenance.length,0),deduplicated=sourceDefinitions-Object.keys(review.document.nodes).length;
    section.setAttribute("aria-label","Canonical schema migration review");heading.textContent="Migrate legacy schema to one canonical property tree";summary.textContent=`${Object.keys(review.document.nodes).length} properties mapped · ${deduplicated} repeated semantic entries deduplicated with all source provenance · rules, documentation, and examples mapped · ${review.conflicts.length} incompatible definitions at generated paths · requirements, structured draft, and path constraints will be replaced atomically.`;
    for(const conflict of [...review.conflicts]){const item=document.createElement("li"),resolution=document.createElement("select");item.textContent=`${conflict.path}: ${conflict.message} `;resolution.append(new Option("Choose resolution",""),new Option("Use string","string"),new Option("Use number","number"),new Option("Use object","object"));resolution.setAttribute("aria-label",`Resolve migration conflict ${conflict.path}`);resolution.addEventListener("change",()=>{if(!resolution.value)return;review.document.nodes[conflict.propertyId]!.type=resolution.value as "string"|"number"|"object";review.conflicts.splice(review.conflicts.indexOf(conflict),1);confirm.disabled=Boolean(review.conflicts.length);});item.append(resolution);list.append(item);}
    confirm.type="button";confirm.textContent="Confirm canonical migration";confirm.disabled=Boolean(review.conflicts.length);confirm.addEventListener("click",()=>persist(confirmCanonicalMigration(state!,review)));section.append(heading,summary,list,confirm);host.append(section);return;
  }
  const editorHost=document.createElement("section");host.append(editorHost);
  mountCanonicalSchemaEditor({host:editorHost,surface:"Builder",load:()=>state!.project.collections[kind].find(({id:entityId})=>entityId===entity.id)!.canonicalSchema as CanonicalSchemaDocument,id,dispatch:(command)=>{const current=state!.project.collections[kind].find(({id:entityId})=>entityId===entity.id)!.canonicalSchema as CanonicalSchemaDocument,result=applyCanonicalCommand(current,command);if(result.status==="applied"||result.status==="rebased")persist(transactProject(state!,`${command.kind} canonical property in ${entity.name}`,(project)=>({...project,collections:{...project.collections,[kind]:project.collections[kind].map((candidate)=>candidate.id===entity.id?{...candidate,canonicalSchema:result.document,...(kind==="profiles"?{requirements:[]}:{})}:candidate)}} as typeof project)));return result;},onUndo:()=>persist(undoProjectTransaction(state!)),onRedo:()=>persist(redoProjectTransaction(state!))});
}

function renderCanonicalProfileOverview(host:HTMLElement):void{
  if(!state||selectedKind!=="profiles"||selectedId)return;const section=document.createElement("section"),heading=document.createElement("h2"),guidance=document.createElement("p"),saved=document.createElement("select"),adopt=document.createElement("button"),review=document.createElement("p");
  section.setAttribute("aria-label","Saved Schema Library adoption");heading.textContent="Adopt a published saved schema";guidance.textContent="Adoption creates one project-owned Shared Profile Draft with source lineage; new profiles use the overview's single Add Shared Profile route.";adopt.type="button";adopt.textContent="Add saved schema to project";saved.setAttribute("aria-label","Saved schema to add");saved.append(new Option("Choose a published saved schema",""),...savedSchemas().map((schema)=>new Option(`${schema.name} revision ${schema.version}`,schema.id)));adopt.addEventListener("click",()=>{const source=savedSchemas().find(({id:schemaId})=>schemaId===saved.value);if(!source){review.textContent="Choose a published saved schema for review.";return;}review.replaceChildren();review.textContent=`Review ${source.name} revision ${source.version}: one project-owned canonical draft will preserve source lineage. `;const confirm=document.createElement("button");confirm.type="button";confirm.textContent="Confirm adding saved schema";confirm.addEventListener("click",()=>{const next=adoptSavedSchema(state!,source);selectedId=next.project.collections.profiles.at(-1)!.id;persist(next);persistNavigation();});review.append(confirm);});section.append(heading,guidance,labeledControl("Saved Schema Library",saved),adopt,review);host.append(section);
}
function labeledControl(text:string,control:HTMLElement):HTMLLabelElement{const label=document.createElement("label");label.append(text,control);return label;}

function renderComposedSchemaWorkspace(host:HTMLElement,entity:ProjectEntity,kind:"pages"|"pageGroups",scope:"Page"|"Page Group"):void{
  if(!state)return;const model=composedSchemaWorkspace(state,entity,scope),section=document.createElement("section"),heading=document.createElement("h2"),summary=document.createElement("p"),columns=document.createElement("div"),rows=document.createElement("div"),propertyChoices=model.rows.flatMap(({path,effective})=>effective.definitionId?[{path,definitionId:effective.definitionId,type:effective.type}]:[]);
  section.className="composed-schema-workspace";section.setAttribute("aria-label",model.heading);section.dataset.schemaStatus=model.status;heading.textContent=model.heading;summary.setAttribute("role","status");summary.className=model.status==="blocked"?"error":"status-text";summary.textContent=`${model.status==="blocked"?"Blocked":"Ready"} · ${model.rows.length} effective properties · ${model.conflictSummary}`;columns.className="composed-schema-columns";columns.setAttribute("aria-hidden","true");for(const label of["Property","Effective definition","Source","Local state","Validation state","Actions"])columns.append(Object.assign(document.createElement("strong"),{textContent:label}));rows.setAttribute("role","table");rows.setAttribute("aria-label",`${model.heading} rows`);
  for(const row of model.rows){const wrapper=document.createElement("article"),overview=document.createElement("div"),toggle=document.createElement("button"),effective=document.createElement("span"),source=document.createElement("span"),local=document.createElement("span"),validation=document.createElement("span"),actions=document.createElement("div"),primary=document.createElement("button"),detail=document.createElement("div"),builder=document.createElement("section"),provenance=document.createElement("ol"),impact=document.createElement("div");
    wrapper.className="composed-schema-row";wrapper.dataset.effectivePropertyPath=row.path;wrapper.dataset.validationState=row.validationState;overview.className="composed-schema-row-overview";overview.setAttribute("role","row");toggle.type=primary.type="button";toggle.className="composed-schema-row-toggle";toggle.textContent=row.path;toggle.setAttribute("aria-expanded","false");effective.textContent=effectivePropertySummary(row.effective)||"constraint";source.textContent=row.source;local.textContent=Object.keys(row.local).length>1?JSON.stringify(row.local):"Inherited";validation.textContent=`${row.validationState} · ${row.message}`;actions.className="composed-schema-row-actions";primary.textContent=row.action==="override"?"Override here":row.action==="reset"?"Reset to parents":"Remove local property";detail.className="composed-schema-row-detail";detail.hidden=true;detail.setAttribute("aria-label",`${row.path} stacked row detail`);
    provenance.setAttribute("aria-label",`${row.path} provenance`);provenance.append(Object.assign(document.createElement("li"),{textContent:"Provenance"}));for(const origin of row.provenance)provenance.append(Object.assign(document.createElement("li"),{textContent:`${origin.scope} ${origin.contributorName} · ${origin.state}`}));for(const repair of row.repairs){const control=document.createElement("button");control.type="button";control.textContent=repair.label;control.addEventListener("click",()=>{const match=(['pages','pageGroups','profiles','events'] as ProjectEntityKind[]).find((collection)=>(state!.project.collections[collection] as ProjectEntity[]).some(({id})=>id===repair.contributorId));if(match){selectedKind=match;selectedId=repair.contributorId;persistNavigation();render();}});impact.append(control);}
    const openDetail=()=>{detail.hidden=false;toggle.setAttribute("aria-expanded","true");builder.querySelector<HTMLElement>("input,select,button")?.focus();};toggle.addEventListener("click",()=>{detail.hidden=!detail.hidden;toggle.setAttribute("aria-expanded",String(!detail.hidden));if(detail.hidden)toggle.focus();});primary.addEventListener("click",()=>{if(row.action==="override"){openDetail();return;}impact.replaceChildren();impact.append(`${row.action==="reset"?"Reset":"Remove"} preview: effective parent value ${row.inherited?effectivePropertySummary(row.inherited):"none"}; affected Page instances will recompile; outputs become stale; one Undo action will be available. `);const confirm=document.createElement("button");confirm.type="button";confirm.textContent=row.action==="reset"?"Confirm reset to parents":"Confirm remove local property";confirm.addEventListener("click",()=>persist(resetComposedSchemaLocalProperty(state!,kind,entity.id,row.path)));impact.append(confirm);openDetail();});const detailAction=document.createElement("button");detailAction.type="button";detailAction.textContent=primary.textContent;detailAction.addEventListener("click",()=>primary.click());actions.append(primary);overview.append(toggle,effective,source,local,validation,actions);detail.append(builder,detailAction,provenance,impact);wrapper.append(overview,detail);rows.append(wrapper);mountComposedSchemaFacetBuilder({host:builder,path:row.path,local:row.local,effective:row.effective,inherited:row.inherited,propertyChoices,onSave:(facets)=>persist(saveComposedSchemaLocalFacets(state!,kind,entity.id,row.path,facets))});
  }
  section.append(heading,summary,columns,rows);host.append(section);const canonical=entity.canonicalSchema as CanonicalSchemaDocument|undefined;if(canonical){const canonicalHost=document.createElement("section");canonicalHost.setAttribute("aria-label",`${entity.name} canonical schema contribution`);section.append(canonicalHost);mountCanonicalSchemaEditor({host:canonicalHost,surface:"Builder",load:()=>state!.project.collections[kind].find(({id:entityId})=>entityId===entity.id)!.canonicalSchema as CanonicalSchemaDocument,id,dispatch:(command)=>{const current=state!.project.collections[kind].find(({id:entityId})=>entityId===entity.id)!.canonicalSchema as CanonicalSchemaDocument,result=applyCanonicalCommand(current,command);if(result.status==="applied"||result.status==="rebased")persist(transactProject(state!,`${command.kind} canonical property in ${entity.name}`,(project)=>({...project,collections:{...project.collections,[kind]:project.collections[kind].map((candidate)=>candidate.id===entity.id?{...candidate,canonicalSchema:result.document}:candidate)}} as typeof project)));return result;},onUndo:()=>persist(undoProjectTransaction(state!)),onRedo:()=>persist(redoProjectTransaction(state!))});}
}

function renderPageGroupMembershipEditor(host:HTMLElement,page:ProjectEntity):void{
  if(!state)return;const section=document.createElement("section"),heading=document.createElement("h3"),guidance=document.createElement("p"),impact=document.createElement("p"),picker=document.createElement("section"),search=document.createElement("input"),results=document.createElement("div"),add=document.createElement("button"),menu=document.createElement("details"),menuSummary=document.createElement("summary"),menuAdd=document.createElement("button"),stack=document.createElement("ol"),effective=document.createElement("ol"),evaluation=document.createElement("section"),observation=document.createElement("select"),changePreview=document.createElement("select"),evaluateMembership=document.createElement("button"),evaluationResult=document.createElement("div"),memberships=orderedPageGroupIds(state.project,page.id),migrationRequired=requiresPageGroupMembershipMigration(state.project,page.id),groups=new Map(state.project.collections.pageGroups.map((group)=>[group.id,group]));
  section.setAttribute("aria-label","Page Group rule stack");heading.textContent="Page Group rule stack";guidance.textContent="Rules apply from top to bottom, general to specific. Later Page Groups may only make legal guarded refinements.";impact.setAttribute("role","status");impact.setAttribute("aria-label","Page Group membership impact");impact.textContent=pageGroupMembershipStatus;picker.setAttribute("aria-label","Add to Page Group picker");picker.hidden=true;search.type="search";search.setAttribute("aria-label","Search Page Groups to add");results.setAttribute("aria-label","Page Group membership search results");add.type=menuAdd.type="button";add.textContent=menuAdd.textContent="Add to Page Group";menu.setAttribute("aria-label",`${page.name} context menu`);menuSummary.textContent="Page actions";menu.append(menuSummary,menuAdd);
  const showPicker=()=>{picker.hidden=false;search.focus();},renderResults=()=>{const term=search.value.trim().toLowerCase();results.replaceChildren(...state!.project.collections.pageGroups.filter((group)=>!memberships.includes(group.id)&&group.name.toLowerCase().includes(term)).map((group)=>{const control=document.createElement("button"),constraints=((group.canonicalSchema as {nodes?:Record<string,unknown>}|undefined)?.nodes?Object.keys((group.canonicalSchema as {nodes:Record<string,unknown>}).nodes).length:((group.schemaConstraints as unknown[]|undefined)??[]).length),applicability=state!.project.collections.applicabilitySets.find(({id})=>id===group.applicabilitySetId);control.type="button";control.textContent=`${group.name} · Purpose ${String(group.purpose??"Page schema contribution")} · Applicability ${applicability?.name??"Always"} · Prospective rule impact ${constraints} properties`;control.addEventListener("click",()=>persist(addPageGroupMembership(state!,page.id,group.id)));return control;}));};search.addEventListener("input",renderResults);add.addEventListener("click",showPicker);menuAdd.addEventListener("click",showPicker);picker.append(search,results);renderResults();
  effective.setAttribute("aria-label","Effective Page Group contribution order");for(const[index,groupId]of memberships.entries()){const group=groups.get(groupId),item=document.createElement("li"),constraints=(group?.schemaConstraints as {path?:string;type?:string;allowedValues?:unknown[]}[]|undefined)??[];item.dataset.effectivePageGroupId=groupId;item.textContent=`${index+1}. ${group?.name??groupId} · ${constraints.map(({path,type,allowedValues})=>`${path??"property"}${type?` type ${type}`:""}${allowedValues?` allowed ${allowedValues.join(", ")}`:""}`).join("; ")||"no local schema contribution"}`;effective.append(item);}
  evaluation.setAttribute("aria-label","Page Group effective schema evaluation");observation.setAttribute("aria-label","Page Group applicability observation");observation.append(new Option("Retail observation","retail"),new Option("Trade observation","trade"),new Option("Overlapping observation","overlap"));changePreview.setAttribute("aria-label","Page Group proposed schema change");changePreview.append(new Option("Stored contributions","stored"),new Option("Retail unsafe type number","unsafe-type"),new Option("Retail unsafe allowed value 4","unsafe-allowed"));evaluateMembership.type="button";evaluateMembership.textContent="Evaluate effective Page Group schema";evaluationResult.setAttribute("aria-label","Rendered Page Group compilation evidence");evaluateMembership.addEventListener("click",()=>{const observed=observation.value==="retail"?{customer_type:"retail"}:observation.value==="trade"?{customer_type:"trade"}:{customer_type:"retail",market:"overlap"},rawContributors=layeredContributorsForPath(state!,layeredContributorPath(state!,page,"Page"),observed),contributors=rawContributors.map((entry)=>entry.scope!=="Page Group"||!entry.applicabilityConditional||entry.active===false||changePreview.value==="stored"?entry:{...entry,constraints:[{path:"/funnel_step",...(changePreview.value==="unsafe-type"?{type:"number"}:{allowedValues:["4"]})}]}),compiled=compileLayeredSchema(contributors,{eventId:"event:page-group-membership-preview",eventRole:"interaction"}),included=compiled.provenance.filter(({scope})=>scope==="Page Group"),inactive=compiled.exclusions.filter(({contributorId},index,all)=>all.findIndex((candidate)=>candidate.contributorId===contributorId)===index);evaluationResult.replaceChildren();const summary=document.createElement("p"),properties=document.createElement("p"),provenance=document.createElement("ol"),effectiveProperties=document.createElement("ul"),issues=document.createElement("ul");summary.textContent=`Status ${compiled.status}. Included stack: ${included.map(({contributorName})=>contributorName).join(", ")||"none"}. Inactive memberships: ${inactive.map(({contributorName})=>contributorName).join(", ")||"none"}. Stored order: ${memberships.map((id)=>groups.get(id)?.name??id).join(" → ")}.`;properties.textContent=`Effective funnel_step: ${String(compiled.properties["/funnel_step"]?.allowedValues?.join(", ")??"unavailable")}.`;provenance.setAttribute("aria-label","Rendered Page Group ordered provenance");for(const contributor of included){const item=document.createElement("li");item.dataset.compiledPageGroupId=contributor.contributorId;item.textContent=`${contributor.contributorName} · ${contributor.scope}`;provenance.append(item);}effectiveProperties.setAttribute("aria-label","Rendered effective Page Group properties");for(const[path,property]of Object.entries(compiled.properties)){const item=document.createElement("li");item.dataset.effectivePropertyPath=path;item.textContent=`${path}: ${JSON.stringify(property)}`;effectiveProperties.append(item);}for(const conflict of compiled.conflicts){const item=document.createElement("li");item.textContent=`${conflict.path}: ${conflict.contributors.join(" before ")} · ${conflict.message}. `;for(const contributorName of conflict.contributors){const contributor=state!.project.collections.pageGroups.find(({name})=>name===contributorName),repair=document.createElement("a");repair.textContent=`Repair ${contributorName}`;repair.href=`?kind=pageGroups&entity=${encodeURIComponent(contributor?.id??contributorName)}&field=canonicalSchema`;item.append(repair," ");}issues.append(item);}evaluationResult.append(summary,properties,provenance,effectiveProperties,issues);});evaluation.append(observation,changePreview,evaluateMembership,evaluationResult);
  for(const[index,groupId]of memberships.entries()){const group=groups.get(groupId),row=document.createElement("li"),summary=document.createElement("span"),actions=document.createElement("div"),open=document.createElement("button"),earlier=document.createElement("button"),later=document.createElement("button"),remove=document.createElement("button");row.dataset.pageGroupMembershipId=groupId;row.tabIndex=-1;summary.textContent=`${index+1}. ${group?.name??groupId} · ${((group?.schemaConstraints as unknown[]|undefined)??[]).length} effective contributions or conflicts`;actions.className="membership-row-actions";for(const control of[open,earlier,later,remove])control.type="button";open.textContent="Open Page Group";earlier.textContent="Move earlier";later.textContent="Move later";remove.textContent="Remove";earlier.disabled=migrationRequired||index===0;later.disabled=migrationRequired||index===memberships.length-1;remove.disabled=migrationRequired;open.addEventListener("click",()=>{selectedKind="pageGroups";selectedId=groupId;persistNavigation();render();});const reorder=(delta:number)=>{pendingPageGroupMembershipReorder={pageId:page.id,pageGroupId:groupId,delta};pageGroupMembershipStatus=`Impact preview before commit: affected properties for ${page.name}; affected Page instances and compiled targets will be recomputed; exports and evidence become stale; project remains Draft with Undo available.`;render();queueMicrotask(()=>document.querySelector<HTMLButtonElement>("[data-confirm-membership-reorder]")?.focus());},keyboardReorder=(event:KeyboardEvent,delta:number)=>{if(event.key!=="Enter"&&event.key!==" ")return;event.preventDefault();reorder(delta);};earlier.addEventListener("click",()=>reorder(-1));later.addEventListener("click",()=>reorder(1));earlier.addEventListener("keydown",(event)=>keyboardReorder(event,-1));later.addEventListener("keydown",(event)=>keyboardReorder(event,1));remove.addEventListener("click",()=>{const review=inspectPageGroupMembershipRemoval(state!.project,page.id,groupId);if(review.blocked){impact.textContent=review.message;for(const action of review.actions){const repair=document.createElement("button");repair.type="button";repair.textContent=action.label;repair.addEventListener("click",()=>persist(action.kind==="move-frame"?moveFlowPageFrame(state!,action.flowId,action.frameId,{pageGroupId:action.pageGroupId!,y:Number(((state!.project.documentationFlowGraphs as Record<string,{pageFrames?:{id:string;position:{y:number}}[]}>)[action.flowId]?.pageFrames??[]).find(({id})=>id===action.frameId)?.position.y??90)}):removeFlowPageFrame(state!,action.flowId,action.frameId)));impact.append(" ",repair);}return;}pageGroupMembershipStatus=`Changed membership ${group?.name??groupId}; affected schema targets and Page instances updated; exports and evidence are stale; Draft status retained; Undo available.`;persist(removePageGroupMembership(state!,page.id,groupId));});actions.append(open,earlier,later,remove);row.append(summary,actions);stack.append(row);}
  if(pendingPageGroupMembershipReorder?.pageId===page.id){const pending=pendingPageGroupMembershipReorder,confirm=document.createElement("button"),cancel=document.createElement("button");confirm.type=cancel.type="button";confirm.textContent="Confirm membership reorder";confirm.dataset.confirmMembershipReorder="true";cancel.textContent="Cancel membership reorder";confirm.addEventListener("click",()=>{pendingPageGroupMembershipReorder=undefined;pageGroupMembershipStatus=`Changed membership order for ${page.name}; affected schema targets and Page instances updated; exports and evidence are stale; Draft status retained; Undo available.`;persist(movePageGroupMembership(state!,pending.pageId,pending.pageGroupId,pending.delta));queueMicrotask(()=>document.querySelector<HTMLElement>(`[data-page-group-membership-id="${CSS.escape(pending.pageGroupId)}"]`)?.focus());});cancel.addEventListener("click",()=>{pendingPageGroupMembershipReorder=undefined;pageGroupMembershipStatus="";render();});impact.append(" ",confirm," ",cancel);}
  const legacy=state.project.collections.pageGroups.some((group)=>((group.pageIds as string[]|undefined)??[]).includes(page.id)),migration=stagePageGroupMembershipMigration(state.project,page.id);if(legacy||migration.missingPageGroupIds.length||migration.duplicatePageGroupIds.length){const review=document.createElement("section"),summary=document.createElement("p"),confirm=document.createElement("button");review.setAttribute("aria-label","Page Group membership migration review");summary.textContent=`Proposed ordered membership: ${migration.proposedPageGroupIds.map((id)=>groups.get(id)?.name??id).join(", ")}. Page-owned order is preserved before group-only memberships. ${migration.missingPageGroupIds.length?`Missing references: ${migration.missingPageGroupIds.join(", ")}.`:"No membership will be lost."}`;confirm.type="button";confirm.textContent="Confirm ordered membership migration";confirm.disabled=Boolean(migration.missingPageGroupIds.length||migration.duplicatePageGroupIds.length);confirm.addEventListener("click",()=>persist(confirmPageGroupMembershipMigration(state!,migration)));review.append(summary,confirm);section.append(review);}
  section.append(heading,guidance,add,menu,picker,stack,effective,evaluation,impact);host.append(section);
}

function writeProjectState(next:ProjectState):void{const result=commitCanonicalProjectState(localStorage,next,{expectedRevision:canonicalRevision,pendingLabel:next.history.undo.at(-1)?.label??"Project edit",...(lastCommittedState?{base:lastCommittedState}:{})});if(result.status==="conflict"){pendingConflict=result;state=result.current;lastCommittedState=structuredClone(result.current);canonicalRevision=result.revision;throw new Error(`Revision conflict: current ${result.revision}; pending ${result.pendingLabel}`);}canonicalRevision=result.revision;pendingConflict=undefined;releasePreflight=undefined;lastCommittedState=structuredClone(next);if(library.activeProjectId){library=replaceActiveProjectState(library,next,result.revision);localStorage.setItem(PROJECT_LIBRARY_STORAGE_KEY,serializeProjectLibrary(library));}}
function showConflictReview():void{if(!pendingConflict)return;const inspection=inspectCanonicalProjectConflict(pendingConflict),dialog=q<HTMLDialogElement>("#project-conflict-review"),fields=q("#project-conflict-fields");q("#project-conflict-summary").textContent=`${pendingConflict.pendingLabel}: ${inspection.pendingFields.length} pending fields, ${inspection.currentFields.length} newer fields, ${inspection.conflictingFields.length} same-field conflicts.`;fields.querySelectorAll("label").forEach((label)=>label.remove());for(const field of inspection.conflictingFields){const label=document.createElement("label"),input=document.createElement("input");input.type="checkbox";input.value=field;label.append(input,` Use pending value for ${field}`);fields.append(label);}if(!dialog.open)dialog.showModal();dialog.querySelector<HTMLElement>("h2")?.focus();}
function persist(next:ProjectState):void{state=next;try{writeProjectState(next);q("#project-state").textContent=`Saved · revision ${canonicalRevision}`;q<HTMLButtonElement>("#retry-save").hidden=true;}catch(error){if(!pendingConflict)state=next.draft?{...next,draft:{...next.draft,status:"Save failed"}}:next;q("#project-state").textContent=pendingConflict?`Conflict at revision ${canonicalRevision}; pending ${pendingConflict.pendingLabel}`:"Save failed";q<HTMLButtonElement>("#retry-save").hidden=false;if(pendingConflict)showConflictReview();}render();renderAssignments();}
function restore():void{const stored=localStorage.getItem(STORAGE_KEY);let singleton:ProjectState|undefined,revision=0;if(stored)try{const envelope=restoreCanonicalProjectEnvelope(stored);singleton=restoreCanonicalProjectState(stored);revision=envelope?.revision??0;}catch{localStorage.removeItem(STORAGE_KEY);}const rawNavigation=localStorage.getItem(NAVIGATION_KEY);let legacyNavigation:{kind:string;id?:string}|undefined;if(rawNavigation)try{legacyNavigation=JSON.parse(rawNavigation) as {kind:string;id?:string};}catch{localStorage.removeItem(NAVIGATION_KEY);}library=migrateSingletonProject(restoreProjectLibrary(localStorage.getItem(PROJECT_LIBRARY_STORAGE_KEY)),singleton?{state:singleton,revision,...(legacyNavigation?{navigation:legacyNavigation}:{})}:undefined);localStorage.setItem(PROJECT_LIBRARY_STORAGE_KEY,serializeProjectLibrary(library));const active=library.activeProjectId?library.projects[library.activeProjectId]:undefined;state=active?structuredClone(active.state):undefined;lastCommittedState=state?structuredClone(state):undefined;canonicalRevision=active?.revision??0;const navigation=library.activeProjectId?resolveProjectNavigation(library,library.activeProjectId):undefined;if(navigation){selectedKind=(navigation.kind as ProjectEntityKind)??selectedKind;selectedId=navigation.id;}}
function persistNavigation():void{if(!state||library.activeProjectId!==state.project.id)return;const navigation={kind:selectedKind,...(selectedId?{id:selectedId}:{})};library=recordProjectNavigation(library,state.project.id,navigation);localStorage.setItem(PROJECT_LIBRARY_STORAGE_KEY,serializeProjectLibrary(library));localStorage.setItem(NAVIGATION_KEY,JSON.stringify({projectId:state.project.id,...navigation}));}
function entitySearchText(value:unknown):string{return JSON.stringify(value).toLowerCase();}
function entitiesForKind(kind:ProjectEntityKind):ProjectEntity[]{if(!state)return[];return kind==="assignments"?searchProjectAssignments(state.project,"").rows:state.project.collections[kind] as ProjectEntity[];}
type EditorField={key:string;label:string;type?:"text"|"number"|"textarea"|"checkbox"|"json"|"condition"|"flow-role";collection?:ProjectEntityKind;multiple?:boolean};
const editorFields:Record<ProjectEntityKind,EditorField[]>={
  profiles:[],pages:[{key:"environment",label:"Environment"},{key:"host",label:"Host matcher"},{key:"pathname",label:"Path matcher"},{key:"query",label:"Query matcher"},{key:"hash",label:"Hash matcher"},{key:"spa",label:"SPA route",type:"checkbox"},{key:"expectedEventIds",label:"Expected events",collection:"events",multiple:true},{key:"profileIds",label:"Requirement profiles",collection:"profiles",multiple:true},{key:"applicabilitySetId",label:"Applicability Set",collection:"applicabilitySets"}],
  pageGroups:[{key:"environment",label:"Environment"},{key:"matcher",label:"Membership matcher"},{key:"profileIds",label:"Requirement profiles",collection:"profiles",multiple:true},{key:"applicabilitySetId",label:"Applicability Set",collection:"applicabilitySets"}],
  events:[{key:"sourceId",label:"Source"},{key:"eventName",label:"Canonical event name"},{key:"role",label:"Documentary role",type:"flow-role"},{key:"trigger",label:"Default documentary trigger"},{key:"schemaDraftId",label:"Reusable payload schema",collection:"schemaDrafts"},{key:"target",label:"Validation target"},{key:"occurrencePolicy",label:"Occurrence policy"},{key:"profileIds",label:"Requirement profiles",collection:"profiles",multiple:true},{key:"applicabilitySetId",label:"Applicability Set",collection:"applicabilitySets"}],
  applicabilitySets:[{key:"priority",label:"Priority",type:"number"},{key:"fallback",label:"Fallback",type:"checkbox"},{key:"condition",label:"Nested All / Any / Not condition",type:"condition"}],
  flows:[{key:"entryCondition",label:"Entry condition",type:"condition"},{key:"exitCondition",label:"Exit condition",type:"condition"},{key:"timeoutMinutes",label:"Timeout minutes",type:"number"},{key:"correlationField",label:"Correlation field"},{key:"profileIds",label:"Requirement profiles",collection:"profiles",multiple:true},{key:"applicabilitySetId",label:"Applicability Set",collection:"applicabilitySets"}],
  fixtures:[{key:"mode",label:"Fixture mode"},{key:"context",label:"Context",type:"json"},{key:"observations",label:"Ordered observations",type:"json"},{key:"payload",label:"Payload",type:"json"},{key:"expected",label:"Expected winner, step, schema and issues",type:"json"},{key:"releasePolicy",label:"Release policy"}],
  schemaDrafts:[],
  assignments:[{key:"schemaDraftId",label:"Schema",collection:"schemaDrafts"},{key:"eventId",label:"Event",collection:"events"},{key:"applicabilitySetId",label:"Applicability Set",collection:"applicabilitySets"},{key:"priority",label:"Priority",type:"number"},{key:"versionPolicy",label:"Version policy"},{key:"schemaRevision",label:"Pinned schema revision",type:"number"}],
};
function fieldControl(field:EditorField,entity:ProjectEntity):HTMLInputElement|HTMLTextAreaElement|HTMLSelectElement|HTMLFieldSetElement{if(field.type==="flow-role"){const select=document.createElement("select");select.name=field.key;select.append(new Option("Interaction","interaction"),new Option("Context-setting","context-setting"));select.value=entity[field.key]==="context-setting"?"context-setting":"interaction";return select;}if(field.collection&&state){const select=document.createElement("select");select.name=field.key;select.multiple=Boolean(field.multiple);for(const optionEntity of state.project.collections[field.collection] as ProjectEntity[]){if(optionEntity.id===entity.id)continue;const option=document.createElement("option");option.value=optionEntity.id;option.textContent=optionEntity.name;option.selected=field.multiple?(entity[field.key] as string[]|undefined)?.includes(optionEntity.id)??false:entity[field.key]===optionEntity.id;select.append(option);}return select;}if(field.type==="condition"){const group=document.createElement("fieldset"),operator=document.createElement("select"),rows=document.createElement("div"),add=document.createElement("button"),condition=entity[field.key] as Condition|undefined;group.name=field.key;group.dataset.conditionBuilder="true";operator.dataset.groupOperator="true";for(const value of["all","any","not"]){const option=document.createElement("option");option.value=value;option.textContent=value==="all"?"All conditions":value==="any"?"Any condition":"Not";operator.append(option);}operator.value=condition&&condition.kind!=="predicate"?condition.kind:"all";const appendPredicate=(predicate?:{field?:string;operator?:string;value?:unknown;values?:readonly unknown[];pattern?:string},negated=false)=>{const row=document.createElement("div"),not=document.createElement("input"),path=document.createElement("input"),comparison=document.createElement("select"),value=document.createElement("input");row.dataset.predicate="true";not.type="checkbox";not.checked=negated;not.setAttribute("aria-label","Negate predicate");path.value=predicate?.field??"";path.placeholder="Context field";path.setAttribute("aria-label","Predicate field");for(const name of["exists","does not exist","equals","does not equal","is one of","matches pattern","greater than","at least","less than","at most","contains","glob","regex"]){const option=document.createElement("option");option.value=name;option.textContent=name;comparison.append(option);}comparison.value=predicate?.operator??"equals";comparison.setAttribute("aria-label","Predicate operator");value.value=predicate?.values?.join(", ")??predicate?.pattern??String(predicate?.value??"");value.setAttribute("aria-label","Predicate value");row.append(not,path,comparison,value);rows.append(row);};const visit=(node:Condition|undefined,negated=false):void=>{if(!node)return;if(node.kind==="predicate")appendPredicate(node,negated);else for(const child of node.conditions)visit(child,node.kind==="not"?!negated:negated);};visit(condition);if(!rows.children.length)appendPredicate();add.type="button";add.textContent="Add predicate";add.addEventListener("click",()=>appendPredicate());group.append(operator,rows,add);return group;}if(field.type==="textarea"||field.type==="json"){const textarea=document.createElement("textarea");textarea.name=field.key;textarea.rows=field.type==="json"?6:3;textarea.value=field.type==="json"?JSON.stringify(entity[field.key]??(field.key==="observations"?[]:{}),null,2):String(entity[field.key]??"");return textarea;}const input=document.createElement("input");input.name=field.key;input.type=field.type??"text";if(field.type==="checkbox")input.checked=Boolean(entity[field.key]);else input.value=String(entity[field.key]??"");return input;}
function editorValue(field:EditorField,control:Element):unknown{if(control instanceof HTMLFieldSetElement&&field.type==="condition"){const kind=control.querySelector<HTMLSelectElement>("[data-group-operator]")!.value as "all"|"any"|"not",conditions=Array.from(control.querySelectorAll<HTMLElement>("[data-predicate]"),(row)=>{const inputs=row.querySelectorAll<HTMLInputElement>("input"),operator=row.querySelector("select")!.value,text=inputs[2]!.value.trim(),predicate:Condition={kind:"predicate",field:inputs[1]!.value.trim(),operator,...(operator==="is one of"?{values:text.split(",").map((entry)=>entry.trim()).filter(Boolean)}:operator==="matches pattern"?{pattern:text}:!["exists","does not exist"].includes(operator)?{value:["greater than","at least","less than","at most"].includes(operator)?Number(text):text}:{})};return inputs[0]!.checked?{kind:"not" as const,conditions:[predicate]}:predicate;}).filter((condition)=>condition.kind!=="predicate"||Boolean(condition.field));return{kind,conditions};}if(control instanceof HTMLSelectElement)return field.multiple?Array.from(control.selectedOptions,({value})=>value):control.value;if(control instanceof HTMLInputElement&&field.type==="checkbox")return control.checked;if(field.type==="number")return Number((control as HTMLInputElement).value);if(field.type==="json")return JSON.parse((control as HTMLTextAreaElement).value||"{}");return(control as HTMLInputElement|HTMLTextAreaElement).value;}
function renderFixtureExecution(form:HTMLFormElement,fixture:ProjectEntity):void{const section=document.createElement("section"),heading=document.createElement("h3"),evidence=document.createElement("p"),run=document.createElement("button"),result=document.createElement("output");section.setAttribute("aria-label","Fixture execution");heading.textContent="Production evaluator replay";const observations=(fixture.observations as Record<string,unknown>[]|undefined)??[],expected=(fixture.expected as Record<string,unknown>|undefined)??{};evidence.textContent=`Captured observation: ${observations.map(({eventName})=>String(eventName??"unnamed")).join(", ")||"none"} · Proposed assertions: status ${String(expected.status??"not set")}; issueCodes ${JSON.stringify(expected.issueCodes??[])}`;run.type="button";run.textContent="Run Fixture";result.id="fixture-run-result";result.setAttribute("aria-live","polite");run.addEventListener("click",()=>{if(!state)return;const compiled=compileSpecificationProject({...createCanonicalProjectEnvelope(state.project,state.draft?.id??"published"),revision:canonicalRevision});if(compiled.status==="blocked"){result.textContent=`Run blocked: ${compiled.diagnostics.map(({field})=>field).join(", ")}`;return;}const execution=runProductionFixture(compiled.plan,fixture),last=execution.steps.at(-1),capturedIdentity=String(fixture.evaluationResultIdentity??"not recorded"),replayIdentity=last?.actual.resultIdentity??"not evaluated",differences=execution.steps.flatMap((step)=>step.differences);result.textContent=`${execution.status.toUpperCase()} · captured evaluator result ${capturedIdentity} · replay result ${replayIdentity} · ${differences.length?differences.join("; "):"status and issueCodes assertions matched"}`;});section.append(heading,evidence,run,result);form.append(section);}
function renderSelectedEntityEditor(content:HTMLElement,entity:ProjectEntity):void{if(!state)return;const section=document.createElement("section"),heading=document.createElement("h2"),form=document.createElement("form"),nameLabel=document.createElement("label"),name=document.createElement("input"),actions=document.createElement("div"),save=document.createElement("button"),duplicate=document.createElement("button"),usage=document.createElement("p");section.className="contextual-editor";heading.textContent=`Edit ${labels[selectedKind].replace(/s$/,"")}`;name.name="name";name.required=true;name.value=entity.name;nameLabel.textContent="Name";nameLabel.append(name);form.append(nameLabel);for(const field of editorFields[selectedKind]){const label=document.createElement("label"),control=fieldControl(field,selectedKind==="flows"&&field.key==="pageGroupIds"?{...entity,pageGroupIds:flowPageGroupLaneIds(state.project,entity.id)}:entity);label.textContent=field.label;label.append(control);form.append(label);}if(selectedKind==="fixtures")renderFixtureExecution(form,entity);if(selectedKind==="schemaDrafts"){const status=document.createElement("p");status.textContent="Draft · canonical payload definition and project lineage";status.setAttribute("role","status");form.append(status);}save.type="submit";save.textContent="Save complete entity";duplicate.type="button";duplicate.textContent="Duplicate";usage.textContent=`Where used: ${whereUsed(entity.id).join(", ")||"None"}`;actions.className="editor-actions";actions.append(save,duplicate);form.append(actions,usage);form.addEventListener("submit",(event)=>{event.preventDefault();if(!state)return;try{const update:Record<string,unknown>={name:name.value.trim()};for(const field of editorFields[selectedKind])update[field.key]=editorValue(field,form.elements.namedItem(field.key) as Element);const laneIds=selectedKind==="flows"?update.pageGroupIds as string[]:undefined;if(laneIds)delete update.pageGroupIds;const edited=transactProject(state,`Edit ${entity.name}`,(project)=>({...project,collections:{...project.collections,[selectedKind]:(project.collections[selectedKind] as ProjectEntity[]).map((candidate)=>{if(candidate.id!==entity.id)return candidate;const merged={...candidate,...update};if(selectedKind==="flows")delete merged.pageGroupIds;return merged;})}} as typeof project));persist(selectedKind==="flows"?applyFlowPageGroupLaneSelection(edited,entity.id,laneIds):edited);}catch(error){q("#project-state").textContent=error instanceof Error?error.message:String(error);}});duplicate.addEventListener("click",()=>{if(!state)return;const{id:ignored,...copy}=entity;persist(addProjectEntity(state,selectedKind,{...structuredClone(copy),name:`${entity.name} copy`},id));});section.append(heading,form);content.append(section);if(selectedKind==="profiles"||selectedKind==="schemaDrafts")renderCanonicalEntityEditor(content,selectedKind,entity);}

function renderTree():void{const tree=q<HTMLUListElement>("#project-tree");tree.replaceChildren();if(!state)return;for(const kind of Object.keys(labels) as ProjectEntityKind[]){const item=document.createElement("li"),button=document.createElement("button"),count=kind==="assignments"?searchProjectAssignments(state.project,"").count:state.project.collections[kind].length;button.type="button";button.textContent=`${labels[kind]} (${count})`;button.dataset.kind=kind;button.setAttribute("aria-current",String(!projectOverview&&kind===selectedKind));button.addEventListener("click",()=>openCollectionOverview(kind));item.append(button);tree.append(item);}const release=document.createElement("li"),button=document.createElement("button");button.type="button";button.textContent=`Releases (${state.project.releases.length})`;button.dataset.kind="releases";release.append(button);tree.append(release);}

function renderCollectionGuidance(content:HTMLElement):void{if(!state)return;const entityName:Record<ProjectEntityKind,string>={profiles:"Profile",applicabilitySets:"Applicability Set",pages:"Page",pageGroups:"Page",events:"Event",flows:"Flow",assignments:"Assignment",fixtures:"Fixture",schemaDrafts:"Schema"},name=entityName[selectedKind],section=document.createElement("section");section.className="project-guidance";if(name){const guidance=entityPurposeGuidance(name),heading=document.createElement("h2"),purpose=document.createElement("p"),example=document.createElement("p"),prerequisites=document.createElement("p"),usedBy=document.createElement("p"),distinction=document.createElement("p");heading.textContent=`About ${name}`;purpose.textContent=guidance.purpose;example.textContent=`Example: ${guidance.example}`;prerequisites.textContent=`Prerequisites: ${guidance.prerequisites.join(", ")}`;usedBy.textContent=guidance.usedBy;distinction.textContent=`Unlike ${guidance.distinguishesFrom}, this entity ${guidance.purpose.toLowerCase()}`;section.append(heading,purpose,example,prerequisites,usedBy,distinction);}if((Object.values(state.project.collections) as ProjectEntity[][]).every((entities)=>entities.length===0)){const guidance=projectAuthoringGuidance(state.project),heading=document.createElement("h2"),tasks=document.createElement("div"),map=document.createElement("ol"),next=document.createElement("button"),reason=document.createElement("p"),worked=document.createElement("details"),workedSummary=document.createElement("summary"),workedBody=document.createElement("p");heading.textContent="Build Retail and Trade by task";tasks.className="task-entry-points";for(const task of guidance.tasks){const button=document.createElement("button");button.type="button";button.textContent=task.label;tasks.append(button);}map.className="specification-map";for(const stage of guidance.map){const item=document.createElement("li");item.textContent=`${stage.name}: ${stage.complete?"Complete":`Blocked — ${stage.blocker}`}`;map.append(item);}next.type="button";next.className="primary-continue";next.textContent=guidance.continue.label;reason.textContent=`${guidance.continue.reason} Unlocks: ${guidance.continue.unlocks}.`;workedSummary.textContent="Retail and Trade worked example";workedBody.textContent="One shared Checkout confirmation Page and Purchase Event combine with Sitewide, Retail, and Trade Profiles; two Flows route Assignments into positive and negative Fixtures, Coverage, Release, and published-plan Live.";worked.append(workedSummary,workedBody);section.append(heading,tasks,map,next,reason,worked);}if(section.childElementCount)content.append(section);}

function replaceProjectRoute(kind:ProjectEntityKind,entityId?:string,action?:"add"):void{if(!state)return;const url=new URL(location.href);url.searchParams.set("project",state.project.id);if(action)url.searchParams.set("route",action);else url.searchParams.delete("route");url.searchParams.set("kind",kind);if(entityId)url.searchParams.set("entity",entityId);else url.searchParams.delete("entity");history.replaceState(null,"",url);}
function openCollectionOverview(kind:ProjectEntityKind,focusId?:string):void{projectOverview=false;creationKind=undefined;removalReview=undefined;selectedKind=kind;selectedId=undefined;persistNavigation();replaceProjectRoute(kind);render();queueMicrotask(()=>{const target=focusId?document.querySelector<HTMLElement>(`[data-entity-id="${CSS.escape(focusId)}"]`):document.querySelector<HTMLElement>(`[data-add-kind="${kind}"]`);target?.focus({preventScroll:true});});}
function openProjectEntityWorkspace(kind:ProjectEntityKind,entityId:string):void{projectOverview=false;creationKind=undefined;removalReview=undefined;selectedKind=kind;selectedId=entityId;persistNavigation();replaceProjectRoute(kind,entityId);render();queueMicrotask(()=>document.querySelector<HTMLElement>(`[data-project-entity-workspace="${CSS.escape(entityId)}"] h1`)?.focus({preventScroll:true}));}
function openProjectCollectionCreation(kind:ProjectEntityKind):void{projectOverview=false;creationKind=kind;removalReview=undefined;selectedKind=kind;selectedId=undefined;persistNavigation();replaceProjectRoute(kind,undefined,"add");render();}
function projectCreationFieldControl(field:ProjectCollectionCreationField):HTMLInputElement|HTMLTextAreaElement|HTMLSelectElement{if(field.collection&&state){const select=document.createElement("select");select.name=field.key;select.multiple=Boolean(field.multiple);for(const entity of state.project.collections[field.collection] as ProjectEntity[])select.append(new Option(entity.name,entity.id));if(!select.options.length)select.append(new Option(`No ${field.label} available`,""));return select;}if(field.control==="select"){const select=document.createElement("select");select.name=field.key;for(const option of field.options??[])select.append(new Option(option.label,option.value));if(field.defaultValue!==undefined)select.value=String(field.defaultValue);return select;}if(field.control==="textarea"){const textarea=document.createElement("textarea");textarea.name=field.key;textarea.rows=3;textarea.value=String(field.defaultValue??"");return textarea;}const input=document.createElement("input");input.name=field.key;input.type=field.control??"text";if(field.control==="checkbox")input.checked=Boolean(field.defaultValue);else input.value=String(field.defaultValue??"");if(field.key==="eventName")input.placeholder="Derived from name when blank";return input;}
function projectCreationFieldValue(field:ProjectCollectionCreationField,control:HTMLInputElement|HTMLTextAreaElement|HTMLSelectElement):unknown{if(control instanceof HTMLSelectElement)return field.multiple?Array.from(control.selectedOptions,({value})=>value).filter(Boolean):control.value;if(control instanceof HTMLInputElement&&field.control==="checkbox")return control.checked;if(field.control==="number")return Number(control.value);return control.value;}
function renderCreationPage(content:HTMLElement,kind:ProjectEntityKind):void{
  if(!state)return;
  const definition=projectCollectionDefinitions[kind],route=projectCollectionCreationRoute(kind),fields=projectCollectionCreationFields[kind],section=document.createElement("section"),heading=document.createElement("h1"),purpose=document.createElement("p"),prerequisites=document.createElement("p"),usedBy=document.createElement("p"),form=document.createElement("form"),name=document.createElement("input"),nameLabel=document.createElement("label"),settings=document.createElement("fieldset"),legend=document.createElement("legend"),cancel=document.createElement("button"),create=document.createElement("button"),feedback=document.createElement("output");
  section.className="collection-lifecycle-page";section.dataset.creationKind=kind;section.dataset.projectRoute="add";section.setAttribute("aria-label",route.label);heading.tabIndex=-1;heading.textContent=route.heading;purpose.textContent=`Purpose: ${definition.purpose}. Example: ${definition.example}.`;prerequisites.textContent=`Prerequisites: ${definition.prerequisites.join(", ")}.`;usedBy.textContent=`Used by: ${definition.consumers.join(", ")}.`;nameLabel.textContent=`${definition.singular} name`;name.name="name";name.required=true;nameLabel.append(name);legend.textContent=`${definition.singular} settings`;settings.dataset.creationFields=kind;settings.append(legend);
  for(const field of fields){const label=document.createElement("label"),control=projectCreationFieldControl(field);label.textContent=field.label;control.dataset.creationField=field.key;label.append(control);settings.append(label);}
  cancel.type="button";create.type="submit";cancel.textContent="Cancel";create.textContent=`Create ${definition.singular}`;cancel.addEventListener("click",()=>openCollectionOverview(kind));form.addEventListener("submit",(event)=>{event.preventDefault();try{const values=Object.fromEntries(fields.map((field)=>[field.key,projectCreationFieldValue(field,form.elements.namedItem(field.key) as HTMLInputElement|HTMLTextAreaElement|HTMLSelectElement)])),next=createProjectCollectionEntity(state!,kind,name.value,id,values),created=next.project.collections[kind].at(-1)!;creationKind=undefined;selectedKind=kind;selectedId=created.id;lifecycleStatus=`Created ${definition.singular} ${created.name} in Draft revision ${canonicalRevision+1}.`;persist(next);persistNavigation();replaceProjectRoute(kind,created.id);queueMicrotask(()=>document.querySelector<HTMLElement>(`[data-project-entity-workspace="${CSS.escape(created.id)}"] h1`)?.focus({preventScroll:true}));}catch(error){feedback.textContent=error instanceof Error?error.message:String(error);}});
  form.append(nameLabel,settings,cancel,create,feedback);section.append(heading,purpose,prerequisites,usedBy,form);content.append(section);q("#project-breadcrumb").textContent=`${state.project.name} / ${definition.overview} / Create`;queueMicrotask(()=>heading.focus({preventScroll:true}));
}
function renderRemovalPage(content:HTMLElement,review:ProjectEntityRemovalReview):void{if(!state)return;const definition=projectCollectionDefinitions[review.kind],section=document.createElement("section"),heading=document.createElement("h1"),summary=document.createElement("p"),dependencies=document.createElement("ul"),actions=document.createElement("div"),cancel=document.createElement("button"),confirm=document.createElement("button");section.className="collection-lifecycle-page";section.dataset.removalKind=review.kind;heading.tabIndex=-1;heading.textContent=`Review removal of ${review.name}`;summary.textContent=review.summary;for(const dependency of review.dependencies){const item=document.createElement("li"),open=document.createElement("button"),kind=dependency.kind==="flowGraph"?"flows":dependency.kind;item.textContent=`${dependency.name}: ${dependency.relationship}. `;open.type="button";open.textContent=`Open ${dependency.name}`;open.addEventListener("click",()=>openProjectEntityWorkspace(kind,dependency.id));item.append(open);dependencies.append(item);}cancel.type=confirm.type="button";cancel.textContent="Cancel removal";confirm.textContent=`Remove ${review.name}`;confirm.disabled=review.blocked;cancel.addEventListener("click",()=>openCollectionOverview(review.kind,review.id));confirm.addEventListener("click",()=>{const entities=state!.project.collections[review.kind] as ProjectEntity[],index=entities.findIndex(({id})=>id===review.id),focus=entities[index+1]?.id??entities[index-1]?.id;removedFocus={kind:review.kind,id:review.id};removalReview=undefined;selectedKind=review.kind;selectedId=undefined;lifecycleStatus=`Removed ${review.name}. Draft saved; dependent evidence is stale. Undo restores the same stable identity.`;persist(removeProjectCollectionEntity(state!,review.kind,review.id));persistNavigation();replaceProjectRoute(review.kind);queueMicrotask(()=>{const target=focus?document.querySelector<HTMLElement>(`[data-entity-id="${CSS.escape(focus)}"]`):document.querySelector<HTMLElement>(`[data-add-kind="${review.kind}"]`);target?.focus({preventScroll:true});});});actions.append(cancel,confirm);section.append(heading,summary,dependencies,actions);content.append(section);q("#project-breadcrumb").textContent=`${state.project.name} / ${definition.overview} / Remove ${review.name}`;queueMicrotask(()=>heading.focus({preventScroll:true}));}

function renderProjectEntityWorkspace(content:HTMLElement,kind:ProjectEntityKind,entity:ProjectEntity):void{if(!state)return;const route=projectEntityWorkspaceRoute(kind,entity.id,entity.name),workspace=document.createElement("section"),heading=document.createElement("h1"),back=document.createElement("button");workspace.dataset.projectEntityWorkspace=entity.id;workspace.dataset.projectEntityKind=kind;workspace.setAttribute("aria-label",route.label);heading.tabIndex=-1;heading.textContent=route.heading;back.type="button";back.textContent=route.backAction;back.addEventListener("click",()=>openCollectionOverview(kind,entity.id));workspace.append(heading,back);content.append(workspace);if(kind==="flows"){const graphHost=document.createElement("div"),inspectorHost=q<HTMLElement>("#flow-inspector-context");graphHost.id="flow-graph-workspace";workspace.append(graphHost);inspectorHost.replaceChildren();renderSelectedEntityEditor(inspectorHost,entity);return;}q("#flow-inspector-context").replaceChildren();renderSelectedEntityEditor(workspace,entity);if(kind==="pages"){renderPageGroupMembershipEditor(workspace,entity);renderComposedSchemaWorkspace(workspace,entity,"pages","Page");}if(kind==="pageGroups"){const members=document.createElement("section"),memberList=document.createElement("ul");members.setAttribute("aria-label","Derived Page Group members");members.append(Object.assign(document.createElement("h3"),{textContent:"Derived members"}));for(const page of pageGroupMembers(state.project,entity.id))memberList.append(Object.assign(document.createElement("li"),{textContent:page.name}));members.append(memberList);workspace.append(members);renderComposedSchemaWorkspace(workspace,entity,"pageGroups","Page Group");}}

function renderWorkspace():void{
  const content=q<HTMLElement>("#workspace-content");content.replaceChildren();if(!state)return;
  if(projectOverview){const heading=document.createElement("h1"),identity=document.createElement("p"),metadata=document.createElement("dl"),openSchemas=document.createElement("button"),details:[[string,string],[string,string],[string,string],[string,string]]=[["Purpose",state.project.description],["Website",state.project.site],["Owner",String(state.project.owner??"")],["Notes",String(state.project.notes??"")]];heading.textContent="Project overview";identity.textContent=`${state.project.name} · stable identity ${state.project.id} · Draft revision ${canonicalRevision}`;for(const[label,value]of details){const term=document.createElement("dt"),description=document.createElement("dd");term.textContent=label;description.textContent=value;metadata.append(term,description);}openSchemas.type="button";openSchemas.textContent="Open Shared Profiles";openSchemas.addEventListener("click",()=>openCollectionOverview("profiles"));content.append(heading,identity,metadata,openSchemas);return;}
  if(creationKind){renderCreationPage(content,creationKind);return;}if(removalReview){renderRemovalPage(content,removalReview);return;}
  const search=q<HTMLInputElement>("#project-search").value.trim().toLowerCase();
  if(search){
    const matches=(Object.keys(labels) as ProjectEntityKind[]).flatMap((kind)=>entitiesForKind(kind).filter((entity)=>entitySearchText(entity).includes(search)).map((entity)=>({kind,entity}))).slice(0,40),heading=document.createElement("h1"),count=document.createElement("p"),list=document.createElement("ul");
    heading.textContent="Global search";count.className="status-text";count.textContent=`${matches.length} matching project entities`;list.className="entity-grid";
    for(const {kind,entity}of matches){const row=document.createElement("li"),select=document.createElement("button"),location=document.createElement("span"),used=document.createElement("span");row.className="entity-row";select.type="button";select.textContent=entity.name;select.addEventListener("click",()=>{q<HTMLInputElement>("#project-search").value="";openProjectEntityWorkspace(kind,entity.id);});location.className="search-location";location.textContent=labels[kind];used.textContent=`Used ${whereUsed(entity.id).length} times`;row.append(select,location,used);list.append(row);}
    content.append(heading,count,list);q("#project-breadcrumb").textContent=`${state.project.name} / Search / ${search}`;return;
  }
  const all=entitiesForKind(selectedKind),selected=all.find(({id})=>id===selectedId);
  q("#project-breadcrumb").textContent=`${state.project.name} / ${labels[selectedKind]}${selectedId?` / ${selected?.name??selectedId}`:""}`;
  q("#inspector-context").textContent=selected?`${selected.name} · Where used: ${whereUsed(selected.id).join(", ")||"None"}`:"Select a project entity.";
  if(selected){renderProjectEntityWorkspace(content,selectedKind,selected);return;}
  q("#flow-inspector-context").replaceChildren();const definition=projectCollectionDefinitions[selectedKind],heading=document.createElement("h1"),primary=document.createElement("button"),guidance=document.createElement("section"),purpose=document.createElement("p"),example=document.createElement("p"),prerequisites=document.createElement("p"),consumers=document.createElement("p"),count=document.createElement("p"),list=document.createElement("ul"),visible=all.slice(0,40);heading.textContent=definition.overview;primary.type="button";primary.textContent=definition.addAction;primary.dataset.addKind=selectedKind;primary.setAttribute("aria-label",`${definition.addAction} to ${state.project.name}`);primary.addEventListener("click",()=>openProjectCollectionCreation(selectedKind));guidance.className="project-guidance";purpose.textContent=definition.purpose;example.textContent=`Example: ${definition.example}.`;prerequisites.textContent=`Prerequisites: ${definition.prerequisites.join(", ")}.`;consumers.textContent=`Used by: ${definition.consumers.join(", ")}.`;guidance.append(purpose,example,prerequisites,consumers);count.className="status-text";count.textContent=all.length?`${visible.length} of ${all.length} rows rendered${all.length>40?" · windowed; scroll to load more":""}`:`No ${definition.overview} yet. ${definition.purpose}.`;list.className="entity-grid";list.setAttribute("role","listbox");
  for(const entity of visible){const row=document.createElement("li"),open=document.createElement("button"),remove=document.createElement("button"),kindText=document.createElement("span"),usage=document.createElement("span");row.className="entity-row";row.dataset.entityId=entity.id;row.tabIndex=-1;row.setAttribute("role","option");row.setAttribute("aria-selected",String(entity.id===selectedId));open.type=remove.type="button";open.textContent=entity.name;open.setAttribute("aria-label",`Open ${entity.name}`);open.addEventListener("click",()=>openProjectEntityWorkspace(selectedKind,entity.id));remove.textContent=`Remove ${entity.name}`;remove.setAttribute("aria-label",`Remove ${definition.singular} ${entity.name}`);remove.addEventListener("click",()=>{removalReview=inspectProjectEntityRemoval(state!,selectedKind,entity.id);selectedId=undefined;replaceProjectRoute(selectedKind);render();});kindText.className="search-location";kindText.textContent=definition.singular;usage.textContent=`Used ${whereUsed(entity.id).length} times`;row.append(open,kindText,usage,remove);list.append(row);}
  if(lifecycleStatus){const status=document.createElement("p");status.className="status-text";status.setAttribute("role","status");status.textContent=lifecycleStatus;content.append(status);}content.append(heading,primary,guidance,count,list);if(hasSavedSchemaAdoptionActions(selectedKind,selectedId))renderCanonicalProfileOverview(content);
}

function whereUsed(identity:string):string[]{if(!state)return[];const result:string[]=[];for(const [kind,entities]of Object.entries(state.project.collections))for(const entity of entities)if(entity.id!==identity&&entitySearchText(entity).includes(identity.toLowerCase()))result.push(`${kind}/${entity.name}`);for(const release of state.project.releases)if(entitySearchText(release.snapshot).includes(identity.toLowerCase()))result.push(`releases/${release.name}`);return result;}

function renderCoverage(offset=0):void{if(!state)return;const compiled=compileSpecificationProject(createCanonicalProjectEnvelope(state.project,state.draft?.id??"published")),content=q<HTMLElement>("#workspace-content"),heading=document.createElement("h1"),summary=document.createElement("p"),controls=document.createElement("nav"),previous=document.createElement("button"),next=document.createElement("button"),rowLabel=document.createElement("label"),rowNumber=document.createElement("input"),go=document.createElement("button"),rows=document.createElement("ul"),limit=40;heading.textContent="Effective requirement coverage";if(compiled.status==="blocked"){summary.textContent=`Coverage blocked: ${compiled.diagnostics.map(({field})=>field).join(", ")}`;content.replaceChildren(heading,summary);return;}const evidence=state.project.collections.fixtures.map((fixture)=>({fixture,result:runProductionFixture(compiled.plan,fixture)})),matrix=buildEffectiveRequirementCoverage(compiled.plan,evidence,{offset,limit});summary.className="status-text";summary.textContent=`Rows ${matrix.rows.length?offset+1:0}–${offset+matrix.rows.length} of ${matrix.totalRows} Page × Event × Flow step × Effective requirement cells`;controls.setAttribute("aria-label","Coverage row window");previous.type=next.type=go.type="button";previous.textContent="Previous 40";next.textContent="Next 40";previous.disabled=offset===0;next.disabled=offset+limit>=matrix.totalRows;previous.addEventListener("click",()=>renderCoverage(Math.max(0,offset-limit)));next.addEventListener("click",()=>renderCoverage(offset+limit));rowLabel.textContent="Go to coverage row";rowNumber.type="number";rowNumber.min="1";rowNumber.max=String(matrix.totalRows);rowNumber.value=String(Math.min(matrix.totalRows,offset+1));rowLabel.append(rowNumber);go.textContent="Open row window";go.addEventListener("click",()=>{const requested=Math.max(1,Math.min(matrix.totalRows,Number(rowNumber.value)||1));renderCoverage(Math.floor((requested-1)/limit)*limit);});controls.append(previous,next,rowLabel,go);rows.className="coverage-grid";for(const row of matrix.rows){const item=document.createElement("li"),open=document.createElement("button"),origin=document.createElement("span"),status=document.createElement("strong");item.className="coverage-row";open.type="button";open.textContent=`${row.requirementPath} · ${row.stepId}`;open.setAttribute("aria-label",`Open ${row.requirementPath} ${row.state} coverage issue`);open.addEventListener("click",()=>{selectedKind="flows";selectedId=row.flowId;history.replaceState(null,"",`?kind=flows&entity=${encodeURIComponent(row.flowId)}&field=${encodeURIComponent(row.stepId)}`);persistNavigation();render();const workspace=q<HTMLElement>("#workspace-pane");queueMicrotask(()=>workspace.focus({preventScroll:true}));});origin.textContent=`schema r${row.schemaRevision} · ${row.profileIds.join(", ")} · ${row.fixtureId??"no proving fixture"}`;status.textContent=row.state;item.append(open,origin,status);rows.append(item);}content.replaceChildren(heading,summary,controls,rows);}

function documentationResult(){if(!state)throw new Error("No project");const fields=["path","type",...Array.from(document.querySelectorAll<HTMLInputElement>(".documentation-field:checked"),({value})=>value)];return exportDocumentation(state.project,{fields,include:{applicability:q<HTMLInputElement>("#documentation-applicability").checked,flows:q<HTMLInputElement>("#documentation-flows").checked,fixtures:q<HTMLInputElement>("#documentation-fixtures").checked,releases:q<HTMLInputElement>("#documentation-releases").checked}});}
function refreshDocumentation():void{q("#documentation-preview").textContent=documentationResult().preview;}
function download(name:string,text:string,type="application/json"):void{const blob=new Blob([`${text}\n`],{type}),url=URL.createObjectURL(blob),link=document.createElement("a");link.href=url;link.download=name;link.click();URL.revokeObjectURL(url);}

function replaceOptions(select:HTMLSelectElement,entities:readonly ProjectEntity[],placeholder:string):void{const value=select.value,empty=document.createElement("option");empty.value="";empty.textContent=placeholder;select.replaceChildren(empty,...entities.map((entity)=>{const option=document.createElement("option");option.value=entity.id;option.textContent=entity.name;return option;}));select.value=value;}
function renderReferenceSelectors():void{if(!state)return;flowGraphBuilder?.renderSelectors();replaceOptions(q("#project-assignment-schema"),state.project.collections.schemaDrafts,"Choose schema");const saved=q<HTMLSelectElement>("#saved-schema-picker"),selectedSaved=saved.value;saved.replaceChildren(new Option("Choose a published saved schema",""),...savedSchemas().map((schema)=>new Option(`${schema.name} · revision ${schema.version}`,schema.id)));saved.value=selectedSaved;const eventSelect=q<HTMLSelectElement>("#project-assignment-event");replaceOptions(eventSelect,state.project.collections.events,"Choose event");for(const event of state.project.collections.events){const eventName=String(event.eventName??"");if(eventName&&eventName!==event.id){const alias=document.createElement("option");alias.value=eventName;alias.textContent=event.name;alias.hidden=true;eventSelect.append(alias);}}replaceOptions(q("#project-assignment-applicability"),state.project.collections.applicabilitySets,"Choose applicability set");}

function renderAssignments():void{
  if(!state)return;
  const result=searchProjectAssignments(state.project,q<HTMLInputElement>("#project-assignment-search").value);
  q("#project-assignment-count").textContent=`${result.count} assignment${result.count===1?"":"s"}`;
  q<HTMLElement>("#project-assignment-empty").hidden=!result.empty;
  q("#project-assignment-conflicts").textContent=result.conflicts.map(({message})=>message).join("; ");
  const list=q<HTMLUListElement>("#project-assignment-list");list.replaceChildren();
  for(const assignment of result.rows){
    const item=document.createElement("li"),edit=document.createElement("button");edit.type="button";
    edit.textContent=`${assignment.name} · ${assignment.schemaId} · ${assignment.eventName} · ${assignment.versionPolicy}${assignment.schemaRevision?` revision ${assignment.schemaRevision}`:""}`;
    edit.addEventListener("click",()=>{
      q<HTMLInputElement>("#project-assignment-id").value=assignment.id;q<HTMLInputElement>("#project-assignment-name").value=assignment.name;q<HTMLSelectElement>("#project-assignment-schema").value=String(assignment.schemaDraftId??assignment.schemaId);q<HTMLInputElement>("#project-assignment-source").value=String(assignment.sourceId);q<HTMLSelectElement>("#project-assignment-event").value=String(assignment.eventId);q<HTMLSelectElement>("#project-assignment-applicability").value=String(assignment.applicabilitySetId);q<HTMLSelectElement>("#project-assignment-target").value=String(assignment.target);q<HTMLInputElement>("#project-assignment-priority").value=String(assignment.priority);q<HTMLSelectElement>("#project-assignment-version-policy").value=String(assignment.versionPolicy);q<HTMLInputElement>("#project-assignment-revision").value=String(assignment.schemaRevision??1);q<HTMLInputElement>("#project-assignment-name").focus();
    });item.append(edit);list.append(item);
  }
}

function render():void{const empty=q<HTMLElement>("#project-empty"),workspace=q<HTMLElement>("#project-workspace");empty.hidden=Boolean(state);workspace.hidden=!state;if(!state){document.title="Specification Studio · No active project";q("#project-context").textContent="No active project";return;}document.title=`Specification Studio · ${state.project.name} · ${state.project.id}`;q("#project-context").textContent=`${state.project.name} · ${state.project.id} · ${state.project.environments[0]} · ${state.draft?`Preview Draft`:`Live ${state.project.currentRelease?"published release":"not published"}`}`;q("#project-state").textContent=pendingConflict?`Conflict at revision ${canonicalRevision}; pending ${pendingConflict.pendingLabel}`:`${state.draft?.status??"Published"} · revision ${canonicalRevision}`;q("#tree-project-name").textContent=state.project.name;q<HTMLButtonElement>("#undo-project").disabled=!state.history.undo.length;q<HTMLButtonElement>("#redo-project").disabled=!state.history.redo.length;q<HTMLElement>("#flow-step-editor").hidden=selectedKind!=="flows"||!selectedId||projectOverview;q<HTMLElement>("#schema-draft-editor").hidden=true;q<HTMLElement>("#assignment-editor").hidden=selectedKind!=="assignments"||projectOverview;q<HTMLElement>("#bulk-property-editor").hidden=true;renderTree();renderWorkspace();layeredSchemaUi?.render();renderReferenceSelectors();flowGraphBuilder?.render();flowDocumentationExportUi?.render();executableFlowBuilder?.render();}

q<HTMLFormElement>("#create-project-form").addEventListener("submit",(event)=>{event.preventDefault();const next=createSpecificationProject({name:q<HTMLInputElement>("#project-name").value.trim(),description:q<HTMLTextAreaElement>("#project-description").value,site:q<HTMLInputElement>("#project-site").value.trim(),environments:["Production","Staging"],id}),at=new Date().toISOString();library=projectLibrary([{state:next,revision:0,createdAt:at,lastModifiedAt:at}],next.project.id);localStorage.setItem(PROJECT_LIBRARY_STORAGE_KEY,serializeProjectLibrary(library));persist(next);q<HTMLElement>("#workspace-pane").focus();});
document.querySelectorAll<HTMLButtonElement>("[data-start-path]").forEach((button)=>button.addEventListener("click",()=>{const path=button.dataset.startPath??"unknown",messages:Record<string,string>={template:"Template project preview staged; confirm to create its complete graph.",import:"Full project migration review is ready for a selected project file.",json:"JSON or JSON Schema requirements staging grid is ready.",spreadsheet:"Spreadsheet requirements staging grid is ready.",adopt:"Saved-schema adoption review is ready with source lineage."},message=messages[path]??"Starting path staged.";localStorage.setItem(START_PATH_KEY,JSON.stringify({path,message}));q("#start-path-status").textContent=message;}));
q<HTMLFormElement>("#add-entity-form").addEventListener("submit",(event)=>{event.preventDefault();if(!state)return;const kind=q<HTMLSelectElement>("#entity-kind").value as ProjectEntityKind,name=q<HTMLInputElement>("#entity-name").value.trim();if(!name)return;const next=createProjectCollectionEntity(state,kind,name,id),created=next.project.collections[kind].at(-1);selectedKind=kind;selectedId=created?.id;q<HTMLInputElement>("#entity-name").value="";persist(next);persistNavigation();render();});
q("#undo-project").addEventListener("click",()=>{if(!state)return;const restored=removedFocus;persist(undoProjectTransaction(state));if(restored){removedFocus=undefined;selectedKind=restored.kind;selectedId=undefined;lifecycleStatus="Removal undone; the same stable identity is restored.";persistNavigation();replaceProjectRoute(restored.kind);render();queueMicrotask(()=>document.querySelector<HTMLElement>(`[data-entity-id="${CSS.escape(restored.id)}"]`)?.focus({preventScroll:true}));}});q("#redo-project").addEventListener("click",()=>{if(state)persist(redoProjectTransaction(state));});q("#project-search").addEventListener("input",renderWorkspace);
q<HTMLFormElement>("#create-project-schema-draft").addEventListener("submit",(event)=>{event.preventDefault();if(!state)return;persist(createProjectSchemaDraft(state,{schemaId:q<HTMLInputElement>("#project-schema-id").value.trim(),name:q<HTMLInputElement>("#project-schema-name").value.trim(),baseRevision:Number(q<HTMLInputElement>("#project-schema-revision").value),description:q<HTMLTextAreaElement>("#project-schema-description").value},id));q("#schema-draft-result").textContent="Saved one integrated working draft; the published revision is unchanged until project release.";});
const savedSchemaDialog=q<HTMLDialogElement>("#saved-schema-review"),savedSchemaChanges=q<HTMLUListElement>("#saved-schema-review-changes");
q("#review-saved-schema").addEventListener("click",()=>{if(!state)return;const source=savedSchemas().find(({id:schemaId})=>schemaId===q<HTMLSelectElement>("#saved-schema-picker").value);if(!source){q("#schema-draft-result").textContent="Choose a published saved schema first.";return;}savedSchemaChanges.replaceChildren();const adopted=state.project.collections.schemaDrafts.find(({id})=>id===source.id);try{if(adopted){const review=stageSavedSchemaSynchronization(state,source);pendingSavedSchema={kind:"synchronize",review};q("#saved-schema-review-summary").textContent=`Synchronize ${source.name} from library revision ${review.fromRevision} to ${review.toRevision}; ${review.localOverrides.length} project-local fields remain unchanged.`;for(const change of review.changes){const item=document.createElement("li");item.textContent=`${change.path}: ${JSON.stringify(change.before)} → ${JSON.stringify(change.after)}`;savedSchemaChanges.append(item);}q("#confirm-saved-schema").textContent="Commit reviewed synchronization";}else{pendingSavedSchema={kind:"adopt",source};q("#saved-schema-review-summary").textContent=`Adopt ${source.name} revision ${source.version} as one project-owned working draft with source lineage. The saved library remains unchanged.`;q("#confirm-saved-schema").textContent="Commit reviewed adoption";}savedSchemaDialog.showModal();savedSchemaDialog.querySelector<HTMLElement>("h2")?.focus();}catch(error){q("#schema-draft-result").textContent=error instanceof Error?error.message:String(error);}});
q("#confirm-saved-schema").addEventListener("click",()=>{if(!state||!pendingSavedSchema)return;const completed=pendingSavedSchema;persist(completed.kind==="adopt"?adoptSavedSchema(state,completed.source):commitSavedSchemaSynchronization(state,completed.review));selectedKind="schemaDrafts";selectedId=completed.kind==="adopt"?completed.source.id:completed.review.schemaId;pendingSavedSchema=undefined;savedSchemaDialog.close();q("#schema-draft-result").textContent=completed.kind==="adopt"?"Saved schema adopted into the canonical project with revision lineage.":"Reviewed saved-schema revision synchronized; local overrides were preserved and affected evidence was marked stale.";render();});
q("#cancel-saved-schema").addEventListener("click",()=>{pendingSavedSchema=undefined;savedSchemaDialog.close();q<HTMLButtonElement>("#review-saved-schema").focus();});
q<HTMLFormElement>("#save-project-assignment").addEventListener("submit",(event)=>{event.preventDefault();if(!state)return;const assignmentId=q<HTMLInputElement>("#project-assignment-id").value||undefined,eventValue=q<HTMLSelectElement>("#project-assignment-event").value,eventEntity=state.project.collections.events.find((candidate)=>candidate.id===eventValue||candidate.eventName===eventValue),applicabilitySetId=q<HTMLSelectElement>("#project-assignment-applicability").value,compatibilityPath=q<HTMLInputElement>("#project-assignment-path").value.trim(),condition:Condition|undefined=!applicabilitySetId&&compatibilityPath?{kind:"predicate",field:compatibilityPath,operator:"equals",value:q<HTMLInputElement>("#project-assignment-value").value}:undefined;try{persist(saveProjectAssignment(state,{...(assignmentId?{id:assignmentId}:{}),name:q<HTMLInputElement>("#project-assignment-name").value.trim(),schemaId:q<HTMLSelectElement>("#project-assignment-schema").value,...(eventEntity?{eventId:eventEntity.id}:{}),eventName:String(eventEntity?.eventName??eventEntity?.name??""),...(applicabilitySetId?{applicabilitySetId}:{}),...(condition?{condition}:{}),sourceId:q<HTMLInputElement>("#project-assignment-source").value.trim(),target:q<HTMLSelectElement>("#project-assignment-target").value,priority:Number(q<HTMLInputElement>("#project-assignment-priority").value),versionPolicy:q<HTMLSelectElement>("#project-assignment-version-policy").value as "pinned"|"follow latest",schemaRevision:Number(q<HTMLInputElement>("#project-assignment-revision").value)},id));q<HTMLInputElement>("#project-assignment-id").value="";renderAssignments();}catch(error){q("#project-assignment-conflicts").textContent=error instanceof Error?error.message:String(error);}});q("#project-assignment-search").addEventListener("input",renderAssignments);
const bulkStageButton=q<HTMLButtonElement>("#commit-bulk-properties"),bulkDetails=bulkStageButton.closest("details")!,bulkFormat=document.createElement("select"),bulkReview=document.createElement("div"),bulkConfirm=document.createElement("button"),bulkRequire=document.createElement("button"),bulkChoices:readonly (readonly [string,string])[]=[["paste","Paste columns"],["csv","Spreadsheet / CSV"],["json","JSON requirements"],["json-schema","JSON Schema"],["template","100-row template"]];for(const[value,label]of bulkChoices){const option=document.createElement("option");option.value=value;option.textContent=label;bulkFormat.append(option);}bulkFormat.setAttribute("aria-label","Bulk input format");bulkReview.id="bulk-stage-review";bulkReview.tabIndex=-1;bulkConfirm.id="confirm-bulk-properties";bulkRequire.id="bulk-mark-required";bulkConfirm.type=bulkRequire.type="button";bulkConfirm.textContent="Commit all staged rows";bulkRequire.textContent="Mark selected Required";bulkConfirm.hidden=bulkRequire.hidden=true;bulkDetails.insertBefore(bulkFormat,q("#bulk-properties"));bulkDetails.insertBefore(bulkReview,q("#bulk-assistance"));bulkDetails.insertBefore(bulkRequire,q("#bulk-assistance"));bulkDetails.insertBefore(bulkConfirm,q("#bulk-assistance"));function renderBulkStage():void{bulkReview.replaceChildren();if(!stagedBulk)return;const summary=document.createElement("p"),table=document.createElement("table"),body=document.createElement("tbody");summary.textContent=`${stagedBulk.rows.length} staged rows · ${stagedBulk.errors.length} fields need repair · project unchanged`;for(const row of stagedBulk.rows.slice(0,40)){const tr=document.createElement("tr"),selected=document.createElement("input"),path=document.createElement("input"),type=document.createElement("input"),error=document.createElement("td");selected.type="checkbox";selected.checked=row.selected;selected.addEventListener("change",()=>{row.selected=selected.checked;});path.value=row.path;type.value=row.type??"";path.addEventListener("change",()=>{row.path=path.value;stagedBulk=stageBulkRequirements("json",JSON.stringify(stagedBulk!.rows.map(({id:selectedId,selected:isSelected,...requirement})=>requirement)));renderBulkStage();});type.addEventListener("change",()=>{row.type=type.value;stagedBulk=stageBulkRequirements("json",JSON.stringify(stagedBulk!.rows.map(({id:selectedId,selected:isSelected,...requirement})=>requirement)));renderBulkStage();});for(const control of[selected,path,type]){const td=document.createElement("td");td.append(control);tr.append(td);}error.textContent=stagedBulk.errors.filter(({rowId})=>rowId===row.id).map(({field,message})=>`${field}: ${message}`).join("; ");tr.append(error);body.append(tr);}table.append(body);bulkReview.append(summary,table);bulkConfirm.hidden=bulkRequire.hidden=false;bulkConfirm.disabled=Boolean(stagedBulk.errors.length);bulkReview.focus();}
bulkStageButton.addEventListener("click",()=>{if(!state||selectedKind!=="profiles"||!selectedId)return;try{stagedBulk=stageBulkRequirements(bulkFormat.value as BulkFormat,q<HTMLTextAreaElement>("#bulk-properties").value);q("#bulk-assistance").textContent="Review, repair, select, and commit the staged grid.";renderBulkStage();}catch(error){q("#bulk-assistance").textContent=error instanceof Error?error.message:String(error);}});bulkRequire.addEventListener("click",()=>{if(!stagedBulk)return;stagedBulk=applyStagedBulkAction(stagedBulk,stagedBulk.rows.filter(({selected})=>selected).map(({id})=>id),{required:true});renderBulkStage();});bulkConfirm.addEventListener("click",()=>{if(!state||!selectedId||!stagedBulk)return;persist(commitStagedBulkRequirements(state,selectedId,stagedBulk));q("#bulk-assistance").textContent=`Committed ${stagedBulk.rows.length} requirements in one revision and one Undo transaction.`;stagedBulk=undefined;renderBulkStage();});
q("#run-preflight").addEventListener("click",()=>{if(!state)return;releasePreflight=projectPreflight(state,canonicalRevision);const result=releasePreflight,content=q("#workspace-content"),section=document.createElement("section"),title=document.createElement("h2"),summary=document.createElement("p"),list=document.createElement("ul");title.textContent="Production evaluator preflight";summary.className="status-text";summary.textContent=result.blockers.length?`${result.contentIdentity} · ${result.blockers.length} blockers · ${result.fixtures.length} fixtures evaluated`:`${result.contentIdentity} · Ready to publish from the compiled production plan`;list.className="preflight-list";for(const blocker of result.blockers){const item=document.createElement("li"),open=document.createElement("button");item.className="error";open.type="button";open.textContent=`${blocker.code}: ${blocker.message}`;open.addEventListener("click",()=>{selectedId=blocker.entityId;history.replaceState(null,"",`?entity=${encodeURIComponent(blocker.entityId)}&field=${encodeURIComponent(blocker.field)}`);render();});item.append(open);list.append(item);}section.append(title,summary,list);content.prepend(section);});
q("#show-coverage").addEventListener("click",()=>renderCoverage());
const documentationDialog=q<HTMLDialogElement>("#documentation-export");q("#generate-documentation").addEventListener("click",(event)=>{if(!state)return;lastInvokingControl=event.currentTarget as HTMLElement;refreshDocumentation();documentationDialog.showModal();documentationDialog.querySelector<HTMLElement>("h2")?.focus();});document.querySelectorAll<HTMLInputElement>("#documentation-export input").forEach((field)=>field.addEventListener("change",refreshDocumentation));q("#copy-documentation").addEventListener("click",async()=>{const result=documentationResult(),html=`<pre>${result.preview.replaceAll("&","&amp;").replaceAll("<","&lt;")}</pre>`;if(globalThis.ClipboardItem&&navigator.clipboard.write)await navigator.clipboard.write([new ClipboardItem({"text/html":new Blob([html],{type:"text/html"}),"text/plain":new Blob([result.clipboard],{type:"text/plain"})})]);else await navigator.clipboard.writeText(result.clipboard);q("#documentation-result").textContent="Documentation table copied once.";});q("#close-documentation").addEventListener("click",()=>{documentationDialog.close();lastInvokingControl?.focus();});
const releaseDialog=q<HTMLDialogElement>("#release-review");q("#publish-project").addEventListener("click",(event)=>{if(!state)return;lastInvokingControl=event.currentTarget as HTMLElement;releasePreflight=projectPreflight(state,canonicalRevision);const preflight=releasePreflight,prior=state.project.releases.at(-1),emptyProject={...state.project,collections:Object.fromEntries(Object.keys(state.project.collections).map((kind)=>[kind,[]])) as unknown as typeof state.project.collections,releases:[]},review=buildReleaseReview(prior?{...state.project,collections:prior.snapshot}:emptyProject,state.project),diff=q("#release-diff");diff.replaceChildren(...review.sections.map((section)=>{const item=document.createElement("li");item.textContent=`${section.kind}: ${section.entityKind}/${section.before??section.after}`;return item;}));q("#release-summary").textContent=preflight.blockers.length?`${preflight.contentIdentity}: publication blocked by ${preflight.blockers.length} issues — ${preflight.blockers.map(({message})=>message).join(" ")}`:`${preflight.contentIdentity}: Release ${state.project.releases.length+1} has ${review.sections.length} structured changes and one reviewed executable plan.`;q<HTMLButtonElement>("#confirm-release").disabled=Boolean(preflight.blockers.length||!review.sections.length);q<HTMLButtonElement>("#confirm-release-close").disabled=Boolean(preflight.blockers.length||!review.sections.length);q<HTMLButtonElement>("#restore-release").disabled=!prior;releaseDialog.showModal();releaseDialog.querySelector<HTMLElement>("h2")?.focus();});q("#cancel-release").addEventListener("click",()=>{releaseDialog.close();lastInvokingControl?.focus();});const confirmRelease=(close:boolean)=>{if(!state||!releasePreflight)return;let next:ProjectState;try{next=publishProjectRelease(state,{id,preflight:releasePreflight,write:(project)=>writeProjectState({project,history:{undo:[],redo:[]}})});}catch(error){q("#project-state").textContent="Save failed";q("#release-summary").textContent=error instanceof Error?error.message:"Publication failed; the prior canonical project bytes remain authoritative.";q<HTMLButtonElement>("#retry-save").hidden=false;return;}state=next;releaseDialog.close();render();if(close)q<HTMLElement>("#project-workspace").hidden=true;else{q("#release-summary").textContent=`Release ${next.project.releases.length} published from ${next.project.releases.at(-1)?.preflightContentIdentity}. Undo by restoring this release as a new Draft.`;q<HTMLElement>("#workspace-pane").focus();}};q("#confirm-release").addEventListener("click",()=>confirmRelease(false));q("#confirm-release-close").addEventListener("click",()=>{confirmRelease(true);lastInvokingControl?.focus();});q("#restore-release").addEventListener("click",()=>{if(!state)return;const release=state.project.releases.at(-1);if(!release)return;persist(restoreReleaseAsDraft(state,release.id,id));releaseDialog.close();q<HTMLElement>("#workspace-pane").focus();});
q("#export-project").addEventListener("click",()=>{if(!state)return;download(`${state.project.name.toLowerCase().replace(/[^a-z0-9]+/g,"-")}-project.json`,exportSpecificationProjectState(state));});
q("#export-standard-schema").addEventListener("click",()=>{if(!state)return;const properties=Object.fromEntries(state.project.collections.profiles.flatMap((profile)=>(profile.canonicalSchema?canonicalRequirements(profile.canonicalSchema):profile.requirements).map((requirement)=>[requirement.path.replace(/^\//,""),{type:requirement.type??"string",...(requirement.allowedValues?{enum:requirement.allowedValues}:{})}]))),schema={$schema:"https://json-schema.org/draft/2020-12/schema",type:"object",properties,"x-lossy-semantics":["applicability","flows","fixtures","draft","releases"]},manifest={format:"my-chrome-utilities.applicability-flow-manifest",version:1,projectId:state.project.id,applicability:state.project.collections.applicabilitySets,flows:state.project.collections.flows,fixtures:state.project.collections.fixtures,draft:state.draft,releases:state.project.releases};download("specification.schema.json",JSON.stringify(schema));download("specification.manifest.json",JSON.stringify(manifest));});
q("#import-project").addEventListener("click",()=>q<HTMLInputElement>("#import-project-file").click());
const importDialog=q<HTMLDialogElement>("#import-review");q<HTMLInputElement>("#import-project-file").addEventListener("change",async(event)=>{const file=(event.currentTarget as HTMLInputElement).files?.[0];if(!file||!state)return;try{stagedImport=stageProjectImport(await file.text(),state);q("#import-summary").textContent=stagedImport.blockers.length?`${stagedImport.blockers.length} blocking collisions; ${stagedImport.diff.sections.length} linked changes.`:`${stagedImport.diff.sections.length} linked changes ready.`;q<HTMLButtonElement>("#commit-import").disabled=Boolean(stagedImport.blockers.length);importDialog.showModal();importDialog.querySelector<HTMLElement>("h2")?.focus();}catch(error){q("#import-summary").textContent=error instanceof Error?error.message:String(error);importDialog.showModal();}});q("#remap-import").addEventListener("click",()=>{if(!stagedImport||!state)return;stagedImport=stageProjectImport(stagedImport.source,state,{projectId:id("project")});q("#import-summary").textContent=`Collision remapped; ${stagedImport.diff.sections.length} linked changes ready.`;q<HTMLButtonElement>("#commit-import").disabled=false;});q("#commit-import").addEventListener("click",()=>{if(!state||!stagedImport)return;persist(commitStagedProjectImport(state,stagedImport,{write:()=>{}}));importDialog.close();q<HTMLElement>("#import-project").focus();});q("#cancel-import").addEventListener("click",()=>{stagedImport=undefined;importDialog.close();q<HTMLElement>("#import-project").focus();});
const conflictDialog=q<HTMLDialogElement>("#project-conflict-review");function completeConflict(strategy:"reapply"|"merge"):void{if(!pendingConflict)return;const fields=Array.from(q("#project-conflict-fields").querySelectorAll<HTMLInputElement>('input:checked'),({value})=>value),resolved=resolveCanonicalProjectConflict(pendingConflict,{strategy,...(strategy==="merge"?{pendingFields:fields}:{})});conflictDialog.close();pendingConflict=undefined;persist(resolved);q<HTMLElement>("#project-inspector").focus();}q("#reload-project-conflict").addEventListener("click",()=>{if(!pendingConflict)return;state=resolveCanonicalProjectConflict(pendingConflict,{strategy:"reload"});lastCommittedState=structuredClone(state);pendingConflict=undefined;conflictDialog.close();q<HTMLButtonElement>("#retry-save").hidden=true;render();q<HTMLElement>("#project-inspector").focus();});q("#reapply-project-conflict").addEventListener("click",()=>completeConflict("reapply"));q("#merge-project-conflict").addEventListener("click",()=>completeConflict("merge"));q("#retry-save").addEventListener("click",()=>{if(pendingConflict){showConflictReview();return;}if(state)persist(state.draft?{...state,draft:{...state.draft,status:"Saved"}}:state);});

const flowBuilderContext=()=>({...state?{state}:{},revision:canonicalRevision,...(selectedKind==="flows"&&selectedId?{flowId:selectedId}:{})});
flowGraphBuilder=installFlowGraphBuilder({context:flowBuilderContext,persist,id});
flowDocumentationExportUi=installFlowDocumentationExportUi({context:flowBuilderContext,renderFlow:()=>{flowGraphBuilder?.render();flowDocumentationExportUi?.render();},openRepair:(contextId,path,repair)=>{const selectPath=()=>setTimeout(()=>Array.from(document.querySelectorAll<HTMLElement>("[data-property-id]")).find((candidate)=>candidate.dataset.propertyId===path||candidate.textContent?.includes(path))?.click(),0);if(repair.startsWith("Open contributing schema ")){const name=repair.slice("Open contributing schema ".length),match=(["profiles","events","pageGroups","pages","flows"] as ProjectEntityKind[]).flatMap((kind)=>state!.project.collections[kind].map((entity)=>({kind,entity}))).find(({entity})=>entity.name===name);if(match){selectedKind=match.kind;selectedId=match.entity.id;persistNavigation();render();selectPath();return;}}layeredSchemaUi?.openGraphOccurrenceSchema(contextId.replace(/^context:/,""),path);}});
executableFlowBuilder=installExecutableFlowBuilder({context:flowBuilderContext,persist,id});
layeredSchemaUi=installLayeredSchemaUi({context:()=>({...state?{state}:{},kind:selectedKind,...(selectedId?{entityId:selectedId}:{})}),persist});
const synchronizeActiveProjectContext=(serialized:string):void=>{const change=activeProjectContextChange(serialized,state?.project.id,canonicalRevision);if(!change.changed)return;library=change.library;pendingConflict=undefined;const active=change.active;state=active?structuredClone(active.state):undefined;lastCommittedState=state?structuredClone(state):undefined;canonicalRevision=active?.revision??0;selectedKind="profiles";selectedId=undefined;creationKind=undefined;projectOverview=Boolean(active);if(active){const navigation=resolveProjectNavigation(library,active.state.project.id);if(navigation){projectOverview=false;selectedKind=navigation.kind as ProjectEntityKind;selectedId=navigation.id;}const url=new URL(location.href);url.searchParams.set("project",active.state.project.id);url.searchParams.delete("route");url.searchParams.set("kind",selectedKind);if(selectedId)url.searchParams.set("entity",selectedId);else url.searchParams.delete("entity");history.replaceState(null,"",url);}else{const url=new URL(location.href);for(const key of["project","route","kind","entity"])url.searchParams.delete(key);history.replaceState(null,"",url);}render();renderAssignments();q("#project-state").textContent=active?`Active project synchronized: ${active.state.project.name} · revision ${active.revision}`:"No active project";};
window.addEventListener("storage",(event)=>{if(event.key!==PROJECT_LIBRARY_STORAGE_KEY||!event.newValue)return;try{synchronizeActiveProjectContext(event.newValue);}catch(error){q("#project-state").textContent=error instanceof Error?error.message:String(error);}});
restore();
if(!state){const stagedStart=localStorage.getItem(START_PATH_KEY);if(stagedStart)try{q("#start-path-status").textContent=(JSON.parse(stagedStart) as {message?:string}).message??"Starting path staged.";}catch{/* ignore invalid start-path recovery */}}
subscribeCanonicalProjectChanges(window as unknown as CanonicalProjectChangeTarget,({revision,state:next})=>{
  if(pendingConflict)return;
  library=restoreProjectLibrary(localStorage.getItem(PROJECT_LIBRARY_STORAGE_KEY))??library;
  const activeRecord=library.activeProjectId===next.project.id?library.projects[next.project.id]:undefined;if(activeRecord&&projectRecordNeedsSynchronization(activeRecord,next,revision)){library=saveProjectState(library,next.project.id,next,revision);localStorage.setItem(PROJECT_LIBRARY_STORAGE_KEY,serializeProjectLibrary(library));}
  state=structuredClone(next);lastCommittedState=structuredClone(next);canonicalRevision=revision;
  render();renderAssignments();
  q("#project-state").textContent=`Updated from canonical revision ${revision}`;
},()=>state?{projectId:state.project.id,revision:canonicalRevision}:undefined);
render();renderAssignments();
const applyRequestedRoute=()=>{const deepKind=routeParameters.get("kind") as ProjectEntityKind|null,deepId=routeParameters.get("entity"),requestedAction=routeParameters.get("route");if(deepKind&&deepKind in labels){projectOverview=false;selectedKind=deepKind;creationKind=requestedAction==="add"?deepKind:undefined;selectedId=creationKind?undefined:deepId&&state&&(state.project.collections[deepKind] as ProjectEntity[]).some(({id})=>id===deepId)?deepId:undefined;persistNavigation();render();queueMicrotask(()=>{const target=creationKind?document.querySelector<HTMLElement>(`[data-creation-kind="${deepKind}"] h1`):selectedId?document.querySelector<HTMLElement>(`[data-project-entity-workspace="${CSS.escape(selectedId)}"] h1`):q<HTMLElement>("#project-inspector");if(target){target.tabIndex=-1;target.focus({preventScroll:true});}});}if(routeParameters.get("view")==="documentation"&&state)q<HTMLElement>("#generate-documentation").click();};
const requestedProject=routeParameters.get("project");if(requestedProject&&requestedProject!==library.activeProjectId){const target=library.projects[requestedProject],dialog=document.createElement("dialog"),heading=document.createElement("h2"),summary=document.createElement("p"),confirm=document.createElement("button"),cancel=document.createElement("button");heading.textContent=`Open ${target?.state.project.name??requestedProject} in Specification Studio`;summary.textContent=`${state?.project.name??"No active project"} → ${target?.state.project.name??requestedProject}. Active context will change before the requested entity is resolved.`;confirm.type=cancel.type="button";confirm.textContent="Confirm project context and continue";cancel.textContent="Cancel";confirm.disabled=!target;confirm.addEventListener("click",()=>{try{library=activateProject(library,requestedProject);localStorage.setItem(PROJECT_LIBRARY_STORAGE_KEY,serializeProjectLibrary(library));const record=library.projects[requestedProject]!;localStorage.setItem(STORAGE_KEY,serializeCanonicalProjectState(record.state,record.revision));state=structuredClone(record.state);lastCommittedState=structuredClone(record.state);canonicalRevision=record.revision;projectOverview=routeParameters.get("route")==="overview";dialog.close();dialog.remove();render();applyRequestedRoute();}catch(error){summary.textContent=error instanceof Error?error.message:String(error);}});cancel.addEventListener("click",()=>{dialog.close();dialog.remove();});dialog.append(heading,summary,confirm,cancel);document.body.append(dialog);dialog.showModal();heading.tabIndex=-1;heading.focus();}else applyRequestedRoute();
