import {addFlowEventOccurrence,addFlowPageFrameToSection,addPropertySetApplication,connectFlowPageFrames,createFlowSection,inspectSectionRemovalWithContents,moveFlowPageFramePresentation,moveFlowSection,movePageFrameToSection,orderedPropertySetApplications,removeFlowSection,removeFlowSectionWithContents,removePropertySetApplication,renameAndResizeFlowSection,reorderPropertySetApplication,setPropertySetApplicationApplicability,type FlowSection,type FlowSectionBounds} from "./data-layer-property-set-flow-section.js";
import type {IdFactory,ProjectEntity,ProjectState} from "./data-layer-specification-project.js";

export interface FlowSectionWorkspaceOptions {host:HTMLElement;state:ProjectState;flowId:string;id:IdFactory;onSave:(state:ProjectState)=>void;}
export interface PropertyCompositionWorkspaceOptions {host:HTMLElement;state:ProjectState;pageId:string;id:IdFactory;onSave:(state:ProjectState)=>void;onOpen?:(propertySetId:string)=>void;}
type SectionFrame=ProjectEntity&{pageId:string;sectionId?:string;position:{x?:number;y:number}};
type SectionGraph={sections?:FlowSection[];pageFrames?:SectionFrame[];occurrences?:ProjectEntity[];relationships?:ProjectEntity[]};
const number=(value:unknown,fallback:number):number=>Number.isFinite(Number(value))?Number(value):fallback;
const graphFor=(state:ProjectState,flowId:string):SectionGraph=>((state.project.documentationFlowGraphs as Record<string,SectionGraph>|undefined)?.[flowId]??{});

export function mountPropertyCompositionWorkspace({host,state,pageId,id,onSave,onOpen}:PropertyCompositionWorkspaceOptions):HTMLElement{
  const page=state.project.collections.pages.find(({id})=>id===pageId);if(!page)throw new Error(`Unknown Page ${pageId}.`);const propertySets=state.project.collections.propertySets,applicabilitySets=state.project.collections.applicabilitySets,byId=new Map(propertySets.map((set)=>[set.id,set])),workspace=document.createElement("section"),heading=document.createElement("h2"),guidance=document.createElement("p"),search=document.createElement("input"),newApplicability=document.createElement("select"),results=document.createElement("div"),review=document.createElement("section"),list=document.createElement("ol");workspace.className="property-composition-workspace";workspace.dataset.propertyComposition=page.id;workspace.setAttribute("aria-label",`Property composition for ${page.name}`);heading.textContent="Property composition";guidance.textContent="Applied Property Sets in general-to-specific order. Each application owns its optional Applicability Set; Shared Profile source inheritance remains selective and separately managed.";search.type="search";search.setAttribute("aria-label","Search Property Sets to apply");newApplicability.setAttribute("aria-label",`Applicability Set for new application on ${page.name}`);newApplicability.append(new Option("Always",""),...applicabilitySets.map((set)=>new Option(set.name,set.id)));results.setAttribute("aria-label","Property Set search results");review.setAttribute("aria-label","Property composition reorder review");review.hidden=true;list.setAttribute("aria-label","Applied Property Sets");
  const renderResults=()=>{const query=search.value.trim().toLocaleLowerCase(),applied=new Set(orderedPropertySetApplications(state.project,page.id).map(({propertySetId})=>propertySetId));results.replaceChildren(...propertySets.filter((set)=>!applied.has(set.id)&&set.name.toLocaleLowerCase().includes(query)).map((set)=>{const button=document.createElement("button");button.type="button";button.textContent=`Apply ${set.name}`;button.setAttribute("aria-label",`Apply Property Set ${set.name} to ${page.name}`);button.addEventListener("click",()=>onSave(addPropertySetApplication(state,page.id,set.id,newApplicability.value||undefined,id)));return button;}));};search.addEventListener("input",renderResults);renderResults();
  const applications=orderedPropertySetApplications(state.project,page.id),stageReorder=(application:typeof applications[number],delta:number)=>{const target=Math.max(0,Math.min(applications.length-1,applications.findIndex(({id})=>id===application.id)+delta)),other=applications[target]??application,next=reorderPropertySetApplication(state,page.id,application.propertySetId,delta),paths=[...new Set([application,other].flatMap(({propertySetId})=>((byId.get(propertySetId)?.schemaConstraints as {path?:string}[]|undefined)??[]).flatMap(({path})=>path?[path]:[])))],summary=document.createElement("p"),confirm=document.createElement("button"),cancel=document.createElement("button");summary.textContent=`Review application order for ${page.name}: ${applications.map(({propertySetId})=>byId.get(propertySetId)?.name??propertySetId).join(" then ")} becomes ${orderedPropertySetApplications(next.project,page.id).map(({propertySetId})=>byId.get(propertySetId)?.name??propertySetId).join(" then ")}. Impacted properties: ${paths.join(", ")||"none"}. Both Property Sets remain visible as effective or superseded provenance; invariant and structural conflicts remain blocked.`;confirm.type=cancel.type="button";confirm.textContent="Confirm application reorder";cancel.textContent="Cancel application reorder";confirm.addEventListener("click",()=>onSave(next));cancel.addEventListener("click",()=>{review.hidden=true;review.replaceChildren();});review.replaceChildren(summary,confirm,cancel);review.hidden=false;confirm.focus();};for(const[order,application]of applications.entries()){const set=byId.get(application.propertySetId),row=document.createElement("li"),summary=document.createElement("p"),applicability=document.createElement("select"),open=document.createElement("button"),earlier=document.createElement("button"),later=document.createElement("button"),remove=document.createElement("button");row.dataset.propertySetApplicationId=application.id;summary.textContent=`${set?.name??application.propertySetId} · applicability ${application.applicabilitySetId??"Always"} · complete effective contribution · provenance Property Set ${set?.name??application.propertySetId}`;applicability.setAttribute("aria-label",`Applicability Set for ${set?.name??application.propertySetId} on ${page.name}`);applicability.append(new Option("Always",""),...applicabilitySets.map((candidate)=>new Option(candidate.name,candidate.id)));applicability.value=application.applicabilitySetId??"";applicability.addEventListener("change",()=>onSave(setPropertySetApplicationApplicability(state,page.id,application.propertySetId,applicability.value||undefined)));for(const control of[open,earlier,later,remove])control.type="button";open.textContent="Open Property Set";open.setAttribute("aria-label",`Open Property Set ${set?.name??application.propertySetId}`);earlier.textContent="Move earlier";later.textContent="Move later";remove.textContent="Remove application";earlier.disabled=order===0;later.disabled=order===applications.length-1;open.addEventListener("click",()=>onOpen?.(application.propertySetId));earlier.addEventListener("click",()=>stageReorder(application,-1));later.addEventListener("click",()=>stageReorder(application,1));remove.addEventListener("click",()=>onSave(removePropertySetApplication(state,page.id,application.propertySetId)));row.append(summary,applicability,open,earlier,later,remove);list.append(row);}workspace.append(heading,guidance,search,newApplicability,results,review,list);host.append(workspace);return workspace;
}

export function mountFlowSectionWorkspace({host,state,flowId,id,onSave}:FlowSectionWorkspaceOptions):HTMLElement{
  const graph=graphFor(state,flowId),sections=graph.sections??[],frames=graph.pageFrames??[],relationships=graph.relationships??[],workspace=document.createElement("section"),heading=document.createElement("h2"),guidance=document.createElement("p"),toolbar=document.createElement("nav"),catalogs=document.createElement("section"),canvas=document.createElement("div"),surface=document.createElement("div"),createForm=document.createElement("form"),name=document.createElement("input"),add=document.createElement("button"),list=document.createElement("div"),placement=document.createElement("fieldset"),placementLegend=document.createElement("legend"),frameSelect=document.createElement("select"),sectionSelect=document.createElement("select"),place=document.createElement("button"),outside=document.createElement("button"),connection=document.createElement("fieldset"),connectionLegend=document.createElement("legend"),sourceSelect=document.createElement("select"),targetSelect=document.createElement("select"),connect=document.createElement("button"),status=document.createElement("output"),viewKey=`flow-section-view:${state.project.id}:${flowId}`,storedView=JSON.parse(sessionStorage.getItem(viewKey)??"{}") as {selectedFrameId?:string;focusLabel?:string};
  workspace.className="flow-section-workspace";workspace.dataset.flowSectionWorkspace=flowId;workspace.setAttribute("aria-label","Flow canvas and outline");heading.textContent="Flow canvas";guidance.textContent="Sections are Flow-owned presentation containers. Page frames, Events, and relationships remain independent of Property composition, applicability, schema provenance, validation, and Assignment targets.";name.required=true;name.placeholder="Section name";name.setAttribute("aria-label","New Section name");add.type="submit";add.textContent="Add Section";createForm.append(name,add);list.setAttribute("aria-label","Synchronized Flow outline");
  let selectedFrameId=storedView.selectedFrameId;const remember=(selected=selectedFrameId,focusLabel?:string)=>{selectedFrameId=selected;sessionStorage.setItem(viewKey,JSON.stringify({...(selected?{selectedFrameId:selected}:{}),...(focusLabel?{focusLabel}:{})}));},save=(next:ProjectState,focusLabel?:string)=>{remember(selectedFrameId,focusLabel);onSave(next);};
  toolbar.className="flow-section-toolbar";toolbar.setAttribute("aria-label","Flow toolbar");catalogs.className="flow-section-catalogs";catalogs.setAttribute("aria-label","Flow component catalogs");const panels=new Map<string,HTMLElement>();for(const label of["Sections","Pages","Events"]){const tool=document.createElement("button"),panel=document.createElement("section");tool.type="button";tool.textContent=label;tool.setAttribute("aria-expanded",String(label==="Sections"));panel.hidden=label!=="Sections";panel.setAttribute("aria-label",`${label} controls`);tool.addEventListener("click",()=>{for(const[candidate,currentPanel]of panels){currentPanel.hidden=candidate!==label;const control=Array.from(toolbar.querySelectorAll("button")).find(({textContent})=>textContent===candidate);control?.setAttribute("aria-expanded",String(candidate===label));}panel.querySelector<HTMLElement>("button,input,select")?.focus();});panels.set(label,panel);toolbar.append(tool);catalogs.append(panel);}panels.get("Sections")!.append(createForm);
  const pagesPanel=panels.get("Pages")!,pageSearch=document.createElement("input"),pageResults=document.createElement("div");pageSearch.type="search";pageSearch.setAttribute("aria-label","Search Pages to add to Flow");const renderPages=()=>{const query=pageSearch.value.trim().toLocaleLowerCase();pageResults.replaceChildren(...state.project.collections.pages.filter(({name})=>name.toLocaleLowerCase().includes(query)).map((page)=>{const control=document.createElement("button");control.type="button";control.textContent=`Add ${page.name}`;control.setAttribute("aria-label",`Add Page ${page.name} outside Sections`);control.addEventListener("click",()=>save(addFlowPageFrameToSection(state,flowId,page.id,undefined,id),control.getAttribute("aria-label")!));return control;}));};pageSearch.addEventListener("input",renderPages);renderPages();pagesPanel.append(pageSearch,pageResults);
  const eventsPanel=panels.get("Events")!,eventFrame=document.createElement("select"),eventChoices=document.createElement("div");eventFrame.setAttribute("aria-label","Page frame for Event");eventFrame.append(new Option("Choose Page frame",""),...frames.map((frame)=>new Option(frame.name,frame.id)));for(const event of state.project.collections.events){const control=document.createElement("button");control.type="button";control.textContent=`Add ${event.name}`;control.setAttribute("aria-label",`Add Event ${event.name} to selected Page frame`);control.addEventListener("click",()=>{if(eventFrame.value)save(addFlowEventOccurrence(state,flowId,eventFrame.value,event.id,id),control.getAttribute("aria-label")!);});eventChoices.append(control);}eventsPanel.append(eventFrame,eventChoices);
  canvas.className="flow-section-canvas";canvas.setAttribute("aria-label","Interactive directional Flow canvas");surface.className="flow-section-canvas-surface";const right=Math.max(720,...sections.map(({bounds})=>bounds.x+bounds.width+32),...frames.map(({position})=>(position.x??24)+220)),bottom=Math.max(360,...sections.map(({bounds})=>bounds.y+bounds.height+32),...frames.map(({position})=>position.y+100));surface.style.width=`${right}px`;surface.style.height=`${bottom}px`;const lines=document.createElementNS("http://www.w3.org/2000/svg","svg");lines.classList.add("flow-section-relationships");lines.setAttribute("aria-label","Flow relationships");lines.setAttribute("width",String(right));lines.setAttribute("height",String(bottom));for(const relationship of relationships){const endpoint=(side:"source"|"target")=>(relationship[`${side}Endpoint`] as {id?:string}|undefined)?.id??relationship[`${side}NodeId`],source=frames.find(({id})=>id===endpoint("source")),target=frames.find(({id})=>id===endpoint("target"));if(!source||!target)continue;const line=document.createElementNS(lines.namespaceURI,"line");line.setAttribute("x1",String((source.position.x??24)+170));line.setAttribute("y1",String(source.position.y+34));line.setAttribute("x2",String(target.position.x??24));line.setAttribute("y2",String(target.position.y+34));line.setAttribute("data-relationship-id",relationship.id);line.setAttribute("aria-label",relationship.name||`${source.name} to ${target.name}`);lines.append(line);}surface.append(lines);for(const section of sections){const region=document.createElement("section"),label=document.createElement("h3");region.className="flow-section-region";region.dataset.flowSectionRegion=section.id;region.setAttribute("aria-label",`Section ${section.name}`);region.style.left=`${section.bounds.x}px`;region.style.top=`${section.bounds.y}px`;region.style.width=`${section.bounds.width}px`;region.style.height=`${section.bounds.height}px`;label.textContent=section.name;region.append(label);surface.append(region);}for(const frame of frames){const card=document.createElement("button"),moveBy=(dx:number,dy:number)=>save(moveFlowPageFramePresentation(state,flowId,frame.id,{x:(frame.position.x??24)+dx,y:frame.position.y+dy}),card.getAttribute("aria-label")!);card.type="button";card.className="flow-section-page-frame";card.dataset.pageFrameId=frame.id;card.textContent=frame.name;card.setAttribute("aria-label",`Page frame ${frame.name}${frame.sectionId?` in ${sections.find(({id})=>id===frame.sectionId)?.name??"Section"}`:" outside Sections"}`);card.setAttribute("aria-pressed",String(storedView.selectedFrameId===frame.id));card.style.left=`${frame.position.x??24}px`;card.style.top=`${frame.position.y}px`;card.addEventListener("click",()=>{remember(frame.id,card.getAttribute("aria-label")!);for(const peer of Array.from(surface.querySelectorAll("[data-page-frame-id]")))peer.setAttribute("aria-pressed",String(peer===card));});card.addEventListener("keydown",(event)=>{const delta:{[key:string]:[number,number]}={ArrowLeft:[-20,0],ArrowRight:[20,0],ArrowUp:[0,-20],ArrowDown:[0,20]},movement=delta[event.key];if(movement){event.preventDefault();moveBy(movement[0],movement[1]);}});let drag:{x:number;y:number}|undefined;card.addEventListener("pointerdown",(event)=>{drag={x:event.clientX,y:event.clientY};try{card.setPointerCapture?.(event.pointerId);}catch{/* Synthetic acceptance pointers have no active device pointer. */}});card.addEventListener("pointerup",(event)=>{if(!drag)return;const dx=event.clientX-drag.x,dy=event.clientY-drag.y;drag=undefined;if(dx||dy)moveBy(dx,dy);});surface.append(card);}canvas.append(surface);
  createForm.addEventListener("submit",(event)=>{event.preventDefault();try{save(createFlowSection(state,flowId,{name:name.value,bounds:{x:24,y:48+sections.length*220,width:640,height:190}},id),"New Section name");}catch(error){status.textContent=error instanceof Error?error.message:String(error);}});
  for(const section of [...sections].sort((left,right)=>left.order-right.order)){
    const row=document.createElement("article"),title=document.createElement("h3"),rename=document.createElement("input"),x=document.createElement("input"),y=document.createElement("input"),width=document.createElement("input"),height=document.createElement("input"),save=document.createElement("button"),move=document.createElement("button"),remove=document.createElement("button"),destructive=document.createElement("button"),review=document.createElement("section");
    row.dataset.flowSectionId=section.id;title.textContent=section.name;rename.value=section.name;rename.setAttribute("aria-label",`Section name ${section.name}`);for(const[control,label,value]of[[x,"Section x",section.bounds.x],[y,"Section y",section.bounds.y],[width,"Section width",section.bounds.width],[height,"Section height",section.bounds.height]] as const){control.type="number";control.value=String(value);control.setAttribute("aria-label",`${label} ${section.name}`);}for(const control of[save,move,remove,destructive])control.type="button";save.textContent="Save name and size";move.textContent="Move Section";remove.textContent="Remove Section and retain Page frames";destructive.textContent="Review remove Section with contents";review.hidden=true;review.setAttribute("aria-label",`Remove ${section.name} with contents review`);
    const bounds=():FlowSectionBounds=>({x:number(x.value,section.bounds.x),y:number(y.value,section.bounds.y),width:number(width.value,section.bounds.width),height:number(height.value,section.bounds.height)});save.addEventListener("click",()=>saveState(renameAndResizeFlowSection(state,flowId,section.id,{name:rename.value,bounds:bounds()}),save.getAttribute("aria-label")!));move.addEventListener("click",()=>saveState(moveFlowSection(state,flowId,section.id,{x:number(x.value,section.bounds.x),y:number(y.value,section.bounds.y)}),move.getAttribute("aria-label")!));remove.addEventListener("click",()=>saveState(removeFlowSection(state,flowId,section.id),"New Section name"));destructive.addEventListener("click",()=>{const impact=inspectSectionRemovalWithContents(state.project,flowId,section.id),summary=document.createElement("p"),confirm=document.createElement("button"),cancel=document.createElement("button");summary.textContent=`Remove Page frames: ${impact.pageFrames.map(({name})=>name).join(", ")||"none"}. Remove relationships: ${impact.relationships.map(({name})=>name).join(", ")||"none"}. Nothing changes until confirmed.`;confirm.type=cancel.type="button";confirm.textContent="Confirm remove Section with contents";cancel.textContent="Cancel";confirm.addEventListener("click",()=>saveState(removeFlowSectionWithContents(state,flowId,section.id,impact),"New Section name"));cancel.addEventListener("click",()=>{review.hidden=true;review.replaceChildren();destructive.focus();});review.replaceChildren(summary,confirm,cancel);review.hidden=false;confirm.focus();});for(const[control,label]of[[save,`Save ${section.name} name and size`],[move,`Move Section ${section.name}`],[remove,`Remove Section ${section.name} and retain Page frames`],[destructive,`Review remove Section ${section.name} with contents`]] as const)control.setAttribute("aria-label",label);row.append(title,rename,x,y,width,height,save,move,remove,destructive,review);list.append(row);
  }
  const saveState=save;placementLegend.textContent="Organize Page frames";frameSelect.setAttribute("aria-label","Page frame to organize");sectionSelect.setAttribute("aria-label","Destination Section");frameSelect.append(new Option("Choose Page frame",""),...frames.map((frame)=>new Option(frame.name,frame.id)));sectionSelect.append(new Option("Choose Section",""),...sections.map((section)=>new Option(section.name,section.id)));place.type=outside.type="button";place.textContent="Place in Section";outside.textContent="Move outside every Section";place.addEventListener("click",()=>{if(frameSelect.value&&sectionSelect.value)saveState(movePageFrameToSection(state,flowId,frameSelect.value,sectionSelect.value),"Page frame to organize");});outside.addEventListener("click",()=>{if(frameSelect.value)saveState(movePageFrameToSection(state,flowId,frameSelect.value),"Page frame to organize");});placement.append(placementLegend,frameSelect,sectionSelect,place,outside);connectionLegend.textContent="Connect Page frames";sourceSelect.setAttribute("aria-label","Relationship source Page frame");targetSelect.setAttribute("aria-label","Relationship target Page frame");sourceSelect.append(new Option("Choose source",""),...frames.map((frame)=>new Option(frame.name,frame.id)));targetSelect.append(new Option("Choose target",""),...frames.map((frame)=>new Option(frame.name,frame.id)));connect.type="button";connect.textContent="Connect Page frames";connect.addEventListener("click",()=>{if(sourceSelect.value&&targetSelect.value)saveState(connectFlowPageFrames(state,flowId,sourceSelect.value,targetSelect.value,id),"Relationship source Page frame");});connection.append(connectionLegend,sourceSelect,targetSelect,connect);workspace.append(heading,guidance,toolbar,catalogs,canvas,list,placement,connection,status);host.append(workspace);queueMicrotask(()=>{const label=storedView.focusLabel;if(label)Array.from(workspace.querySelectorAll<HTMLElement>("button,input,select")).find((control)=>control.getAttribute("aria-label")===label)?.focus({preventScroll:true});});return workspace;
}
