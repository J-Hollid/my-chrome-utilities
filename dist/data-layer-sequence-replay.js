const clone = (value) => structuredClone(value);
export function createSequence(id, name, sessionId, templates) { return { id, name, originatingSessionId: sessionId, steps: templates.map((t, index) => ({ id: `step:${index + 1}`, templateId: t.id, templateVersion: t.version, enabled: true, destination: t.destination, delayMs: 0, breakpoint: false })) }; }
export function updateStep(sequence, id, change) { return { ...sequence, steps: sequence.steps.map(step => step.id === id ? { ...step, ...clone(change) } : step) }; }
export function reorderStep(sequence, from, to) { const steps = [...sequence.steps]; const [step] = steps.splice(from, 1); if (!step)
    return sequence; steps.splice(to, 0, step); return { ...sequence, steps }; }
export function duplicateStep(sequence, id) { const step = sequence.steps.find(item => item.id === id); return step ? { ...sequence, steps: [...sequence.steps, { ...step, id: `${step.id}:copy` }] } : sequence; }
export function removeStep(sequence, id) { return { ...sequence, steps: sequence.steps.filter(step => step.id !== id) }; }
export function readiness(sequence, templates, adapters) { const tm = new Map(templates.map(t => [t.id, t])); const am = new Map(adapters.map(a => [a.id, a])); const blocked = sequence.steps.filter(s => s.enabled).flatMap(s => { const t = tm.get(s.templateId); const a = t && am.get(t.sourceId); return !t || !a || !a.enabled || a.destination !== s.destination || !a.capabilities.includes("push") ? [s.id] : []; }); return { runnable: blocked.length === 0, blocked }; }
export function runSequence(sequence, templates, adapters, pageUrl, action, now = () => new Date().toISOString()) { const ready = readiness(sequence, templates, adapters); if (!ready.runnable)
    throw new Error(`Not runnable: ${ready.blocked.join(", ")}`); const tm = new Map(templates.map(t => [t.id, t])); const steps = sequence.steps.filter(s => s.enabled).slice(0, action === "Run step" ? 1 : undefined).map(step => { const t = tm.get(step.templateId); return { stepId: step.id, templateId: t.id, templateVersion: step.templateVersion, sourceId: t.sourceId, destination: step.destination, payload: clone(step.payloadOverride ?? t.payload), timestamp: now(), result: "completed" }; }); return Object.freeze({ id: `execution:${sequence.id}:${steps.length}`, sequenceId: sequence.id, pageUrl, result: "completed", steps: Object.freeze(steps), immutable: true }); }
export function updatePinnedVersion(sequence, stepId, version) { return updateStep(sequence, stepId, { templateVersion: version }); }
//# sourceMappingURL=data-layer-sequence-replay.js.map