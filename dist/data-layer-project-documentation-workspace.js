import { documentationWorksheet, renderFlowDocumentationClipboard, zipDocumentationFiles } from "./data-layer-flow-table-documentation-export.js";
import { createProjectDocumentationTheme, projectDocumentationSafeText } from "./data-layer-project-documentation-records.js";
export { createProjectDocumentationSet, createProjectDocumentationTheme, parseProjectDocumentationTheme, serializeProjectDocumentationTheme } from "./data-layer-project-documentation-records.js";
const clone = (value) => structuredClone(value);
const freeze = (value) => { if (value && typeof value === "object" && !Object.isFrozen(value)) {
    Object.freeze(value);
    for (const child of Object.values(value))
        freeze(child);
} return value; };
const hash = (value) => { let result = 2166136261; for (const character of value) {
    result ^= character.charCodeAt(0);
    result = Math.imul(result, 16777619);
} return (result >>> 0).toString(16).padStart(16, "0"); };
const safeText = projectDocumentationSafeText;
const xml = (value) => String(value ?? "").replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;");
const html = (value) => xml(value).replaceAll("'", "&#39;");
export function themeFingerprint(theme) { return hash(JSON.stringify(createProjectDocumentationTheme(theme))); }
export function compileProjectDocumentationSnapshot(input) { const copied = clone(input), incomplete = copied.diagnostics.length > 0, title = incomplete ? "Draft — incomplete" : safeText(copied.set.name) || copied.projectName, snapshotHash = hash(JSON.stringify({ set: copied.set, theme: copied.theme, revisions: copied.sourceRevisions, tables: copied.tables })); return freeze({ ...copied, title, incomplete, snapshotHash }); }
export function projectDocumentationSnapshotStale(snapshot, current) { const changedSources = Object.keys(snapshot.sourceRevisions).filter((id) => snapshot.sourceRevisions[id] !== current[id]); return { stale: changedSources.length > 0, changedSources }; }
export function selectProjectDocumentationTables(snapshot, selection) { const ids = selection.scope === "complete" ? snapshot.set.sections.filter(({ selected }) => selected).map(({ id }) => id) : selection.scope === "current" ? [selection.currentSectionId] : [...selection.selectedSectionIds], wanted = new Set(ids), byId = new Map(snapshot.tables.map((table) => [table.id, table])); return snapshot.set.sections.flatMap(({ id }) => wanted.has(id) && byId.has(id) ? [byId.get(id)] : []); }
export function applyProjectDocumentationTheme(table, theme) { return freeze({ ...clone(table), themeFingerprint: themeFingerprint(theme) }); }
export function renderProjectDocumentationClipboard(snapshot, selection) {
    if (snapshot.incomplete && !selection.confirmIncomplete)
        throw new Error("Confirm incomplete documentation before export.");
    const fingerprint = themeFingerprint(snapshot.theme), style = snapshot.theme.highlightedHeadings ? "highlighted" : snapshot.theme.borders ? "bordered" : "plain", tables = selectProjectDocumentationTables(snapshot, selection), border = snapshot.theme.borders ? "border:1px solid #666;" : "", padding = snapshot.theme.density === "compact" ? "2px 4px" : "5px 7px", tableStyle = `border-collapse:collapse;font-family:${snapshot.theme.typography.family};font-size:${snapshot.theme.typography.bodySize}pt;color:${snapshot.theme.colors.accent}`, headingStyle = `${border}padding:${padding};font-family:${snapshot.theme.typography.family};font-size:${snapshot.theme.typography.headingSize}pt;white-space:normal;${snapshot.theme.highlightedHeadings ? `background:${snapshot.theme.colors.heading};color:#fff;` : ""}`, cellStyle = `${border}padding:${padding};white-space:pre-wrap;overflow-wrap:anywhere;`, identity = [snapshot.theme.clientName, snapshot.theme.headerText].filter(Boolean).join(" · "), footer = snapshot.theme.footerText;
    const rendered = tables.map((table) => { const documentTitle = snapshot.incomplete ? `${snapshot.title} — ${table.title}` : table.title, value = renderFlowDocumentationClipboard(table, { style, documentTitle }), columns = `<colgroup>${table.headings.map((heading) => `<col style="width:${snapshot.theme.columnWidths[heading] ?? Math.min(80, Math.max(10, heading.length + 4))}ch">`).join("")}</colgroup>`, logo = snapshot.theme.logo ? `<img src="${html(snapshot.theme.logo)}" alt="${html(snapshot.theme.clientName || snapshot.theme.name)} logo" style="max-height:48px;max-width:180px">` : "", header = `<header>${logo}${identity ? `<p>${html(identity)}</p>` : ""}</header>`, sharedFooter = footer ? `<footer>${html(footer)}</footer>` : ""; let rich = value.html.replace(/<table[^>]*>/u, `<table data-theme-fingerprint="${fingerprint}" data-density="${snapshot.theme.density}" style="${tableStyle}">${columns}`).replace(/<th(?: style="[^"]*")?>/gu, `<th style="${headingStyle}">`).replace(/<td(?: style="[^"]*")?>/gu, `<td style="${cellStyle}">`); if (snapshot.theme.striping)
        rich = rich.replace(/<tbody>([\s\S]*?)<\/tbody>/u, (_all, body) => `<tbody>${String(body).replace(/<tr>/gu, (row, index) => `<tr${index % 2 === 1 ? ` style="background:${snapshot.theme.colors.stripe}"` : ""}>`)}</tbody>`); return { plain: [identity, documentTitle, value.plain, footer].filter(Boolean).join("\n"), html: `<section data-documentation-table="${html(table.id)}">${header}${rich}${sharedFooter}</section>` }; });
    return { plain: rendered.map(({ plain }) => plain).join("\n\n"), html: rendered.map(({ html }) => html).join("") };
}
function workbookNames(names) { const used = new Set(); return names.map((raw, index) => { const base = safeText(raw).replace(/[\\/*?:[\]]/gu, " ").replace(/\s+/gu, " ").replace(/^'+|'+$/gu, "") || `Sheet ${index + 1}`; let candidate = base.slice(0, 31), sequence = 1; while (used.has(candidate.toLowerCase())) {
    sequence += 1;
    const suffix = ` (${sequence})`;
    candidate = `${base.slice(0, 31 - suffix.length)}${suffix}`;
} used.add(candidate.toLowerCase()); return candidate; }); }
const excelColor = (value) => `FF${value.slice(1).toUpperCase()}`;
function workbookLogo(theme) {
    const match = /^data:image\/(png|jpeg|gif);base64,([a-z0-9+/]+={0,2})$/iu.exec(theme.logo);
    if (!match)
        return undefined;
    const extension = match[1].toLowerCase() === "jpeg" ? "jpg" : match[1].toLowerCase(), contentType = extension === "jpg" ? "image/jpeg" : `image/${extension}`;
    try {
        return { extension, contentType, bytes: Uint8Array.from(atob(match[2]), character => character.charCodeAt(0)) };
    }
    catch {
        return undefined;
    }
}
const drawing = (index) => `<?xml version="1.0" encoding="UTF-8" standalone="yes"?><xdr:wsDr xmlns:xdr="http://schemas.openxmlformats.org/drawingml/2006/spreadsheetDrawing" xmlns:a="http://schemas.openxmlformats.org/drawingml/2006/main" xmlns:r="http://schemas.openxmlformats.org/officeDocument/2006/relationships"><xdr:oneCellAnchor><xdr:from><xdr:col>0</xdr:col><xdr:colOff>0</xdr:colOff><xdr:row>0</xdr:row><xdr:rowOff>0</xdr:rowOff></xdr:from><xdr:ext cx="1714500" cy="457200"/><xdr:pic><xdr:nvPicPr><xdr:cNvPr id="${index}" name="Documentation client logo"/><xdr:cNvPicPr/></xdr:nvPicPr><xdr:blipFill><a:blip r:embed="rId1"/><a:stretch><a:fillRect/></a:stretch></xdr:blipFill><xdr:spPr><a:prstGeom prst="rect"><a:avLst/></a:prstGeom></xdr:spPr></xdr:pic><xdr:clientData/></xdr:oneCellAnchor></xdr:wsDr>`;
const drawingRelationship = (logo) => `<?xml version="1.0" encoding="UTF-8" standalone="yes"?><Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships"><Relationship Id="rId1" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/image" Target="../media/documentation-logo.${logo.extension}"/></Relationships>`;
const worksheetRelationship = (index) => `<?xml version="1.0" encoding="UTF-8" standalone="yes"?><Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships"><Relationship Id="rId1" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/drawing" Target="../drawings/drawing${index}.xml"/></Relationships>`;
function documentationStyles(theme) { const border = theme.borders ? `<border><left style="thin"><color rgb="FF666666"/></left><right style="thin"><color rgb="FF666666"/></right><top style="thin"><color rgb="FF666666"/></top><bottom style="thin"><color rgb="FF666666"/></bottom><diagonal/></border>` : "<border><left/><right/><top/><bottom/><diagonal/></border>"; return `<?xml version="1.0" encoding="UTF-8" standalone="yes"?><styleSheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main" data-theme-fingerprint="${themeFingerprint(theme)}"><fonts count="2"><font><sz val="${theme.typography.bodySize}"/><color rgb="${excelColor(theme.colors.accent)}"/><name val="${xml(theme.typography.family)}"/></font><font><b/><sz val="${theme.typography.headingSize}"/><color rgb="FFFFFFFF"/><name val="${xml(theme.typography.family)}"/></font></fonts><fills count="4"><fill><patternFill patternType="none"/></fill><fill><patternFill patternType="gray125"/></fill><fill><patternFill patternType="solid"><fgColor rgb="${excelColor(theme.colors.heading)}"/><bgColor indexed="64"/></patternFill></fill><fill><patternFill patternType="solid"><fgColor rgb="${excelColor(theme.colors.stripe)}"/><bgColor indexed="64"/></patternFill></fill></fills><borders count="1">${border}</borders><cellStyleXfs count="1"><xf numFmtId="0" fontId="0" fillId="0" borderId="0"/></cellStyleXfs><cellXfs count="3"><xf numFmtId="0" fontId="0" fillId="0" borderId="0" xfId="0" applyFont="1" applyBorder="${theme.borders ? 1 : 0}" applyAlignment="1"><alignment wrapText="1" vertical="top"/></xf><xf numFmtId="0" fontId="1" fillId="${theme.highlightedHeadings ? 2 : 0}" borderId="0" xfId="0" applyFont="1" applyFill="${theme.highlightedHeadings ? 1 : 0}" applyBorder="${theme.borders ? 1 : 0}" applyAlignment="1"><alignment wrapText="1" vertical="top"/></xf><xf numFmtId="0" fontId="0" fillId="3" borderId="0" xfId="0" applyFont="1" applyFill="1" applyBorder="${theme.borders ? 1 : 0}" applyAlignment="1"><alignment wrapText="1" vertical="top"/></xf></cellXfs><cellStyles count="1"><cellStyle name="Normal" xfId="0" builtinId="0"/></cellStyles></styleSheet>`; }
function themedWorksheet(table, theme, hasLogo) { const height = theme.density === "compact" ? 16 : 22, widths = table.headings.map((heading) => theme.columnWidths[heading] ?? Math.min(80, Math.max(10, heading.length + 4))), columns = `<cols>${widths.map((width, index) => `<col min="${index + 1}" max="${index + 1}" width="${width}" customWidth="1"/>`).join("")}</cols>`, header = [theme.clientName, theme.headerText].filter(Boolean).join(" · "), headerFooter = `<headerFooter><oddHeader>${xml(header)}</oddHeader><oddFooter>${xml(theme.footerText)}</oddFooter></headerFooter>`, drawingPart = hasLogo ? `<drawing r:id="rId1"/>` : ""; return documentationWorksheet(table).replace('xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main"', `xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main"${hasLogo ? ' xmlns:r="http://schemas.openxmlformats.org/officeDocument/2006/relationships"' : ""}`).replace("<sheetData>", `${columns}<sheetData>`).replace("</worksheet>", `${drawingPart}${headerFooter}</worksheet>`).replace(/<row r="(\d+)"([^>]*)>([\s\S]*?)<\/row>/gu, (_, raw, attributes, content) => { const row = Number(raw), heading = String(attributes).includes("data-concept-heading") || String(attributes).includes("data-concept-columns"), style = heading || row <= 2 ? 1 : theme.striping && row % 2 === 0 ? 2 : 0; return `<row r="${row}"${attributes} ht="${height}" customHeight="1">${String(content).replace(/<c /gu, `<c s="${style}" `)}</row>`; }); }
export function writeProjectDocumentationWorkbook(snapshot, selection) {
    if (snapshot.incomplete && !selection.confirmIncomplete)
        throw new Error("Confirm incomplete documentation before export.");
    const selected = selectProjectDocumentationTables(snapshot, selection);
    if (!selected.length)
        throw new Error("Choose at least one documentation section.");
    const tables = selected.map((table) => ({ ...table, title: [snapshot.title, snapshot.theme.clientName, table.title, snapshot.theme.headerText].filter(Boolean).join(" · ") })), names = workbookNames(selected.map(({ title }) => title)), logo = workbookLogo(snapshot.theme), workbook = `<?xml version="1.0" encoding="UTF-8" standalone="yes"?><workbook xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main" xmlns:r="http://schemas.openxmlformats.org/officeDocument/2006/relationships"><sheets>${names.map((name, index) => `<sheet name="${xml(name)}" sheetId="${index + 1}" r:id="rId${index + 1}"/>`).join("")}</sheets></workbook>`, relationships = `<?xml version="1.0" encoding="UTF-8" standalone="yes"?><Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships">${names.map((_, index) => `<Relationship Id="rId${index + 1}" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/worksheet" Target="worksheets/sheet${index + 1}.xml"/>`).join("")}<Relationship Id="rId${names.length + 1}" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/styles" Target="styles.xml"/></Relationships>`, rootRels = `<?xml version="1.0" encoding="UTF-8" standalone="yes"?><Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships"><Relationship Id="rId1" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/officeDocument" Target="xl/workbook.xml"/></Relationships>`, types = `<?xml version="1.0" encoding="UTF-8" standalone="yes"?><Types xmlns="http://schemas.openxmlformats.org/package/2006/content-types"><Default Extension="rels" ContentType="application/vnd.openxmlformats-package.relationships+xml"/><Default Extension="xml" ContentType="application/xml"/>${logo ? `<Default Extension="${logo.extension}" ContentType="${logo.contentType}"/>` : ""}<Override PartName="/xl/workbook.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet.main+xml"/><Override PartName="/xl/styles.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.styles+xml"/>${names.map((_, index) => `<Override PartName="/xl/worksheets/sheet${index + 1}.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.worksheet+xml"/>${logo ? `<Override PartName="/xl/drawings/drawing${index + 1}.xml" ContentType="application/vnd.openxmlformats-officedocument.drawing+xml"/>` : ""}`).join("")}</Types>`, files = [{ name: "[Content_Types].xml", content: types }, { name: "_rels/.rels", content: rootRels }, { name: "xl/workbook.xml", content: workbook }, { name: "xl/_rels/workbook.xml.rels", content: relationships }, { name: "xl/styles.xml", content: documentationStyles(snapshot.theme) }, ...tables.map((table, index) => ({ name: `xl/worksheets/sheet${index + 1}.xml`, content: themedWorksheet(table, snapshot.theme, Boolean(logo)) }))];
    if (logo) {
        files.push({ name: `xl/media/documentation-logo.${logo.extension}`, content: logo.bytes });
        for (const [index] of tables.entries())
            files.push({ name: `xl/worksheets/_rels/sheet${index + 1}.xml.rels`, content: worksheetRelationship(index + 1) }, { name: `xl/drawings/drawing${index + 1}.xml`, content: drawing(index + 1) }, { name: `xl/drawings/_rels/drawing${index + 1}.xml.rels`, content: drawingRelationship(logo) });
    }
    return zipDocumentationFiles(files);
}
//# sourceMappingURL=data-layer-project-documentation-workspace.js.map